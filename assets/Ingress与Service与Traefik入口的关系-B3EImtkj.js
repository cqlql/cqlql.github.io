import{D as e,f as t,u as n}from"./runtime-core.esm-bundler-BHJ3o1LK.js";import{t as r}from"./plugin-vue_export-helper-BDNMzG2s.js";var i=JSON.parse(`{"path":"/%E5%90%8E%E7%AB%AF/%E4%BA%91%E5%8E%9F%E7%94%9F/k3s/%E5%8E%9F%E7%90%86%E4%B8%8E%E9%80%89%E5%9E%8B/Ingress%E4%B8%8EService%E4%B8%8ETraefik%E5%85%A5%E5%8F%A3%E7%9A%84%E5%85%B3%E7%B3%BB.html","title":"Ingress / Service / Traefik 入口的关系","lang":"zh-CN","frontmatter":{"title":"Ingress / Service / Traefik 入口的关系","icon":"mdi:routes","sort":8,"description":"排查「业务入口高可用」时，最容易混淆的就是 Ingress、Service、Traefik 三者的分工。本文厘清它们「各管一层」，并回答几个高频认知盲区。 第七~十节是几个高频提问的实测回答（都带验证命令与输出）： 「一个服务除了 VIP 还有哪些 IP 能访问？」「VIP 会漂移吗？」「VIP 在就说明服务好吗？」 「为什么 Ingress 的 ho...","head":[["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"Ingress / Service / Traefik 入口的关系\\",\\"image\\":[\\"\\"],\\"dateModified\\":\\"2026-09-23T08:43:11.000Z\\",\\"author\\":[{\\"@type\\":\\"Person\\",\\"name\\":\\"桥黎\\",\\"url\\":\\"\\"}]}"],["meta",{"property":"og:url","content":"https://docs.cqlql.top/%E5%90%8E%E7%AB%AF/%E4%BA%91%E5%8E%9F%E7%94%9F/k3s/%E5%8E%9F%E7%90%86%E4%B8%8E%E9%80%89%E5%9E%8B/Ingress%E4%B8%8EService%E4%B8%8ETraefik%E5%85%A5%E5%8F%A3%E7%9A%84%E5%85%B3%E7%B3%BB.html"}],["meta",{"property":"og:site_name","content":"开发笔记"}],["meta",{"property":"og:title","content":"Ingress / Service / Traefik 入口的关系"}],["meta",{"property":"og:description","content":"排查「业务入口高可用」时，最容易混淆的就是 Ingress、Service、Traefik 三者的分工。本文厘清它们「各管一层」，并回答几个高频认知盲区。 第七~十节是几个高频提问的实测回答（都带验证命令与输出）： 「一个服务除了 VIP 还有哪些 IP 能访问？」「VIP 会漂移吗？」「VIP 在就说明服务好吗？」 「为什么 Ingress 的 ho..."}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:locale","content":"zh-CN"}],["meta",{"property":"og:updated_time","content":"2026-09-23T08:43:11.000Z"}],["meta",{"property":"article:modified_time","content":"2026-09-23T08:43:11.000Z"}]]},"git":{"createdTime":1788011054000,"updatedTime":1790152991000,"contributors":[{"name":"cql","username":"cql","email":"cql.ql@qq.com","commits":3,"url":"https://github.com/cql"}]},"readingTime":{"minutes":9.28,"words":2784},"filePathRelative":"后端/云原生/k3s/原理与选型/Ingress与Service与Traefik入口的关系.md","autoDesc":true}`),a={name:`Ingress与Service与Traefik入口的关系.md`};function o(r,i,a,o,s,c){return e(),n(`div`,null,[...i[0]||=[t(`<blockquote><p>排查「业务入口高可用」时，最容易混淆的就是 Ingress、Service、Traefik 三者的分工。本文厘清它们「各管一层」，并回答几个高频认知盲区。</p><p>第七~十节是几个<strong>高频提问</strong>的实测回答（都带验证命令与输出）： 「一个服务除了 VIP 还有哪些 IP 能访问？」「VIP 会漂移吗？」「VIP 在就说明服务好吗？」 「为什么 Ingress 的 <code>host</code> 不能写 IP，Service 的 <code>loadBalancerIP</code> 却可以？」</p></blockquote><h2 id="一、traefik-在哪-为什么项目-k8s-目录里看不到它的配置" tabindex="-1"><a class="header-anchor" href="#一、traefik-在哪-为什么项目-k8s-目录里看不到它的配置"><span>一、Traefik 在哪？为什么项目 k8s 目录里看不到它的配置</span></a></h2><p><strong>Traefik 是 K3s 内置并自动部署的</strong>（装 K3s 时一起装），跑在 <code>kube-system</code> 命名空间，<strong>不在你项目的 <code>k8s/</code> 目录里</strong>。</p><p>Traefik 有两个实体：</p><table><thead><tr><th>实体</th><th>位置</th><th>说明</th></tr></thead><tbody><tr><td><strong>Traefik 控制器</strong>（真正干活的那个）</td><td><code>kube-system</code>，Helm/K3s 自动部署</td><td><code>kubectl get deploy -n kube-system traefik</code></td></tr><tr><td><strong>Traefik Service</strong>（对外入口）</td><td><code>kube-system</code></td><td><code>type: LoadBalancer</code>，<code>EXTERNAL-IP</code> 即入口 IP</td></tr></tbody></table><blockquote><p>项目 <code>k8s/base/ingress.yaml</code> 只是<strong>给 Traefik 下达「路由规则」</strong>（&quot;这个 path 转到某个 Service&quot;），<strong>不是在部署 Traefik 本体</strong>。所以「项目里看不到 Traefik 配置文件」是正常的。</p></blockquote><h2 id="二、ingress-与-service-各管一层-不是替代关系" tabindex="-1"><a class="header-anchor" href="#二、ingress-与-service-各管一层-不是替代关系"><span>二、Ingress 与 Service 各管一层，不是替代关系</span></a></h2><table><thead><tr><th></th><th>Service（<code>service.yaml</code>）</th><th>Ingress（<code>ingress.yaml</code>）</th></tr></thead><tbody><tr><td>解决的问题</td><td>「把流量送进某个应用」</td><td>「按规则把流量分发到不同应用」</td></tr><tr><td>本质</td><td>给一组 Pod 提供稳定的访问入口（IP/端口）</td><td>基于 Host/Path 的<strong>七层路由规则</strong></td></tr><tr><td>类比</td><td>房间的「门牌号」</td><td>大楼的「前台/导航」</td></tr><tr><td>能省吗</td><td>❌ 不能（Ingress 最终也要指向 Service）</td><td>视场景而定</td></tr><tr><td>填 IP 的字段</td><td><code>spec.loadBalancerIP</code>（<strong>填 IP</strong>）</td><td><code>spec.rules[].host</code>（<strong>只能填域名</strong>）</td></tr></tbody></table><blockquote><p>最后一行的差异经常被问「为什么 Grafana 能固定 IP、Ingress 却不能」，原因见第十节。</p></blockquote><h3 id="什么时候可以不用-ingress" tabindex="-1"><a class="header-anchor" href="#什么时候可以不用-ingress"><span>什么时候可以不用 Ingress？</span></a></h3><table><thead><tr><th>场景</th><th>需要 Ingress 吗</th></tr></thead><tbody><tr><td><strong>多个服务</strong>共用一个入口（按 host/path 分发）</td><td>✅ 强烈需要</td></tr><tr><td>需要 <strong>TLS/HTTPS 终止</strong>、统一中间件、限流</td><td>✅ 需要</td></tr><tr><td><strong>只有一个服务</strong>，且已有 LoadBalancer VIP 直达</td><td>⚠️ 可不用（但 Ingress 仍带来路由/TLS 统一管理的好处）</td></tr><tr><td>用 NodePort 裸暴露</td><td>用 Ingress 更规范</td></tr></tbody></table><h2 id="三、ingress-不「分到-ip」-高可用靠-traefik-入口" tabindex="-1"><a class="header-anchor" href="#三、ingress-不「分到-ip」-高可用靠-traefik-入口"><span>三、Ingress 不「分到 IP」，高可用靠 Traefik 入口</span></a></h2><p><strong>Ingress 本身没有 IP、没有 Pod</strong>，它只是「一段路由规则」。真正对外提供入口的是 <strong>Ingress Controller（Traefik）</strong>。</p><p>所以「Ingress 分不到 IP，怎么高可用」的正确答案是：<strong>高可用由 Traefik 的入口 VIP 承担，而不是 Ingress 本身</strong>。</p><p>完整链路分两层高可用：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>客户端</span></span>
<span class="line"><span>  ↓</span></span>
<span class="line"><span>Traefik（Ingress Controller，真正的入口）← 入口高可用靠「Traefik 的 VIP 漂移」</span></span>
<span class="line"><span>  ↓</span></span>
<span class="line"><span>Ingress 规则（把请求路由到某个 Service）← 规则无状态，天然无单点</span></span>
<span class="line"><span>  ↓</span></span>
<span class="line"><span>Service → Pod ← 应用高可用靠「多副本 + Service 负载均衡」</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><table><thead><tr><th>层</th><th>高可用靠什么</th></tr></thead><tbody><tr><td>入口（Traefik）</td><td>Traefik 的 <code>LoadBalancer</code> Service 拿到<strong>可漂移的 VIP</strong></td></tr><tr><td>路由（Ingress）</td><td>规则无状态，无需管</td></tr><tr><td>应用（Pod）</td><td>Deployment 多副本 + Service 负载均衡</td></tr></tbody></table><blockquote><p>关键：<strong>Ingress 规则完全不用动</strong>——它指向的是 <code>passup-backend</code> 这个 Service，Service 通过 selector 找 Pod。Traefik 入口漂移后，流量照样经过 Ingress 规则路由到正确的 Service。</p></blockquote><h2 id="四、servicelb-vs-kube-vip-services-易混淆" tabindex="-1"><a class="header-anchor" href="#四、servicelb-vs-kube-vip-services-易混淆"><span>四、ServiceLB vs Kube-vip <code>--services</code>（易混淆）</span></a></h2><p>Traefik 之所以有 EXTERNAL-IP，可能来自<strong>两种不同机制</strong>，务必区分：</p><table><thead><tr><th>机制</th><th>谁提供</th><th>IP 来源</th><th>能否漂移</th></tr></thead><tbody><tr><td><strong>ServiceLB</strong></td><td>K3s 内置（<code>svclb-traefik</code> Pod）</td><td>每个节点的<strong>物理 IP</strong></td><td>❌ 不漂移</td></tr><tr><td><strong>Kube-vip <code>--services</code></strong></td><td>需额外部署独立 Kube-vip</td><td>从地址池分配的<strong>真正 VIP</strong></td><td>✅ 可漂移</td></tr></tbody></table><blockquote><p>如果 Traefik 的 EXTERNAL-IP 是各节点的物理 IP（如 <code>.201</code>/<code>.202</code>，网卡上是 <code>/24</code> 而非 <code>/32</code>），说明走的是 <strong>ServiceLB</strong>，<strong>不具备漂移能力</strong>。只有部署独立 <code>--services</code> Kube-vip，才能拿到真正可漂移的 VIP。</p></blockquote><h2 id="五、traefik-service-能删吗" tabindex="-1"><a class="header-anchor" href="#五、traefik-service-能删吗"><span>五、Traefik Service 能删吗？</span></a></h2><p><strong>能删（K8s 资源都能删），但强烈不建议删，删了会出大问题：</strong></p><ol><li><strong>整个集群入口立刻中断</strong>：所有走 Ingress 的业务流量全断。</li><li><strong>不会自动重建</strong>：Traefik Service 是 Helm release 的一部分，手动 <code>delete</code> 后平时<strong>不会自动恢复</strong>，会一直处于「入口丢失」状态。</li></ol><blockquote><p>⚠️ 区分：之前讨论「删 Service 会导致 VIP 变」是<strong>解释 VIP 生命周期规则</strong>，<strong>不是建议删 Traefik Service</strong>。Traefik Service 是集群的「总大门」，删了等于拆门，楼里所有服务都进不去。</p><p>若真需重建 Traefik 入口，正确做法是走 Helm（<code>helm upgrade</code> 或重新 apply Traefik chart），而非手动 <code>delete svc</code>。</p></blockquote><h2 id="六、完整高可用拼图" tabindex="-1"><a class="header-anchor" href="#六、完整高可用拼图"><span>六、完整高可用拼图</span></a></h2><table><thead><tr><th>环节</th><th>高可用手段</th><th>常见缺口</th></tr></thead><tbody><tr><td>入口（Traefik）</td><td>可漂移 VIP</td><td>⚠️ 常缺（ServiceLB 给的是节点物理 IP，不漂移）</td></tr><tr><td>路由（Ingress）</td><td>规则无状态</td><td>✅ 天然无单点</td></tr><tr><td>应用（Pod）</td><td>多副本 + Service LB</td><td>✅ 通常已具备</td></tr></tbody></table><blockquote><p><strong>补缺口的方法</strong>：部署独立 <code>--services</code> Kube-vip，给 Traefik Service 分配可漂移的固定 VIP，前端统一指向它，即可打通「入口层高可用」这最后一块。</p></blockquote><h2 id="七、一个服务对外到底有几个入口-ip" tabindex="-1"><a class="header-anchor" href="#七、一个服务对外到底有几个入口-ip"><span>七、一个服务对外到底有几个入口 IP</span></a></h2><p>问「除了入口 VIP，还有哪些 IP 能访问？」——答案不是「只有一个」，而是分三类：</p><table><thead><tr><th>地址形式</th><th>例子（本集群）</th><th>说明</th></tr></thead><tbody><tr><td><strong>入口 VIP</strong></td><td><code>http://172.16.0.180/</code></td><td><strong>推荐</strong>。由 Kube-vip 宣告，持有它的节点挂了会自动漂移</td></tr><tr><td><strong>节点 IP + NodePort</strong></td><td><code>http://172.16.0.211:32663/</code></td><td>⚠️ <strong>只有「本节点有该 Service 后端 Pod」的节点才通</strong>，见第九节</td></tr><tr><td>ClusterIP / Pod IP</td><td><code>http://10.43.x.x/</code></td><td>只在集群内可访问</td></tr></tbody></table><div class="language-bash line-numbers-mode" data-highlighter="shiki" data-ext="bash" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-bash"><span class="line"><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;"># 一次性看清入口 VIP 与 NodePort</span></span>
<span class="line"><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">kubectl</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;"> -n</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> kube-system</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> get</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> svc</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> traefik</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;"> -o</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> custom-columns=</span><span style="--shiki-light:#0184BC;--shiki-dark:#56B6C2;">\\</span></span>
<span class="line"><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">&#39;EXTERNAL-IP:.status.loadBalancer.ingress[*].ip,PORT:.spec.ports[*].port,NODEPORT:.spec.ports[*].nodePort&#39;</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><blockquote><p>⚠️ NodePort 的端口号是<strong>自动分配</strong>的，Service 重建后会变。要长期依赖它，必须显式指定 <code>spec.ports[].nodePort</code> 固定下来。<strong>给外部反向代理（如外层 nginx）配上游时，优先指向 VIP 而不是节点 NodePort</strong> —— VIP 会随节点故障漂移，NodePort 绑死在某一台上。</p></blockquote><h2 id="八、vip-会漂移-服务可用" tabindex="-1"><a class="header-anchor" href="#八、vip-会漂移-服务可用"><span>八、VIP 会漂移 ≠ 服务可用</span></a></h2><p><strong>VIP 是二层 ARP 层面的事：它只保证「这个 IP 有人应答」，不保证后面有后端。</strong></p><p>实测（把 Grafana 缩到 0 个副本）：</p><div class="language-bash line-numbers-mode" data-highlighter="shiki" data-ext="bash" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-bash"><span class="line"><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">kubectl</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;"> -n</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> monitoring</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> scale</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> deploy</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> grafana</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;"> --replicas=0</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>Service 的 EXTERNAL-IP  仍是 172.16.0.181     ← 看着一切正常</span></span>
<span class="line"><span>Service 的 vipHost      仍是 k2</span></span>
<span class="line"><span>https://172.16.0.181/api/health  -&gt;  000（连不上）   ← 实际已经不可用</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>所以：</p><ul><li><strong>别用 <code>ping</code> 得通 VIP、或 <code>EXTERNAL-IP</code> 有值来判断服务是否活着</strong>；</li><li>要判断就用<strong>真实业务探测</strong>（例如 <code>/api/health</code>、首页 HTTP 状态码）。</li></ul><p>区分两种「挂」很关键：</p><table><thead><tr><th>挂的是谁</th><th>VIP 行为</th><th>服务是否可用</th></tr></thead><tbody><tr><td><strong>持有 VIP 的那个节点</strong></td><td>漂到别的合格节点</td><td>✅ 可用（漂移就是为了这个）</td></tr><tr><td><strong>真正跑 Pod 的那个节点</strong></td><td>VIP 照样能被别的节点宣告</td><td>❌ <strong>不可用，而且 VIP 看起来还是好的</strong></td></tr></tbody></table><blockquote><p>后者是「单副本 + 本地存储」类组件的固有问题：Pod 被 <code>local-path</code> 的 PV 绑死在某一台节点上， <strong>VIP 漂移救不了它</strong>。缓解手段是让组件本身有第二份（换共享存储），或由集群外的探针兜底。</p></blockquote><h2 id="九、externaltrafficpolicy-决定-vip-能落在哪些节点" tabindex="-1"><a class="header-anchor" href="#九、externaltrafficpolicy-决定-vip-能落在哪些节点"><span>九、<code>externalTrafficPolicy</code>：决定 VIP 能落在哪些节点</span></a></h2><p>同一个集群里，两个 LoadBalancer Service 的漂移能力可能<strong>完全不同</strong>，根因是这个字段：</p><table><thead><tr><th></th><th><code>.180</code>（Traefik）</th><th><code>.181</code>（Grafana）</th></tr></thead><tbody><tr><td><code>externalTrafficPolicy</code></td><td><code>Local</code></td><td><code>Cluster</code>（LoadBalancer 默认值）</td></tr><tr><td>有资格持有 VIP 的节点</td><td>只有<strong>本节点有后端 Pod</strong> 的节点</td><td><strong>任意节点</strong></td></tr><tr><td>本集群实测落点</td><td>k2（Traefik Pod 在 k1、k2）</td><td><strong>k2（而 Grafana Pod 在 k3）</strong></td></tr></tbody></table><div class="language-bash line-numbers-mode" data-highlighter="shiki" data-ext="bash" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-bash"><span class="line"><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;"># 查这两个字段</span></span>
<span class="line"><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">kubectl</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> get</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> svc</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;"> -A</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;"> -o</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> custom-columns=</span><span style="--shiki-light:#0184BC;--shiki-dark:#56B6C2;">\\</span></span>
<span class="line"><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">&#39;NS:.metadata.namespace,NAME:.metadata.name,POLICY:.spec.externalTrafficPolicy,VIPHOST:.metadata.annotations.kube-vip\\.io/vipHost&#39;</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong><code>.181</code> 由 k2 宣告、而 Grafana Pod 在 k3</strong> —— 这本身就证明 <code>Cluster</code> 策略下， 没有本地 Pod 的节点也能持有 VIP。</p><h3 id="由此带来的一个反直觉现象-某些节点的-nodeport-不通" tabindex="-1"><a class="header-anchor" href="#由此带来的一个反直觉现象-某些节点的-nodeport-不通"><span>由此带来的一个反直觉现象：某些节点的 NodePort 不通</span></a></h3><p><code>Local</code> 策略的语义是「<strong>只把流量转给本节点上的 Pod</strong>」。所以一个节点上如果<strong>没有该 Service 的 Pod</strong>， 打到它的 NodePort 流量会<strong>直接被丢弃</strong>（表现为连接超时/拒绝，<strong>不是 502</strong>）。</p><p>本集群实测（各试 3 次）：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>172.16.0.211:32663   3/3 成功     ← k1 有 Traefik Pod</span></span>
<span class="line"><span>172.16.0.212:32663   3/3 成功     ← k2 有 Traefik Pod</span></span>
<span class="line"><span>172.16.0.213:32663   0/3 成功     ← k3 没有 Traefik Pod</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><blockquote><p>⚠️ <strong>别把这个误判成「k3 挂了」。</strong> 同一时刻 k3 的 <code>6443</code> 正常返回 401、节点状态 <code>Ready</code>、 上面还跑着别的 Pod —— 只是这个 Service 的 NodePort 在它上面不工作。 判断节点是否健康要看节点本身，不要看某个 Service 的 NodePort。</p></blockquote><div class="language-bash line-numbers-mode" data-highlighter="shiki" data-ext="bash" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-bash"><span class="line"><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;"># 谁有后端 Pod（决定 Local 策略下哪些节点能用）</span></span>
<span class="line"><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">kubectl</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;"> -n</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;"> &lt;</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">n</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">s&gt; </span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">get</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> endpoints</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;"> &lt;</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">sv</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">c&gt; </span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;">-o</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> jsonpath=&#39;{range .subsets[*].addresses[*]}{.ip}  node={.nodeName}{&quot;\\n&quot;}{end}&#39;</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="十、为什么-ingress-的-host-不能写-ip-service-的-loadbalancerip-却可以" tabindex="-1"><a class="header-anchor" href="#十、为什么-ingress-的-host-不能写-ip-service-的-loadbalancerip-却可以"><span>十、为什么 Ingress 的 <code>host</code> 不能写 IP，Service 的 <code>loadBalancerIP</code> 却可以</span></a></h2><p>同一个 IP，一个能写、一个不能，实测对比：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>Service.spec.loadBalancerIP: 172.16.0.199                -&gt; created (server dry run)   ✅</span></span>
<span class="line"><span>Service 注解 kube-vip.io/loadbalancerIPs: &quot;172.16.0.199&quot; -&gt; created (server dry run)   ✅</span></span>
<span class="line"><span>Ingress.spec.rules[].host: 172.16.0.199                  -&gt; must be a DNS name, not an IP address  ❌</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><table><thead><tr><th></th><th>Service 的 <code>loadBalancerIP</code></th><th>Ingress 的 <code>host</code></th></tr></thead><tbody><tr><td><strong>语义</strong></td><td><strong>给这个 Service 分配一个地址</strong></td><td><strong>匹配 Host 头等于它的请求</strong></td></tr><tr><td>层次</td><td>L4（TCP 转发）</td><td>L7（HTTP 路由）</td></tr><tr><td>该填什么</td><td><strong>就是 IP</strong></td><td><strong>必须是域名</strong></td></tr></tbody></table><ul><li><strong>Service 能写 IP</strong>：<code>LoadBalancer</code> 的语义就是「给我一个能从集群外访问的地址」，那<strong>本来就是 IP</strong>； Kube-vip 读到注解后用它做 ARP 宣告。</li><li><strong>Ingress 必须写域名</strong>：<code>host</code> <strong>不是地址，是「拿请求的 <code>Host:</code> 头去比对的条件」</strong>。 K8s 认为按 IP 分流没有意义（一个 IP 直接挂 Service 就行，何必再过一层 L7），所以只允许域名 —— Ingress 的用途本来就是「按域名分流」。</li></ul><blockquote><p>一句话：<strong>Grafana 那个 IP 是「地址」，Ingress 那个 host 是「匹配规则」。</strong></p></blockquote><h3 id="那想固定怎么办" tabindex="-1"><a class="header-anchor" href="#那想固定怎么办"><span>那想固定怎么办</span></a></h3><table><thead><tr><th>做法</th><th>可行性</th></tr></thead><tbody><tr><td>Ingress 的 <code>host</code> 写 IP</td><td>❌ API 层直接拒绝</td></tr><tr><td>换 Traefik 的 <code>IngressRoute</code> CRD（<code>match: Host(...)</code> 是自由字符串）</td><td>⚠️ 能通过校验，但<strong>不建议</strong>：绑定了外层代理必须发 <code>Host: &lt;该 IP&gt;</code>，否则整站 404 而 Traefik 只回 404 不报错；且会让后端看到错误的 Host</td></tr><tr><td>给 Service 加 <code>loadBalancerIP</code> / Kube-vip 注解</td><td>✅ 这才是「固定入口 IP」的正解（L4 直达，不需要域名）</td></tr></tbody></table><blockquote><p>走 Service LoadBalancer 的代价是<strong>失去路径分流</strong>（L4 不做路径路由）。 多个站点要挂在同一个入口下按 <code>/a</code>、<code>/b</code> 分流时，还是得用 Ingress，并给它配一个<strong>真实域名</strong>。</p></blockquote><h2 id="十一、排查口诀" tabindex="-1"><a class="header-anchor" href="#十一、排查口诀"><span>十一、排查口诀</span></a></h2><blockquote><p><strong>入口 404 / 连不上</strong>：先确认<strong>直连 Pod/ClusterIP 是否通</strong>（判断后端是否健康）→ 看 <strong>Traefik 日志</strong>（判断是路由失效还是入口问题）→ 检查 <strong>Ingress 注解</strong>（尤其 middleware 引用）→ 确认<strong>入口 IP 是 ServiceLB 物理 IP 还是可漂移 VIP</strong>。</p></blockquote><p>几条针对本文新问题的口诀：</p><blockquote><p><strong>「VIP 通」不等于「服务好」</strong>：<code>ping</code> 得通 VIP、<code>EXTERNAL-IP</code> 有值，都只说明「这个 IP 有人应答」。 判断服务死活要用<strong>业务探测</strong>（<code>/api/health</code>、首页状态码）。</p></blockquote><blockquote><p><strong>某个节点的 NodePort 不通</strong>：先看该 Service 的 <code>externalTrafficPolicy</code>。 是 <code>Local</code> 且这个节点上没有它的 Pod → <strong>流量会被丢弃，这是设计行为</strong>，不是节点故障。 用 <code>kubectl get endpoints</code> 确认哪些节点有后端。</p></blockquote><blockquote><p><strong>「单实例 + 本地存储」的组件，VIP 漂移救不了它</strong>：Pod 被 PV 绑死在某一台， 那台一挂，VIP 还能被别的节点宣告，但后面已经没有后端。要么给组件第二份，要么靠集群外探针兜底。</p></blockquote><blockquote><p><strong>入口要固定 IP</strong>：别在 Ingress 的 <code>host</code> 上想办法（只能填域名）。 要么给 Service 配 <code>loadBalancerIP</code>（L4 直达），要么给 Ingress 配真实域名。</p></blockquote>`,71)]])}var s=r(a,[[`render`,o]]);export{i as _pageData,s as default};