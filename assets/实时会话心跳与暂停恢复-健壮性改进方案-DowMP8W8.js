import{A as e,D as t,L as n,f as r,m as i,p as a,s as o,u as s}from"./runtime-core.esm-bundler-BHJ3o1LK.js";import{t as c}from"./plugin-vue_export-helper-BDNMzG2s.js";var l=JSON.parse(`{"path":"/%E9%A1%B9%E7%9B%AE/%E9%9D%A2%E8%AF%95%E5%8A%A9%E6%89%8B/%E6%9E%B6%E6%9E%84%E8%AE%BE%E8%AE%A1/%E5%AE%9E%E6%97%B6%E4%BC%9A%E8%AF%9D%E5%BF%83%E8%B7%B3%E4%B8%8E%E6%9A%82%E5%81%9C%E6%81%A2%E5%A4%8D-%E5%81%A5%E5%A3%AE%E6%80%A7%E6%94%B9%E8%BF%9B%E6%96%B9%E6%A1%88.html","title":"实时会话心跳与暂停恢复-健壮性改进方案","lang":"zh-CN","frontmatter":{"title":"实时会话心跳与暂停恢复-健壮性改进方案","icon":"mdi:heart-pulse","sort":120,"description":"实时会话心跳与暂停恢复：健壮性改进方案 文档定位：问题记录 + 目标架构 + 落地记录。 记录 pass-up.backend 实时会话链路中识别到的健壮性改进项，给出最终架构边界，并保留每一项的落地情况与验证证据。 所有&quot;现状&quot;均以当时代码为准；每项都标注了「已实施 / 已证伪」，便于回溯当时的判断依据。 文档状态：本文记录的全部...","head":[["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"实时会话心跳与暂停恢复-健壮性改进方案\\",\\"image\\":[\\"\\"],\\"dateModified\\":\\"2026-09-21T09:34:07.000Z\\",\\"author\\":[{\\"@type\\":\\"Person\\",\\"name\\":\\"桥黎\\",\\"url\\":\\"\\"}]}"],["meta",{"property":"og:url","content":"https://docs.cqlql.top/%E9%A1%B9%E7%9B%AE/%E9%9D%A2%E8%AF%95%E5%8A%A9%E6%89%8B/%E6%9E%B6%E6%9E%84%E8%AE%BE%E8%AE%A1/%E5%AE%9E%E6%97%B6%E4%BC%9A%E8%AF%9D%E5%BF%83%E8%B7%B3%E4%B8%8E%E6%9A%82%E5%81%9C%E6%81%A2%E5%A4%8D-%E5%81%A5%E5%A3%AE%E6%80%A7%E6%94%B9%E8%BF%9B%E6%96%B9%E6%A1%88.html"}],["meta",{"property":"og:site_name","content":"开发笔记"}],["meta",{"property":"og:title","content":"实时会话心跳与暂停恢复-健壮性改进方案"}],["meta",{"property":"og:description","content":"实时会话心跳与暂停恢复：健壮性改进方案 文档定位：问题记录 + 目标架构 + 落地记录。 记录 pass-up.backend 实时会话链路中识别到的健壮性改进项，给出最终架构边界，并保留每一项的落地情况与验证证据。 所有&quot;现状&quot;均以当时代码为准；每项都标注了「已实施 / 已证伪」，便于回溯当时的判断依据。 文档状态：本文记录的全部..."}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:locale","content":"zh-CN"}],["meta",{"property":"og:updated_time","content":"2026-09-21T09:34:07.000Z"}],["meta",{"property":"article:modified_time","content":"2026-09-21T09:34:07.000Z"}]]},"git":{"createdTime":1789743078000,"updatedTime":1789983247000,"contributors":[{"name":"cql","username":"cql","email":"cql.ql@qq.com","commits":2,"url":"https://github.com/cql"}]},"readingTime":{"minutes":36.21,"words":10862},"filePathRelative":"项目/面试助手/架构设计/实时会话心跳与暂停恢复-健壮性改进方案.md","autoDesc":true}`),u={name:`实时会话心跳与暂停恢复-健壮性改进方案.md`};function d(c,l,u,d,f,p){let m=e(`RouteLink`);return t(),s(`div`,null,[l[7]||=r(`<h1 id="实时会话心跳与暂停恢复-健壮性改进方案" tabindex="-1"><a class="header-anchor" href="#实时会话心跳与暂停恢复-健壮性改进方案"><span>实时会话心跳与暂停恢复：健壮性改进方案</span></a></h1><blockquote><p><strong>文档定位：问题记录 + 目标架构 + 落地记录。</strong> 记录 <code>pass-up.backend</code> 实时会话链路中识别到的健壮性改进项，给出最终架构边界，并保留每一项的<strong>落地情况与验证证据</strong>。 所有&quot;现状&quot;均以当时代码为准；每项都标注了「已实施 / 已证伪」，便于回溯当时的判断依据。</p><p><strong>文档状态：本文记录的全部事项已闭环</strong>（P0 / P0.1 / P1 / P2 含前端 / P2.1 / P3 / 测试与 CI）。 其中 P0 经历过一次修正 —— 本文早期版本把它判为「当前正在发生的缺陷」，<strong>已用 curl 端到端实测证伪</strong> （正常路径 cleanup 是收敛的）；它现在记录的是「判断错在哪、以及剩下哪个真实边界」，并已修复。</p></blockquote><h2 id="_1-背景与范围" tabindex="-1"><a class="header-anchor" href="#_1-背景与范围"><span>1. 背景与范围</span></a></h2><p>实时面试会话（<code>/ws/audio</code>）当前具备两项自愈能力：</p><ol><li><strong>ASR 内层重连</strong>：豆包 ASR 连接中断后由 <code>AudioStreamSession#consumeLoop</code> 自动重建，对前端无感。</li><li><strong>暂停 / 逻辑会话重连</strong>：用户主动暂停或断线后，逻辑会话（<code>InterviewSessionState</code>）保留 15 分钟，前端凭 <code>resume=&lt;逻辑会话ID&gt;</code> 重连复用。</li></ol><p>本文档的核心不是&quot;把某个超时从 5 分钟改成几分钟&quot;，而是要把下面<strong>四件目前被耦合在一起的事情彻底解耦</strong>：</p><table><thead><tr><th style="text-align:left;">关注点</th><th style="text-align:left;">回答的问题</th><th style="text-align:left;">改造前载体</th><th style="text-align:left;">现状</th></tr></thead><tbody><tr><td style="text-align:left;"><strong>连接存活检测</strong></td><td style="text-align:left;">客户端还活着吗？</td><td style="text-align:left;">寄生在业务消息上的 <code>lastHeartbeatTime</code></td><td style="text-align:left;">✅ 独立心跳 + 音频兜底，自适应（P2）</td></tr><tr><td style="text-align:left;"><strong>逻辑会话生命周期</strong></td><td style="text-align:left;">这场面试（逻辑上）还在进行吗？</td><td style="text-align:left;">JVM <code>logicalSessions</code></td><td style="text-align:left;">✅ 内存优先 + DB 兜底重建（P1）</td></tr><tr><td style="text-align:left;"><strong>AI 上下文恢复</strong></td><td style="text-align:left;">重连后 AI 还能接着上一句说吗？</td><td style="text-align:left;">JVM <code>InterviewSessionState#context</code></td><td style="text-align:left;">✅ 内存优先 + 从 <code>interview_message</code> 恢复（P1）</td></tr><tr><td style="text-align:left;"><strong>计费时间轴</strong></td><td style="text-align:left;">该扣多少秒？</td><td style="text-align:left;">Redis <code>lastHeartbeatTime</code>（扣费基准）</td><td style="text-align:left;">✅ 与连接活跃度彻底分开（P2 / P2.1）</td></tr></tbody></table><p>四者混用同一套时间戳/内存结构，是本文多数问题的根源；P2 / P2.1 已把「连接存活」与「计费」拆开，P1 负责剩下的「逻辑会话生命周期」与「AI 上下文恢复」。</p><h2 id="_2-现状速览" tabindex="-1"><a class="header-anchor" href="#_2-现状速览"><span>2. 现状速览</span></a></h2><blockquote><p>下表为<strong>改造前</strong>的取值，便于对照；改造后的取值见各 P 项。</p></blockquote><table><thead><tr><th style="text-align:left;">机制</th><th style="text-align:left;">代码位置</th><th style="text-align:left;">改造前</th><th style="text-align:left;">现状</th></tr></thead><tbody><tr><td style="text-align:left;">连接失联阈值（老客户端 / 音频判据）</td><td style="text-align:left;"><code>AudioStreamSession#AUDIO_IDLE_TIMEOUT_MS</code></td><td style="text-align:left;">5 分钟</td><td style="text-align:left;">5 分钟（不变，见 P2）</td></tr><tr><td style="text-align:left;">连接失联阈值（声明心跳的客户端）</td><td style="text-align:left;"><code>AudioStreamSession#HEARTBEAT_TIMEOUT_MS</code></td><td style="text-align:left;">—</td><td style="text-align:left;"><strong>60 秒</strong>（P2 新增）</td></tr><tr><td style="text-align:left;">心跳扫描周期</td><td style="text-align:left;"><code>WsHeartbeatScheduler</code> <code>@Scheduled(fixedDelay)</code></td><td style="text-align:left;">30 秒</td><td style="text-align:left;"><strong>10 秒</strong>（P2）</td></tr><tr><td style="text-align:left;">暂停会话保留时长</td><td style="text-align:left;"><code>AudioStreamingService#PAUSED_TTL_MS</code></td><td style="text-align:left;">15 分钟</td><td style="text-align:left;">15 分钟</td></tr><tr><td style="text-align:left;">pending 面试回收时长</td><td style="text-align:left;"><code>AudioStreamingService#PENDING_TTL_MS</code></td><td style="text-align:left;">3 分钟</td><td style="text-align:left;">3 分钟</td></tr><tr><td style="text-align:left;">ASR 重连参数</td><td style="text-align:left;"><code>AudioStreamSession</code></td><td style="text-align:left;">3 次 / 500ms~3s 退避</td><td style="text-align:left;">不变</td></tr><tr><td style="text-align:left;">计费&quot;失联&quot;阈值</td><td style="text-align:left;"><code>InterviewBillingScheduler#SILENCE_TIMEOUT_MS</code></td><td style="text-align:left;">30 秒（语义错误）</td><td style="text-align:left;"><strong>已删除</strong>，改用 <code>isConnectionLost()</code>（P2）</td></tr></tbody></table><p>关键事实（改造前）：</p><ul><li>心跳<strong>不是独立 ping/pong</strong>，而是&quot;寄生&quot;在业务消息上：仅 <code>onOpen</code> / <code>receiveAudio</code> / <code>receiveText</code> 刷新 <code>AudioStreamSession#lastHeartbeatTime</code>（<code>AudioStreamingService</code> 中统一调用 <code>refreshHeartbeat</code>）。</li><li>逻辑会话 <code>logicalSessions</code> 是 <code>AudioStreamingService</code> 内的 <code>ConcurrentHashMap</code>，<strong>纯 JVM 内存</strong>，不落库。</li><li>但<strong>对话内容本身是落库的</strong>：<code>interview_message</code> 表按轮次全量记录 user/assistant 消息（异步写入），只是当前没有任何代码路径用它重建 AI 上下文。详见 P1「DB 现有的可重建原料」。</li><li>逻辑会话与物理连接<strong>已经是两个对象</strong>：<code>InterviewSessionState</code>（逻辑，跨暂停存活）与 <code>AudioStreamSession</code>（物理，一次连接一个），二者通过 <code>InterviewSessionState#currentConnectionId</code> 关联。这为后续拆分提供了现成基础。</li></ul><hr><h2 id="_3-待改进项" tabindex="-1"><a class="header-anchor" href="#_3-待改进项"><span>3. 待改进项</span></a></h2><blockquote><p>排序说明：P0 与 P0.1 是「排查中发现的缺陷」（P0 经实测证伪）；P1 / P2 是架构级健壮性改造；P3 是工程治理。<strong>均已完成。</strong></p></blockquote><h3 id="p0-强踢旧连接可能绕过统一-cleanup-——-实测证伪-仅剩边界场景" tabindex="-1"><a class="header-anchor" href="#p0-强踢旧连接可能绕过统一-cleanup-——-实测证伪-仅剩边界场景"><span>P0｜强踢旧连接可能绕过统一 cleanup —— <strong>实测证伪，仅剩边界场景</strong></span></a></h3><blockquote><p><strong>结论先行：本文早期版本把这条判为&quot;当前正在发生的 bug&quot;，curl 端到端实测后已证伪。</strong> 正常路径下 cleanup 是收敛的。剩下的只是一个窄边界场景，且已修复。</p></blockquote><p><strong>曾经的判断（错误）</strong></p><p><code>AudioStreamingService#onOpen</code> 强踢旧连接时，<code>close()</code> 之后紧跟 <code>streamSessions.remove(oldConnectionId)</code>：</p><div class="language-java line-numbers-mode" data-highlighter="shiki" data-ext="java" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-java"><span class="line"><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">close</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">(</span><span style="--shiki-light:#E45649;--shiki-dark:#E5C07B;">oldStreamSession</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">getSession</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">(),</span><span style="--shiki-light:#E45649;--shiki-dark:#E5C07B;"> CloseStatus</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#E45649;--shiki-dark:#E5C07B;">POLICY_VIOLATION</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">)</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">;</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E5C07B;">streamSessions</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">remove</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">(oldConnectionId);</span><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">   // 原判断：这一行会让后续回调拿不到对象</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>推理依据是 <code>AudioStreamingService#onClose</code> 依赖 <code>streamSessions.remove()</code> 的返回值做守卫：</p><div class="language-java line-numbers-mode" data-highlighter="shiki" data-ext="java" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-java"><span class="line"><span style="--shiki-light:#C18401;--shiki-dark:#E5C07B;">AudioStreamSession</span><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;"> streamingService </span><span style="--shiki-light:#383A42;--shiki-dark:#56B6C2;">=</span><span style="--shiki-light:#E45649;--shiki-dark:#E5C07B;"> streamSessions</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">remove</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">(</span><span style="--shiki-light:#E45649;--shiki-dark:#E5C07B;">session</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">getId</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">());</span></span>
<span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">if</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;"> (streamingService </span><span style="--shiki-light:#383A42;--shiki-dark:#56B6C2;">!=</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;"> null</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">) {</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E5C07B;">    streamingService</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">onClose</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">();</span><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">          // 只有 remove 拿到对象时才做业务清理</span></span>
<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">}</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>于是推出：先 remove → 回调拿到 <code>null</code> → <code>AudioStreamSession#onClose()</code> 永不执行 → 不发 <code>InterviewEndedEvent</code> → <code>interview</code> 悬挂 <code>active</code>。</p><p><strong>实测结果：不成立</strong></p><p>用 curl 建立两条 WebSocket 连接（A 活跃 + 手动写入 Redis 计费会话，再用 B 触发强踢）实测日志：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>12:05:20.550 [mcat-handler-5] 正在强踢旧会话 logicalSessionId=e3717fdd..., connectionId=e3717fdd...</span></span>
<span class="line"><span>12:05:20.550 [mcat-handler-5] 主动关闭: sessionId=e3717fdd..., status=CloseStatus[code=1008]</span></span>
<span class="line"><span>12:05:20.555 [mcat-handler-5] [音频会话] 会话关闭: connectionId=e3717fdd...   ← onClose 执行了</span></span>
<span class="line"><span>12:05:20.556 [mcat-handler-5] 已关闭: sessionId=e3717fdd...</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>同一个线程（<code>mcat-handler-5</code>）</strong>、<code>close()</code> 之后 5ms 内，<code>AudioStreamSession#onClose()</code> 就已经跑完了。原因是：<strong>Tomcat 会在 <code>session.close(status)</code> 内部同步回调 <code>afterConnectionClosed</code></strong>，也就是回调发生在 <code>streamSessions.remove(oldConnectionId)</code> 这一行<strong>之前</strong>。等 onOpen 执行到那行时，条目早已被回调摘掉，<code>remove</code> 是个 no-op。</p><p><strong>因此</strong>：<code>streamSessions.remove(oldConnectionId)</code> 在正常路径下是冗余代码，而不是&quot;抢先摘除&quot;。原判断把「异步回调」当成了前提，实际是同步的。</p><p><strong>残留的真实边界（这才是该修的部分）</strong></p><p><code>close()</code> 有两种情况不会触发回调，此时那行 <code>remove</code> 就真的变成了&quot;抢先摘除&quot;：</p><table><thead><tr><th style="text-align:left;">情况</th><th style="text-align:left;">代码路径</th><th style="text-align:left;">后果</th></tr></thead><tbody><tr><td style="text-align:left;"><code>session.close()</code> 抛异常</td><td style="text-align:left;"><code>close()</code> 内 catch 掉，继续执行 <code>remove</code></td><td style="text-align:left;">条目被摘掉，回调即使稍后到达也拿不到对象</td></tr><tr><td style="text-align:left;"><code>session.isOpen()</code> 为 false</td><td style="text-align:left;"><code>close()</code> 直接 no-op，继续执行 <code>remove</code></td><td style="text-align:left;">同上</td></tr></tbody></table><p>一旦条目被摘掉，<strong>心跳扫描（<code>evictDeadSessions</code> 遍历 <code>streamSessions</code>）也再也扫不到它</strong>，于是：</p><ol><li><code>logicalSessions</code> 永久残留一条 <strong>ACTIVE</strong> 记录（<code>evictExpiredPausedSessions()</code> 只回收 <code>PAUSED</code>），直到进程重启；</li><li><code>interview</code> 停在 <code>active</code>（<code>forceTerminateActiveSession</code> 只结计费 + 删 Redis key，<strong>不发布 <code>InterviewEndedEvent</code></strong>，所以 <code>endInterview()</code> 不会被调用）。</li></ol><p><strong>已实施的修复（1 行）</strong></p><p>删除那行 <code>remove</code>，只保留&quot;请求关闭&quot;：</p><div class="language-java line-numbers-mode" data-highlighter="shiki" data-ext="java" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-java"><span class="line"><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">// 只「请求关闭」，不在此处摘除 streamSessions：</span></span>
<span class="line"><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">// 容器会在 close() 内部同步回调 afterConnectionClosed → onClose()，由它统一完成</span></span>
<span class="line"><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">// 「摘除注册表 + 业务清理」。若在此抢先 remove，一旦 close 抛异常 / 连接已关闭导致</span></span>
<span class="line"><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">// 回调不到达，条目已被摘掉，心跳扫描就再也扫不到它。</span></span>
<span class="line"><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">close</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">(</span><span style="--shiki-light:#E45649;--shiki-dark:#E5C07B;">oldStreamSession</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">getSession</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">(),</span><span style="--shiki-light:#E45649;--shiki-dark:#E5C07B;"> CloseStatus</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#E45649;--shiki-dark:#E5C07B;">POLICY_VIOLATION</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">)</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">;</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>修复后语义：<strong>onOpen 只负责请求关闭；摘除与业务清理统一交给关闭回调；回调没来就由心跳扫描兜底。</strong> 这正是 §4 原则一要的形态，且不依赖&quot;回调是同步的&quot;这个实现细节。</p><p><strong>验证</strong></p><ul><li>新增单测 <code>onOpen_踢出旧会话时close失败_旧会话必须留在注册表以便心跳兜底回收</code>：把该行加回去 → 用例失败；删除 → 用例通过（已实测双向验证）。</li><li>端到端重跑：修复前后正常路径行为一致（A 的 <code>onClose()</code> 均执行，<code>interview</code> 均被正确清理），无回归。</li></ul><p><strong>顺带发现的既存问题：单元测试整体失配（已全部修复）</strong></p><p>排查 P0 时发现测试套件长期处于红灯状态（<strong>79 用例 / 6 失败 / 18 错误</strong>），根因是三类「测试没跟上生产代码演进」：</p><table><thead><tr><th style="text-align:left;">测试类</th><th style="text-align:left;">失配原因</th><th style="text-align:left;">修复</th></tr></thead><tbody><tr><td style="text-align:left;"><code>AudioStreamingServiceTest</code></td><td style="text-align:left;">「物理 / 逻辑会话 ID 分离」重构后：缺 <code>InterviewPrepareService</code> / <code>InterviewService</code> / <code>InterviewRepository</code> / <code>ApplicationEventPublisher</code> 四个 mock（<code>onOpen</code> 以 <code>INTERVIEW_PREPARE_FAILED</code> 提前返回）；强踢用例只往 <code>streamSessions</code> 放会话、没往 <code>logicalSessions</code> 放，而 <code>resolveActiveConnectionId()</code> 只查 <code>logicalSessions</code></td><td style="text-align:left;">补齐 mock + <code>putLogicalSession</code> 辅助方法；21/21</td></tr><tr><td style="text-align:left;"><code>BillingServiceTest</code></td><td style="text-align:left;">账本从「单级 <code>decreaseBalance</code>」演进为「<strong>两级扣减</strong>」（礼包批次 FIFO → 聚合余额 native SQL），测试仍在 stub 已不再调用的 <code>decreaseBalance</code></td><td style="text-align:left;">按两级扣减重写，补 FIFO 优先级 / 批次流水带 batchId / 无限资格放行等用例；18/18</td></tr><tr><td style="text-align:left;"><code>InterviewBillingServiceTest</code></td><td style="text-align:left;">扣费入口由 <code>consumeAsset</code> 改为 <code>tryConsume</code>（走无限资格豁免），且秒数由「截断」改为「<strong>四舍五入</strong>」</td><td style="text-align:left;">改断言 + 新增四舍五入边界用例；16/16</td></tr><tr><td style="text-align:left;"><code>PassUpApplicationTests</code> / <code>InterviewBillingIntegrationTest</code> / <code>AudioStreamingWebSocketIntegrationTest</code></td><td style="text-align:left;"><code>application-test.yml</code> 缺 <code>@NotBlank</code> 占位值（<code>security.refresh.hash-secret</code>、<code>doubao.realtime.api-key</code>、<code>doubao.llm.api-key</code>），且 datasource 无口令；另需本地存在 <code>pass_up_test</code> 库</td><td style="text-align:left;">补占位值 + 口令改为 <code>\${SPRING_DATASOURCE_PASSWORD:123456}</code>；3 个类全绿</td></tr></tbody></table><p><strong>现状：<code>mvn test</code> → 85 用例 / 0 失败 / 0 错误。</strong></p><p><strong>根因是流程而非代码</strong>：<code>.gitea/workflows/deploy.yml</code> 只在打 tag 时构建镜像，且构建命令带 <code>-DskipTests</code> —— <strong>测试从未在流水线里跑过</strong>，所以这套红灯能长期存在而无人发现。已新增 <code>.gitea/workflows/test.yml</code>：每次 push / PR 用 <code>maven:3.9-eclipse-temurin-25</code> 容器（与 <code>docker/Dockerfile</code> 同一构建镜像）跑 <code>mvn test</code>，postgres / redis 走 <code>services:</code>，连接串用环境变量覆盖 <code>application-test.yml</code>。</p><blockquote><p>验证边界：<code>mvn -B -s docker/maven-settings.xml test</code> 已在<strong>同一个容器镜像</strong>内实测通过（85/0/0），包括从<strong>空库</strong>启动由 Flyway 完成迁移的场景。Gitea Actions 的 <code>container:</code> / <code>services:</code> 编排本身未在本机跑过（无 runner 访问），首次上线需观察一次。</p></blockquote><p><strong>P0.1｜重连失败会留下「无物理连接的 ACTIVE」逻辑会话（已修）</strong></p><p>原判断里「<code>logicalSessions</code> 的 ACTIVE 僵尸没有任何回收路径」这半句是对的，顺着它往下追，找到了确切触发路径：</p><div class="language-java line-numbers-mode" data-highlighter="shiki" data-ext="java" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-java"><span class="line"><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">// onOpen 重连分支</span></span>
<span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">try</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;"> {</span></span>
<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">    streamSession </span><span style="--shiki-light:#383A42;--shiki-dark:#56B6C2;">=</span><span style="--shiki-light:#E45649;--shiki-dark:#E5C07B;"> sessionFactory</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">create</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">(session, listener, interviewContext, resumeState);</span></span>
<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">} </span><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">catch</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;"> (</span><span style="--shiki-light:#C18401;--shiki-dark:#E5C07B;">RuntimeException</span><span style="--shiki-light:#383A42;--shiki-light-font-style:inherit;--shiki-dark:#E06C75;--shiki-dark-font-style:italic;"> e</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">) {</span></span>
<span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">    if</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;"> (resumeState </span><span style="--shiki-light:#383A42;--shiki-dark:#56B6C2;">==</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;"> null</span><span style="--shiki-light:#383A42;--shiki-dark:#56B6C2;"> &amp;&amp;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;"> ...</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">) { </span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">endInterview</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">(</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">...</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">)</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">;</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;"> }   </span><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">// ← 重连路径不回滚</span></span>
<span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">    throw</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;"> e</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">;</span></span>
<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">}</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>失败点在 <code>AudioStreamSession</code> 构造函数<strong>内部</strong>，此时它已经执行过 <code>state.markActive()</code> + <code>state.bindConnection(新连接 ID)</code>，但该连接从未 <code>put</code> 进 <code>streamSessions</code>。触发条件不是臆想：构造函数里的 <code>publishResumed()</code> → <code>resumeBilling()</code> 要打 Redis，<strong>Redis 抖动 / 连接池耗尽</strong>即抛异常；executor 已关闭时 <code>submit()</code> 也会被拒。</p><p>一旦发生，逻辑会话停在 <strong>ACTIVE</strong> 且 <code>currentConnectionId</code> 指向一个不存在的连接：</p><table><thead><tr><th style="text-align:left;">回收路径</th><th style="text-align:left;">为什么扫不到</th></tr></thead><tbody><tr><td style="text-align:left;"><code>evictDeadSessions()</code></td><td style="text-align:left;">遍历 <code>streamSessions</code>，而它不在里面</td></tr><tr><td style="text-align:left;"><code>evictExpiredPausedSessions()</code></td><td style="text-align:left;">要求 <code>isPaused()</code>，它是 ACTIVE</td></tr></tbody></table><p>→ 永久残留到进程重启（前端若用同一个 <code>resume=</code> 再连一次可以自愈，但用户不回来就一直占着）。</p><p><strong>修复</strong>：<code>catch</code> 块里对重连路径补回滚 —— <code>resumeState.markPaused()</code>。<code>markPaused()</code> 只允许 <code>ACTIVE → PAUSED</code>，同时解绑悬空连接并重置计时，于是回到暂停 TTL 的回收范围内。</p><p><strong>验证</strong>：新增单测 <code>onOpen_重连时物理会话创建失败_逻辑会话必须退回PAUSED待TTL回收</code>，桩件里复现构造函数已做的 <code>markActive()</code> + <code>bindConnection()</code> 再抛异常；<strong>不加修复 → 断言失败（<code>expected: &lt;true&gt; but was: &lt;false&gt;</code>）；加上 → 通过</strong>（双向实测）。</p><p><strong>另外一点仍成立</strong>：<code>forceTerminateActiveSession</code> 不发布 <code>InterviewEndedEvent</code> —— 它只做「最后一次扣费 + 删 Redis key」。这正是上面边界场景会导致 <code>interview</code> 悬挂的原因。</p><p><strong>可复用的既有资产（留给 P1 / §4 原则三）</strong></p><p><code>connectionId</code> 守卫这个模式<strong>项目里已经写过了</strong> —— <code>InterviewBillingService#stopBilling</code>：</p><div class="language-java line-numbers-mode" data-highlighter="shiki" data-ext="java" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-java"><span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">if</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;"> (</span><span style="--shiki-light:#383A42;--shiki-dark:#56B6C2;">!</span><span style="--shiki-light:#E45649;--shiki-dark:#E5C07B;">currentSessionId</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">equals</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">(</span><span style="--shiki-light:#E45649;--shiki-dark:#E5C07B;">sessionInfo</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">getSessionId</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">())</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">) {</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E5C07B;">    log</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">info</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">(</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">&quot;[面试计费] 跳过清理: 检测到新会话已覆盖，旧会话不予错杀...&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">);</span></span>
<span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">    return</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">;</span></span>
<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">}</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>会话层若要做同类守卫（例如防旧连接 <code>onClose</code> 误杀 resume 后的新连接），照搬即可，不需要新设计。</p><p><strong>涉及文件</strong>：<code>AudioStreamingService#onOpen</code>（已改）、<code>AudioStreamingServiceTest</code>（已修）</p><hr><h3 id="p1-逻辑会话仅存在-jvm-内存-进程重启后无法恢复" tabindex="-1"><a class="header-anchor" href="#p1-逻辑会话仅存在-jvm-内存-进程重启后无法恢复"><span>P1｜逻辑会话仅存在 JVM 内存，进程重启后无法恢复</span></a></h3><blockquote><p>原标题为&quot;暂停会话为纯内存态&quot;，不够准确。真正的问题不是&quot;暂停&quot;这个动作，而是 <code>logicalSessions = ConcurrentHashMap</code> —— <strong>只要 <code>InterviewSessionState</code> 在 JVM 内，无论 ACTIVE 还是 PAUSED，它本质都是运行态。</strong></p></blockquote><p><strong>现状</strong></p><ul><li>逻辑会话（含 <code>systemPrompt</code>、对话 <code>context</code>、<code>interviewId</code>、<code>resume/job</code> 快照引用）只存在于 <code>AudioStreamingService#logicalSessions</code>（内存）。</li><li>暂停只落状态：<code>InterviewRepository#pauseInterview</code> 仅把 <code>interview.status</code> 置为 <code>paused</code>，<strong>不写任何上下文</strong>。</li><li>但<strong>对话内容本身在 DB 里是全的</strong>。这一点必须先说清楚，否则容易误判为&quot;只有靠 Redis 才能恢复&quot;。</li></ul><p><strong>DB 现有的可重建原料（已核实）</strong></p><table><thead><tr><th style="text-align:left;">数据</th><th style="text-align:left;">位置</th><th style="text-align:left;">说明</th></tr></thead><tbody><tr><td style="text-align:left;">对话历史</td><td style="text-align:left;"><code>interview_message</code>（V5 建表）</td><td style="text-align:left;">每轮 user/assistant 全量落库，<strong>不做裁剪</strong>；字段 <code>role</code> / <code>content</code> / <code>created_at</code></td></tr><tr><td style="text-align:left;">逻辑会话 ID</td><td style="text-align:left;"><code>interview.session_id</code></td><td style="text-align:left;"><code>activateInterview</code> 写入首次建连的 WebSocket sessionId，重连不更新 → 实际就是 logicalSessionId</td></tr><tr><td style="text-align:left;">业务状态</td><td style="text-align:left;"><code>interview.status</code></td><td style="text-align:left;">pending / active / paused / ended</td></tr><tr><td style="text-align:left;">面试快照</td><td style="text-align:left;"><code>interview.resume_snapshot_json</code> / <code>job_snapshot_json</code></td><td style="text-align:left;">建连时固化的<strong>裁剪后</strong>简历与岗位，即 AI 实际看到的输入</td></tr><tr><td style="text-align:left;">面试参数</td><td style="text-align:left;"><code>interview.mode</code> / <code>language</code> / <code>detail_level</code> / <code>interview_style</code></td><td style="text-align:left;">重连复用所需</td></tr><tr><td style="text-align:left;">读取接口</td><td style="text-align:left;"><code>GET /api/interviews/{id}/messages</code></td><td style="text-align:left;">已用于历史详情展示；<strong>目前没有任何代码路径用它重建 AI 上下文</strong></td></tr></tbody></table><p>消息写入链路：<code>AudioStreamSession#handleUserMessage / handleAssistantMessage</code> → <code>InterviewAsyncService#saveUserMessage / saveAssistantMessage</code> → <code>InterviewMessageService#saveMessage</code>（<strong>线程池异步</strong>）。</p><p><strong>问题 / 影响</strong></p><ol><li>进程重启 / 发版后内存清空 → 用户 15 分钟内重连也会 <code>RESUME_EXPIRED</code>（<code>AudioStreamingService</code> 重连分支 <code>logicalSessions.get(resumeSessionId) == null</code>），恢复失败。</li><li>数据库 <code>paused</code> 记录可能<strong>永久悬挂</strong>：回收逻辑 <code>evictExpiredPausedSessions()</code> 是遍历内存 map 触发 <code>InterviewEndedEvent</code>；重启后 map 为空，无人再把该记录置为 <code>ended</code>。</li><li><strong>旧连接回调误杀新会话</strong>（重启之外的真实风险，见 §4 原则三）：重连时新旧 <code>AudioStreamSession</code> <strong>共享同一个 <code>InterviewSessionState</code> 对象</strong>（<code>resumeState</code> 直接赋值给 <code>this.state</code>）。若旧连接的 <code>onClose()</code> 延迟到达且其 <code>paused</code> 标志未被置位，会执行 <code>state.markClosed()</code> → 迁入 <code>ENDED</code> + 发布 <code>InterviewEndedEvent</code>，<strong>把新连接正在进行的面试直接终结</strong>；随后 <code>AudioStreamingService#onClose</code> 因 <code>isEnded()</code> 为真，还会把 <code>logicalSessions</code> 中的条目一并删除。</li></ol><p><strong>建议方向</strong></p><p><strong>（1）先纠正概念：不要把&quot;Redis 持久化&quot;等同于&quot;会话持久化&quot;。</strong></p><p>Redis 更准确的定位是 <strong>逻辑会话运行态的持久化/恢复载体</strong>；DB 中的 <code>Interview</code> 仍然是业务生命周期的<strong>最终事实来源</strong>。三层职责应明确分开：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>DB      = 面试业务事实（Interview）</span></span>
<span class="line"><span>Redis   = 可恢复的逻辑会话运行态（realtime:session:{sessionId}）</span></span>
<span class="line"><span>JVM Map = 当前进程的快速缓存（logicalSessions）</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>（2）持久化对象是「最小可恢复运行态」，不是完整聊天历史。</strong></p><blockquote><p>将 <code>InterviewSessionState</code> 的<strong>最小可恢复运行态</strong>持久化到 Redis，并设置与暂停生命周期一致的 TTL。Redis 不作为完整聊天历史存储，仅保存恢复 AI 会话所需的 <code>interviewId</code>、必要的 prompt/context 摘要及有限数量的最近消息；完整业务快照仍以 DB 为准。</p></blockquote><p>这一定位有三个直接约束：</p><ul><li><strong>TTL 与暂停生命周期一致</strong>：Redis key 的过期时间应等于 <code>PAUSED_TTL_MS</code>（当前 15 分钟），两者不可各写一套，否则会出现&quot;逻辑会话已过期但 Redis 还在&quot;或反之的中间态。</li><li><strong>上下文有界</strong>：Redis 只承载&quot;够 AI 接着聊&quot;的最小信息，不是消息归档。</li><li><strong>但&quot;最小&quot;不等于&quot;过度压缩&quot;</strong>：摘要压缩得太狠，恢复后同样会失忆：</li></ul><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>用户暂停 → JVM 重启 → Redis 恢复 → 重建 AI 会话</span></span>
<span class="line"><span>        → 上下文不完整 → AI「失忆」或前后逻辑不一致</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>因此 Redis 中的结构应至少覆盖：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>realtime:session:{sessionId}</span></span>
<span class="line"><span>├── interviewId</span></span>
<span class="line"><span>├── sessionId / logicalSessionId</span></span>
<span class="line"><span>├── systemPrompt</span></span>
<span class="line"><span>├── context              （有界，见下）</span></span>
<span class="line"><span>├── resume / job snapshot 引用</span></span>
<span class="line"><span>├── status               （ACTIVE / PAUSED）</span></span>
<span class="line"><span>├── createdAt</span></span>
<span class="line"><span>├── pausedAt</span></span>
<span class="line"><span>└── expireAt</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><div class="language-json line-numbers-mode" data-highlighter="shiki" data-ext="json" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-json"><span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">{</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">  &quot;interviewId&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: </span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">&quot;...&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">,</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">  &quot;status&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: </span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">&quot;PAUSED&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">,</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">  &quot;systemPrompt&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: </span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">&quot;...&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">,</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">  &quot;context&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: [],</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">  &quot;pausedAt&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: </span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">&quot;...&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">,</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">  &quot;expireAt&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: </span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">&quot;...&quot;</span></span>
<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">}</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>context</code> 只保存<strong>有限数量的最近消息</strong>，沿用现有裁剪语义（<code>AudioStreamSession#MAX_CONTEXT = 50</code>，即最多保留 <code>MAX_CONTEXT * 2</code> 条消息）后落盘，更早的对话不回填。注意这里的有界是<strong>刻意设计</strong>，不是数据缺失：内存有上限，而 DB 的 <code>interview_message</code> 是<strong>全量不裁剪</strong>的，真要更早的内容，DB 才是唯一来源。<code>expireAt</code> 由 <code>PAUSED_TTL_MS</code> 统一推导，不单独配置。</p><blockquote><p>更正：本文早前版本与 <code>AGENTS.md</code> §3.5 都写 <code>MAX_CONTEXT = 10</code>，实际代码是 <strong>50</strong>。</p></blockquote><p><strong>（3）恢复链路必须是多级回退，而不是&quot;Redis miss 即失败&quot;。</strong></p><p>Redis 也不是永久数据源，必须显式定义降级：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>resumeSession(sessionId)</span></span>
<span class="line"><span>        │</span></span>
<span class="line"><span>        ├── JVM logicalSessions</span></span>
<span class="line"><span>        │       ↓ miss</span></span>
<span class="line"><span>        ├── DB interview + system_prompt + interview_message</span></span>
<span class="line"><span>        │       ↓ 尝试重建</span></span>
<span class="line"><span>        └── 无法恢复 → RESUME_EXPIRED</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><blockquote><p><strong>注</strong>：这是设计初稿，当时以为还需要一层 Redis。实现时砍掉了 —— 理由见下文「P1 已完成」小节。</p></blockquote><p>这比&quot;Redis miss → RESUME_EXPIRED&quot;健壮得多。关键前提是：<strong>DB 是否真有足够的数据重建逻辑会话？</strong> 结论是&quot;基本够，但有三个缺口&quot;，必须分能力说清楚，不能笼统地讲&quot;DB 不够&quot;：</p><table><thead><tr><th style="text-align:left;">能力</th><th style="text-align:left;">依赖数据</th><th style="text-align:left;">DB 是否足够</th></tr></thead><tbody><tr><td style="text-align:left;"><strong>业务会话可恢复</strong></td><td style="text-align:left;"><code>interview.status</code> + 快照 + 面试参数</td><td style="text-align:left;"><strong>是</strong>，DB 完全够</td></tr><tr><td style="text-align:left;"><strong>AI 对话上下文可恢复</strong></td><td style="text-align:left;">最近 N 轮 user/assistant</td><td style="text-align:left;"><strong>是</strong>，<code>interview_message</code> 全量在库</td></tr><tr><td style="text-align:left;"><strong>AI 会话精确复现</strong></td><td style="text-align:left;">渲染后的 <code>systemPrompt</code> + 内存态标志位</td><td style="text-align:left;"><strong>否</strong>，见下三个缺口</td></tr></tbody></table><p><strong>DB 重建的三个真实缺口</strong></p><ol><li><strong>异步落库的丢失窗口</strong>：消息经 <code>InterviewAsyncService</code> 线程池异步写库，进程被 kill（非优雅关闭）时，队列中未落库的最后一两轮会丢。<code>@PreDestroy</code> 只在优雅关闭时等 30 秒，<code>kill -9</code> 完全兜不住。</li><li><strong>渲染后的 <code>systemPrompt</code> 不可复现</strong>：DB 存的是快照<strong>输入</strong>（简历 / 岗位 JSON），不是 <code>buildSystemPrompt()</code> 的<strong>输出</strong>。提示词模板一改版，重建出的 systemPrompt 就与当时不同 → 恢复后 AI 的人设与规则可能漂移。</li><li><strong>内存态独有字段</strong>：<code>initialQuestionSent</code>（MOCK 首问防重发）、<code>startedPublished</code>（计费起算幂等）、精确的 <code>lastActiveTime</code>。部分可由&quot;有无消息&quot;推断，但不显式、不可靠。</li></ol><blockquote><p>因此 Redis 的价值不是&quot;唯一上下文来源&quot;，而是 <strong>保住不可由 DB 复现的运行态 + 免去重建成本</strong>。也正因为 DB 重建是一条真能跑通的路径，§5.5 的多级回退才有意义，而不是纸面兜底。</p></blockquote><p><strong>（4）兜底回收</strong>：增加&quot;DB 悬挂 <code>paused</code> 记录&quot;清理任务（按超时时间分批置为 <code>ended</code> + <code>SESSION_EXPIRED</code>），不再依赖内存 map 遍历。</p>`,95),o(`p`,null,[l[1]||=o(`strong`,null,`（5）`,-1),l[2]||=a(` 可与 `,-1),i(m,{to:`/%E9%A1%B9%E7%9B%AE/%E9%9D%A2%E8%AF%95%E5%8A%A9%E6%89%8B/%E6%9E%B6%E6%9E%84%E8%AE%BE%E8%AE%A1/%E5%AE%9E%E6%97%B6%E9%9D%A2%E8%AF%95%E4%BC%9A%E8%AF%9D%E9%87%8D%E6%9E%84-%E9%A2%86%E5%9F%9F%E4%BA%8B%E4%BB%B6%E8%A7%A3%E8%80%A6.html`},{default:n(()=>[...l[0]||=[a(`实时面试会话重构-领域事件解耦`,-1)]]),_:1}),l[3]||=a(` 第 8 节"计费状态与逻辑会话状态机统一快照"合并设计。`,-1)]),l[8]||=r(`<hr><h4 id="p1-已完成-db-重建逻辑会话-全部落地" tabindex="-1"><a class="header-anchor" href="#p1-已完成-db-重建逻辑会话-全部落地"><span>P1 已完成：DB 重建逻辑会话（<strong>全部落地</strong>）</span></a></h4><blockquote><p><strong>落地进度</strong>：迁移列 + 提示词前移 + 两个坑的修法 + 多级恢复链路，全部完成并通过端到端验证。</p><p>已交付：</p><ul><li><code>V25__interview_system_prompt_and_paused_at.sql</code> —— 加 <code>system_prompt TEXT</code> 与 <code>paused_at TIMESTAMPTZ</code>（+ <code>(status, paused_at)</code> 索引）</li><li>新增 <code>SystemPromptBuilder</code>（从 <code>AudioStreamSession</code> 抽出，纯函数），<code>InterviewPrepareService</code> 在建连准备阶段渲染并落库</li><li><code>AudioStreamSession</code> 改为纯消费；老数据 <code>system_prompt</code> 为 NULL 时由 <code>AudioStreamingService#resolveSystemPrompt</code> 按快照重算兜底</li><li><code>startBilling</code> 加幂等守卫；新增按 <code>paused_at</code> 判定的悬挂暂停记录兜底回收（<code>WsHeartbeatScheduler</code> 每轮调用）</li><li><strong>新增 <code>LogicalSessionRebuilder</code></strong>：内存未命中时从 DB 重建逻辑会话（见下）</li></ul><p><strong>验证</strong>：全量 <code>mvn test</code> <strong>116/0/0</strong>；全新库启动（Flyway 建 25 个迁移 + Hibernate <code>validate</code> 通过）；真实建连后 <code>interview.system_prompt</code> 落库 <strong>3592 字符</strong>；<strong>重启后端后带 <code>resume</code> 重连 → 日志 <code>[逻辑会话重建] 已从 DB 重建逻辑会话: interviewId=13, dbStatus=paused, 历史消息=2条, systemPrompt=3592字符</code></strong>（改造前这里会直接 <code>RESUME_EXPIRED</code>）。</p></blockquote><p><strong>恢复链路：实际只需要两级</strong></p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>resumeSession(sessionId)</span></span>
<span class="line"><span>  → JVM logicalSessions        （同进程，命中即用）</span></span>
<span class="line"><span>  → LogicalSessionRebuilder    （从 DB 重建）</span></span>
<span class="line"><span>  → RESUME_EXPIRED</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>原方案设计的是四级（JVM → Redis → DB → 失败），<strong>最终砍掉了 Redis 那一级</strong>：DB 已经具备重建一个完整 AI 会话的全部事实（V25 的 <code>system_prompt</code>、全量 <code>interview_message</code>、V24 的面试参数与快照），resume 又是低频操作（用户显式暂停后回来），多一次 DB 读完全可接受；再插一层 Redis 只会多一处不一致的可能。</p><p><strong>重建的关键判定</strong></p><table><thead><tr><th style="text-align:left;">判定</th><th style="text-align:left;">依据</th><th style="text-align:left;">失败码</th></tr></thead><tbody><tr><td style="text-align:left;">记录是否存在</td><td style="text-align:left;"><code>interview.session_id = 逻辑会话ID</code></td><td style="text-align:left;"><code>RESUME_EXPIRED</code></td></tr><tr><td style="text-align:left;">归属是否匹配</td><td style="text-align:left;"><code>interview.user_id</code></td><td style="text-align:left;"><code>RESUME_FORBIDDEN</code></td></tr><tr><td style="text-align:left;">状态是否可恢复</td><td style="text-align:left;"><code>paused</code> / <code>active</code>（<code>pending</code> / <code>ended</code> 不可）</td><td style="text-align:left;"><code>RESUME_EXPIRED</code></td></tr><tr><td style="text-align:left;">暂停是否超时</td><td style="text-align:left;"><strong><code>interview.paused_at</code> + <code>PAUSED_TTL_MS</code></strong>（不依赖内存 <code>lastActiveTime</code>）</td><td style="text-align:left;"><code>RESUME_EXPIRED</code></td></tr></tbody></table><blockquote><p>老数据 <code>paused_at</code> 为 NULL 时按<strong>不可恢复</strong>处理：无法判定就不放行。</p></blockquote><p><strong>重建时按需推导的两个内存态标志</strong></p><ul><li><code>initialQuestionSent</code>（MOCK 首问防重发）：按&quot;历史消息里有没有 assistant 消息&quot;推导 —— 有就说明 AI 面试官已经开过场。</li><li><code>startedPublished</code>（计费起算幂等）：<strong>不从 DB 推导</strong>，由 <code>startBilling</code> 的幂等守卫兜住（见上文坑 2）。</li></ul><p><strong>两个刻意的设计选择</strong></p><ol><li><strong>按需重建，不做启动预热</strong>：预热要为所有暂停会话常驻内存，还要处理&quot;预热期间又来连接&quot;的竞态，而收益只在用户真的回来时才体现。</li><li><strong>建连成功后才登记注册表</strong>：重建态在 <code>sessionFactory.create()</code> 成功之后才 <code>logicalSessions.put()</code>。若建连失败就不登记 —— 否则会留下「无物理连接的 ACTIVE」僵尸（P0.1 那类问题）。</li></ol><p><strong>涉及文件</strong>：<code>LogicalSessionRebuilder</code>（新增）、<code>InterviewSessionState</code>（新增 <code>restoreContext</code> / <code>markInitialQuestionSent</code>）、<code>InterviewMessageRepository</code>（新增倒序分页查询）、<code>AudioStreamingService#onOpen</code></p><p>上面缺口 2 的解法<strong>不是</strong>&quot;把 systemPrompt 塞进 Redis&quot;，而是<strong>在 <code>interview</code> 表新增一列</strong>：</p><div class="language-sql line-numbers-mode" data-highlighter="shiki" data-ext="sql" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-sql"><span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">ALTER</span><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;"> TABLE</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;"> interview </span><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">ADD</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;"> COLUMN system_prompt </span><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">TEXT</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">;</span></span>
<span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">COMMENT ON COLUMN </span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;">interview</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;">system_prompt</span><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;"> IS</span></span>
<span class="line"><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">  &#39;建连时渲染后的系统提示词快照（AI 实际看到的完整 prompt）。写一次、永不更新；&#39;</span></span>
<span class="line"><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">  &#39;提示词模板改版也不回填历史行，否则历史复盘会漂移&#39;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">;</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>为什么落 DB 而不是 Redis</strong></p><ol><li>它是<strong>一次生成、永不改变的事实</strong>，不是运行态。Redis 是缓存 —— 把不可变事实放缓存里，事实就可能丢。</li><li>这是 V24 既有决策的同一原则往下推一格：V24 存的是 AI 实际看到的<strong>输入</strong>（裁剪后简历/岗位），而 systemPrompt 是 AI 实际看到的<strong>东西本身</strong>。既然&quot;输入&quot;都冻结了，&quot;输出&quot;更没有理由靠重算（重算依赖模板版本，一改版就漂移）。</li><li><strong>它让 DB 单独就能重建完整 AI 会话</strong> —— 多级回退的第三级从&quot;近似兜底&quot;变成<strong>完整路径</strong>，Redis 随之从&quot;必需持久化层&quot;降级为&quot;可选加速缓存&quot;。P1 的形状因此显著简化。</li></ol><p><strong>具体约束</strong></p><ul><li><strong>独立列</strong>，不要塞进 <code>resume_snapshot_json</code>（那是&quot;输入&quot;，混进去破坏该列语义）。</li><li>建连时<strong>一次写入，永不更新</strong>；模板改版<strong>不回填</strong>历史行。</li><li>老行该列为 <code>NULL</code> → 恢复时回退到&quot;用快照重算&quot;（接受漂移）。天然渐进，无需数据迁移。</li><li>Redis 里<strong>先不放</strong>：少一处不一致的可能；resume 频率低，多一次 DB 读完全可接受，等有性能问题再加缓存。</li><li>体积：实测模板输入 <code>resume_json</code> 3176B + <code>job_context_json</code> 349B，裁剪后快照约 2964 字符 → 渲染后 prompt 约 <strong>3.5~4 KB</strong>，与现有快照列同量级（<code>interview_message</code> 才是大头）。</li></ul><p><strong>代价：写入时机需要一次重构（纯搬家，不改行为）</strong></p><p><code>buildSystemPrompt()</code> 目前在 <code>AudioStreamSession</code> <strong>构造函数</strong>里执行，而面试记录在那之前就已创建。要落库需二选一：</p><ul><li><strong>(a) 推荐</strong>：把提示词构建提到 <code>InterviewPrepareService</code>（建连准备阶段），随快照一起落库，再经 <code>InterviewContext</code> 传给会话。顺带把&quot;纯函数式的提示词构建&quot;从会话类挪到准备阶段，职责更合理。</li><li>(b) 会话构造完再回写 DB：多一次 UPDATE，且有&quot;构造成功但回写失败&quot;的窗口。</li></ul><p><strong>另外两个必须先解决的坑（否则会返工）—— 均已修</strong></p><ol><li><strong><code>interview</code> 表没有 <code>paused_at</code> 列</strong>（V15 只改了 status 约束）。所以上面的&quot;兜底回收 DB 悬挂 paused 记录&quot;光有任务不够 —— 没有 <code>paused_at</code> 就无法按 TTL 判定，只能拿 <code>created_at</code> / <code>start_time</code> 猜。 <strong>已修</strong>：V25 加列；<code>pauseInterview</code> 写入、<code>resumeInterview</code> 清空；新增 <code>InterviewRepository#endExpiredPaused</code> + <code>AudioStreamingService#evictExpiredPausedInterviews</code>，由 <code>WsHeartbeatScheduler</code> 每轮调用。 与内存路径互不冲突（已被内存路径结束的记录 status 已是 ended，不会命中条件），且刻意<strong>不发布</strong> <code>InterviewEndedEvent</code> —— 这些记录本就没有活跃连接，计费侧由连接失联判断兜底结算。</li><li><strong><code>startedPublished</code>（计费起算幂等）无法从 DB 推导</strong>。重启后逻辑会话从 DB 重建，该标志默认 <code>false</code>；用户 resume 时 ASR ready 会再次 <code>publishStarted()</code> → <code>startBilling()</code> → <strong>用新的起算点覆盖 Redis 计费会话，用户已用时长被清零</strong>。这是真会踩的坑。 <strong>已修</strong>：<code>InterviewBillingService#startBilling</code> 加幂等守卫 —— Redis 中已有计费会话时直接跳过并告警，绝不覆盖。 正常&quot;新面试顶替旧面试&quot;不受影响（<code>onOpen</code> 已先 <code>forceTerminateActiveSession</code> 删掉旧 key）；残留 key 会被计费调度器的连接失联判断在约 10 秒内清掉。</li></ol><p><strong>涉及文件</strong>：<code>AudioStreamingService</code>、<code>InterviewSessionState</code>、<code>InterviewRepository</code>、<code>InterviewBillingService</code>、<code>InterviewPrepareService</code>、<code>AudioStreamSession</code>、新增 Flyway 迁移（<code>system_prompt</code> + <code>paused_at</code>）</p><hr><h3 id="p2-物理连接缺少独立存活检测-存在死连接计费泄漏-——-已实施" tabindex="-1"><a class="header-anchor" href="#p2-物理连接缺少独立存活检测-存在死连接计费泄漏-——-已实施"><span>P2｜物理连接缺少独立存活检测，存在死连接计费泄漏 —— <strong>已实施</strong></span></a></h3><blockquote><p>结论：<strong>引入独立心跳，而不是继续压缩业务心跳阈值。</strong> 把&quot;心跳&quot;绑在音频/业务消息上，无论阈值调到多少都是错的解法。</p></blockquote><p><strong>原状</strong></p><ul><li><code>HEARTBEAT_TIMEOUT_MS = 5 分钟</code>，叠加 30 秒扫描周期 → 最坏约 5.5 分钟才踢出。</li><li>心跳仅由业务消息刷新，无独立 ping。</li></ul><p><strong>关键事实澄清（原判断里有一处是错的）</strong></p><p>原文写&quot;若前端持续推流（含静音帧），5 分钟不会误杀；若前端改为 VAD，静默 &gt;5 分钟会被误判&quot;。<strong>前半句把静音帧的来源搞错了</strong>：</p><ul><li>静音帧是<strong>后端</strong>发的 —— <code>DoubaoAsrClient#scheduleKeepAlive()</code> 每空闲 3 秒向<strong>豆包</strong>补一帧 200ms 静音 PCM，用于维持<strong>后端↔豆包</strong>的 ASR 长连接（AGENTS.md §4.2 里的&quot;客户端&quot;指 <code>DoubaoAsrClient</code> 这个 HTTP 客户端，不是浏览器）。</li><li>前端<strong>本来就是 VAD</strong>：AGENTS.md §6.7.2 明确写着&quot;前端本地 VAD 不上传静音&quot;，静默期<strong>没有任何音频帧</strong>。</li></ul><p>这个澄清改变了结论：<strong>60 秒的音频判据会误杀健康会话</strong>（用户思考一分钟是常态），因此&quot;退选方案：把超时缩短到 60~120 秒、依赖前端持续推帧&quot;是不可行的 —— 前端并不持续推帧。<strong>缩短超时必须以应用层心跳为前提。</strong></p><p><strong>问题 2：两条时间轴被混为一谈（已核实）</strong></p><ul><li><code>InterviewBillingScheduler#SILENCE_TIMEOUT_MS</code>（30 秒&quot;幽灵会话&quot;检查）比较的 Redis <code>lastHeartbeatTime</code> <strong>实为&quot;扣费基准&quot;</strong>，由计费任务自身每次 <code>doBilling</code> 刷新（约每 10 秒），<strong>恒不会超时</strong> —— 这道防线实际上从未生效。</li><li><code>AudioStreamingService#hasActiveStreamSession</code> 依赖 <code>WebSocketSession#isOpen()</code>，半开连接仍返回 <code>true</code>。</li><li>优雅关闭（收到关闭帧）会立即触发 <code>onClose</code> → 停止计费，<strong>无泄漏</strong>；半开连接（移动网络掉线、进程被杀等无关闭帧）则两道防线全部失效。</li><li><strong>结果：直到 5 分钟心跳踢出前持续计费，最多约 5 分钟时长被误扣。</strong></li></ul><p><strong>已实施的方案</strong></p><p><strong>（1）新增应用层心跳协议</strong>：上行文本控制消息 <code>{&quot;type&quot;:&quot;heartbeat&quot;}</code>（<code>ControlMessage#TYPE_HEARTBEAT</code>），前端 20~30 秒发一次。 不用 WebSocket 原生 ping frame —— 容器会自动回 pong，但 <strong>ping 不会透传到应用层</strong>，后端无从据此刷新活跃度。</p><p><strong>（2）时间戳拆分 + 音频不再是心跳</strong>：</p><div class="language-java line-numbers-mode" data-highlighter="shiki" data-ext="java" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-java"><span class="line"><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">// 改前：语义错误</span></span>
<span class="line"><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">receiveAudio</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">() </span><span style="--shiki-light:#C18401;--shiki-dark:#C678DD;">-&gt;</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;"> refreshHeartbeat</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">()</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">// 改后</span></span>
<span class="line"><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">receiveAudio</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">()  </span><span style="--shiki-light:#C18401;--shiki-dark:#C678DD;">-&gt;</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;"> updateAudioActivity</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">()</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">;</span><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">          // 只更新音频活跃度</span></span>
<span class="line"><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">heartbeatMsg</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">()  </span><span style="--shiki-light:#C18401;--shiki-dark:#C678DD;">-&gt;</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;"> refreshConnectionHeartbeat</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">()</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">;</span><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">   // 唯一刷新连接活跃度的入口</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>（3）判据随客户端能力自适应</strong> —— 这是让后端<strong>可以先于前端上线</strong>的关键：</p><table><thead><tr><th style="text-align:left;">客户端</th><th style="text-align:left;">判据</th><th style="text-align:left;">阈值</th></tr></thead><tbody><tr><td style="text-align:left;">未发过心跳（老前端）</td><td style="text-align:left;">音频活跃度</td><td style="text-align:left;"><strong>5 分钟</strong>（与改造前一致，不会误杀）</td></tr><tr><td style="text-align:left;">发过一个心跳（新前端）</td><td style="text-align:left;">只看连接心跳</td><td style="text-align:left;"><strong>60 秒</strong>（音频再旧也不影响，死连接 1 分钟内被发现）</td></tr></tbody></table><p>发过一个心跳即永久切到严格判据，无需开关、无需前后端同时发布。</p><p><strong>（4）单一判据来源</strong>：<code>AudioStreamingService#isConnectionLost(逻辑会话ID)</code> 是连接存活的唯一判定入口， 心跳扫描（<code>WsHeartbeatScheduler</code>，扫描周期 30 秒 → <strong>10 秒</strong>）与计费兜底（<code>InterviewBillingScheduler</code>）共用它。 计费侧那段自刷新基准的&quot;幽灵会话&quot;检查被替换为真实连接活跃度判断，<code>SILENCE_TIMEOUT_MS</code> 常量删除。</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>客户端</span></span>
<span class="line"><span>  │ {&quot;type&quot;:&quot;heartbeat&quot;} 每 20~30s</span></span>
<span class="line"><span>  ▼</span></span>
<span class="line"><span>AudioStreamSession#lastConnectionHeartbeatTime</span></span>
<span class="line"><span>  │</span></span>
<span class="line"><span>  ├── WsHeartbeatScheduler（每 10s）→ isConnectionLost() → 踢出 → onClose → 停止计费</span></span>
<span class="line"><span>  └── InterviewBillingScheduler（每 10s）→ isConnectionLost() → 兜底结算</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>验证</strong></p><ul><li>新增 <code>AudioStreamSessionHeartbeatTest</code>（6 用例）：老客户端静默 4 分钟仍存活 / 6 分钟判失联；已声明心跳的客户端<strong>音频停滞 10 分钟也不判失联</strong>；心跳超 60 秒判失联；<strong>音频帧不刷新连接心跳</strong>；<code>heartbeat</code> 消息刷新并声明支持；<code>PAUSE</code> 消息不刷新。</li><li><code>AudioStreamingServiceTest</code> 新增 4 个 <code>isConnectionLost</code> 用例（逻辑会话不存在 / 已暂停 → <code>null</code>；存活 → <code>false</code>；失联 → <code>true</code>）。</li><li>全量 <code>mvn test</code>：<strong>95 用例 / 0 失败 / 0 错误</strong>。</li></ul><p><strong>前端侧已同步实现</strong>（两端都改完，间隔 <strong>25 秒</strong>，建连后立即先发一次以便尽早切到严格判据）：</p><table><thead><tr><th style="text-align:left;">端</th><th style="text-align:left;">文件</th><th style="text-align:left;">实现</th></tr></thead><tbody><tr><td style="text-align:left;">Web（<code>pass-up.frontend/apps/user</code>）</td><td style="text-align:left;"><code>features/realtime-voice/hooks/useVoiceWebSocket.ts</code></td><td style="text-align:left;"><code>startHeartbeat()</code>，随 <code>handleOpen</code> 启动、<code>cleanup()</code> 停止</td></tr><tr><td style="text-align:left;">Web 协议常量</td><td style="text-align:left;">同目录 <code>protocol.ts</code></td><td style="text-align:left;">新增 <code>CLIENT_CONTROL</code>（<code>pause</code> / <code>heartbeat</code>），<code>sendControl</code> 改为强类型</td></tr><tr><td style="text-align:left;">小程序（<code>pass-up.frontend-mp</code>）</td><td style="text-align:left;"><code>features/interview/ws.ts</code></td><td style="text-align:left;"><code>InterviewSocket#sendControl</code> + <code>startHeartbeat</code> / <code>stopHeartbeat</code>，随 <code>onOpen</code> / <code>onError</code> / <code>onClose</code> / <code>close()</code> 启停</td></tr></tbody></table><p>前端验证：Web 端 <code>tsc -b --noEmit</code> 与 <code>eslint</code> 全绿；小程序端改动文件类型错误数为 0（该工程 <code>tsc --noEmit</code> 另有 54 个既存错误，与本次无关，且未配置 typecheck 脚本）。</p><p><strong>端到端验证（已在有真实 ASR Key 的环境补齐）</strong></p><blockquote><p>早前版本这里写着&quot;60 秒阈值的端到端行为没法在本机跑 —— 本地没有可用的豆包 ASR Key，会话会因 ASR 不可用在约 5.7 秒内被动关闭&quot;。 <strong>该限制已被打破</strong>：在一台注入了真实豆包 ASR Key 的实例上（会话可以活过 60 秒）完成了真实验证。</p></blockquote><table><thead><tr><th style="text-align:left;">场景</th><th style="text-align:left;">结果</th></tr></thead><tbody><tr><td style="text-align:left;">从不发心跳（老客户端）</td><td style="text-align:left;">✅ 75 秒内<strong>未</strong>被踢 —— 沿用 5 分钟音频判据，向后兼容成立</td></tr><tr><td style="text-align:left;">声明过心跳后静默</td><td style="text-align:left;">✅ 收到 <code>HEARTBEAT_TIMEOUT</code>；服务端 <code>heartbeatCapable=true, idleMs=69161, timeoutMs=60000</code></td></tr><tr><td style="text-align:left;">按 25 秒节奏发心跳</td><td style="text-align:left;">✅ 90 秒未被踢，共 4 次心跳，收到 9 次 <code>TIME_UPDATE</code></td></tr></tbody></table><p><strong>前端心跳也已在真实浏览器里验证</strong>（这是最后一块空白）。难点与破法：</p><ol><li><code>useRealtimeVoice#start()</code> 的顺序是<strong>先 <code>startCapture()</code>（<code>getDisplayMedia</code>）再 <code>connect()</code></strong> —— 屏幕采集被拒就不会建连，也就没有心跳，而自动化浏览器弹不出选择器。 破法：把 <code>getDisplayMedia</code> / <code>getUserMedia</code> 替换成返回「静音音轨的合成 MediaStream」， <strong>只替换音频源，被测的心跳代码一行没动</strong>。</li><li><code>onHeartbeatReceived()</code> 没有日志，无法直接断言&quot;收到心跳&quot;。破法（决定性）：先让浏览器正常发一会儿心跳， 再把 <code>WebSocket.prototype.send</code> 打补丁<strong>丢弃 <code>heartbeat</code> 帧</strong>（连接保持打开 = 模拟客户端静默）。 于是前端计数器证明<strong>定时器在按 25 秒触发</strong>，服务端若在 60 秒后以 <code>heartbeatCapable=true</code> 踢出， 就证明<strong>之前确实收到过心跳</strong>（该标志只在收到心跳时翻转；从未收到则会走 5 分钟音频判据、不会在 60 秒踢）。</li></ol><p>实测：前端 <code>__droppedHeartbeats=2</code>（75 秒内触发 2 次 → 25 秒节奏）；服务端 <code>[心跳超时] heartbeatCapable=true, idleMs=61129, timeoutMs=60000</code>；UI 由「实时监听中」转为「未开始」。 <code>idleMs=61129</code> 与「建连时那一次立即心跳 + 补丁丢掉了 25s/50s 两次」完全对得上。</p><p>心跳语义另有<strong>单测级覆盖</strong>（含边界），WS 传输层由既有 <code>AudioStreamingWebSocketIntegrationTest</code> 覆盖。</p><p><strong>设计取舍：为什么静音保活留在后端，而不是让前端发静音帧（避免重复讨论）</strong></p><p>链路上有<strong>两个跳</strong>，各自有独立的保活需求，不要混为一谈：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>浏览器 ──(上行音频 / 心跳)──► 后端 ──(PCM + 静音保活)──► 豆包 ASR</span></span>
<span class="line"><span>          ▲                            ▲</span></span>
<span class="line"><span>  客户端↔后端 存活                     后端↔豆包 存活（豆包 8 秒硬超时）</span></span>
<span class="line"><span>  → 心跳 20 B / 25 s                   → 静音帧 6400 B / 3 s</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>曾考虑过&quot;让前端发静音帧&quot;，好处是能同时保护两跳、把两套保活合并成一套。<strong>结论是不采用</strong>，理由：</p><table><thead><tr><th style="text-align:left;">维度</th><th style="text-align:left;">前端发静音帧</th><th style="text-align:left;">后端保活 + 前端心跳（当前）</th></tr></thead><tbody><tr><td style="text-align:left;">上行带宽</td><td style="text-align:left;">~7.7 MB/小时（6400 B / 3 s，走<strong>用户移动上行</strong>）</td><td style="text-align:left;">~2.9 KB/小时（20 B / 25 s，走已建好的 socket）</td></tr><tr><td style="text-align:left;">电量</td><td style="text-align:left;">麦克风 / 编码 / 射频持续工作</td><td style="text-align:left;">无额外开销</td></tr><tr><td style="text-align:left;">与音频管线解耦</td><td style="text-align:left;">❌ 重新耦合 —— 前端换更严 VAD、用户静音、iOS 挂起 AudioContext 都会让&quot;存活信号&quot;消失</td><td style="text-align:left;">✅ 独立信号，这些场景下仍有效</td></tr><tr><td style="text-align:left;">后端能否独立维持 ASR</td><td style="text-align:left;">❌ 依赖前端；前端一抖动 ASR 就断</td><td style="text-align:left;">✅ 后端自持保活与重连（<code>consumeLoop</code>）</td></tr><tr><td style="text-align:left;">前端改动量</td><td style="text-align:left;">需在静默期维持采集+编码+上传</td><td style="text-align:left;">在已有 socket 上发 20 字节文本帧</td></tr></tbody></table><p>带宽差约 <strong>2600 倍</strong>，且是用户侧移动流量；把存活判据绑回音频管线又正好抵消 P2 的收益。</p><p><strong>已知缺口（唯一真正偏向&quot;前端发静音&quot;的场景）</strong>：后端保活会让&quot;连接活着、但前端音频管线已死&quot;伪装成&quot;用户很安静&quot;。 覆盖它的正确做法不是持续上传静音，而是加<strong>音频管线看门狗</strong>：心跳正常但连续 N 分钟无音频帧（且非暂停态）时向前端下发告警，让前端自检音频链路。目前未实现。</p><p><strong>涉及文件</strong>：<code>ControlMessage</code>、<code>AudioStreamSession</code>、<code>AudioStreamingService</code>、<code>WsHeartbeatScheduler</code>、<code>InterviewBillingScheduler</code>、<code>AGENTS.md</code>（§3.6 协议 + 新增 §3.7 存活判据）</p><hr><h3 id="p2-1-计费秒数的取整方式与基准推进-——-已修" tabindex="-1"><a class="header-anchor" href="#p2-1-计费秒数的取整方式与基准推进-——-已修"><span>P2.1｜计费秒数的取整方式与基准推进 —— <strong>已修</strong></span></a></h3><p><strong>原状</strong></p><div class="language-java line-numbers-mode" data-highlighter="shiki" data-ext="java" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-java"><span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">int</span><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;"> actualSeconds </span><span style="--shiki-light:#383A42;--shiki-dark:#56B6C2;">=</span><span style="--shiki-light:#E45649;--shiki-dark:#E5C07B;"> Math</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">round</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">(durationMs </span><span style="--shiki-light:#383A42;--shiki-dark:#56B6C2;">/</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;"> 1000.0f</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">);</span></span>
<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">...</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E5C07B;">sessionInfo</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">setLastHeartbeatTime</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">(now);</span><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">   // ← 基准直接跳到 now</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>为什么这是个问题</strong>：计费是<strong>周期性结算</strong>的（<code>InterviewBillingScheduler</code> 每 10 秒一次，另有暂停/结束时的结算）， 而每轮结算的间隔几乎不可能是整数秒。基准跳到 <code>now</code> 会把这一轮的零头<strong>永久丢弃</strong>，于是：</p><table><thead><tr><th style="text-align:left;">取整方式</th><th style="text-align:left;">每轮误差</th><th style="text-align:left;">累计效果</th></tr></thead><tbody><tr><td style="text-align:left;"><code>Math.round</code> + 基准跳到 now</td><td style="text-align:left;">±0.5 秒，但零头被丢弃</td><td style="text-align:left;">期望无偏；<strong>若结算周期稳定地偏大</strong>（如恒定 10.6 秒），会稳定进位 → <strong>系统性多扣</strong></td></tr><tr><td style="text-align:left;"><code>floor</code>（截断）+ 基准跳到 now</td><td style="text-align:left;">恒为 −0.5 秒</td><td style="text-align:left;"><strong>系统性少扣约 0.5 秒/轮 ≈ 5%</strong></td></tr></tbody></table><p>也就是说：<strong>单独把四舍五入改成截断会更糟</strong>（每小时面试少扣约 3 分钟）。</p><p><strong>修法：截断 + 零头结转</strong></p><div class="language-java line-numbers-mode" data-highlighter="shiki" data-ext="java" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-java"><span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">int</span><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;"> actualSeconds </span><span style="--shiki-light:#383A42;--shiki-dark:#56B6C2;">=</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;"> (</span><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">int</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">) (durationMs </span><span style="--shiki-light:#383A42;--shiki-dark:#56B6C2;">/</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;"> 1000</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">)</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">;</span><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">          // 截断：不足 1 秒不计</span></span>
<span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">if</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;"> (actualSeconds </span><span style="--shiki-light:#383A42;--shiki-dark:#56B6C2;">&lt;=</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;"> 0</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">) </span><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">return</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;"> 0</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">;</span><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">                        // 不动基准，零头留到下一轮</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E5C07B;">sessionInfo</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">setUsedSeconds</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">(used </span><span style="--shiki-light:#383A42;--shiki-dark:#56B6C2;">+</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;"> actualSeconds);</span></span>
<span class="line"><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">// 关键：基准按「已计费秒数」前进，而不是跳到 now</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E5C07B;">sessionInfo</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">setLastHeartbeatTime</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">(base </span><span style="--shiki-light:#383A42;--shiki-dark:#56B6C2;">+</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;"> actualSeconds </span><span style="--shiki-light:#383A42;--shiki-dark:#56B6C2;">*</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;"> 1000L</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">);</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>基准只前进「已计费的整秒」，不足 1 秒的零头自然结转到下一轮。效果：</p><ul><li><strong>不超扣</strong>：任意时刻累计计费 ≤ 真实时长（误差恒 &lt; 1 秒，且只发生在会话结束时）；</li><li><strong>不系统性少扣</strong>：零头不被丢弃，长期无漂移；</li><li>语义上真正实现了「不足 1 秒不计费」。</li></ul><blockquote><p>暂停 / 恢复会重置基准（<code>resumeBilling</code> 设 <code>lastHeartbeatTime = now</code>），每轮丢弃 &lt; 1 秒 —— 这是<strong>有意的</strong>：暂停期间本就不该计费。</p></blockquote><p><strong>验证</strong>：<code>InterviewBillingServiceTest</code> 两条用例把语义钉住 —— <code>updateStatus_不足1秒_不扣费且不推进基准</code>（断言基准不变）、 <code>updateStatus_基准按已计费秒数前进_零头结转到下一轮</code>（1500ms → 只扣 1 秒，且基准恰前进 1000ms 而非跳到 now）。</p><p><strong>涉及文件</strong>：<code>InterviewBillingService#doBilling</code></p><hr><h3 id="p3-运行参数配置化-——-已实施" tabindex="-1"><a class="header-anchor" href="#p3-运行参数配置化-——-已实施"><span>P3｜运行参数配置化 —— <strong>已实施</strong></span></a></h3><p>新增 <code>com.xiaodingtie.passup.modules.realtime.config.RealtimeProperties</code> （<code>@Component @Validated @ConfigurationProperties(prefix = &quot;realtime&quot;)</code>），把散落在 <code>AudioStreamSession</code> / <code>AudioStreamingService</code> / <code>WsHeartbeatScheduler</code> 里的 9 个 <code>private static final</code> 常量集中外置。<strong>默认值与外置前完全一致</strong>，不配也能跑。</p><table><thead><tr><th style="text-align:left;">属性</th><th style="text-align:left;">默认</th><th style="text-align:left;">原常量</th></tr></thead><tbody><tr><td style="text-align:left;"><code>realtime.heartbeat-timeout-ms</code></td><td style="text-align:left;">60000</td><td style="text-align:left;"><code>AudioStreamSession#HEARTBEAT_TIMEOUT_MS</code></td></tr><tr><td style="text-align:left;"><code>realtime.audio-idle-timeout-ms</code></td><td style="text-align:left;">300000</td><td style="text-align:left;"><code>AudioStreamSession#AUDIO_IDLE_TIMEOUT_MS</code></td></tr><tr><td style="text-align:left;"><code>realtime.paused-ttl-ms</code></td><td style="text-align:left;">900000</td><td style="text-align:left;"><code>AudioStreamingService#PAUSED_TTL_MS</code></td></tr><tr><td style="text-align:left;"><code>realtime.pending-ttl-ms</code></td><td style="text-align:left;">180000</td><td style="text-align:left;"><code>AudioStreamingService#PENDING_TTL_MS</code></td></tr><tr><td style="text-align:left;"><code>realtime.heartbeat-scan-interval-ms</code></td><td style="text-align:left;">10000</td><td style="text-align:left;"><code>WsHeartbeatScheduler</code> 的 <code>@Scheduled(fixedDelay)</code></td></tr><tr><td style="text-align:left;"><code>realtime.asr.max-reconnect-attempts</code></td><td style="text-align:left;">3</td><td style="text-align:left;"><code>AudioStreamSession#MAX_ASR_RECONNECT_ATTEMPTS</code></td></tr><tr><td style="text-align:left;"><code>realtime.asr.reconnect-base-backoff-ms</code></td><td style="text-align:left;">500</td><td style="text-align:left;"><code>ASR_RECONNECT_BASE_BACKOFF_MILLIS</code></td></tr><tr><td style="text-align:left;"><code>realtime.asr.reconnect-max-backoff-ms</code></td><td style="text-align:left;">3000</td><td style="text-align:left;"><code>ASR_RECONNECT_MAX_BACKOFF_MILLIS</code></td></tr><tr><td style="text-align:left;"><code>realtime.asr.reconnect-reset-ms</code></td><td style="text-align:left;">30000</td><td style="text-align:left;"><code>ASR_RECONNECT_RESET_MILLIS</code></td></tr></tbody></table><p><strong>三个实现要点</strong></p><ol><li><strong><code>@Scheduled</code> 拿不到注入的 bean</strong>，只能用属性占位符： <code>@Scheduled(fixedDelayString = &quot;\${realtime.heartbeat-scan-interval-ms:10000}&quot;)</code>（冒号后是默认值）。</li><li><strong>不把默认值写进 <code>application.yml</code></strong> —— 只在 <code>RealtimeProperties</code> 里定义一处，yml 里放注释形式的可调项说明，避免两处数值漂移。</li><li><strong>顺手消除了一处双份定义</strong>：计费侧的 <code>PAUSED_TIMEOUT_MINUTES = 15</code> 改为从 <code>realtime.paused-ttl-ms</code> 推导 （<code>InterviewBillingService#pausedTimeoutMinutes</code>，向上取整到分钟）。两者必须一致 —— 计费会话要活得比逻辑会话的可恢复窗口更长， 否则用户点「继续」时逻辑会话能重建、计费却已过期；各写一份数字迟早会漂移。</li></ol><p><strong>校验与守护</strong></p><ul><li><code>@Validated</code> + 各值 <code>@Min(1)</code>：配错在启动阶段直接失败并给出明确提示（实测 <code>realtime.heartbeat-timeout-ms=0</code> → <code>APPLICATION FAILED TO START</code> / <code>Property: realtime.heartbeatTimeoutMs</code> / <code>Reason: 最小不能小于1</code>）。</li><li>类注释里写明两条<strong>跨端约束</strong>：心跳阈值必须明显大于前端心跳间隔（两端 25 秒）；暂停 TTL 必须大于前端「暂停→继续」的预期最大间隔。</li><li>新增 <code>RealtimePropertiesTest</code> 钉住全部默认值，并断言「心跳阈值 ≥ 前端间隔 × 2」「扫描周期 × 2 ≤ 最短失联阈值」。</li></ul><p><strong>验证</strong>：全量 <code>mvn test</code> <strong>120/0/0</strong>；运行时实测覆盖生效（<code>--realtime.heartbeat-scan-interval-ms=3000</code> → 日志显示扫描间隔正好 3.0 秒）。</p><p><strong>涉及文件</strong>：新增 <code>RealtimeProperties</code>、<code>RealtimePropertiesTest</code>；改造 <code>AudioStreamSession</code>、<code>AudioStreamSessionDependencies</code>、<code>AudioStreamSessionFactory</code>、<code>AudioStreamingService</code>、<code>LogicalSessionRebuilder</code>、<code>WsHeartbeatScheduler</code>、<code>InterviewBillingService</code>、<code>application.yml</code>（注释说明）</p><hr><blockquote><p>原判断：工程治理项、非健壮性故障、可与前两项分开排期 —— 这点没错，实际也是最后做的。 原方案（抽 <code>@ConfigurationProperties(prefix = &quot;realtime&quot;)</code> + 安全默认值 + 校验注释）<strong>原样执行，无需修正</strong>。</p></blockquote><hr><h2 id="_4-健壮性原则" tabindex="-1"><a class="header-anchor" href="#_4-健壮性原则"><span>4. 健壮性原则</span></a></h2><p>在动 P1/P2 之前，先把下面三条定为不可违背的原则，否则改完仍是补丁叠补丁。</p><p><strong>原则一：任何物理连接退出路径最终都必须进入统一 cleanup。</strong></p><p>物理连接的销毁<strong>不能</strong>直接决定逻辑面试生命周期。<code>afterConnectionClosed</code> 不能被视为唯一清理入口，需要心跳扫描、异常路径、计费耗尽、用户主动结束共同兜底。</p><blockquote><p><strong>曾经的疑似反例（见 P0，已证伪）</strong>：<code>onOpen</code> 强踢旧连接时 <code>close()</code> 之后紧跟 <code>streamSessions.remove(oldConnectionId)</code>。原判断认为这会让回调拿不到对象、<code>onClose()</code> 被 <code>if (streamingService != null)</code> 挡掉。实测表明 Tomcat 在 <code>close()</code> 内部<strong>同步</strong>回调 <code>afterConnectionClosed</code>，因此回调先于那行执行，正常路径没有问题；真正出问题的是 <code>close()</code> 抛异常 / 连接已关闭这两种回调不到达的情况。该行已删除。</p><p>由此得到一条更稳的规则：<strong>不要让&quot;从 map 里删掉&quot;和&quot;做业务清理&quot;成为两件事。</strong> <code>streamSessions.remove()</code> 的返回值同时承担了「移除」和「幂等守卫」两个职责，一旦有路径只想移除不想清理（或反之），就会静默出错。正确形态是：<code>cleanup()</code> 内部自己完成移除，并对重复调用幂等。</p></blockquote><p><strong>原则二：物理连接与逻辑会话严格分层。</strong></p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>物理连接断开  →  逻辑会话仍然存在  →  重新建立 WebSocket</span></span>
<span class="line"><span>             →  重新绑定逻辑会话  →  恢复 AI 上下文继续面试</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>原则三：所有 connection cleanup 必须带 <code>connectionId</code> 守卫。</strong></p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>连接 A → resume → 连接 B 建立 → A 的 afterConnectionClosed() 延迟到达</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>若 A 的关闭回调无条件执行 <code>pauseInterview()</code> / <code>stopBilling()</code> / <code>removeLogicalSession()</code>，就会把 B 正在运行的逻辑会话错误关掉。因此回调内必须先判断：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>当前连接是不是我？</span></span>
<span class="line"><span>  是 → cleanup</span></span>
<span class="line"><span>  否 → stale callback，忽略</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>InterviewSessionState</code> 已具备 <code>currentConnectionId</code> + <code>bindConnection</code> / <code>unbindConnection</code>，守卫所需的元数据<strong>已经存在</strong>，只是清理路径尚未使用它。</p><hr><h2 id="_5-目标架构" tabindex="-1"><a class="header-anchor" href="#_5-目标架构"><span>5. 目标架构</span></a></h2><p>实时面试会话最终分为三个层次，外加一条独立的计费时间轴：</p><h3 id="_5-1-业务生命周期" tabindex="-1"><a class="header-anchor" href="#_5-1-业务生命周期"><span>5.1 业务生命周期</span></a></h3><p><code>Interview</code></p><ul><li>DB 持久化</li><li><code>RUNNING / PAUSED / ENDED / EXPIRED</code></li><li>业务事实来源</li></ul><h3 id="_5-2-逻辑会话" tabindex="-1"><a class="header-anchor" href="#_5-2-逻辑会话"><span>5.2 逻辑会话</span></a></h3><p><code>InterviewSessionState</code></p><ul><li><code>interviewId</code></li><li><code>systemPrompt</code></li><li>AI context</li><li>session metadata</li><li>可跨 WebSocket 连接继续</li><li>Redis 持久化运行态</li><li>JVM Map 作为本地缓存</li></ul><h3 id="_5-3-物理连接" tabindex="-1"><a class="header-anchor" href="#_5-3-物理连接"><span>5.3 物理连接</span></a></h3><p><code>AudioStreamSession</code></p><ul><li>WebSocket</li><li>PCM queue</li><li>Doubao ASR connection</li><li>heartbeat（独立于业务音频）</li><li>connection lifecycle</li></ul><h3 id="_5-4-分层关系" tabindex="-1"><a class="header-anchor" href="#_5-4-分层关系"><span>5.4 分层关系</span></a></h3><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>DB</span></span>
<span class="line"><span>业务事实</span></span>
<span class="line"><span>  │</span></span>
<span class="line"><span>  └── Interview</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Redis</span></span>
<span class="line"><span>可恢复运行态</span></span>
<span class="line"><span>  │</span></span>
<span class="line"><span>  └── InterviewSessionState</span></span>
<span class="line"><span></span></span>
<span class="line"><span>JVM</span></span>
<span class="line"><span>本地缓存</span></span>
<span class="line"><span>  │</span></span>
<span class="line"><span>  └── logicalSessions</span></span>
<span class="line"><span></span></span>
<span class="line"><span>WebSocket</span></span>
<span class="line"><span>物理连接</span></span>
<span class="line"><span>  │</span></span>
<span class="line"><span>  └── AudioStreamSession</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Billing</span></span>
<span class="line"><span>独立计费时间轴</span></span>
<span class="line"><span>  │</span></span>
<span class="line"><span>  └── lastBillingTime（≠ lastConnectionHeartbeat）</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="_5-5-目标恢复链路" tabindex="-1"><a class="header-anchor" href="#_5-5-目标恢复链路"><span>5.5 目标恢复链路</span></a></h3><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>resumeSession(sessionId)</span></span>
<span class="line"><span>  → JVM logicalSessions                     （最快，同进程）</span></span>
<span class="line"><span>  → Redis realtime:session:{id}             （跨进程，含渲染后 systemPrompt）</span></span>
<span class="line"><span>  → DB interview + snapshot + interview_message</span></span>
<span class="line"><span>                                            （兜底重建：快照重建 systemPrompt，</span></span>
<span class="line"><span>                                              最近 N 条消息重建 context）</span></span>
<span class="line"><span>  → RESUME_EXPIRED</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>第三级是<strong>真能跑通的路径</strong>，不是纸面兜底：<code>interview_message</code> 全量在库，快照在库，面试参数在库。它的已知局限是异步落库的丢失窗口与 <code>systemPrompt</code> 的版本漂移（见 P1 三个缺口）。</p><hr><h2 id="_6-优先级建议" tabindex="-1"><a class="header-anchor" href="#_6-优先级建议"><span>6. 优先级建议</span></a></h2><table><thead><tr><th style="text-align:left;">优先级</th><th style="text-align:left;">项</th><th style="text-align:left;">理由</th><th style="text-align:left;">预估工作量</th></tr></thead><tbody><tr><td style="text-align:left;"><s>P0</s></td><td style="text-align:left;">强踢旧连接绕过统一 cleanup</td><td style="text-align:left;"><strong>已证伪</strong>（正常路径无问题）；残留的 close 失败边界<strong>已修</strong></td><td style="text-align:left;">✅ 已完成（删 1 行）</td></tr><tr><td style="text-align:left;"><s>P0.1</s></td><td style="text-align:left;">重连失败留下「无物理连接的 ACTIVE」逻辑会话</td><td style="text-align:left;">两条回收路径都扫不到 → 永久残留；已定位触发条件并修复</td><td style="text-align:left;">✅ 已完成（补回滚 + 双向验证的单测）</td></tr><tr><td style="text-align:left;"><s>—</s></td><td style="text-align:left;">单元测试整体失配 + 测试未进 CI</td><td style="text-align:left;">测试长期红灯无人发现，根因是流水线不跑测试</td><td style="text-align:left;">✅ 已完成（85/0/0 + 新增 <code>test.yml</code>）</td></tr><tr><td style="text-align:left;"><s>P2</s></td><td style="text-align:left;">独立心跳 + 死连接检测 + 计费时间轴拆分</td><td style="text-align:left;">消除半开连接的计费泄漏；连接存活与音频解耦</td><td style="text-align:left;">✅ 已完成，<strong>前后端均已端到端验证</strong>（含真实浏览器）</td></tr><tr><td style="text-align:left;"><s>P2.1</s></td><td style="text-align:left;">计费秒数取整与基准推进</td><td style="text-align:left;">原实现每轮丢弃零头，取整方式会带来系统性偏差</td><td style="text-align:left;">✅ 已完成（截断 + 零头结转）</td></tr><tr><td style="text-align:left;">P1a</td><td style="text-align:left;">提示词落库 + <code>paused_at</code> + 计费幂等守卫</td><td style="text-align:left;">让 DB 具备&quot;单独重建完整 AI 会话&quot;的能力，并补上两个重启后会踩的坑</td><td style="text-align:left;">✅ 已完成（V25 + SystemPromptBuilder + 兜底回收 + startBilling 守卫）</td></tr><tr><td style="text-align:left;"><s>P1b</s></td><td style="text-align:left;">逻辑会话跨进程恢复</td><td style="text-align:left;">内存未命中时从 DB 重建：JVM → DB → RESUME_EXPIRED</td><td style="text-align:left;">✅ 已完成（<code>LogicalSessionRebuilder</code> + 端到端验证）</td></tr><tr><td style="text-align:left;"><s>P3</s></td><td style="text-align:left;">运行参数配置化</td><td style="text-align:left;">运维可调性，低风险，纯工程治理</td><td style="text-align:left;">✅ 已完成（<code>RealtimeProperties</code> + 校验 + 默认值测试）</td></tr></tbody></table><blockquote><p><strong>本文档记录的全部事项（P0 / P0.1 / P1 / P2 含前端 / P2.1 / P3 / 测试与 CI）已全部闭环。</strong> P1 收尾时把原设计的四级恢复砍成了两级（JVM → DB）：DB 已能单独重建完整会话，Redis 那一级是多余的。 后续若要继续演进，方向见 §5 目标架构与 <code>AGENTS.md</code>。 <strong>P2 有一个待前端配合的动作</strong>：前端需以 20~30 秒间隔发送 <code>{&quot;type&quot;:&quot;heartbeat&quot;}</code>。在此之前后端行为与改造前完全一致（老客户端走 5 分钟音频判据），不会误杀。</p></blockquote><h2 id="_7-待验证假设" tabindex="-1"><a class="header-anchor" href="#_7-待验证假设"><span>7. 待验证假设</span></a></h2><ol><li>前端音频推流策略：持续推帧 还是 仅说话时推帧？（决定 P2 阈值与误杀风险）</li><li>生产日志是否出现 <code>RESUME_EXPIRED</code> / <code>重连失败：逻辑会话不存在或已过期</code>？出现即证实 P1 已造成真实恢复失败。</li><li>生产日志是否出现&quot;旧连接 <code>onClose</code> 晚于新连接建立&quot;的时序（可用 <code>connectionId</code> / <code>logicalSessionId</code> 交叉比对）？出现即证实 §4 原则三已被触发。</li><li>是否发生过&quot;用户被多扣费&quot;且时间点与网络掉线吻合？（印证 P2 计费泄漏）</li><li>DB 中是否存在&quot;面试正常结束但末尾消息缺失&quot;的记录（对比 <code>interview.end_time</code> 与 <code>max(interview_message.created_at)</code>）？出现即证实异步落库存在丢失窗口。</li><li><s>P0 的现场证据</s> —— <strong>已用 curl 端到端实测替代</strong>：本地起后端 → 建连接 A → 手动写入 Redis 计费会话 → 建连接 B 触发强踢，实测 A 的 <code>onClose()</code> 正常执行、<code>interview</code> 正常清理。P0 证伪。</li><li>仍值得在线上跑一次：<code>select count(*) from interview where status = &#39;active&#39; and end_time is null and start_time &lt; now() - interval &#39;1 hour&#39;</code>。若结果非 0，说明存在<strong>其它</strong>退出路径漏了（例如 P1 的 <code>paused</code> 悬挂、或半开连接场景），值得单独追查。</li></ol><h2 id="_8-参考" tabindex="-1"><a class="header-anchor" href="#_8-参考"><span>8. 参考</span></a></h2>`,129),o(`ul`,null,[l[5]||=o(`li`,null,[o(`code`,null,`AGENTS.md`),a(` §6.5 暂停 / 重连（Pause / Resume）`)],-1),o(`li`,null,[i(m,{to:`/%E9%A1%B9%E7%9B%AE/%E9%9D%A2%E8%AF%95%E5%8A%A9%E6%89%8B/%E6%9E%B6%E6%9E%84%E8%AE%BE%E8%AE%A1/%E5%AE%9E%E6%97%B6%E9%9D%A2%E8%AF%95%E4%BC%9A%E8%AF%9D%E9%87%8D%E6%9E%84-%E9%A2%86%E5%9F%9F%E4%BA%8B%E4%BB%B6%E8%A7%A3%E8%80%A6.html`},{default:n(()=>[...l[4]||=[a(`实时面试会话重构-领域事件解耦`,-1)]]),_:1})]),l[6]||=o(`li`,null,[o(`code`,null,`websocket计时扣费.md`)],-1)])])}var f=c(u,[[`render`,d]]);export{l as _pageData,f as default};