import{A as e,D as t,L as n,f as r,m as i,p as a,s as o,u as s}from"./runtime-core.esm-bundler-BHJ3o1LK.js";import{t as c}from"./plugin-vue_export-helper-BDNMzG2s.js";var l=JSON.parse(`{"path":"/%E5%90%8E%E7%AB%AF/%E4%BA%91%E5%8E%9F%E7%94%9F/k3s/%E8%90%BD%E5%9C%B0/K3s%20%E6%96%B9%E6%A1%88%E6%8E%A7%E5%88%B6%E5%8F%B0-%E6%96%B9%E6%A1%88.html","title":"K3s 方案控制台（方案）","lang":"zh-CN","frontmatter":{"title":"K3s 方案控制台（方案）","icon":"mdi:application-cog","sort":15,"description":"K3s 方案控制台（把已验证的方案产品化） 文档定位：方案定稿 / 尚未开始实施。 上游两份方案都已实机验证： [K3s HA 一键部署方案](./K3s HA 一键部署方案.md) —— cluster-infra/k3s-ha/，verify 全绿、VIP 真实漂移 —— cluster-infra/monitoring/，17 条规则 + 端到端...","head":[["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"K3s 方案控制台（方案）\\",\\"image\\":[\\"\\"],\\"dateModified\\":\\"2026-09-24T10:14:13.000Z\\",\\"author\\":[{\\"@type\\":\\"Person\\",\\"name\\":\\"桥黎\\",\\"url\\":\\"\\"}]}"],["meta",{"property":"og:url","content":"https://docs.cqlql.top/%E5%90%8E%E7%AB%AF/%E4%BA%91%E5%8E%9F%E7%94%9F/k3s/%E8%90%BD%E5%9C%B0/K3s%20%E6%96%B9%E6%A1%88%E6%8E%A7%E5%88%B6%E5%8F%B0-%E6%96%B9%E6%A1%88.html"}],["meta",{"property":"og:site_name","content":"开发笔记"}],["meta",{"property":"og:title","content":"K3s 方案控制台（方案）"}],["meta",{"property":"og:description","content":"K3s 方案控制台（把已验证的方案产品化） 文档定位：方案定稿 / 尚未开始实施。 上游两份方案都已实机验证： [K3s HA 一键部署方案](./K3s HA 一键部署方案.md) —— cluster-infra/k3s-ha/，verify 全绿、VIP 真实漂移 —— cluster-infra/monitoring/，17 条规则 + 端到端..."}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:locale","content":"zh-CN"}],["meta",{"property":"og:updated_time","content":"2026-09-24T10:14:13.000Z"}],["meta",{"property":"article:modified_time","content":"2026-09-24T10:14:13.000Z"}]]},"git":{"createdTime":1790244853000,"updatedTime":1790244853000,"contributors":[{"name":"cql","username":"cql","email":"cql.ql@qq.com","commits":1,"url":"https://github.com/cql"}]},"readingTime":{"minutes":58.58,"words":17574},"filePathRelative":"后端/云原生/k3s/落地/K3s 方案控制台-方案.md","autoDesc":true}`),u={name:`K3s 方案控制台-方案.md`};function d(c,l,u,d,f,p){let m=e(`RouteLink`);return t(),s(`div`,null,[l[11]||=o(`h1`,{id:`k3s-方案控制台-把已验证的方案产品化`,tabindex:`-1`},[o(`a`,{class:`header-anchor`,href:`#k3s-方案控制台-把已验证的方案产品化`},[o(`span`,null,`K3s 方案控制台（把已验证的方案产品化）`)])],-1),o(`blockquote`,null,[l[5]||=o(`p`,null,[o(`strong`,null,`文档定位：方案定稿 / 尚未开始实施。`),a(` 上游两份方案都已实机验证：`)],-1),o(`ul`,null,[l[4]||=o(`li`,null,[a(`[K3s HA 一键部署方案](./K3s HA 一键部署方案.md) —— `),o(`code`,null,`cluster-infra/k3s-ha/`),a(`，`),o(`code`,null,`verify`),a(` 全绿、VIP 真实漂移`)],-1),o(`li`,null,[i(m,{to:`/%E5%90%8E%E7%AB%AF/%E4%BA%91%E5%8E%9F%E7%94%9F/k3s/%E8%90%BD%E5%9C%B0/%E5%8F%AF%E8%A7%82%E6%B5%8B%E6%80%A7%E6%A0%88%E8%BF%81%E7%A7%BB%E8%87%B3%E9%9B%86%E7%BE%A4%E5%86%85-%E6%96%B9%E6%A1%88.html`},{default:n(()=>[...l[0]||=[a(`可观测性栈迁移至集群内（方案）`,-1)]]),_:1}),l[1]||=a(` —— `,-1),l[2]||=o(`code`,null,`cluster-infra/monitoring/`,-1),l[3]||=a(`，17 条规则 + 端到端钉钉链路`,-1)])]),l[6]||=r(`<p>本文回答的是<strong>下一个问题</strong>：脚本已经跑通了，为什么还需要一个控制台、以及它到底该做什么。</p><p><strong>定位</strong>（注意不是「部署控制台」）：</p><blockquote><p><strong>K3s 方案控制台</strong> —— 把已验证的 K3s HA、VIP、Traefik、Registry、监控等基础设施方案， 固化为<strong>可配置、可校验、可执行、可观测</strong>的操作系统。</p></blockquote><p>「部署控制台」容易让人理解成「一个带网页的部署脚本」，而这个项目的实际设计不是那个。 它真正要回答的不是「如何执行 <code>deploy.sh</code>」，而是：</p><blockquote><p><strong>我怎样确定这套基础设施方案现在处于正确状态？</strong></p></blockquote><p>这也解释了为什么已经有 <code>./deploy.sh all</code>，仍然会觉得「不踏实」。 它要闭合的是一个环：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>准备一次方案配置 → 自动校验 → 看到执行计划 → 确认影响面</span></span>
<span class="line"><span>       → 执行 → 实时日志 → 16 项验证</span></span>
<span class="line"><span>       → 明确告诉你：这套集群现在到底对不对</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>第一版不追求功能多。<strong>这个环跑通，就已经解决了最初那个痛点</strong>—— 不是让我少敲几个命令，而是让我不用一直担心「是不是哪里忘了、是不是哪个参数错了」。</p>`,8)]),l[12]||=r(`<h2 id="一、起点-脚本减少了「手工操作」-但没有减少「人的不确定性」" tabindex="-1"><a class="header-anchor" href="#一、起点-脚本减少了「手工操作」-但没有减少「人的不确定性」"><span>一、起点：脚本减少了「手工操作」，但没有减少「人的不确定性」</span></a></h2><p>三份手工文档收敛成 <code>./deploy.sh all</code> 之后，复杂度并没有等比下降。它分三层：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>操作复杂度   要敲多少条命令、按什么顺序        →  已消灭（main() 阶段编排）</span></span>
<span class="line"><span>决策复杂度   每个值该填什么、为什么是这个值     →  已转移（config.env 逐行写明理由）</span></span>
<span class="line"><span>验证复杂度   我现在到底在不在一个有效状态      →  部分自动化，但输出是给人看的</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>真正让人不踏实的不是第一层，是<strong>第三层</strong>：脚本说「完成」了，我怎么知道它真的对？</p><p>这一点在两份方案里其实已经被反复印证——看《K3s HA 一键部署方案》§四「比手工文档多固化的八件事」 的「漏掉的后果」一列，出现频率最高的词组是同一个：</p><blockquote><p><strong>「看起来一切正常」</strong></p></blockquote><ul><li>污点丢了 → 不报错，只是少一个节点通告 VIP</li><li><code>curl -k</code> 通了 → 证书缺 SAN 被掩盖，直到 <code>kubectl</code> 访问 apiserver 才炸 <code>x509</code></li><li>servicelb 没禁 → <code>EXTERNAL-IP</code> 是节点物理 IP，看着像正常</li></ul><p>所以「控制台」要解决的，本质上是<strong>把「看起来正常」和「真的正常」之间的缝补上，并且让它可见</strong>。</p><h2 id="二、先划边界-哪些已经在脚本里了" tabindex="-1"><a class="header-anchor" href="#二、先划边界-哪些已经在脚本里了"><span>二、先划边界：哪些已经在脚本里了</span></a></h2><p>在动手之前必须承认一件事：<strong>三层里有两层已经存在，而且质量不低。</strong></p><table><thead><tr><th style="text-align:left;">层</th><th style="text-align:left;">现状</th><th style="text-align:left;">证据（<code>cluster-infra/</code>）</th></tr></thead><tbody><tr><td style="text-align:left;">输入</td><td style="text-align:left;"><code>config.env</code> 是唯一事实来源，每行都写了「为什么是这个值 / 改它要连带改什么」</td><td style="text-align:left;"><code>k3s-ha/config.env</code> 11 KB、<code>monitoring/config.env</code> 8.8 KB</td></tr><tr><td style="text-align:left;">校验</td><td style="text-align:left;"><code>preflight</code> 已经在<strong>重算拓扑</strong>，不是检查配置有没有填</td><td style="text-align:left;"><code>check_traefik_replicas</code>、VIP 冲突检测、代理真下载一次安装脚本、渲染残留 <code>__XXX__</code> 检测</td></tr><tr><td style="text-align:left;">执行</td><td style="text-align:left;">阶段幂等，且区分「配置幂等」与「无副作用」</td><td style="text-align:left;"><code>main()</code> 的 <code>PREFLIGHT_REPLICAS_SOFT</code> 分流</td></tr><tr><td style="text-align:left;">状态</td><td style="text-align:left;"><code>verify</code> 16 项，<strong>查链路不查配置</strong></td><td style="text-align:left;">控制面走到 <code>/readyz</code> + 证书 SAN；业务面逐节点 curl</td></tr><tr><td style="text-align:left;">输出</td><td style="text-align:left;">三种前缀 + 退出码</td><td style="text-align:left;"><code>ok ✓</code> / <code>warn !</code> / <code>die [错误]</code>；<code>phase_verify</code> 末尾 <code>return &quot;$fail&quot;</code></td></tr></tbody></table><p><strong>也就是说：校验层和状态层已经写好了，缺的只是「机器可读」和「人类可读」之间那一层薄壳。</strong></p><p>这决定了整个项目的性质——<strong>它不是从零做一个运维平台，是给一个已经正确的判定逻辑做视图。</strong></p><h2 id="三、架构与边界-gui-里不许复制「方案判定逻辑」" tabindex="-1"><a class="header-anchor" href="#三、架构与边界-gui-里不许复制「方案判定逻辑」"><span>三、架构与边界：GUI 里不许复制「方案判定逻辑」</span></a></h2><h3 id="_3-1-目标架构" tabindex="-1"><a class="header-anchor" href="#_3-1-目标架构"><span>3.1 目标架构</span></a></h3><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>                    Browser</span></span>
<span class="line"><span>                       │</span></span>
<span class="line"><span>                       ▼</span></span>
<span class="line"><span>              ┌─────────────────┐</span></span>
<span class="line"><span>              │  React + antd   │</span></span>
<span class="line"><span>              │  状态 / 配置 /   │</span></span>
<span class="line"><span>              │  任务 / 日志     │</span></span>
<span class="line"><span>              └────────┬────────┘</span></span>
<span class="line"><span>                       │ HTTP / SSE</span></span>
<span class="line"><span>                       ▼</span></span>
<span class="line"><span>              ┌─────────────────┐</span></span>
<span class="line"><span>              │    Go API       │</span></span>
<span class="line"><span>              │  Task Manager   │</span></span>
<span class="line"><span>              │  Auth           │</span></span>
<span class="line"><span>              │  Stream         │</span></span>
<span class="line"><span>              │  JSON Adapter   │</span></span>
<span class="line"><span>              └────────┬────────┘</span></span>
<span class="line"><span>                       │ exec</span></span>
<span class="line"><span>                       ▼</span></span>
<span class="line"><span>              ┌─────────────────┐</span></span>
<span class="line"><span>              │   deploy.sh     │</span></span>
<span class="line"><span>              │  preflight      │</span></span>
<span class="line"><span>              │  prepare        │</span></span>
<span class="line"><span>              │  server-first   │</span></span>
<span class="line"><span>              │  join           │</span></span>
<span class="line"><span>              │  kube-vip       │</span></span>
<span class="line"><span>              │  traefik        │</span></span>
<span class="line"><span>              │  verify         │</span></span>
<span class="line"><span>              └────────┬────────┘</span></span>
<span class="line"><span>                       │ SSH</span></span>
<span class="line"><span>          ┌────────────┼────────────┐</span></span>
<span class="line"><span>          ▼            ▼            ▼</span></span>
<span class="line"><span>         k1           k2           k3</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>Kubernetes API / Prometheus 暂时放在虚线区（P3）</strong>，在 P2 之前不进 Go API：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>     ┌───────────────┐        ┌───────────────┐</span></span>
<span class="line"><span>     │ Kubernetes API│        │  Prometheus   │</span></span>
<span class="line"><span>     │      P3       │        │      P3       │</span></span>
<span class="line"><span>     └───────┬───────┘        └───────┬───────┘</span></span>
<span class="line"><span>             └──────────┬─────────────┘</span></span>
<span class="line"><span>                        ▼</span></span>
<span class="line"><span>                     Go API</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="_3-2-边界怎么划-区分「方案判定」和「控制台自身校验」" tabindex="-1"><a class="header-anchor" href="#_3-2-边界怎么划-区分「方案判定」和「控制台自身校验」"><span>3.2 边界怎么划：区分「方案判定」和「控制台自身校验」</span></a></h3><p>这是本文最重要的一条，也是最容易在实施时被违反的一条。</p><p>初稿写的是「<strong>校验逻辑必须留在脚本里，一行都不许搬进后端</strong>」——<strong>原则对，但表述过死</strong>。 因为 Go 后端必然会有自己的校验，例如：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>任务是否正在运行</span></span>
<span class="line"><span>用户是否有权限执行</span></span>
<span class="line"><span>参数是否完整</span></span>
<span class="line"><span>是否允许同时执行两个部署任务</span></span>
<span class="line"><span>当前任务是否可以取消</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>这些<strong>不是 K3s 方案校验</strong>，不能也不该塞进 Bash。准确的说法是：</p><blockquote><p><strong>K3s 集群的事实判定与方案校验，以 <code>deploy.sh</code> 为唯一事实来源，Go 不复制这些判定逻辑； Go 可以拥有控制台自身的任务、权限、状态和参数校验。</strong></p></blockquote><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>Go</span></span>
<span class="line"><span>│</span></span>
<span class="line"><span>├── 控制台校验（Go 自己的领域）</span></span>
<span class="line"><span>│   ├── 参数完整性</span></span>
<span class="line"><span>│   ├── 任务状态</span></span>
<span class="line"><span>│   ├── 并发控制</span></span>
<span class="line"><span>│   └── 权限</span></span>
<span class="line"><span>│</span></span>
<span class="line"><span>└── 调用 Bash</span></span>
<span class="line"><span>       │</span></span>
<span class="line"><span>       └── K3s 事实校验（deploy.sh 是唯一事实来源）</span></span>
<span class="line"><span>           ├── VIP 冲突 / 同网段 / 空闲</span></span>
<span class="line"><span>           ├── kube-vip 两套 DaemonSet</span></span>
<span class="line"><span>           ├── Traefik 副本数 vs eligible nodes</span></span>
<span class="line"><span>           ├── TLS SAN</span></span>
<span class="line"><span>           ├── Registry 可达性</span></span>
<span class="line"><span>           └── …</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>判据很简单：</p><blockquote><p><strong>如果判定的是「集群是否符合本方案要求」，由 Bash 负责； 如果判定的是「控制台是否允许执行某个操作」，由 Go 负责。</strong></p></blockquote><p>这条比「要不要 SSH」更本质。因为以后必然会出现这种情形：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>Go 调用 verify --json</span></span>
<span class="line"><span>        ↓</span></span>
<span class="line"><span>Go 根据结果决定：</span></span>
<span class="line"><span>  · 是否允许显示「执行」</span></span>
<span class="line"><span>  · 是否要求二次确认</span></span>
<span class="line"><span>  · 是否允许继续下一阶段</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>这<strong>仍然是 Go 的控制流程</strong>，而不是 Go 在<strong>重新判断 K3s 是否正确</strong>。 两者容易混淆，区别在于：前者消费 <code>verify</code> 的结论，后者自己产出一个结论。</p><h4 id="为什么方案判定不能复制到-go" tabindex="-1"><a class="header-anchor" href="#为什么方案判定不能复制到-go"><span>为什么方案判定不能复制到 Go</span></a></h4><p>理由不是「懒得搬」，而是三条都不可妥协的：</p><ol><li><strong>离线测试已经压在这套 bash 上了。</strong> <code>k3s-ha/tests/selftest.sh</code>（79 项，秒级）+ <code>integration.sh</code>（88 项）。 把 <code>check_traefik_replicas</code> 翻译成 Go，等于把这 167 项断言的保护范围砍掉一半。</li><li><strong>两份事实来源必然漂移。</strong> 一旦 Go 里也有一份「副本数 vs eligible」的判断， 两边就会在某个边界条件下给出不同答案——而那时候你<strong>不知道该信谁</strong>。 这恰恰是控制台要消灭的「不可确认」，它会亲手把它造回来。</li><li><strong>校验的价值在于「每次执行都重算」。</strong> 方案 §四 第 5~7 条的原话是 「不只是写进清单，而是写成了<strong>每次执行都会重算的校验</strong>」。搬到 Go 就变回静态配置了。</li></ol><h3 id="_3-3-推论-go-后端第一版可以薄到不可思议" tabindex="-1"><a class="header-anchor" href="#_3-3-推论-go-后端第一版可以薄到不可思议"><span>3.3 推论：Go 后端第一版可以薄到不可思议</span></a></h3><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>POST /api/tasks            →  os/exec 起一个 bash deploy.sh &lt;phase&gt;</span></span>
<span class="line"><span>GET  /api/tasks/{id}/logs  →  读 stdout/stderr，按行推 SSE/WebSocket</span></span>
<span class="line"><span>GET  /api/state            →  跑 verify --json，返回结构化结果</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>没有 ORM、没有数据库、没有 K8s 客户端。这些不是「第一版可以省略」，而是 <strong>在 P2 之前都不应该出现</strong>。加上 §3.2 划出来的那四类控制台校验，就是第一版 Go 的全部内容。</p><h2 id="四、动作的「影响面」必须是一等公民" tabindex="-1"><a class="header-anchor" href="#四、动作的「影响面」必须是一等公民"><span>四、动作的「影响面」必须是一等公民</span></a></h2><p>这是现有脚本已经想清楚、但 GUI 最容易做丢的一件事。</p><p><code>deploy.sh</code> 的头部注释明说：</p><blockquote><p>幂等 = 「配置幂等」，而不是「执行无副作用」……<strong><code>traefik</code> 阶段会触发一次 Traefik 滚动重启</strong> ——所以不能把 <code>all</code> 当成「零影响重复执行」。只想「看看现状」请用 <code>./deploy.sh verify</code>。</p></blockquote><p>对照成控制台的动作表：</p><table><thead><tr><th style="text-align:left;">动作</th><th style="text-align:left;">改集群吗</th><th style="text-align:left;">影响面</th><th style="text-align:left;">GUI 该怎么表现</th></tr></thead><tbody><tr><td style="text-align:left;"><code>verify</code></td><td style="text-align:left;">否，只读</td><td style="text-align:left;">无</td><td style="text-align:left;">绿色，随时可点，结果即首页</td></tr><tr><td style="text-align:left;"><code>preflight</code></td><td style="text-align:left;">否，但会 SSH 到每台 + 渲染清单到临时目录</td><td style="text-align:left;">无</td><td style="text-align:left;">绿色，带「会连 3 台机器」提示</td></tr><tr><td style="text-align:left;"><code>render</code></td><td style="text-align:left;">否</td><td style="text-align:left;">无</td><td style="text-align:left;">绿色，输出可下载的渲染产物</td></tr><tr><td style="text-align:left;"><code>taint</code> / <code>kube-vip</code> / <code>traefik</code></td><td style="text-align:left;">是</td><td style="text-align:left;"><strong><code>traefik</code> 会滚动重启入口</strong></td><td style="text-align:left;">黄色，需二次确认</td></tr><tr><td style="text-align:left;"><code>all</code></td><td style="text-align:left;">是</td><td style="text-align:left;">包含上面全部 + 可能的 k3s 重启</td><td style="text-align:left;">黄色，<strong>不许做成无脑大按钮</strong></td></tr><tr><td style="text-align:left;"><code>uninstall</code></td><td style="text-align:left;">是</td><td style="text-align:left;">需 <code>CONFIRM_UNINSTALL=yes</code>，且<strong>≠ 恢复出厂</strong></td><td style="text-align:left;">红色，必须逐条列出「不会恢复什么」</td></tr></tbody></table><p>最后一行值得单独说。<code>uninstall</code> 的语义在方案 §八 里被刻意写清楚了：</p><blockquote><p><strong><code>uninstall</code> = 移除本方案管理的 kube-vip / Traefik 定制资源；K3s 本身及 <code>servicelb</code> 的禁用状态不自动恢复。</strong></p></blockquote><p>如果 GUI 把它渲染成一个「删除集群」按钮，用户点完会以为回到了干净状态，实际不是。 <strong>这类「边界语义」正是控制台最该显式表达的东西</strong>——脚本用文档写了，控制台要把它变成界面的一部分。</p><h2 id="五、技术栈" tabindex="-1"><a class="header-anchor" href="#五、技术栈"><span>五、技术栈</span></a></h2><h3 id="_5-1-前端-react-typescript-ant-design-对齐-pass-up-frontend" tabindex="-1"><a class="header-anchor" href="#_5-1-前端-react-typescript-ant-design-对齐-pass-up-frontend"><span>5.1 前端：React + TypeScript + Ant Design（对齐 <code>pass-up.frontend</code>）</span></a></h3><p><strong>不引入第二套心智</strong>——直接沿用已有的 React 基线。基线取自 <code>pass-up.frontend</code>：</p><table><thead><tr><th style="text-align:left;">项</th><th style="text-align:left;">取值</th><th style="text-align:left;">出处</th></tr></thead><tbody><tr><td style="text-align:left;">框架</td><td style="text-align:left;">React 19.2 + react-dom 19.2</td><td style="text-align:left;"><code>pnpm-workspace.yaml</code> 的 <code>catalog:</code></td></tr><tr><td style="text-align:left;">语言</td><td style="text-align:left;">TypeScript ~6.0</td><td style="text-align:left;">同上</td></tr><tr><td style="text-align:left;">构建</td><td style="text-align:left;">Vite 8.1</td><td style="text-align:left;">同上</td></tr><tr><td style="text-align:left;">组件库</td><td style="text-align:left;"><strong>antd 6.5</strong> + <code>@ant-design/icons</code> 6.3</td><td style="text-align:left;"><code>apps/admin</code>、<code>packages/shared</code> 的 dependencies</td></tr><tr><td style="text-align:left;">路由</td><td style="text-align:left;">react-router-dom 7.18</td><td style="text-align:left;"><code>apps/admin</code></td></tr><tr><td style="text-align:left;">状态</td><td style="text-align:left;">zustand 5.0</td><td style="text-align:left;"><code>catalog:</code></td></tr><tr><td style="text-align:left;">请求 / 时间</td><td style="text-align:left;">axios 1.18 + dayjs</td><td style="text-align:left;"><code>apps/admin</code></td></tr><tr><td style="text-align:left;">Lint</td><td style="text-align:left;">oxlint 1.76 + eslint 10</td><td style="text-align:left;">顶层 <code>devDependencies</code></td></tr></tbody></table><p><strong>工程结构照抄 <code>pass-up.frontend</code> 的形状</strong>：pnpm workspace + <code>apps/*</code> + <code>packages/*</code>， 依赖版本走 <code>catalog:</code> 集中管理，<code>packages/</code> 下复用 <code>shared</code> / <code>vite-config</code> / <code>tsconfig</code> / <code>eslint-config</code> 四个基础包。这样两个项目的依赖升级、lint 规则、tsconfig 是同一套心智，不用维护两份。</p><blockquote><p>一个提醒：<code>pass-up.frontend</code> 用了 <code>@ant-design/x</code>（antd 的 AI 对话组件库）。 控制台不需要它，别顺手带上。</p></blockquote><h4 id="明确不用-tailwind-css" tabindex="-1"><a class="header-anchor" href="#明确不用-tailwind-css"><span>明确不用 Tailwind CSS</span></a></h4><p><code>pass-up.frontend</code> 目前带着 <code>tailwindcss@4.3</code>，但<strong>控制台项目不带</strong>。</p><p>理由不只是「可读性」——在这个项目上它还有一层更硬的问题：</p><ol><li><strong>可读性</strong>：控制台的 UI 是<strong>大量表单 + 状态表格 + 日志流 + 步骤条 + 拓扑图</strong>， 这些几乎全是 antd 组件；真正需要自定义样式的只有布局外壳和少数状态色。 用 utility class 表达这些，会把「一个组件长什么样」拆散到十几个 class 字符串里。</li><li><strong>优先级打架（这条才是关键）</strong>：antd 6 用的是 <strong>CSS-in-JS</strong>，样式注入顺序不受你控制。 想覆盖 antd 组件内部结构时，Tailwind 的 utility 经常压不住，最后要靠 <code>!important</code> 兜底 ——<strong>这是维护成本，不是审美问题</strong>。</li><li><strong>antd 已经把视觉规范收敛在 theme token 里了</strong>，再叠一层 utility 等于两套规范并存。</li></ol><p><strong>替代方案</strong>（按推荐顺序）：</p><table><thead><tr><th style="text-align:left;">场景</th><th style="text-align:left;">做法</th></tr></thead><tbody><tr><td style="text-align:left;">布局</td><td style="text-align:left;">antd 的 <code>Layout</code> / <code>Grid</code> / <code>Space</code> / <code>Flex</code>，<strong>不写自定义 CSS</strong></td></tr><tr><td style="text-align:left;">需要微调</td><td style="text-align:left;"><strong>CSS Modules</strong>（<code>xxx.module.css</code>）——类名有语义、可跳转、可被 lint</td></tr><tr><td style="text-align:left;">全局主题</td><td style="text-align:left;"><code>ConfigProvider</code> + theme token（<code>components</code> 覆盖），而不是用 class 盖 antd 内部结构</td></tr><tr><td style="text-align:left;">状态色（通过 / 警告 / 失败）</td><td style="text-align:left;">定义<strong>一组语义化 CSS 变量</strong>，禁止散落的 <code>text-green-600</code> 这类写法</td></tr></tbody></table><blockquote><p>状态色那一条对本项目尤其重要：<code>verify</code> 的三态（<code>✓ 通过</code> / <code>! 警告</code> / <code>[错误]</code> 失败） 是整个界面的核心语义，它必须是<strong>一处定义、全局引用</strong>的 token， 而不是每个组件各写一遍颜色类名。</p></blockquote><h3 id="_5-2-后端-go-但理由不是「别和-passup-绑在一起」" tabindex="-1"><a class="header-anchor" href="#_5-2-后端-go-但理由不是「别和-passup-绑在一起」"><span>5.2 后端：Go，但理由不是「别和 PassUp 绑在一起」</span></a></h3><p>「避免技术栈耦合」是对的，但太弱了。真正的理由是<strong>部署形态</strong>：</p><table><thead><tr><th style="text-align:left;">维度</th><th style="text-align:left;">Go</th><th style="text-align:left;">Java</th></tr></thead><tbody><tr><td style="text-align:left;">产物</td><td style="text-align:left;">单静态二进制，<code>./k3s-console</code> 即跑</td><td style="text-align:left;">JAR + JVM</td></tr><tr><td style="text-align:left;">运行时资源占用</td><td style="text-align:left;"><strong>低</strong>，适合部署在资源受限的 Linux 管理机上</td><td style="text-align:left;">JVM 基线本身就不小</td></tr><tr><td style="text-align:left;">进程/流编排</td><td style="text-align:left;"><code>os/exec</code> + goroutine 天然顺手</td><td style="text-align:left;">需线程池 + 流处理框架</td></tr><tr><td style="text-align:left;">SSH</td><td style="text-align:left;"><code>golang.org/x/crypto/ssh</code> 一等公民</td><td style="text-align:left;">需 JSch / sshd 库</td></tr></tbody></table><blockquote><p>「资源占用低」是<strong>定性描述</strong>，不写具体 RSS 数字。 实际占用取决于 Go runtime、HTTP server、SSH 连接数、子进程、日志缓存、并发任务、 以及以后是否引入 K8s client —— 把某个数字写成选型依据，它会很快变成一个过时且误导的硬指标。 真正的技术理由就是上表那三条稳定的：<strong>单静态二进制 / <code>os/exec</code> + goroutine / SSH 生态</strong>。</p></blockquote><p><strong>最关键的一条是内存</strong>：这个控制台大概率要和被它管理的集群跑在同一批机器上， 而那三台节点是 <strong>7.7 GiB / 台</strong>——正是这个数字，逼得 PassUp 后端的 <code>limits.memory</code> 从 8Gi 收到 2Gi（<code>-XX:MaxRAMPercentage=70</code> 按 limit 算堆，8Gi limit → 5.6Gi 堆）。 <strong>在一个刚因为内存紧张而调整过 JVM 参数的集群旁边，再放一个 JVM 控制台，是自相矛盾的。</strong></p><h3 id="_5-3-关于-client-go-它是-p3-的事-而且给不了你一半的状态" tabindex="-1"><a class="header-anchor" href="#_5-3-关于-client-go-它是-p3-的事-而且给不了你一半的状态"><span>5.3 关于 <code>client-go</code>：它是 P3 的事，而且给不了你一半的状态</span></a></h3><p>那份建议把「接 Kubernetes API」放在 V3，方向对，但<strong>理由说反了</strong>——它给人的印象是 「K8s API 比较高级所以放后面」。真实原因是：</p><blockquote><p><strong><code>verify</code> 的 16 项里，有一半是「节点本地事实」，<code>client-go</code> 永远拿不到。</strong></p></blockquote><p>逐项拆开看：</p><table><thead><tr><th style="text-align:left;">判定项</th><th style="text-align:left;">数据来源</th></tr></thead><tbody><tr><td style="text-align:left;">节点状态 / 两套 DaemonSet / 业务 VIP / <code>externalTrafficPolicy</code> / 副本分布 / 副本数-vs-eligible / 节点污点</td><td style="text-align:left;"><strong>k8s API</strong>（7 项）</td></tr><tr><td style="text-align:left;">控制面 VIP 是否落在网卡（<code>/32</code>）/ 6443 TCP / <code>/readyz</code> / <strong>证书 SAN</strong> / 业务连通 / <strong>逐节点 VIP 通告</strong> / <strong><code>registries.yaml</code> 逐字节一致</strong></td><td style="text-align:left;"><strong>SSH 到节点</strong>（7 项）</td></tr><tr><td style="text-align:left;">本机直连 / 版本固定提醒</td><td style="text-align:left;">本地（2 项）</td></tr></tbody></table><p>那 7 项 SSH 项里有几条是<strong>整份验收里最值钱的</strong>：</p><ul><li><strong>证书 SAN</strong>：<code>curl -k</code> 会跳过校验，只有 <code>openssl s_client</code> 才能发现漏写 <code>tls-san</code> （本环境真实踩过，用 VIP 访问 apiserver 报 <code>x509</code>）；</li><li><strong>逐节点 VIP 通告</strong>：<code>externalTrafficPolicy: Local</code> 下每个承载 Traefik 的节点各自通告 VIP， 「某台没在通告」是<strong>最隐蔽的一类故障</strong>；</li><li><strong><code>registries.yaml</code> 一致</strong>：<code>cmp</code> 逐字节比对，容器拉镜像行为是否和脚本产物一致。</li></ul><p><strong>结论</strong>：控制台的状态层<strong>不是</strong>一个「接 K8s API」的问题，而是一个 <strong>「把 verify 链路跑一遍并结构化」</strong>的问题。<code>client-go</code> 只能覆盖 7/16， 而且是最容易用 <code>kubectl</code> 拿的那 7 项。</p><p>所以正确顺序是：<strong>先把 <code>verify</code> 变成机器可读，再谈 <code>client-go</code>。</strong></p><h4 id="而且-client-go-可以不急着做" tabindex="-1"><a class="header-anchor" href="#而且-client-go-可以不急着做"><span>而且 <code>client-go</code> 可以不急着做</span></a></h4><p>这一点值得单独写下来，因为它是<strong>优先级</strong>问题，不只是<strong>顺序</strong>问题。</p><p>本项目最核心的价值不是「查看 Pod / Deployment / Node」——那些 Headlamp 已经做得很好， 再做一个不会更好。真正的核心链路是：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>deploy.sh → verify → 方案判定 → 控制台展示</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>所以 P3 接 <code>client-go</code> 的定位是：</p><blockquote><p><strong>为了补充交互和实时状态，而不是为了替代 <code>verify</code>。</strong></p></blockquote><p>一旦这条原则写清楚，<code>client-go</code> 就从「必须做的 V3」变成了「<strong>有明确增量才做的可选项</strong>」。 如果某天发现「节点 CPU / 内存实时曲线」确实需要它，再加； 如果 <code>kubectl</code> 的输出已经够用，就不加。<strong>不做也是一种合法结论。</strong></p><h3 id="_5-4-图表-p3-再定-但候选已经收窄" tabindex="-1"><a class="header-anchor" href="#_5-4-图表-p3-再定-但候选已经收窄"><span>5.4 图表：P3 再定，但候选已经收窄</span></a></h3><p>antd 自身不带图表。本项目需要图的地方有两处，<strong>优先用非图表的表达</strong>：</p><table><thead><tr><th style="text-align:left;">需求</th><th style="text-align:left;">首选</th><th style="text-align:left;">理由</th></tr></thead><tbody><tr><td style="text-align:left;">拓扑关系（节点 → VIP → Traefik → 业务）</td><td style="text-align:left;"><strong>手写 SVG / 纯 CSS</strong>，不用图库</td><td style="text-align:left;">拓扑是<strong>固定形状</strong>（3 节点 + 2 个 VIP），不是数据驱动的图。引图库反而更绕</td></tr><tr><td style="text-align:left;">时序指标（CPU / 内存 / targets）</td><td style="text-align:left;">到 P3 再选：<code>@ant-design/charts</code> 或 ECharts</td><td style="text-align:left;">前者与 antd 同源、主题自动一致；后者生态更大、运维场景更常见</td></tr></tbody></table><p><strong>不要在 P1 引入任何图表库</strong>——P1 的全部内容是「一张状态卡 + 若干表格」， 表格能表达的东西不要画成图。</p><h2 id="六、控制台自己跑在哪-一台-linux-管理机-p0-就要定" tabindex="-1"><a class="header-anchor" href="#六、控制台自己跑在哪-一台-linux-管理机-p0-就要定"><span>六、控制台自己跑在哪：一台 Linux 管理机（P0 就要定）</span></a></h2><p><strong>结论：跑在一台 Linux 管理机上，控制台自己不进被它管理的集群。</strong></p><p>这不是个可以往后拖的问题——后面的 SSH 私钥、<code>kubectl</code>、<code>bash</code>、<code>docker</code>、网络、<code>kubeconfig</code>、 任务日志全都依赖它。所以它必须在 P0/P1 就定下来。</p><h3 id="_6-1-为什么不能跑在-windows" tabindex="-1"><a class="header-anchor" href="#_6-1-为什么不能跑在-windows"><span>6.1 为什么不能跑在 Windows</span></a></h3><p>目标是「<strong>让这个东西本身比手工执行脚本更可靠</strong>」。如果控制台自己依赖 Git Bash、<code>cygpath</code>、Windows 版 SSH、本机 Docker，那它<strong>重新制造了一层不确定性</strong>—— 而这层不确定性还是你自己要维护的：</p><ul><li><code>cygpath -m</code>（Windows 原生 kubectl 读不了 Git Bash 的 <code>/c/...</code> 路径）；</li><li>MSYS 会把「看着像 Unix 绝对路径」的环境变量值改写掉（<code>VITE_BASE_PATH=/admin</code> 被转成 <code>&lt;PortableGit&gt;/admin</code>，导致 admin 整页白屏而 HTTP 全 200）；</li><li>构建还会被沙箱的删除保护 shim 拦下，要额外带 <code>CODEBUDDY_SAFE_DELETE_ENABLED=0</code>。</li></ul><p>这些都是真实踩过的坑，但它们<strong>是开发环境的问题，不该成为运行时的依赖</strong>。</p><h3 id="_6-2-为什么也不建议跑在集群节点上" tabindex="-1"><a class="header-anchor" href="#_6-2-为什么也不建议跑在集群节点上"><span>6.2 为什么也不建议跑在集群节点上</span></a></h3><p>三个选项对照：</p><table><thead><tr><th style="text-align:left;">选项</th><th style="text-align:left;">满足</th><th style="text-align:left;">问题</th></tr></thead><tbody><tr><td style="text-align:left;"><strong>A. Windows 本机</strong></td><td style="text-align:left;">SSH、本机 kubectl</td><td style="text-align:left;">依赖 Git Bash / cygpath / 本机 Docker（见 §6.1）</td></tr><tr><td style="text-align:left;"><strong>B. 集群内某个节点</strong></td><td style="text-align:left;">SSH、kubectl、bash 全有</td><td style="text-align:left;">① 节点是 <strong>7.7 GiB</strong>，控制台要和业务抢内存；② 控制台管的就是这个集群，<strong>集群挂了控制台也没了</strong>，等于把「看门狗」和「被看护对象」放一起；③ <code>build-image</code> 用的是<strong>本机 docker</strong>，节点上没有</td></tr><tr><td style="text-align:left;"><strong>C. 集群外一台 Linux 管理机</strong></td><td style="text-align:left;">全部满足</td><td style="text-align:left;">需要一台机器 + 一次部署——<strong>这是唯一可接受的成本</strong></td></tr></tbody></table><p>选项 B 的第 ② 条是决定性的：控制台的价值是「集群不对时告诉你哪里不对」， 它<strong>必须在集群之外</strong>才能履行这个职责。</p><h3 id="_6-3-部署形态" tabindex="-1"><a class="header-anchor" href="#_6-3-部署形态"><span>6.3 部署形态</span></a></h3><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>                   Windows</span></span>
<span class="line"><span>                  Browser</span></span>
<span class="line"><span>                     │</span></span>
<span class="line"><span>                     ▼</span></span>
<span class="line"><span>             ┌───────────────┐</span></span>
<span class="line"><span>             │ Linux 管理机  │</span></span>
<span class="line"><span>             │               │</span></span>
<span class="line"><span>             │ K3s Console   │</span></span>
<span class="line"><span>             │ Go + React    │</span></span>
<span class="line"><span>             │               │</span></span>
<span class="line"><span>             │ deploy.sh     │</span></span>
<span class="line"><span>             │ SSH Key       │</span></span>
<span class="line"><span>             └───────┬───────┘</span></span>
<span class="line"><span>                     │</span></span>
<span class="line"><span>          ┌──────────┼──────────┐</span></span>
<span class="line"><span>          ▼          ▼          ▼</span></span>
<span class="line"><span>         k1         k2         k3</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>建议直接做成一个容器镜像，脚本<strong>随镜像打包</strong>而不是挂载宿主机目录—— 这样「控制台用的脚本版本」和「镜像 tag」是绑定的，可复现， 也不会出现「镜像里是 A 版脚本、宿主机挂载的是 B 版」这种新的不可确认。</p><h4 id="这个位置带来的直接价值-集群全挂时它还在" tabindex="-1"><a class="header-anchor" href="#这个位置带来的直接价值-集群全挂时它还在"><span>这个位置带来的直接价值：集群全挂时它还在</span></a></h4><p>这是「跑在集群外」最实际的收益，值得单独写出来。即使三台节点全部失联：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>k1 ❌</span></span>
<span class="line"><span>k2 ❌</span></span>
<span class="line"><span>k3 ❌</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>只要管理机还活着，你仍然能看到：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>Cluster unreachable</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>并且能继续做：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>SSH 检查  →  网络检查  →  重新部署  →  恢复</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>如果控制台跑在集群里（§6.2 选项 B），这个场景下<strong>你会同时失去集群和控制台</strong>， 连「它到底怎么了」都问不出来。<strong>这才符合「控制台」的定位</strong>—— 它的职责是在系统不正常的时候仍然可用。</p><h3 id="_6-4-无论跑在哪都必须满足的两条" tabindex="-1"><a class="header-anchor" href="#_6-4-无论跑在哪都必须满足的两条"><span>6.4 无论跑在哪都必须满足的两条</span></a></h3><ol><li><strong>SSH 私钥是控制台的最高价值凭据</strong>，它能登录全部节点。 控制台自身的认证/授权等级不能低于这把钥匙——否则它就是集群最弱的一环。</li><li><strong><code>build-image</code> 是唯一一个「必须在有 docker 的机器上跑」的阶段。</strong> 要么在管理机上装 docker，要么把镜像构建挪到 CI（<code>docker-infra/</code> 已有 CI）。 建议后者——构建本来就不该是控制台运行时的职责。</li></ol><h2 id="七、真正的缺口清单-读代码读出来的" tabindex="-1"><a class="header-anchor" href="#七、真正的缺口清单-读代码读出来的"><span>七、真正的缺口清单（读代码读出来的）</span></a></h2><p>这些是<strong>现有脚本里确实没有、但方案文档的 checklist 里已经要求</strong>的项。 控制台的 V2 校验层就应该是补这些，而不是另起炉灶。</p><h3 id="_7-1-内网-registry-端口可达性-——-方案要求了-脚本没查" tabindex="-1"><a class="header-anchor" href="#_7-1-内网-registry-端口可达性-——-方案要求了-脚本没查"><span>7.1 内网 Registry 端口可达性 —— 方案要求了，脚本没查</span></a></h3><p><code>K3s HA 一键部署方案</code> §6.1 的 checklist 明确写着：</p><blockquote><p><strong>镜像加速可用</strong>：三台都能访问内网 Registry 的三个端口（<code>curl -sI http://172.16.0.222:5001/v2/</code> / <code>:5002/v2/</code> 有响应即可）。</p></blockquote><p>但 <code>phase_preflight</code> 实际检查的是 <strong>k3s 下载代理</strong>（<code>K3S_DOWNLOAD_PROXY</code> 逐台真下载一次安装脚本）， <strong>没有查 Registry 三个端口</strong>。它只在 <code>README.md</code> 的排障表里以「怎么排查」的形式出现。</p><p>这个缺口的代价不低——README 自己写了：</p><blockquote><p>containerd <strong>不会自动回退官方源</strong>，所以要么修缓存，要么临时把镜像换成 <code>172.16.0.222:5000/...</code></p></blockquote><p>即：代理缓存不可达时，<code>ghcr.io</code> 上的 kube-vip 会<strong>直接拉不动</strong>，且没有回退。</p><p><code>monitoring/deploy.sh</code> 里还有一处<strong>死注释</strong>，说明这个检查本来打算做：</p><div class="language-bash line-numbers-mode" data-highlighter="shiki" data-ext="bash" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-bash"><span class="line"><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">  # 镜像可达性（只探测不拉取，省时间）</span></span>
<span class="line"><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">  log</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> &quot;镜像版本：prometheus=</span><span style="--shiki-light:#E45649;--shiki-dark:#98C379;">\${</span><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">PROMETHEUS_IMAGE</span><span style="--shiki-light:#E45649;--shiki-dark:#98C379;">}</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> loki=</span><span style="--shiki-light:#E45649;--shiki-dark:#98C379;">\${</span><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">LOKI_IMAGE</span><span style="--shiki-light:#E45649;--shiki-dark:#98C379;">}</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> alloy=</span><span style="--shiki-light:#E45649;--shiki-dark:#98C379;">\${</span><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">ALLOY_IMAGE</span><span style="--shiki-light:#E45649;--shiki-dark:#98C379;">}</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">&quot;</span></span>
<span class="line"><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">  ok</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> &quot;preflight 通过&quot;</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>注释说「只探测不拉取」，但下面只 <code>log</code> 了版本号，<strong>一个探测都没做</strong>。</p><h3 id="_7-2-节点规格下限-os-cpu-内存-——-方案假设了-脚本没验" tabindex="-1"><a class="header-anchor" href="#_7-2-节点规格下限-os-cpu-内存-——-方案假设了-脚本没验"><span>7.2 节点规格下限（OS / CPU / 内存）—— 方案假设了，脚本没验</span></a></h3><p><code>prepare</code> 阶段实际做的事是：<code>hostname</code> → <code>/etc/hosts</code> 托管块 → 关 swap → <code>modprobe</code> → <code>apt update/upgrade</code> → <code>ufw</code> → 建 auto-manifests 目录。<strong>没有一条检查节点规格。</strong></p><p>而方案 §2.1 是按「Ubuntu 24.04.2 LTS、7.7 GiB / 4 vCPU」写的， 监控方案的内存取值（<code>limits</code>、PVC、<code>ResourceQuota</code>）全都建立在这个规格上。</p><p>建议补的判据（都是 SSH 一条命令）：</p><div class="language-bash line-numbers-mode" data-highlighter="shiki" data-ext="bash" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-bash"><span class="line"><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;"># 这三条现在都没有，但都影响后续所有取值</span></span>
<span class="line"><span style="--shiki-light:#0184BC;--shiki-dark:#56B6C2;">.</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> /etc/os-release</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;"> &amp;&amp; </span><span style="--shiki-light:#0184BC;--shiki-dark:#56B6C2;">echo</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> &quot;</span><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">$VERSION_ID</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">&quot;</span><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">     # 期望 24.04</span></span>
<span class="line"><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">nproc</span><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">                                        # 期望 &gt;= 2</span></span>
<span class="line"><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">awk</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> &#39;/MemTotal/{print int($2/1024/1024)}&#39;</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> /proc/meminfo</span><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">   # 期望 &gt;= 4（GiB）</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="_7-3-vip-的「空闲」与「同网段」——-只查了一半" tabindex="-1"><a class="header-anchor" href="#_7-3-vip-的「空闲」与「同网段」——-只查了一半"><span>7.3 VIP 的「空闲」与「同网段」—— 只查了一半</span></a></h3><p><code>preflight</code> 查的是「VIP 不等于任何节点物理 IP」和「<code>VIP_CP ≠ VIP_TRAEFIK</code>」—— <strong>这两条是防止漂移时抢 ARP 的硬铁律，是对的。</strong></p><p>但方案 §6.1 的 checklist 还要求：</p><ul><li>VIP 在局域网内 <strong>ping 不通</strong>（未被占用）</li><li>VIP 与节点 <strong>同网段</strong></li></ul><p>这两条目前是纯手工步骤。控制台可以把它变成一条自动校验。</p><h3 id="_7-4-跨配置文件的事实一致性-——-只-warn-不拦" tabindex="-1"><a class="header-anchor" href="#_7-4-跨配置文件的事实一致性-——-只-warn-不拦"><span>7.4 跨配置文件的事实一致性 —— 只 warn，不拦</span></a></h3><p>这是<strong>两份 config.env 之间唯一的共享事实</strong>，也是最容易漂移的地方：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>k3s-ha/config.env            monitoring/config.env</span></span>
<span class="line"><span>────────────────────         ────────────────────</span></span>
<span class="line"><span>NODE_TAINTS=                 TAINT_KEY=&quot;k3s-monitoring&quot;</span></span>
<span class="line"><span>  &quot;k3 k3s-monitoring:          TAINT_EFFECT=&quot;NoSchedule&quot;</span></span>
<span class="line"><span>        NoSchedule&quot;           INFRA_NODE=&quot;k3&quot;</span></span>
<span class="line"><span>                             （且 INFRA_NODE 必须在 k3s-ha 的 NODES 里）</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>monitoring/config.env</code> 自己写了「改一处必须改另一处」，但 <code>monitoring/deploy.sh</code> 的 <code>phase_preflight</code> 发现污点缺失时<strong>只是 warn，不中断</strong>：</p><div class="language-bash line-numbers-mode" data-highlighter="shiki" data-ext="bash" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-bash"><span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">  else</span></span>
<span class="line"><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">    warn</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> &quot;</span><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">$INFRA_NODE</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> 缺少污点 </span><span style="--shiki-light:#E45649;--shiki-dark:#98C379;">\${</span><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">TAINT_KEY</span><span style="--shiki-light:#E45649;--shiki-dark:#98C379;">}</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">:</span><span style="--shiki-light:#E45649;--shiki-dark:#98C379;">\${</span><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">TAINT_EFFECT</span><span style="--shiki-light:#E45649;--shiki-dark:#98C379;">}</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">（当前：$(</span><span style="--shiki-light:#0184BC;--shiki-dark:#56B6C2;">printf</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> &#39;%s&#39; &quot;</span><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">$taints</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">&quot; </span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">|</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;"> tr</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> &#39;\\n&#39; &#39; &#39;)）&quot;</span></span>
<span class="line"><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">    warn</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> &quot;  这不会挡住部署（nodeSelector 仍会把单实例组件钉住），但说明 k3s-ha 的&quot;</span></span>
<span class="line"><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">    warn</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> &quot;  NODE_TAINTS 与这里的 TAINT_KEY 已经不一致了，请回去核对 cluster-infra/k3s-ha/config.env&quot;</span></span>
<span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">  fi</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>warn 的理由是成立的（不挡部署），但<strong>这意味着没有任何一个地方能回答「这两份配置现在一致吗」</strong>。 这正是控制台该拥有的「跨文件事实视图」。</p><h3 id="_7-5-逐项状态不可机器读" tabindex="-1"><a class="header-anchor" href="#_7-5-逐项状态不可机器读"><span>7.5 逐项状态不可机器读</span></a></h3><p><code>verify</code> 现在输出的是给人看的东西，虽然结构已经很像机读：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>    ✓ 全部 3 台都能经代理下载安装脚本</span></span>
<span class="line"><span>    ! 本机 kubectl 经 172.16.0.210 访问失败（本机与该网段不通时属预期）</span></span>
<span class="line"><span>  [Traefik 业务入口]</span></span>
<span class="line"><span>    ✓ 业务 VIP 正确（可漂移）</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>[ -t 1 ]</code> 已经做了 ANSI 自动降级（非 TTY 不带色），所以解析是<strong>可行</strong>的； 但靠正则匹配 <code>✓</code>/<code>!</code> 属于<strong>脆弱耦合</strong>——文案改一个字就断。</p><p><strong>建议 P0 就做</strong>：给 <code>verify</code> / <code>preflight</code> 加 <code>--json</code>，<strong>判定逻辑一行不改</strong>，只改输出层。 协议形状见 §8.1——注意这不是「加个输出格式」，而是给 Bash 定一份<strong>结果协议</strong>。</p><h2 id="八、分期" tabindex="-1"><a class="header-anchor" href="#八、分期"><span>八、分期</span></a></h2><table><thead><tr><th style="text-align:left;">期</th><th style="text-align:left;">内容</th><th style="text-align:left;">为什么是这个顺序</th></tr></thead><tbody><tr><td style="text-align:left;"><strong>P0</strong></td><td style="text-align:left;"><code>verify</code> / <code>preflight</code> 加 <code>--json</code> —— <strong>Bash 的结果协议</strong>（§8.1）</td><td style="text-align:left;">不动判定逻辑、不改现有工作流；这是整个项目唯一的地基</td></tr><tr><td style="text-align:left;"><strong>P1</strong></td><td style="text-align:left;">只读控制台：状态首页 + 拓扑事实 + <strong>方案配置视图</strong> + 配置一致性视图（§8.2）</td><td style="text-align:left;">零风险，且立刻兑现「不可确认 → 可确认」</td></tr><tr><td style="text-align:left;"><strong>P2</strong></td><td style="text-align:left;">动作编排：带<strong>影响面标签</strong> + 二次确认 + 实时日志流（§8.3）</td><td style="text-align:left;">此时才有「按钮」，且每个按钮的危险等级已明确（§四）</td></tr><tr><td style="text-align:left;"><strong>P3</strong></td><td style="text-align:left;">补 §7.1~7.3 的校验 + <code>client-go</code> / Prometheus API 指标面板（§8.4）</td><td style="text-align:left;">校验补齐后，指标才有意义（否则「面板全绿」和「真的正常」还是两件事）</td></tr><tr><td style="text-align:left;"><strong>P4</strong></td><td style="text-align:left;"><strong>第二类受管对象：PassUp</strong>（状态 → 动作）（§8.7）</td><td style="text-align:left;">此时「协议 → 控制层 → UI → 任务」的骨架已经被 P0~P2 验证过一轮，PassUp 是它的第二个消费者。<strong>第一版不改名、不扩范围</strong></td></tr></tbody></table><h3 id="_8-1-p0-json-不是「输出格式」-是-bash-的结果协议" tabindex="-1"><a class="header-anchor" href="#_8-1-p0-json-不是「输出格式」-是-bash-的结果协议"><span>8.1 P0：<code>--json</code> 不是「输出格式」，是 Bash 的<strong>结果协议</strong></span></a></h3><p>这一点值得单独强调，因为它决定了整个项目的性质。</p><p>做完 P0 之后，链路不再是「Shell → GUI」，而是：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>deploy.sh</span></span>
<span class="line"><span>  │</span></span>
<span class="line"><span>  ├── 人类输出（现在的 ✓ / ! / [错误]）</span></span>
<span class="line"><span>  │</span></span>
<span class="line"><span>  └── JSON 输出  ←── 结果协议</span></span>
<span class="line"><span>          ↓</span></span>
<span class="line"><span>        Go API</span></span>
<span class="line"><span>          ↓</span></span>
<span class="line"><span>       React</span></span>
<span class="line"><span>          ↓</span></span>
<span class="line"><span>      （未来）AI</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><blockquote><p><strong>GUI 不应该理解 Bash 的文字输出，而应该理解 Bash 的结果协议。</strong></p></blockquote><p>逐项的最小形状（<strong>四字段是必须的，P0 可以只做这四个</strong>）：</p><div class="language-json line-numbers-mode" data-highlighter="shiki" data-ext="json" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-json"><span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">{</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">  &quot;id&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: </span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">&quot;traefik.replica_eligible&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">,</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">  &quot;level&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: </span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">&quot;ok&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">,</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">  &quot;detail&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: </span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">&quot;replicas=2, eligible_nodes=2&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">,</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">  &quot;fix&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: </span><span style="--shiki-light:#0184BC;--shiki-dark:#D19A66;">null</span></span>
<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">}</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>失败项：</p><div class="language-json line-numbers-mode" data-highlighter="shiki" data-ext="json" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-json"><span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">{</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">  &quot;id&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: </span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">&quot;tls.apiserver_san&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">,</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">  &quot;level&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: </span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">&quot;error&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">,</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">  &quot;detail&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: </span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">&quot;VIP 172.16.0.210 is not included in certificate SAN&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">,</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">  &quot;fix&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: </span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">&quot;检查 TLS_SAN 配置并重新执行 server-first&quot;</span></span>
<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">}</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>建议再加两个字段</strong>（可选，但趁 P0 一起做最省事——事后补要动所有判定点）：</p><div class="language-json line-numbers-mode" data-highlighter="shiki" data-ext="json" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-json"><span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">{</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">  &quot;id&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: </span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">&quot;traefik.replica_eligible&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">,</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">  &quot;level&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: </span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">&quot;ok&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">,</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">  &quot;detail&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: </span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">&quot;replicas=2, eligible_nodes=2&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">,</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">  &quot;fix&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: </span><span style="--shiki-light:#0184BC;--shiki-dark:#D19A66;">null</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">,</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">  &quot;source&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: </span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">&quot;cluster&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">,</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">  &quot;duration_ms&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: </span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;">183</span></span>
<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">}</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><table><thead><tr><th style="text-align:left;">可选字段</th><th style="text-align:left;">取值</th><th style="text-align:left;">用途</th></tr></thead><tbody><tr><td style="text-align:left;"><code>source</code></td><td style="text-align:left;"><code>node</code> / <code>cluster</code> / <code>local</code> / <code>config</code></td><td style="text-align:left;">UI 直接显示「数据来自：节点 / Kubernetes / 本机 / 配置」。<strong>它同时也是 §3.2 那条边界的自证</strong>——<code>source=cluster</code> 的项就是由 Bash 判定的</td></tr><tr><td style="text-align:left;"><code>duration_ms</code></td><td style="text-align:left;">整数</td><td style="text-align:left;">排查「为什么 <code>verify</code> 花了 12 秒」。一眼能看出是谁慢：<code>registry.reachability 85ms</code> / <code>tls.apiserver_san 120ms</code> / <code>business.connectivity 7800ms</code></td></tr></tbody></table><blockquote><p><code>source</code> 的取值正好对应 §5.3 里 verify 三项数据来源的划分（k8s API / SSH 到节点 / 本地）， 所以它不是额外发明的维度，而是把已有的事实显式化。</p></blockquote><p>字段约定：</p><table><thead><tr><th style="text-align:left;">字段</th><th style="text-align:left;">含义</th></tr></thead><tbody><tr><td style="text-align:left;"><code>id</code></td><td style="text-align:left;"><strong>稳定标识</strong>——文案可以改，<code>id</code> 不能改。前端按 <code>id</code> 做跳转 / 筛选 / 忽略，不按文案匹配</td></tr><tr><td style="text-align:left;"><code>level</code></td><td style="text-align:left;"><code>ok</code> / <code>warn</code> / <code>error</code> / <code>skip</code>，对应现有三种前缀，也是 §5.1 那组状态色 token 的来源（<code>skip</code> 见下）</td></tr><tr><td style="text-align:left;"><code>detail</code></td><td style="text-align:left;">给人看的一句话，含关键数值（<code>replicas=2, eligible_nodes=2</code>）</td></tr><tr><td style="text-align:left;"><code>fix</code></td><td style="text-align:left;"><strong>可复制的修复命令或动作</strong>，没有则 <code>null</code>。脚本里其实已经写了不少（如 <code>→ 跑 ./deploy.sh taint</code>），P0 只是把它们变成结构化字段</td></tr></tbody></table><p>顶层再包一层：</p><div class="language-json line-numbers-mode" data-highlighter="shiki" data-ext="json" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-json"><span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">{</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">  &quot;phase&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: </span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">&quot;verify&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">,</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">  &quot;started_at&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: </span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">&quot;2026-09-24T11:40:09+08:00&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">,</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">  &quot;summary&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: { </span><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">&quot;ok&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: </span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;">14</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">, </span><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">&quot;warn&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: </span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;">2</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">, </span><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">&quot;error&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: </span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;">0</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">, </span><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">&quot;skip&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: </span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;">1</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;"> },</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">  &quot;items&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: []</span></span>
<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">}</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h4 id="skip-是第-4-态-不是「通过」" tabindex="-1"><a class="header-anchor" href="#skip-是第-4-态-不是「通过」"><span><code>skip</code> 是第 4 态，不是「通过」</span></a></h4><p>实施时补的（初稿只约定了三态）。理由：判定项会因为<strong>外部条件不具备</strong>而根本没跑—— 节点上没有 <code>openssl</code>、本机没有 kubectl、<code>MANAGE_REGISTRY=false</code>、首台还没装 k3s…… 这些既不是 <code>ok</code> 也不是 <code>error</code>。如果它们干脆不出现在结果里，消费方就无法区分 「这项通过了」和「这项压根没跑」——<strong>而这恰好就是本项目要防的「看起来一切正常」</strong>。</p><p>所以 <code>skip</code> 必须显式出现，且<strong>在 UI 上不能和 <code>ok</code> 共用颜色</strong>。</p><h4 id="人类输出保持逐字节不变" tabindex="-1"><a class="header-anchor" href="#人类输出保持逐字节不变"><span>人类输出保持逐字节不变</span></a></h4><p><code>--json</code> 是<strong>增量</strong>：不加参数时 <code>./deploy.sh verify</code> 的输出与加协议层之前<strong>逐字节一致</strong>， 退出码也不变（判定逻辑一行没改）。实现上靠两个约定：</p><ul><li>原来打印过的行（<code>ok</code> / <code>warn</code> / <code>log</code>）<strong>原样保留</strong>，协议项用「只记录不打印」的通道挂上去；</li><li>原来没打印的项（如 <code>kubevip.daemonset_*</code> 只在缺失时才 warn）不会凭空多出一行 ✓。</li></ul><p>验证方式：同一场景下 <code>git stash</code> 前后各跑一次 <code>verify</code>，<code>diff</code> 应为空。</p><h4 id="p0-已落地的判定项-id-实施结果" tabindex="-1"><a class="header-anchor" href="#p0-已落地的判定项-id-实施结果"><span>P0 已落地的判定项 id（实施结果）</span></a></h4><blockquote><p>下面是 P0（<code>verify</code> / <code>preflight</code>）的 32 项。<strong>P1 又加了 6 项</strong>（三个只读命令的判定项）， 快照总数 38 项 —— 见 §8.2「三个只读命令」与附录 v6。 <code>selftest.sh</code> 第 12 节对这份清单做快照断言：<strong>id 是对外契约，文案可改、id 不可改</strong>。</p></blockquote><p><code>preflight</code>（含被 <code>verify</code> 复用的部分）：</p><table><thead><tr><th style="text-align:left;">id</th><th style="text-align:left;">source</th><th style="text-align:left;">说明</th></tr></thead><tbody><tr><td style="text-align:left;"><code>env.tools</code></td><td style="text-align:left;">local</td><td style="text-align:left;">ssh / scp / sed 是否存在（<code>die</code>）</td></tr><tr><td style="text-align:left;"><code>config.nodes</code></td><td style="text-align:left;">config</td><td style="text-align:left;"><code>NODES</code> 声明是否可解析</td></tr><tr><td style="text-align:left;"><code>config.vip_cp</code> / <code>config.vip_traefik</code></td><td style="text-align:left;">config</td><td style="text-align:left;">两个 VIP 是否已填</td></tr><tr><td style="text-align:left;"><code>vip.not_node_ip</code></td><td style="text-align:left;">config</td><td style="text-align:left;"><strong>VIP 不得等于任何节点物理 IP</strong>（硬约束 1）</td></tr><tr><td style="text-align:left;"><code>vip.distinct</code></td><td style="text-align:left;">config</td><td style="text-align:left;">控制面 VIP 与业务 VIP 不得相同</td></tr><tr><td style="text-align:left;"><code>node.count_ha</code></td><td style="text-align:left;">config</td><td style="text-align:left;">节点数是否够 etcd 高可用</td></tr><tr><td style="text-align:left;"><code>ssh.reachable</code></td><td style="text-align:left;">node</td><td style="text-align:left;">逐台 SSH 是否可登录</td></tr><tr><td style="text-align:left;"><code>k3s.install_source</code></td><td style="text-align:left;">config</td><td style="text-align:left;">官方源但没配代理 → warn</td></tr><tr><td style="text-align:left;"><code>k3s.download_proxy</code></td><td style="text-align:left;">node</td><td style="text-align:left;">逐台经代理真下载一次安装脚本</td></tr><tr><td style="text-align:left;"><code>render.placeholders</code></td><td style="text-align:left;">local</td><td style="text-align:left;">渲染后是否残留 <code>__PLACEHOLDER__</code>（<code>die</code>）</td></tr><tr><td style="text-align:left;"><code>traefik.replica_eligible</code></td><td style="text-align:left;">cluster</td><td style="text-align:left;">副本数 vs 可承载节点数（preflight 硬校验 / verify 软复核共用同一 id）</td></tr></tbody></table><p><code>verify</code>：</p><table><thead><tr><th style="text-align:left;">id</th><th style="text-align:left;">source</th><th style="text-align:left;">说明</th></tr></thead><tbody><tr><td style="text-align:left;"><code>node.list</code></td><td style="text-align:left;">cluster</td><td style="text-align:left;"><code>kubectl get nodes</code> 是否成功（<strong>NotReady 本身不算失败</strong>，与改动前一致）</td></tr><tr><td style="text-align:left;"><code>node.taint_spec</code></td><td style="text-align:left;">config</td><td style="text-align:left;"><code>NODE_TAINTS</code> 条目是否合法</td></tr><tr><td style="text-align:left;"><code>node.taint_present</code></td><td style="text-align:left;">cluster</td><td style="text-align:left;">逐节点核对声明的污点是否真的在</td></tr><tr><td style="text-align:left;"><code>kubevip.daemonset_controlplane</code> / <code>_services</code></td><td style="text-align:left;">cluster</td><td style="text-align:left;">两套 DaemonSet 是否在位</td></tr><tr><td style="text-align:left;"><code>vip.cp_bound</code></td><td style="text-align:left;">node</td><td style="text-align:left;">控制面 VIP 的 <code>/32</code> 落在哪个节点</td></tr><tr><td style="text-align:left;"><code>vip.tcp_6443</code></td><td style="text-align:left;">node</td><td style="text-align:left;">经 VIP 的 6443 TCP 是否可达</td></tr><tr><td style="text-align:left;"><code>apiserver.readyz</code></td><td style="text-align:left;">node</td><td style="text-align:left;">经 VIP 请求 <code>/readyz</code>（含 k3s 关闭匿名时 401/403 属预期的分支）</td></tr><tr><td style="text-align:left;"><code>apiserver.readyz_authed</code></td><td style="text-align:left;">cluster</td><td style="text-align:left;">节点侧已认证复核 apiserver 是否真健康</td></tr><tr><td style="text-align:left;"><code>tls.apiserver_san</code></td><td style="text-align:left;">node</td><td style="text-align:left;"><strong>证书 SAN 是否含 VIP</strong>（<code>curl -k</code> 查不出来）</td></tr><tr><td style="text-align:left;"><code>local.direct_readyz</code></td><td style="text-align:left;">local</td><td style="text-align:left;">本机 kubectl 经 VIP 直连（不通属预期，只提示）</td></tr><tr><td style="text-align:left;"><code>traefik.external_ip</code></td><td style="text-align:left;">cluster</td><td style="text-align:left;">业务 VIP 是否为配置值（是节点物理 IP 则不漂移）</td></tr><tr><td style="text-align:left;"><code>traefik.external_traffic_policy</code></td><td style="text-align:left;">cluster</td><td style="text-align:left;">是否 <code>Local</code></td></tr><tr><td style="text-align:left;"><code>traefik.replica_distribution</code></td><td style="text-align:left;">cluster</td><td style="text-align:left;">副本是否摊在 ≥2 个节点（反亲和是 preferred，会静默失效）</td></tr><tr><td style="text-align:left;"><code>traefik.replica_landing</code></td><td style="text-align:left;">cluster</td><td style="text-align:left;">落点数是否 ≥ <code>TRAEFIK_REPLICAS</code></td></tr><tr><td style="text-align:left;"><code>business.connectivity</code></td><td style="text-align:left;">node</td><td style="text-align:left;"><code>http://VIP/</code> 是否有响应（404 也算通）</td></tr><tr><td style="text-align:left;"><code>business.vip_advertised@&lt;节点名&gt;</code></td><td style="text-align:left;">node</td><td style="text-align:left;">逐台确认「本机 → 业务 VIP」（Local 策略下各自通告）</td></tr><tr><td style="text-align:left;"><code>registry.registries_yaml@&lt;节点名&gt;</code></td><td style="text-align:left;">node</td><td style="text-align:left;">逐台比对 <code>registries.yaml</code> 与脚本产物</td></tr><tr><td style="text-align:left;"><code>registry.registries_yaml</code></td><td style="text-align:left;">config</td><td style="text-align:left;">未开启 <code>MANAGE_REGISTRY</code> 时的整段 <code>skip</code></td></tr><tr><td style="text-align:left;"><code>version.pinned</code></td><td style="text-align:left;">config</td><td style="text-align:left;"><code>K3S_VERSION</code> 是否钉死（可复现的最后一环）</td></tr></tbody></table><blockquote><p>带 <code>@&lt;节点名&gt;</code> 后缀的 id 是<strong>逐节点展开</strong>的：同一项在 3 台节点上会产出 3 条， 前缀稳定、后缀可变，消费方按前缀分组即可。</p></blockquote><p><strong>为什么 <code>id</code> 必须稳定</strong>：现在外部若要读 <code>verify</code> 的结论，只能正则匹配 <code>✓</code> / <code>!</code> —— 这是<strong>脆弱耦合，文案改一个字就断</strong>。而 <code>id</code> 让「判定项是什么」和「怎么显示」彻底解耦。</p><p><strong>这一层做对了，后面所有消费者（CLI、Go API、Web UI、未来的 AI）复用同一套事实结果。</strong> 这比「做 GUI」本身更重要。</p><h3 id="_8-2-p1-只读控制台" tabindex="-1"><a class="header-anchor" href="#_8-2-p1-只读控制台"><span>8.2 P1：只读控制台</span></a></h3><p>P1 的全部内容是「一张状态卡 + 若干表格」，<strong>不引入任何图表库</strong>（§5.4）。四个视图：</p><table><thead><tr><th style="text-align:left;">视图</th><th style="text-align:left;">内容</th></tr></thead><tbody><tr><td style="text-align:left;"><strong>状态首页</strong></td><td style="text-align:left;"><code>verify --json</code> 的渲染结果，按 <code>level</code> 分组，每项带 <code>detail</code> 与 <code>fix</code></td></tr><tr><td style="text-align:left;"><strong>拓扑事实</strong></td><td style="text-align:left;">VIP 持有者（<code>plndr-cp-lock</code> 的 <code>holderIdentity</code>）、漂移次数（<code>leaseTransitions</code>）、<code>eligible nodes</code> vs <code>TRAEFIK_REPLICAS</code></td></tr><tr><td style="text-align:left;"><strong>方案配置视图</strong></td><td style="text-align:left;">见下</td></tr><tr><td style="text-align:left;"><strong>配置一致性视图</strong></td><td style="text-align:left;">§7.4 的跨文件事实（<code>k3s-ha</code> 的 <code>NODE_TAINTS</code> ↔ <code>monitoring</code> 的 <code>TAINT_KEY</code> / <code>INFRA_NODE</code>）</td></tr></tbody></table><h4 id="方案配置视图-只读-但必须标注来源" tabindex="-1"><a class="header-anchor" href="#方案配置视图-只读-但必须标注来源"><span>方案配置视图：只读，但必须标注来源</span></a></h4><p>P1 需要能「看到当前方案配置是什么」，<strong>但不是自由编辑器</strong>：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>┌─────────────────────────────────────┐</span></span>
<span class="line"><span>│ K3s HA 配置                         │</span></span>
<span class="line"><span>├─────────────────────────────────────┤</span></span>
<span class="line"><span>│ 节点                                │</span></span>
<span class="line"><span>│   k1   172.16.0.211    control      │</span></span>
<span class="line"><span>│   k2   172.16.0.212    control      │</span></span>
<span class="line"><span>│   k3   172.16.0.213    control      │</span></span>
<span class="line"><span>│                                     │</span></span>
<span class="line"><span>│ Control Plane VIP     172.16.0.210  │</span></span>
<span class="line"><span>│ Traefik VIP           172.16.0.180  │</span></span>
<span class="line"><span>│ Traefik replicas      2             │</span></span>
<span class="line"><span>│ Registry              172.16.0.222:5000</span></span>
<span class="line"><span>└─────────────────────────────────────┘</span></span>
<span class="line"><span></span></span>
<span class="line"><span>来源：k3s-ha/config.env</span></span>
<span class="line"><span>[查看原始配置]        ← 不是 [修改]</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>P1 不提供修改。</strong> 这与 §十「配置的唯一事实来源必须是可 git diff 的文件，GUI 只是它的视图」 是同一条约束。真要改配置时走的是「打开文件 / 提交 PR」，而不是在网页上填表单 ——后者会重新引入决策复杂度（§十）。</p><p>「来源」那一行是刻意加的：它让用户始终知道<strong>这个值是从哪个文件读出来的</strong>， 而不是让界面变成一个凭空产生配置的地方。</p><h4 id="p1-已落地的数据出口-三个只读命令-实施结果" tabindex="-1"><a class="header-anchor" href="#p1-已落地的数据出口-三个只读命令-实施结果"><span>P1 已落地的数据出口：三个只读命令（实施结果）</span></a></h4><p>P1 前端需要三份数据：「当前方案配置」「拓扑事实」「跨文件一致性」。 它们<strong>没有</strong>让 Go 去解析 <code>config.env</code>，而是给 <code>deploy.sh</code> 加了三个只读子命令：</p><table><thead><tr><th style="text-align:left;">命令</th><th style="text-align:left;">回答什么</th><th style="text-align:left;">连集群吗</th></tr></thead><tbody><tr><td style="text-align:left;"><code>show-config --json</code></td><td style="text-align:left;">脚本当前解析到的方案配置（节点 / 污点 / VIP / 副本 / 版本 / registry）</td><td style="text-align:left;">否</td></tr><tr><td style="text-align:left;"><code>topology --json</code></td><td style="text-align:left;">VIP 持有者与漂移次数、可承载节点数 vs 副本数、副本实际落点</td><td style="text-align:left;">是（不可达时如实报 <code>reachable:false</code>，不失败）</td></tr><tr><td style="text-align:left;"><code>consistency --json</code></td><td style="text-align:left;">§7.4 的跨文件事实（<code>NODE_TAINTS</code> ↔ <code>TAINT_KEY</code> / <code>INFRA_NODE</code>）</td><td style="text-align:left;">否</td></tr></tbody></table><p><strong>为什么不放在 Go 里</strong>：</p><ol><li><strong><code>config.env</code> 是 bash 语法</strong>（含数组），<code>source</code> 才是它的权威解析器。 在 Go 里重写一遍解析器，等于把「配置的唯一事实来源」复制成两份，必然漂移。</li><li>这三个命令<strong>故意不跑 <code>preflight</code></strong>。<code>preflight</code> 要 SSH 到每台机器， 而这些命令存在的意义之一恰恰是「集群或网络不正常时也能看」—— 配置有问题时它们如实报出「脚本读到了什么」，而不是被 preflight 挡在门外。</li></ol><p>这三个命令是 §8.5「Go 后端可以薄到不可思议」的兑现： <strong>Go 侧没有一行配置解析、没有一行方案判定</strong>，只有「放行哪些命令」这一张表。</p><h4 id="go-侧的边界落在哪-一张命令表-实施结果" tabindex="-1"><a class="header-anchor" href="#go-侧的边界落在哪-一张命令表-实施结果"><span>Go 侧的边界落在哪：一张命令表（实施结果）</span></a></h4><p>§3.2 划的边界「控制台是否允许执行某操作 → Go」，实施后就是 <code>internal/api/commands.go</code> 里的一张表。P1 时它是两个<strong>正交</strong>字段：</p><ul><li><code>read_only</code>：这个命令改不改集群；</li><li><code>allowed</code>：<strong>P1 允不允许从控制台发起</strong>。</li></ul><p>写操作全部 <code>read_only:false, allowed:false</code>，并且<strong>照样列出来</strong>，附上影响面文案。 理由就是 §四 那一条：用户看到一个没有按钮的界面，会自己去服务器上敲 <code>all</code> —— 那才是真正危险的，因为他不会有机会看到「<code>traefik</code> 会滚动重启入口」。</p><p>所以 <code>/api/commands</code> 把写操作连同影响面一起返回，界面照实展示「为什么这里没有按钮」； <code>POST /api/run</code> 拒绝时返回 <strong>403 + <code>command_not_allowed</code> + 影响面原文</strong>，而不是一个通用错误。</p><p>另外把「命令存在」与「命令被允许」分成两个错误码：未知命令是 <code>400 unknown_command</code>， 存在但不放行是 <code>403 command_not_allowed</code>。两者的排错方向完全不同。</p><blockquote><p>P2 把 <code>read_only</code> 升级成了 <code>severity</code>（三档），<code>allowed</code> 保持不变。见 §8.3。</p></blockquote><h4 id="前端相对-§5-1-基线的两处偏离-实施结果" tabindex="-1"><a class="header-anchor" href="#前端相对-§5-1-基线的两处偏离-实施结果"><span>前端相对 §5.1 基线的两处偏离（实施结果）</span></a></h4><table><thead><tr><th style="text-align:left;">偏离</th><th style="text-align:left;">理由</th></tr></thead><tbody><tr><td style="text-align:left;"><code>@k3s-console/vite-config</code> 砍掉 <code>@tailwindcss/vite</code> / <code>code-inspector-plugin</code> / <code>react-compiler</code> / <code>assetsDir: assets&lt;version&gt;</code></td><td style="text-align:left;">前三个与「把已正确的判定逻辑可视化」无关，少一个 babel 环节就少一处构建风险；<code>assetsDir</code> 是给「静态资源走对象存储、新旧 chunk 并存」用的（见 PassUp 前端），而控制台的前端产物由 Go 二进制直接托管，Vite 默认的 content-hash 文件名已经够用。因为不用 Tailwind，<code>main.tsx</code> 里那层 <code>StyleProvider layer</code> 也随之消失</td></tr><tr><td style="text-align:left;">新增第 5 个视图「动作清单」</td><td style="text-align:left;">§8.2 定的是 4 个视图。实现时发现 §四 那条约束（影响面是一等公民）如果不落到界面上，就永远只是一句文档；而把「当前不开放的动作 + 各自影响面」列出来，恰恰是最诚实的呈现方式</td></tr></tbody></table><h4 id="p1-实施结果-实测" tabindex="-1"><a class="header-anchor" href="#p1-实施结果-实测"><span>P1 实施结果（实测）</span></a></h4><ul><li><strong>四态语义在真实数据上成立</strong>：<code>verify --json</code> 22 项 → <code>ok 21 / warn 1 / error 0 / skip 0</code>。 唯一那条 warn 是 <code>local.direct_readyz</code>（本机 kubectl 经控制面 VIP 访问失败）—— 控制台所在的本机不在集群网段，属预期。</li><li><strong>§5.3 的判断被实测确认</strong>：<code>source</code> 分布 <code>cluster 10 / node 10 / local 1 / config 1</code>。 <code>client-go</code> 只能覆盖 <code>cluster</code> 那 10 项，另外 12 项它拿不到。</li><li><strong><code>skip</code> 不是纸面设计</strong>：静态检查发现脚本里共 12 处 <code>skip</code> 分支。</li><li><strong>三个只读命令的实测耗时</strong>（Windows + Git Bash）：<code>show-config</code> 14.7s、 <code>consistency</code> 6.6s、<code>topology</code> 31.5s、<code>verify</code> 202.6s。 ⚠️ 这个量级<strong>不代表部署形态</strong>：同一条 <code>show-config</code> 在 Linux 管理机上是百毫秒级， 这里 13 秒几乎全是 Git Bash 的进程开销。它的直接后果是<strong>前端必须统一走 SSE</strong>—— 「快的轮询一次、慢的流式」这种按快慢分叉的写法，在本机开发时会让「快」的那条看起来像卡住。</li><li><strong>真浏览器验收</strong>：5 个视图全部渲染成功，<strong>控制台 0 error</strong>（含 <code>pageerror</code>）。 这一条不能省：接口返回正确 JSON 和页面渲染出来是两件事，中间任何一步出问题 （构建产物、路由、antd 版本、CSS-in-JS 注入）都会得到 <strong>HTTP 全 200 的白屏</strong>。</li><li><strong>Go 侧测试 13 项</strong>：<code>api</code> 8 项（拒绝写操作 / 未知命令 / 多余参数 / 命令表自洽）、 <code>task</code> 5 项。其中最关键的一条是把「<strong>退出码 1 + 合法协议 JSON → <code>Done</code> 而不是 <code>Failed</code></strong>」 固定成回归测试 —— 这是整个 P1 最容易搞错的语义：把它归成失败， 界面就会把「一次成功的检查，结论是有问题」显示成「控制台出故障了」。</li><li><strong><code>deploy.sh</code> 离线自测 84 项（80 通过 / 0 失败 / 4 跳过）</strong>；协议项 id 快照由 32 项扩到 <strong>38 项</strong> （新增的 6 项就是上面三个只读命令的判定项）。</li></ul><h3 id="_8-3-p2-动作编排" tabindex="-1"><a class="header-anchor" href="#_8-3-p2-动作编排"><span>8.3 P2：动作编排</span></a></h3><p><code>preflight</code> / <code>taint</code> / <code>traefik</code> / <code>all</code> 带<strong>影响面标签</strong>（§四）、二次确认、实时日志流。</p><p><code>uninstall</code> 建议<strong>放到 P2 的最后</strong>，且必须带 §四 那条「不会恢复什么」的清单。</p><h4 id="三档-severity-取代-read-only" tabindex="-1"><a class="header-anchor" href="#三档-severity-取代-read-only"><span>三档 <code>severity</code> 取代 <code>read_only</code></span></a></h4><p>P2 把 P1 的 <code>read_only</code> 换成了 <code>severity</code>。原因是那个布尔值一直在承担两件事， 而 P2 需要它同时驱动「上什么色」和「要不要确认」：</p><table><thead><tr><th style="text-align:left;"><code>severity</code></th><th style="text-align:left;">含义</th><th style="text-align:left;">颜色</th><th style="text-align:left;">默认确认</th></tr></thead><tbody><tr><td style="text-align:left;"><code>readonly</code></td><td style="text-align:left;">不改任何东西</td><td style="text-align:left;">绿</td><td style="text-align:left;">点了就跑</td></tr><tr><td style="text-align:left;"><code>write</code></td><td style="text-align:left;">会改集群或节点</td><td style="text-align:left;">黄</td><td style="text-align:left;">二次确认</td></tr><tr><td style="text-align:left;"><code>destructive</code></td><td style="text-align:left;">不可逆，且<strong>不等于恢复出厂</strong></td><td style="text-align:left;">红</td><td style="text-align:left;">手打 + 逐条勾选</td></tr></tbody></table><p><code>IsReadOnly()</code> 由 <code>severity</code> 派生，所以「只读」这个概念没有消失，只是不再是<strong>存储形态</strong> —— 这也是为什么它和 <code>allowed</code> 仍然是正交的（<code>allowed</code> 是「这台控制台当前允不允许」， 由启动参数决定，与命令本身的性质无关）。</p><p>颜色沿用与判定项同一套语义 token（<code>--k3s-ok/warn/error</code>）：界面上 「绿 = 没影响、黄 = 有影响、红 = 不可逆」应当是<strong>同一个心智</strong>， 不管它是用来标记一个判定项还是一个动作。</p><h4 id="确认阶梯-分级而不是一刀切" tabindex="-1"><a class="header-anchor" href="#确认阶梯-分级而不是一刀切"><span>确认阶梯：分级而不是一刀切</span></a></h4><p><code>confirm</code> 四档，对应四种代价：</p><table><thead><tr><th style="text-align:left;"><code>confirm</code></th><th style="text-align:left;">命令</th><th style="text-align:left;">界面要求</th></tr></thead><tbody><tr><td style="text-align:left;"><code>none</code></td><td style="text-align:left;">全部只读命令</td><td style="text-align:left;">不弹窗</td></tr><tr><td style="text-align:left;"><code>button</code></td><td style="text-align:left;"><code>kubeconfig</code> / <code>prepare</code> / <code>server-first</code> / <code>join</code> / <code>taint</code> / <code>kube-vip</code> / <code>traefik</code></td><td style="text-align:left;">摊开影响面，点确认</td></tr><tr><td style="text-align:left;"><code>type</code></td><td style="text-align:left;"><code>all</code></td><td style="text-align:left;">还要<strong>手打命令名</strong></td></tr><tr><td style="text-align:left;"><code>type+ack</code></td><td style="text-align:left;"><code>uninstall</code></td><td style="text-align:left;">手打 + 逐条勾选「不会恢复什么」</td></tr></tbody></table><p><strong>为什么不统一成「确定吗？」</strong>：如果 <code>all</code> 和 <code>verify</code> 弹的是同一个框，用户会养成 闭眼点确定的习惯 —— 而那个习惯恰好会在 <code>uninstall</code> 上出事。分级的目的是让确认<strong>携带信息量</strong>。</p><p><code>phrase</code> 必须<strong>原样</strong>相等：Go 侧直接比较 <code>req.Confirm != spec.Phrase</code>， <strong>刻意不做 <code>TrimSpace</code>、不做大小写归一</strong>。<code>&quot; all&quot;</code> 和 <code>&quot;ALL&quot;</code> 恰恰是「粘贴 / 手滑」 最容易产生的两种形态，放宽等于把这道闸门关掉。这条被 <code>TestWriteCommandWithWrongPhraseIsRejected</code> 钉死（第一版实现就漏了 <code>TrimSpace</code>，是测试抓住的）。</p><p><strong>确认必须在服务端校验</strong>，不能只做在界面上：</p><ul><li>界面上的确认是给人看的；</li><li>Go 侧的校验是给「绕过界面直接打 API」看的。</li></ul><p>两者都要有，但<strong>只有后者是边界</strong>。<code>handleRun</code> 的顺序是 <code>command_not_allowed</code>(403) → <code>confirmation_required</code>(400) → <code>acknowledgement_required</code>(400)， <code>writer_busy</code>(409) 在最后。</p><h4 id="uninstall-的「不会恢复什么」是脚本自己的原文" tabindex="-1"><a class="header-anchor" href="#uninstall-的「不会恢复什么」是脚本自己的原文"><span><code>uninstall</code> 的「不会恢复什么」是脚本自己的原文</span></a></h4><p>四条勾选项<strong>不是</strong>在 Go 里另写一遍，而是 <code>phase_uninstall</code> 收尾时 <code>warn</code> 打印的那四行原文。 §四 说「脚本用文档写了，控制台要把它变成界面的一部分」—— 变成勾选项就是字面意义上的「一部分」。</p><p><code>missingAcks()</code> 返回<strong>缺哪几条</strong>，接口把缺的那几条原文回给前端， 所以界面能说「还有 N 条未确认」，而不是一个通用错误。</p><p><code>CONFIRM_UNINSTALL=yes</code> 由 Go 在放行之后注入，并且<strong>刻意不下发到前端</strong>（<code>json:&quot;-&quot;</code>）： 前端不该也不需要知道这个开关。脚本的闸门仍然有效，而不是被控制台绕过去 —— <code>TestUninstallSatisfiesScriptGate</code> 用一个会回显该环境变量的桩脚本把这条固定住。</p><h4 id="写操作串行-且写期间只读也一并拒绝" tabindex="-1"><a class="header-anchor" href="#写操作串行-且写期间只读也一并拒绝"><span>写操作串行，且写期间只读也一并拒绝</span></a></h4><p><code>taint</code> 和 <code>traefik</code> 同时跑，结果不是「更快」，而是两个进程按各自的节奏改同一批节点对象 —— 那种状态连排错都无从下手。所以 <code>Manager</code> 持一个 <code>exclusiveID</code>，第二个写操作拿到 <strong>409 <code>writer_busy</code></strong>。</p><p><strong>写操作期间只读命令也一并拒绝</strong>：写操作跑到一半时读出来的状态是<strong>中间态</strong>， 拿它做判断只会误导人。界面如实反映这一点（所有按钮都禁用并说明原因）， 而不是让用户点下去才发现。</p><h4 id="取消-是-cancelled-不是-failed" tabindex="-1"><a class="header-anchor" href="#取消-是-cancelled-不是-failed"><span>取消：是 <code>cancelled</code>，不是 <code>failed</code></span></a></h4><p><code>POST /api/tasks/{id}/cancel</code> 杀掉脚本进程。它单列一个状态，因为<strong>用户看到的文案完全不同</strong>：</p><ul><li><code>failed</code>：脚本自己坏了 → 去查日志；</li><li><code>cancelled</code>：人按了取消 → 「<strong>不会回滚已经做完的步骤</strong>，集群可能停在中间状态； <code>deploy.sh</code> 的每个阶段都是幂等的，重跑对应阶段即可收敛」。</li></ul><p>把它归成 <code>failed</code> 会让人以为是脚本的问题，去查半天日志。</p><p><code>Cancel()</code> 只对「正在跑」的任务有效。每个任务从 <code>Manager</code> 的父 ctx 派生自己的子 ctx， <strong>不能用 HTTP 请求的 ctx</strong> —— <code>verify</code> 要跑 3 分钟，请求早就返回了。</p><h4 id="部署级闸门-allow-write-false" tabindex="-1"><a class="header-anchor" href="#部署级闸门-allow-write-false"><span>部署级闸门：<code>-allow-write=false</code></span></a></h4><p>启动参数。打开时整台控制台退化成 P1 只读：<code>/api/commands</code> 会把写操作的 <code>allowed</code> 改写成 <code>false</code>，所以界面上<strong>根本不会渲染出可点的执行按钮</strong>，而不是点了才报错； 直接打 API 也得到 403。</p><p>这一层存在的理由是部署形态：把控制台放到一台别人也能访问的机器上时， 不必指望界面上的确认弹窗。</p><h4 id="前端-动作清单从「说明」变成「操作」" tabindex="-1"><a class="header-anchor" href="#前端-动作清单从「说明」变成「操作」"><span>前端：动作清单从「说明」变成「操作」</span></a></h4><ul><li>按 <code>severity</code> 分三组（只读 / 写操作 / 破坏性），每组带语义色图标与一句话提示。</li><li>新增<strong>「发起」列</strong>，写的是「点了就跑 / 弹窗确认 / 手打命令名 / 手打 + 逐条勾选」。 这一列是这一页存在的主要理由：它让用户在<strong>点之前</strong>就能看出 <code>uninstall</code> 比 <code>verify</code> 贵。 把阶梯藏进弹窗里就晚了 —— 那时人已经决定要点了。</li><li>点执行后按 <code>confirm</code> 弹确认，确认后<strong>执行面板</strong>（Drawer）自动打开： 实时日志 + 结果 + 中止按钮。</li><li>执行槽位放在 store 而不是组件里：<code>all</code> 要跑几分钟，用户中途切去看拓扑是很自然的事。 组件卸载会断掉 SSE 订阅，但脚本挂在 Go 的 Manager 上下文上照跑； 回来时凭 <code>taskId</code> 重新订阅，服务端会把缓冲的日志整段重放 （<code>Task.Subscribe</code> 在同一把锁里原子地取「已缓冲日志 + 实时通道」）。 重放意味着本地<strong>必须先清空日志</strong>，否则会看到重复行。</li><li>只读视图也补上了「中止」：<code>verify</code> 在本环境要跑一分钟，只有写操作能停是没道理的。</li></ul><h4 id="p2-实施结果-实测" tabindex="-1"><a class="header-anchor" href="#p2-实施结果-实测"><span>P2 实施结果（实测）</span></a></h4><ul><li><strong>写通道在真机上跑通</strong>：通过界面发起 <code>kubeconfig</code>（§四 点名的「风险最低的写操作」）， 确认阶梯 → 执行 → 实时日志 → 结果全部走通。产物 <code>server</code> 指向控制面 VIP （<code>172.16.0.210</code>），<strong>重跑前后 sha256 完全一致</strong> —— 幂等成立。</li><li><strong>确认阶梯四级全部实测</strong>（真浏览器）：<code>button</code> 级没有输入框且确认按钮直接可点； <code>type</code> 级在 <code>ALL</code> / <code>&quot; all&quot;</code> 下保持禁用、原样输入 <code>all</code> 才可点； <code>type+ack</code> 级列出 4 条勾选项，只手打短语仍禁用、4 条全勾才可点。 破坏性确认按钮实测计算样式为 <code>rgb(255, 77, 79)</code> 的 danger 样式 —— 「破坏性要看起来就是破坏性」这条被断言固定住，而不是靠肉眼看截图。</li><li><strong>执行面板</strong>：日志在 1~2 秒内出现（<strong>早于结果</strong>，所以「实时」是真的）， <code>show-config</code> 全流程约 11~20 秒，<strong>控制台 0 error</strong>（含 <code>pageerror</code>）。</li><li><strong>只读模式验收</strong>：<code>-allow-write=false</code> 下 5 个只读命令仍可点、 9 个写操作<strong>仍然列出</strong>但按钮全部禁用，绕过界面直接打 API 得到 <code>403 command_not_allowed</code>。</li><li><strong>Go 侧测试 20 项</strong>（<code>api</code> 15 + <code>task</code> 5）。新增的关键几条：确认串必须原样、 勾选必须覆盖全部、写操作串行（第二个 409 <code>writer_busy</code>，且写期间的只读也 409）、 取消后状态是 <code>cancelled</code> 且文案含「不会回滚」、<code>-allow-write=false</code> 下既隐藏又拒绝。</li><li><strong><code>deploy.sh</code> 离线自测 85 项（81 通过 / 0 失败 / 4 跳过）</strong>； 集成测试 88 项（87 通过 / 0 失败 / 1 跳过）。 协议项 id 快照由 38 项扩到 <strong>43 项</strong>（新增的 5 项见下）。</li></ul><h4 id="顺带修掉的一个-deploy-sh-缺陷-写操作「报告成功但什么都没做」" tabindex="-1"><a class="header-anchor" href="#顺带修掉的一个-deploy-sh-缺陷-写操作「报告成功但什么都没做」"><span>顺带修掉的一个 <code>deploy.sh</code> 缺陷：写操作「报告成功但什么都没做」</span></a></h4><p>验证写通道时发现 <code>phase_kubeconfig</code> 会<strong>打印「已写入」并返回 0，而目标文件一个字都没改</strong>。</p><p>根因有两层：</p><ol><li><strong><code>main</code> 里的 <code>case ... || rc=$?</code> 让 <code>set -e</code> 在阶段内部整体失效。</strong> 那一层是刻意加的（<code>verify</code> 判定不通过要 <code>return 1</code>，而不是在 <code>emit_json</code> 之前退出）， 但 bash 对 AND-OR 列表有豁免：位于 <code>||</code> 左边的命令，其 <code>set -e</code> 被忽略， <strong>而且这个豁免贯穿整个调用树</strong>。所以「命令失败会自动停」这个假设在 <code>phase_*</code> 内部不成立。 （最小复现：函数里写 <code>false</code>，它后面的语句照样执行。）</li><li><code>phase_kubeconfig</code> 原本用 <code>sed -i</code> 且<strong>不看退出码</strong>。<code>sed -i</code> 内部是 「写临时文件 + rename」，rename 被拒时（受控目录 / 只读挂载 / 沙箱）原文件不改， 却在同目录留下一个<strong>含集群凭据</strong>的临时文件 —— 实测权限是默认的 <code>644</code>。</li></ol><p>后果正是本方案要消灭的那种「看起来一切正常」：控制台会把一次什么都没做的写操作显示成「完成」。</p><p>改法：这个阶段的每一步都显式检查退出码，失败时 <code>die</code> 出带 <code>DIE_ID</code> / <code>DIE_SOURCE</code> / <code>DIE_FIX</code> 的 error 判定项（新增 5 个 id： <code>kubeconfig.fetch</code> / <code>.rewrite</code> / <code>.vip</code> / <code>.chmod</code> / <code>.replace</code>）， 并且不再用 <code>sed -i</code>，改成「显式临时文件 + <code>mv</code>」；<code>mv</code> 是唯一的原子替换步骤。 另外补了一条「改写后必须真的出现 VIP」的检查 —— 否则正则会安静地写出一份指向 <strong>节点 IP</strong> 的 kubeconfig：平时能用，一换主就断，而且看不出是哪一步出的问题。</p><p>实测：写不进去时现在返回 <strong>退出码 1 + 一条 error 判定项 + <code>fix</code> 建议</strong>， 界面显示「完成，但有判定未通过」并<strong>列出是哪一项</strong>（<code>kubeconfig.replace</code>）。</p><p>⚠️ <strong>同一类问题在其他阶段仍然存在</strong>：任何 <code>remote_script</code> 调用的失败都不会中止阶段。 这次只修了被证明坏掉的那一处（它恰好是 P2 暴露给用户的写操作）， <code>main</code> 里加了注释说明这个陷阱，<code>selftest</code> 也加了一条静态断言防止它被改回去。 其余阶段要不要逐个补检查，是 P3 的活。</p><h3 id="_8-4-p3-补齐校验-指标" tabindex="-1"><a class="header-anchor" href="#_8-4-p3-补齐校验-指标"><span>8.4 P3：补齐校验 + 指标</span></a></h3><p>补 §7.1~7.3 的校验，再接 <code>client-go</code> / Prometheus API。</p><p>顺序不能反：<strong>校验补齐后指标才有意义</strong>——否则「面板全绿」和「真的正常」仍然是两件事 （这正是 §一 里「看起来一切正常」的另一种形态）。</p><p>注意 §5.3 的结论：<code>client-go</code> 是<strong>可选项</strong>，定位是「补充交互和实时状态，而不是替代 <code>verify</code>」。</p><h3 id="_8-5-go-后端第一版的结构-刻意保持薄" tabindex="-1"><a class="header-anchor" href="#_8-5-go-后端第一版的结构-刻意保持薄"><span>8.5 Go 后端第一版的结构：刻意保持薄</span></a></h3><p>第一版真的只有这些：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>internal/</span></span>
<span class="line"><span>├── task/</span></span>
<span class="line"><span>│   ├── manager.go</span></span>
<span class="line"><span>│   └── task.go</span></span>
<span class="line"><span>├── executor/</span></span>
<span class="line"><span>│   └── shell.go</span></span>
<span class="line"><span>├── api/</span></span>
<span class="line"><span>│   ├── task.go</span></span>
<span class="line"><span>│   └── state.go</span></span>
<span class="line"><span>└── auth/</span></span>
<span class="line"><span>    └── ...</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>不要</strong>一开始就铺成这样：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>domain/  application/  infrastructure/</span></span>
<span class="line"><span>repository/  kubernetes/  cluster/  deployment/  ...</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>后者是「一个业务后端」的形状，而这个项目本质上是一个<strong>控制层</strong>—— 它自己不持有领域模型，领域模型在 <code>deploy.sh</code> 和 <code>config.env</code> 里。 照 DDD 分层铺开，很容易把一个本该很薄的控制层重新做成一个复杂后端， 而且会诱使你把 §3.2 明令禁止的方案判定逻辑搬进来（有了 <code>domain/cluster/</code> 就会想往里放东西）。</p><h3 id="_8-6-开工顺序" tabindex="-1"><a class="header-anchor" href="#_8-6-开工顺序"><span>8.6 开工顺序</span></a></h3><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>① 改 deploy.sh：verify --json / preflight --json      ✅ P0</span></span>
<span class="line"><span>        ↓</span></span>
<span class="line"><span>② CLI 自己验证 JSON（此时还没有任何 UI，但协议已被真实使用）  ✅ P0</span></span>
<span class="line"><span>        ↓</span></span>
<span class="line"><span>③ Go 最小 API（exec + SSE）                            ✅ P1</span></span>
<span class="line"><span>        ↓</span></span>
<span class="line"><span>④ React 只读首页                                       ✅ P1</span></span>
<span class="line"><span>        ↓</span></span>
<span class="line"><span>⑤ P1 跑通                                             ✅ P1</span></span>
<span class="line"><span>        ↓</span></span>
<span class="line"><span>⑥ 再做 P2 的执行按钮                                   ✅ P2</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>最关键的一条：不要先做 UI。</strong></p><p>真正的第一步是<strong>把 <code>deploy.sh verify</code> / <code>preflight</code> 做成稳定的机器可读结果协议</strong>。 协议一旦稳定，后面的 CLI、Go、React、AI、日志、监控<strong>都只是消费者</strong>。</p><p>这也正是本方案与「给脚本套一个网页」的分界线： 先有协议，再有界面；而不是先有界面，再想办法从彩色文本里抠信息。</p><p><strong>P2 之后追加的一条经验</strong>：⑥ 之所以放在最后，不只是因为「界面后做」—— 写按钮会<strong>逼你去验证写通道</strong>，而正是在那一步才发现 <code>phase_kubeconfig</code> 一直在「报告成功但什么都没做」（见 §8.3 末节）。 换句话说，<strong>只读界面永远发现不了这个缺陷</strong>：它只消费结果，不执行动作。</p><h3 id="_8-7-p4-下一阶段-应用受管对象-——-passup" tabindex="-1"><a class="header-anchor" href="#_8-7-p4-下一阶段-应用受管对象-——-passup"><span>8.7 P4（下一阶段）：应用受管对象 —— PassUp</span></a></h3><blockquote><p>本节只定<strong>定位与边界</strong>，不改动 P0~P3 的任何设计，也没有任何代码落地。</p></blockquote><p><strong>决策：要加，但作为「第二类受管对象」，而不是把 PassUp 塞进 <code>deploy.sh</code>。</strong></p><p>现在只有一个受管对象，而且这一点没被显式写出来过：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>控制台</span></span>
<span class="line"><span>└── 受管对象：K3s 基础设施方案</span></span>
<span class="line"><span>      deploy.sh  →  verify --json</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>P4 之后变成两个，<strong>各自持有自己的事实来源与验证协议</strong>：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>控制台</span></span>
<span class="line"><span>├── 受管对象：K3s 基础设施方案</span></span>
<span class="line"><span>│     deploy.sh         →  verify --json</span></span>
<span class="line"><span>└── 受管对象：PassUp 应用</span></span>
<span class="line"><span>      passup-deploy.sh  →  verify --json</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>控制台只负责统一消费这两份协议。<strong>第一版不改名、不扩范围</strong> —— 「K3s 方案控制台」这个名字先留着，等真的有两类受管对象跑起来了再谈改名。</p><h4 id="为什么不能塞进-deploy-sh" tabindex="-1"><a class="header-anchor" href="#为什么不能塞进-deploy-sh"><span>为什么不能塞进 <code>deploy.sh</code></span></a></h4><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>deploy.sh</span></span>
<span class="line"><span>  ├── k3s</span></span>
<span class="line"><span>  ├── kube-vip</span></span>
<span class="line"><span>  ├── traefik</span></span>
<span class="line"><span>  ├── monitoring</span></span>
<span class="line"><span>  └── passup        ← 不要这样</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>deploy.sh</code> 是「K3s 方案」的事实来源。一旦它开始管 PassUp，它就会重新长成一个<strong>巨型总管</strong>， 而 §3.2 那条边界（判定归 Bash）会随之变模糊 —— 因为「Bash 现在管两件事」， 「哪件事该由 Bash 判定」就说不清了。</p><p>正确形态是<strong>每个受管对象自己带协议</strong>，控制台做统一的消费者。 这和 §三 的边界并不冲突：判定仍然全在 Bash，只是从「一个脚本」变成「一组脚本」。</p><h4 id="硬边界-控制台不许理解-passup-的业务代码" tabindex="-1"><a class="header-anchor" href="#硬边界-控制台不许理解-passup-的业务代码"><span>硬边界：控制台不许理解 PassUp 的业务代码</span></a></h4><div class="language-go line-numbers-mode" data-highlighter="shiki" data-ext="go" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-go"><span class="line"><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">// 明确禁止出现这种代码</span></span>
<span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">if</span><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;"> passupBackendReady</span><span style="--shiki-light:#383A42;--shiki-dark:#56B6C2;"> &amp;&amp;</span><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;"> redisReady</span><span style="--shiki-light:#383A42;--shiki-dark:#56B6C2;"> &amp;&amp;</span><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;"> minioReady</span><span style="--shiki-light:#383A42;--shiki-dark:#56B6C2;"> &amp;&amp;</span><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;"> flywayVersion</span><span style="--shiki-light:#383A42;--shiki-dark:#56B6C2;"> ==</span><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;"> xxx</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;"> { ... }</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>理由与 §3.2 完全同源：<strong>判定留在脚本里</strong>。PassUp 的状态判定写在 <code>passup-deploy.sh</code> 里， 控制台只消费它的 <code>--json</code>。否则这套规则会散落在 Go 里，既无法被 CLI 复用，也无法被测试。</p><h4 id="协议统一-顶层加一个-system" tabindex="-1"><a class="header-anchor" href="#协议统一-顶层加一个-system"><span>协议统一：顶层加一个 <code>system</code></span></a></h4><p>现有协议顶层是 <code>phase</code> / <code>summary</code> / <code>items</code>。要让两个受管对象共用一个消费端， 顶层加 <code>system</code>（<code>k3s</code> / <code>monitoring</code> / <code>passup</code>）：</p><div class="language-json line-numbers-mode" data-highlighter="shiki" data-ext="json" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-json"><span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">{</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">  &quot;system&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: </span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">&quot;passup&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">,</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">  &quot;phase&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: </span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">&quot;verify&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">,</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">  &quot;summary&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: { </span><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">&quot;ok&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: </span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;">12</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">, </span><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">&quot;warn&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: </span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;">1</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">, </span><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">&quot;error&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: </span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;">0</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">, </span><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">&quot;skip&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: </span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;">2</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;"> },</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">  &quot;items&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: [</span></span>
<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">    { </span><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">&quot;id&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: </span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">&quot;backend.ready&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">, </span><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">&quot;level&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: </span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">&quot;ok&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">, </span><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">&quot;detail&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: </span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">&quot;2/2 ready&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">, </span><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">&quot;source&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: </span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">&quot;cluster&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;"> }</span></span>
<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">  ]</span></span>
<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">}</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>这是<strong>向后兼容</strong>的增量（Go 侧对未知字段本来就不敏感），但它会牵动一处实现： <strong>命令表要加一个 <code>system</code> 维度</strong> —— 现在 <code>-script</code> 只指向一个脚本，脚本路径不再唯一。</p><h4 id="先做「passup-状态」-不要先做「部署-passup」" tabindex="-1"><a class="header-anchor" href="#先做「passup-状态」-不要先做「部署-passup」"><span>先做「PassUp 状态」，不要先做「部署 PassUp」</span></a></h4><p>只读控制台解决的是「<strong>我不知道现在到底对不对</strong>」，这个价值高于「点一下部署」。 而且 P2 已经证明了一件事（§8.6）：写按钮会<strong>逼你去验证写通道</strong>， 而那一步才暴露了「报告成功但什么都没做」。所以顺序是：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>① PassUp 状态（只读，含版本 / 入口 / 各组件 Ready / 迁移版本 / 外部依赖）</span></span>
<span class="line"><span>        ↓</span></span>
<span class="line"><span>② PassUp verify（并入统一协议）</span></span>
<span class="line"><span>        ↓</span></span>
<span class="line"><span>③ 动作面：部署 / 重启 / 回滚 / 迁移 / 停止</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>动作面直接复用 P2 的机制</strong>：<code>severity</code> / <code>confirm</code> / <code>task</code> / SSE / <code>cancelled</code> / <code>writer_busy</code>。 也就是说 P2 那套不是 K3s 专用 —— P4 会让它升格成<strong>通用任务执行框架</strong>。</p><p>动作的危险等级建议（实施时再定）：</p><table><thead><tr><th style="text-align:left;">动作</th><th style="text-align:left;">建议 <code>severity</code></th><th style="text-align:left;">备注</th></tr></thead><tbody><tr><td style="text-align:left;">部署 / 重启</td><td style="text-align:left;"><code>write</code></td><td style="text-align:left;">可重复执行</td></tr><tr><td style="text-align:left;">迁移（Flyway）</td><td style="text-align:left;"><code>destructive</code> + <code>type+ack</code></td><td style="text-align:left;">见下方「回滚 ≠ 迁移回退」</td></tr><tr><td style="text-align:left;">回滚</td><td style="text-align:left;"><code>write</code></td><td style="text-align:left;">指镜像回滚</td></tr><tr><td style="text-align:left;">停止</td><td style="text-align:left;"><code>destructive</code></td><td style="text-align:left;">—</td></tr></tbody></table><p>⚠️ <strong><code>回滚</code> 和 <code>迁移回退</code> 是两件事，界面上必须分开写。</strong> 数据库迁移（Flyway）<strong>没有</strong>真正的回滚；能回滚的是镜像版本。 把两者合成一个「回滚」按钮，等于给出虚假的安全感 —— 这正是 §四 要防的东西。</p><h4 id="⚠️-两个必须先定的问题" tabindex="-1"><a class="header-anchor" href="#⚠️-两个必须先定的问题"><span>⚠️ 两个必须先定的问题</span></a></h4><p><strong>① 写串行是全局的，还是按受管对象的？</strong></p><p>现在是<strong>全局一把锁</strong>（<code>ErrBusy</code>）。PassUp 部署要 <code>kubectl apply</code>，K3s 的 <code>taint</code> / <code>all</code> 也要动同一个集群 —— 这两个<strong>应该互斥</strong>。但 <code>kubeconfig</code>（只写本机文件）和 PassUp 部署 之间其实没有冲突。第一版建议<strong>保持全局锁</strong>（简单、保守），等真的被挡住再按对象细分。</p><p><strong>② PassUp 的「状态」里，PostgreSQL / Redis / MinIO 根本不在集群里。</strong></p><p>PassUp 后端依赖目前仍在<strong>宿主机 Docker（172.16.0.222）</strong>，<code>pass-up.backend</code> 仓库里 <strong>没有</strong> postgres / redis / minio 清单（见 <code>PassUp后端部署清单.md</code> 第九节勘误）。 所以状态视图里这几项的正确来源是<strong>宿主机 Docker</strong>，不是 k8s 对象 —— 照搬 <code>kubectl get pods</code> 会得到「查不到」，而「查不到」和「不健康」是两回事。 同理 <code>ai-service</code> 当前是宿主机上的 <code>resume-ai:8006</code>，要不要独立成一个受管对象待定。</p><h4 id="p4-的价值-一条完整的验证链" tabindex="-1"><a class="header-anchor" href="#p4-的价值-一条完整的验证链"><span>P4 的价值：一条完整的验证链</span></a></h4><p>K3s 单独看，更像一个「基础设施验收工具」。接上 PassUp 之后才形成完整的一条链：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>机器 → 集群 → 基础设施 → 应用 → 用户入口</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>即 <code>Node → K3s HA → kube-vip / Traefik → PassUp → Backend / AI → PostgreSQL / Redis / MinIO → 外网入口</code>。</p><p>这条链上任何一环断掉，「整体状态」都不该是绿的。<strong>这才是本项目一开始要回答的那个问题。</strong></p><h2 id="九、参考项目" tabindex="-1"><a class="header-anchor" href="#九、参考项目"><span>九、参考项目</span></a></h2><table><thead><tr><th style="text-align:left;">项目</th><th style="text-align:left;">关系</th></tr></thead><tbody><tr><td style="text-align:left;"><strong>Headlamp</strong></td><td style="text-align:left;">Kubernetes Dashboard 已于 2026-06 归档，官方博客推荐迁移到 Headlamp。<strong>重点学它的交互</strong>（多集群、可扩展、应用视图）</td></tr><tr><td style="text-align:left;">Kubernetes Dashboard</td><td style="text-align:left;">虽已归档，<code>Node</code> / <code>Pod</code> / <code>Deployment</code> / <code>Events</code> 的页面组织方式仍有参考价值</td></tr></tbody></table><p><strong>但要看清区别</strong>——这决定了本项目不是「再做一个 Dashboard」：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>Headlamp            →  管理「任意 Kubernetes」</span></span>
<span class="line"><span>本项目控制台        →  固化「这一套 K3s 方案」</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>本项目最有价值的部分，恰恰是 Headlamp 天然不会有的那些：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>3 节点 → K3s HA → embedded etcd → kube-vip</span></span>
<span class="line"><span>      → 控制面 VIP → Traefik → 业务 VIP</span></span>
<span class="line"><span>      → externalTrafficPolicy: Local</span></span>
<span class="line"><span>      → 副本数 / eligible nodes 约束   ← 通用工具不知道这条</span></span>
<span class="line"><span>      → Registry → Verify Chain</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>「副本数必须 ≥ 可承载 Traefik 的节点数」这种约束，<strong>通用工具不可能内置</strong>， 因为它不是 K8s 的规则，是<strong>这套方案的规则</strong>。把它固化进界面，才是这个项目存在的理由。</p><h2 id="十、明确不做什么" tabindex="-1"><a class="header-anchor" href="#十、明确不做什么"><span>十、明确不做什么</span></a></h2><table><thead><tr><th style="text-align:left;">不做</th><th style="text-align:left;">理由</th></tr></thead><tbody><tr><td style="text-align:left;">图形化 YAML 编辑器</td><td style="text-align:left;">配置的唯一事实来源必须是<strong>可 git diff 的文件</strong>，GUI 只是它的视图</td></tr><tr><td style="text-align:left;">任意字段的自由表单</td><td style="text-align:left;">自由表单会<strong>重新引入决策复杂度</strong>，正好是控制台要消灭的东西。要的是「声明拓扑，副本数自己算」</td></tr><tr><td style="text-align:left;">在 Go 里复制 <strong>K3s 方案判定</strong>（VIP / Traefik / TLS SAN / Registry…）</td><td style="text-align:left;">见 §3.2。这是硬约束。注意边界：<strong>控制台自身的</strong>任务/权限/并发/参数校验属于 Go，不在此列</td></tr><tr><td style="text-align:left;">多集群管理</td><td style="text-align:left;">P2 之前不引入。本项目的价值在「深」不在「广」</td></tr><tr><td style="text-align:left;">一个笼统的「一键部署」大按钮</td><td style="text-align:left;">见 §四。动作必须带影响面</td></tr><tr><td style="text-align:left;">P1 阶段提供「修改配置」</td><td style="text-align:left;">见 §8.2。P1 的方案配置视图<strong>只读</strong>，改配置走文件 + 提交</td></tr><tr><td style="text-align:left;">P1 引入图表库</td><td style="text-align:left;">见 §5.4。P1 是「一张状态卡 + 若干表格」，表格能表达的不画图</td></tr></tbody></table><h2 id="十一、关联阅读" tabindex="-1"><a class="header-anchor" href="#十一、关联阅读"><span>十一、关联阅读</span></a></h2>`,305),o(`ul`,null,[l[9]||=o(`li`,null,`[K3s HA 一键部署方案（自动化）](./K3s HA 一键部署方案.md) —— §四「多固化的八件事」、§七「16 项验收判据」`,-1),o(`li`,null,[i(m,{to:`/%E5%90%8E%E7%AB%AF/%E4%BA%91%E5%8E%9F%E7%94%9F/k3s/%E8%90%BD%E5%9C%B0/%E5%8F%AF%E8%A7%82%E6%B5%8B%E6%80%A7%E6%A0%88%E8%BF%81%E7%A7%BB%E8%87%B3%E9%9B%86%E7%BE%A4%E5%86%85-%E6%96%B9%E6%A1%88.html`},{default:n(()=>[...l[7]||=[a(`可观测性栈迁移至集群内（方案）`,-1)]]),_:1}),l[8]||=a(` —— §十「实施结果」`,-1)]),l[10]||=r(`<li>代码：<code>cluster-infra/k3s-ha/</code>（<code>deploy.sh</code> + <code>config.env</code> + <code>manifests/</code> + <code>tests/</code>）</li><li>代码：<code>cluster-infra/monitoring/</code>（<code>deploy.sh</code> + <code>config.env</code> + 各组件清单）</li><li><strong>前端基线</strong>：<code>pass-up.frontend</code> —— §5.1 的技术栈与工程结构直接取自该仓 （<code>pnpm-workspace.yaml</code> 的 <code>catalog:</code> + <code>apps/*</code> + <code>packages/{shared,vite-config,tsconfig,eslint-config}</code>）</li>`,3)]),l[13]||=r('<h2 id="附录-修订记录" tabindex="-1"><a class="header-anchor" href="#附录-修订记录"><span>附录：修订记录</span></a></h2><h3 id="v8-p4-定位决策回填-未实施" tabindex="-1"><a class="header-anchor" href="#v8-p4-定位决策回填-未实施"><span>v8（P4 定位决策回填，<strong>未实施</strong>）</span></a></h3><p>P2 实施完成后确定了下一阶段的定位：<strong>控制台从「一个受管对象」演进为「一组受管对象」， PassUp 作为第二类接入</strong>。本版<strong>只记录决策与边界</strong>，不改动 P0~P3 的任何设计， 也没有任何代码落地。</p><table><thead><tr><th style="text-align:left;">#</th><th style="text-align:left;">改动</th><th style="text-align:left;">说明</th></tr></thead><tbody><tr><td style="text-align:left;">1</td><td style="text-align:left;"><strong>新增 §8.7（P4：应用受管对象）</strong></td><td style="text-align:left;">明确「不把 PassUp 塞进 <code>deploy.sh</code>」：那会让它重新长成巨型总管，并让 §3.2 的边界变模糊。正确形态是每个受管对象各自持有事实来源与验证协议（<code>passup-deploy.sh</code> + <code>verify --json</code>），控制台只做统一消费</td></tr><tr><td style="text-align:left;">2</td><td style="text-align:left;"><strong>§八 分期表加 P4 行</strong></td><td style="text-align:left;"><strong>第一版不改名、不扩范围</strong>；「K3s 方案控制台」这个名字等真有两类对象跑起来再谈</td></tr><tr><td style="text-align:left;">3</td><td style="text-align:left;"><strong>协议顶层加 <code>system</code></strong></td><td style="text-align:left;"><code>k3s</code> / <code>monitoring</code> / <code>passup</code>。向后兼容的增量（Go 对未知字段不敏感），但会牵动实现：<strong>命令表要加 <code>system</code> 维度</strong>，因为 <code>-script</code> 不再只指向一个脚本</td></tr><tr><td style="text-align:left;">4</td><td style="text-align:left;"><strong>明确禁止控制台理解 PassUp 业务代码</strong></td><td style="text-align:left;">与 §3.2 同源：<code>if backendReady &amp;&amp; redisReady &amp;&amp; flywayVersion == xxx</code> 这类判定不许进 Go，否则规则散落且不可测</td></tr><tr><td style="text-align:left;">5</td><td style="text-align:left;"><strong>动作面复用 P2 机制</strong></td><td style="text-align:left;"><code>severity</code> / <code>confirm</code> / <code>task</code> / SSE / <code>cancelled</code> / <code>writer_busy</code> 不是 K3s 专用 —— P4 会让它升格成<strong>通用任务执行框架</strong></td></tr><tr><td style="text-align:left;">6</td><td style="text-align:left;"><strong>记录两个待定问题</strong></td><td style="text-align:left;">① 写串行保持<strong>全局一把锁</strong>还是按受管对象细分（建议先全局）；② PassUp 的 PG / Redis / MinIO 在<strong>宿主机 Docker</strong> 而非集群，状态来源不能照搬 k8s 对象 ——「查不到」和「不健康」是两回事</td></tr><tr><td style="text-align:left;">7</td><td style="text-align:left;"><strong>记下「回滚 ≠ 迁移回退」</strong></td><td style="text-align:left;">Flyway 迁移没有真回滚，能回滚的是镜像版本。合成一个「回滚」按钮等于给出虚假的安全感</td></tr></tbody></table><h3 id="v7-p2-实施后回填" tabindex="-1"><a class="header-anchor" href="#v7-p2-实施后回填"><span>v7（P2 实施后回填）</span></a></h3><p>P2 已在 <code>console/</code> 落地（动作编排）。<strong>判定逻辑仍然一行未改</strong>， <code>deploy.sh</code> 只动了一处<strong>被写通道暴露出来的缺陷</strong>（<code>phase_kubeconfig</code>，见 §8.3 末节）。</p><table><thead><tr><th style="text-align:left;">#</th><th style="text-align:left;">改动</th><th style="text-align:left;">说明</th></tr></thead><tbody><tr><td style="text-align:left;">1</td><td style="text-align:left;"><strong><code>read_only</code> 布尔 → <code>severity</code> 三档</strong></td><td style="text-align:left;">那个布尔值一直在承担两件事（上什么色 / 要不要确认）。P2 需要它同时驱动两者，于是拆成 <code>readonly</code>/<code>write</code>/<code>destructive</code>。<code>IsReadOnly()</code> 改为派生，概念没丢，只是不再是存储形态</td></tr><tr><td style="text-align:left;">2</td><td style="text-align:left;"><strong>新增 <code>confirm</code> 确认阶梯（四档）</strong></td><td style="text-align:left;"><code>none</code>/<code>button</code>/<code>type</code>/<code>type+ack</code>。理由：如果 <code>all</code> 和 <code>verify</code> 弹同一个「确定吗？」，确认就不携带信息量，用户会养成闭眼点确定的习惯 —— 而那个习惯会在 <code>uninstall</code> 上出事</td></tr><tr><td style="text-align:left;">3</td><td style="text-align:left;"><strong><code>phrase</code> 必须原样相等</strong></td><td style="text-align:left;">明确写进方案：不做 <code>TrimSpace</code>、不做大小写归一。<code>&quot; all&quot;</code> / <code>&quot;ALL&quot;</code> 正是「粘贴 / 手滑」的典型形态，放宽等于关掉闸门</td></tr><tr><td style="text-align:left;">4</td><td style="text-align:left;"><strong><code>acknowledge</code> 用脚本自己的原文</strong></td><td style="text-align:left;"><code>uninstall</code> 的四条勾选项直接取自 <code>phase_uninstall</code> 打印的原文，而不是在 Go 里另写一遍 —— §四「变成界面的一部分」的字面实现</td></tr><tr><td style="text-align:left;">5</td><td style="text-align:left;"><strong>取消是 <code>cancelled</code>，不是 <code>failed</code></strong></td><td style="text-align:left;">单列状态：两者的用户文案完全不同（前者要说清「不会回滚，重跑对应阶段即可收敛」）</td></tr><tr><td style="text-align:left;">6</td><td style="text-align:left;"><strong>写操作串行 + 写期间只读也拒绝</strong></td><td style="text-align:left;">新增 <code>409 writer_busy</code>。写操作跑到一半时读出来的是中间态，拿它做判断只会误导人</td></tr><tr><td style="text-align:left;">7</td><td style="text-align:left;"><strong>部署级闸门 <code>-allow-write=false</code></strong></td><td style="text-align:left;">打开时 <code>/api/commands</code> 改写 <code>allowed</code>，界面不渲染可点按钮；直接打 API 也 403。界面上的确认是给人看的，这一层才是给「绕过界面」看的</td></tr><tr><td style="text-align:left;">8</td><td style="text-align:left;"><strong>§8.3 补「<code>set -e</code> 在阶段内部失效」这一条</strong></td><td style="text-align:left;"><code>main</code> 的 `case ...</td></tr><tr><td style="text-align:left;">9</td><td style="text-align:left;"><strong>id 快照 38 → 43 项</strong></td><td style="text-align:left;">新增 <code>kubeconfig.{fetch,rewrite,vip,chmod,replace}</code></td></tr><tr><td style="text-align:left;">10</td><td style="text-align:left;"><strong>§8.6 补「只读界面发现不了这类缺陷」</strong></td><td style="text-align:left;">⑥ 放最后的另一层理由：写按钮会逼你去验证写通道，而正是在那一步才暴露了「报告成功但什么都没做」</td></tr></tbody></table><h3 id="v6-p1-实施后回填" tabindex="-1"><a class="header-anchor" href="#v6-p1-实施后回填"><span>v6（P1 实施后回填）</span></a></h3><p>P1（只读控制台）已落地：<code>deploy.sh</code> 加三个只读数据出口，Go 后端 + React 前端四个只读视图跑通。 <strong>判定逻辑仍然一行未改</strong>。按实际情况回填了五处：</p><table><thead><tr><th style="text-align:left;">#</th><th style="text-align:left;">改动</th><th style="text-align:left;">说明</th></tr></thead><tbody><tr><td style="text-align:left;">1</td><td style="text-align:left;"><strong>§8.2 补「三个只读命令」</strong></td><td style="text-align:left;">P1 需要的配置 / 拓扑 / 一致性数据，做成 <code>show-config</code> / <code>topology</code> / <code>consistency --json</code>，而不是让 Go 解析 <code>config.env</code>。理由：<code>config.env</code> 是 bash 语法，<code>source</code> 才是权威解析器；且这三个命令<strong>故意不跑 preflight</strong>，集群坏的时候也要能用</td></tr><tr><td style="text-align:left;">2</td><td style="text-align:left;"><strong>§8.2 补「Go 侧边界 = 一张命令表」</strong></td><td style="text-align:left;">§3.2 划的边界落到实处：<code>internal/api/commands.go</code> 用 <code>read_only</code> / <code>allowed</code> 两个正交字段。<strong>写操作照样列出来</strong>并带影响面文案，因为「没有按钮」会把人逼回服务器手敲 <code>all</code></td></tr><tr><td style="text-align:left;">3</td><td style="text-align:left;"><strong>§8.2 补「前端两处偏离」</strong></td><td style="text-align:left;">① <code>vite-config</code> 砍掉 Tailwind / code-inspector / react-compiler / 版本化 assetsDir；② 新增第 5 个视图「动作清单」——让 §四 那条约束真的落到界面上</td></tr><tr><td style="text-align:left;">4</td><td style="text-align:left;"><strong>§8.2 补 P1 实测结果</strong></td><td style="text-align:left;">四态语义在真机上成立（22 项 = ok 21 / warn 1）；<code>source</code> 分布 <code>cluster 10 / node 10 / local 1 / config 1</code> 确认 §5.3；真浏览器 5 视图 0 error</td></tr><tr><td style="text-align:left;">5</td><td style="text-align:left;"><strong>协议项 id 快照 32 → 38</strong></td><td style="text-align:left;">新增的 6 项即三个只读命令的判定项（<code>topology.*</code> 3 项 + <code>config.*</code> 3 项；<code>show-config</code> 只出 <code>data</code>、无判定项）。<code>selftest.sh</code> 第 12 节已同步</td></tr></tbody></table><p>实施中确认 / 修正的三点：</p><ul><li><strong>「退出码 1」的语义必须固定成回归测试</strong>。<code>verify</code> 判定不通过时返回 1， 那是<strong>业务结果</strong>不是执行失败 —— Go 侧必须把它归成 <code>Done</code>。搞错了，界面就会把 「一次成功的检查，结论是有问题」显示成「控制台出故障了」。 已写成 <code>internal/task/manager_test.go</code> 的第一条用例。</li><li><strong>Windows 开发期的耗时</strong>让「按快慢分叉」的客户端写法被排除掉了。 <code>show-config</code> 在 Git Bash 下要 13 秒（Linux 上百毫秒级）， 于是前端对<strong>所有</strong>命令统一走 SSE，不做「快的轮询一次」。 这条是环境逼出来的结论，不是设计偏好。</li><li><strong><code>--json</code> 的 <code>data</code> 字段是 P1 加的</strong>（<code>emit_json</code> 多接一个可选参数）。 它只由三个只读命令使用，<code>verify</code> / <code>preflight</code> 仍然只有 <code>items</code> —— <strong>判定项与事实数据刻意分开</strong>：<code>items</code> 回答「符不符合要求」，<code>data</code> 只是「读到了什么」。</li></ul><h3 id="v5-p0-实施后回填" tabindex="-1"><a class="header-anchor" href="#v5-p0-实施后回填"><span>v5（P0 实施后回填）</span></a></h3><p>P0 已在 <code>cluster-infra/k3s-ha/deploy.sh</code> 落地（<code>verify</code> / <code>preflight</code> 加 <code>--json</code>）。 <strong>判定逻辑一行未改</strong>，只加了报告层。实施中按实际情况回填了四处：</p><table><thead><tr><th style="text-align:left;">#</th><th style="text-align:left;">改动</th><th style="text-align:left;">说明</th></tr></thead><tbody><tr><td style="text-align:left;">1</td><td style="text-align:left;"><strong>§8.1 补第 4 态 <code>skip</code></strong></td><td style="text-align:left;">初稿只约定 <code>ok</code>/<code>warn</code>/<code>error</code>。实施时发现判定项会因外部条件不具备而<strong>根本没跑</strong>（节点没 <code>openssl</code>、本机没 kubectl、<code>MANAGE_REGISTRY=false</code>、首台未装 k3s）。若不显式出现，消费方分不清「通过」和「没跑」——<strong>正是本项目要防的「看起来一切正常」</strong></td></tr><tr><td style="text-align:left;">2</td><td style="text-align:left;"><strong>§8.1 补「人类输出逐字节不变」</strong></td><td style="text-align:left;">把「<code>--json</code> 是增量」写成可验证的约定：原来打印过的行原样保留，协议项走「只记录不打印」的通道；原来没打印的项不会凭空多出一行 ✓。验证方式 <code>git stash</code> 前后 <code>diff</code> 为空</td></tr><tr><td style="text-align:left;">3</td><td style="text-align:left;"><strong>§8.1 补已落地 id 清单</strong></td><td style="text-align:left;">32 个 id（<code>preflight</code> 11 + <code>verify</code> 20，其中 <code>traefik.replica_eligible</code> 两阶段共用）。逐节点项用 <code>xxx@&lt;节点名&gt;</code> 后缀展开，前缀稳定、后缀可变</td></tr><tr><td style="text-align:left;">4</td><td style="text-align:left;"><strong><code>level</code> 取值表同步为四态</strong></td><td style="text-align:left;"><code>summary</code> 示例也补上 <code>skip</code> 计数</td></tr></tbody></table><p>实施中确认的两点（与方案一致，无需改）：</p><ul><li><strong><code>source</code> 的四分类与实际数据来源完全对得上</strong>。真机跑 <code>verify --json</code> 的实测分布： 22 个协议项里 <code>cluster</code> 10、<code>node</code> 10、<code>local</code> 1、<code>config</code> 1。 协议项比 §5.3 的「16 项判据」多，是因为<strong>逐节点判据会展开</strong>（<code>business.vip_advertised</code> ×2、 <code>registry.registries_yaml</code> ×3），加上 <code>traefik.replica_eligible</code> 与 preflight 共用。 但方向与 §5.3 一致：<strong><code>client-go</code> 只能覆盖 <code>cluster</code> 那 10 项，另外 12 项它拿不到</strong> —— 其中 <code>node</code> 那 10 项（证书 SAN / 逐节点 VIP 通告 / <code>registries.yaml</code> 逐字节一致）恰恰是最值钱的。</li><li><strong><code>duration_ms</code> 直接暴露了 Git Bash 的进程开销</strong>：初版用 <code>$(date ...)</code> 取值， 单项耗时被算成 1071ms（实际 20ms 的 <code>sleep</code>）。改成 bash 5 内建的 <code>EPOCHREALTIME</code> （零 fork）后才准。这个字段在 Windows 上排查脚本性能时意外地好用。 实测最慢的三项都是「逐台 SSH」类：<code>traefik.replica_eligible</code> 9.2s、 <code>vip.cp_bound</code> 6.7s、<code>business.vip_advertised@k2</code> 4.3s —— 一眼能看出该优化谁。</li></ul><h3 id="v4-定稿前的七处收敛" tabindex="-1"><a class="header-anchor" href="#v4-定稿前的七处收敛"><span>v4（定稿前的七处收敛）</span></a></h3><p>评审意见认为 v3 已可作为<strong>方案定稿</strong>，只提了七处小修正。逐条落实：</p><table><thead><tr><th style="text-align:left;">#</th><th style="text-align:left;">改动</th><th style="text-align:left;">说明</th></tr></thead><tbody><tr><td style="text-align:left;">1</td><td style="text-align:left;"><strong>§3.2 判据换成本质定义</strong></td><td style="text-align:left;">原判据「要不要 SSH 到节点」被替换为：<strong>判定「集群是否符合本方案要求」→ Bash；判定「控制台是否允许执行某操作」→ Go</strong>。补充说明两者易混：前者<strong>消费</strong> <code>verify</code> 的结论，后者自己<strong>产出</strong>结论</td></tr><tr><td style="text-align:left;">2</td><td style="text-align:left;"><strong>§8.1 补两个可选字段</strong></td><td style="text-align:left;"><code>source</code>（<code>node</code>/<code>cluster</code>/<code>local</code>/<code>config</code>）+ <code>duration_ms</code>。明确<strong>四字段是必须的、P0 可只做四个</strong>；两个可选字段建议趁 P0 一起加（事后补要动所有判定点）</td></tr><tr><td style="text-align:left;">3</td><td style="text-align:left;"><strong>§5.2 删掉「~10 MB RSS」</strong></td><td style="text-align:left;">具体数字会随 Go runtime / HTTP server / SSH 连接 / 子进程 / 并发任务 / K8s client 变化，写成选型依据会变成过时且误导的硬指标。改为定性描述「资源占用低，适合部署在资源受限的 Linux 管理机上」，稳定的理由只留三条</td></tr><tr><td style="text-align:left;">4</td><td style="text-align:left;"><strong>§5.3 降 <code>client-go</code> 优先级</strong></td><td style="text-align:left;">从「P3 要做」降为<strong>「有明确增量才做的可选项」</strong>，并写明定位：<strong>补充交互和实时状态，而不是替代 <code>verify</code></strong>。补一句「<strong>不做也是一种合法结论</strong>」</td></tr><tr><td style="text-align:left;">5</td><td style="text-align:left;"><strong>§6.3 补「集群全挂时它还在」</strong></td><td style="text-align:left;">把「跑在集群外」的收益写实：k1/k2/k3 全失联时仍能看到 <code>Cluster unreachable</code>，并继续做 SSH 检查 / 网络检查 / 重新部署。这是「控制台」定位的正面证明</td></tr><tr><td style="text-align:left;">6</td><td style="text-align:left;"><strong>开头「填一次配置」→「准备一次方案配置」</strong></td><td style="text-align:left;">避免让人误以为 P1 就有网页表单。真实流程是 <code>config.env → Git → 控制台读取 → 展示 → preflight 重算</code>，<strong>配置来自文件，不是来自表单</strong></td></tr><tr><td style="text-align:left;">7</td><td style="text-align:left;"><strong>新增 §8.5 / §8.6</strong></td><td style="text-align:left;">§8.5 给出 Go 第一版的薄结构（<code>task</code>/<code>executor</code>/<code>api</code>/<code>auth</code>），并明确警告不要铺 DDD 分层——那会诱使把 §3.2 禁止的方案判定搬进来。§8.6 给出开工顺序，核心是<strong>不要先做 UI</strong></td></tr></tbody></table><p>另：文档定位由「设计文档」改为「<strong>方案定稿</strong>」。</p><h3 id="v3-定位提升-四处收敛" tabindex="-1"><a class="header-anchor" href="#v3-定位提升-四处收敛"><span>v3（定位提升 + 四处收敛）</span></a></h3><p>按评审意见改的四处，其中第 1 条是<strong>表述准确性问题</strong>，第 4 条是<strong>架构问题</strong>：</p><table><thead><tr><th style="text-align:left;">#</th><th style="text-align:left;">改动</th><th style="text-align:left;">说明</th></tr></thead><tbody><tr><td style="text-align:left;">1</td><td style="text-align:left;"><strong>§3.2 边界表述改准</strong></td><td style="text-align:left;">初稿「校验逻辑必须留在脚本里，一行都不许搬进后端」<strong>原则对但表述过死</strong>。改为：K3s 集群的<strong>事实判定与方案校验</strong>以 <code>deploy.sh</code> 为唯一事实来源，Go 不复制；<strong>Go 可以拥有控制台自身的</strong>任务 / 权限 / 状态 / 参数校验。判据：这条判定要不要 SSH 到节点或读集群状态？</td></tr><tr><td style="text-align:left;">2</td><td style="text-align:left;"><strong>§8.1 把 P0 提升为「结果协议」</strong></td><td style="text-align:left;"><code>--json</code> 不是输出格式，是 Bash 的结果协议——<strong>GUI 不该理解 Bash 的文字，而该理解它的结果协议</strong>。补了字段约定（<code>id</code>/<code>level</code>/<code>detail</code>/<code>fix</code> + 顶层 <code>summary</code>），并写明 <code>id</code> 必须稳定（文案可改、<code>id</code> 不可改），因为正则匹配 <code>✓</code>/<code>!</code> 是脆弱耦合</td></tr><tr><td style="text-align:left;">3</td><td style="text-align:left;"><strong>§8.2 P1 新增「方案配置视图」</strong></td><td style="text-align:left;">展示解析后的配置，<strong>标注来源文件</strong>，提供 <code>[查看原始配置]</code> 而非 <code>[修改]</code>。P1 不提供修改</td></tr><tr><td style="text-align:left;">4</td><td style="text-align:left;"><strong>§六「跑在哪」由未决改为结论</strong></td><td style="text-align:left;"><strong>跑在一台集群外的 Linux 管理机上</strong>，控制台自己不进被它管理的集群。决定性理由：控制台的价值是「集群不对时告诉你哪里不对」，它必须在集群之外；且节点只有 7.7 GiB，不该和业务抢内存。建议做成容器镜像、脚本随镜像打包</td></tr><tr><td style="text-align:left;">5</td><td style="text-align:left;"><strong>定位提升半级</strong></td><td style="text-align:left;">标题由「K3s 部署控制台」改为「<strong>K3s 方案控制台</strong>」——把已验证的 K3s HA / VIP / Traefik / Registry / 监控方案，固化为可配置、可校验、可执行、可观测的操作系统。「部署控制台」容易被理解成「一个带网页的部署脚本」</td></tr><tr><td style="text-align:left;">6</td><td style="text-align:left;"><strong>新增 §3.1 目标架构图</strong></td><td style="text-align:left;">含 Browser → React → Go API → <code>deploy.sh</code> → SSH → k1/k2/k3 主链路，以及 P3 才接入的 Kubernetes API / Prometheus 虚线区</td></tr></tbody></table><h3 id="v2-前端技术栈修正" tabindex="-1"><a class="header-anchor" href="#v2-前端技术栈修正"><span>v2（前端技术栈修正）</span></a></h3><p>初稿把前端定为 Vue 3 + Element Plus，依据是「主力项目 <code>mgmt-frontend-inner</code> 是 Vue」。 <strong>这个判断是错的</strong>——它只看了那一个仓，漏了 <code>pass-up.frontend</code>，而后者才是 React：</p><table><thead><tr><th style="text-align:left;">仓</th><th style="text-align:left;">栈</th><th style="text-align:left;">事实</th></tr></thead><tbody><tr><td style="text-align:left;"><code>mgmt-frontend-inner</code></td><td style="text-align:left;">Vue 3 + Element Plus</td><td style="text-align:left;">确实存在（Vben 风格 monorepo）</td></tr><tr><td style="text-align:left;"><code>pass-up.frontend</code></td><td style="text-align:left;"><strong>React 19 + antd 6 + Vite 8 + zustand</strong></td><td style="text-align:left;"><code>apps/admin</code>、<code>apps/user</code>、<code>packages/shared</code> 全是 <code>react</code> + <code>react-dom</code> + <code>antd</code></td></tr></tbody></table><p>本版修正：</p><ul><li><strong>§5.1 重写</strong>为 React + TypeScript + Ant Design，技术栈逐项对齐 <code>pass-up.frontend</code>， 并明确<strong>不用 Tailwind CSS</strong>（可读性 + 与 antd CSS-in-JS 的优先级冲突）；</li><li><strong>新增 §5.4</strong>：图表延后到 P3，P1 不引入任何图表库；</li><li>§十一 补上前端基线的出处。</li></ul><p>其余结论（§三 校验逻辑留在 bash、§四 动作影响面、§5.3 <code>client-go</code> 的定位、 §七 五个缺口、§八 分期）<strong>不受影响</strong>——它们与前端栈无关。</p><h3 id="v1-初稿" tabindex="-1"><a class="header-anchor" href="#v1-初稿"><span>v1（初稿）</span></a></h3><p>首次成文：把两份已实机验证的方案（k3s-ha / monitoring）作为上游， 给出控制台的边界、动作影响面、五个缺口清单与分期。</p>',32)])}var f=c(u,[[`render`,d]]);export{l as _pageData,f as default};