import{D as e,f as t,u as n}from"./runtime-core.esm-bundler-BHJ3o1LK.js";import{t as r}from"./plugin-vue_export-helper-BDNMzG2s.js";var i=JSON.parse(`{"path":"/%E9%A1%B9%E7%9B%AE/%E9%9D%A2%E8%AF%95%E5%8A%A9%E6%89%8B/%E6%A8%A1%E6%8B%9F%E9%9D%A2%E8%AF%95%E7%AC%AC%E4%BA%8C%E7%89%88-%E8%81%94%E8%B0%83%E4%B8%8E%E5%86%92%E7%83%9F%E6%B8%85%E5%8D%95.html","title":"模拟面试 V2：联调与冒烟清单（Step 1 / 2 / 3 / 4）","lang":"zh-CN","frontmatter":{"description":"模拟面试 V2：联调与冒烟清单（Step 1 / 2 / 3 / 4） 用途：交付与回归时照着做一遍，就能确认 V2 的四条链路没坏 —— ① 文字作答（Step 1）、② 独立识别通道 /ws/asr（Step 2）、③ 治理：配额 / 指标 / 埋点 / 日志（Step 3）、 ④ 识别通道的使用资格与回收（Step 4：必须有一场进行中的面试；面...","head":[["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"模拟面试 V2：联调与冒烟清单（Step 1 / 2 / 3 / 4）\\",\\"image\\":[\\"\\"],\\"dateModified\\":\\"2026-09-24T10:14:05.000Z\\",\\"author\\":[{\\"@type\\":\\"Person\\",\\"name\\":\\"桥黎\\",\\"url\\":\\"\\"}]}"],["meta",{"property":"og:url","content":"https://docs.cqlql.top/%E9%A1%B9%E7%9B%AE/%E9%9D%A2%E8%AF%95%E5%8A%A9%E6%89%8B/%E6%A8%A1%E6%8B%9F%E9%9D%A2%E8%AF%95%E7%AC%AC%E4%BA%8C%E7%89%88-%E8%81%94%E8%B0%83%E4%B8%8E%E5%86%92%E7%83%9F%E6%B8%85%E5%8D%95.html"}],["meta",{"property":"og:site_name","content":"开发笔记"}],["meta",{"property":"og:title","content":"模拟面试 V2：联调与冒烟清单（Step 1 / 2 / 3 / 4）"}],["meta",{"property":"og:description","content":"模拟面试 V2：联调与冒烟清单（Step 1 / 2 / 3 / 4） 用途：交付与回归时照着做一遍，就能确认 V2 的四条链路没坏 —— ① 文字作答（Step 1）、② 独立识别通道 /ws/asr（Step 2）、③ 治理：配额 / 指标 / 埋点 / 日志（Step 3）、 ④ 识别通道的使用资格与回收（Step 4：必须有一场进行中的面试；面..."}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:locale","content":"zh-CN"}],["meta",{"property":"og:updated_time","content":"2026-09-24T10:14:05.000Z"}],["meta",{"property":"article:modified_time","content":"2026-09-24T10:14:05.000Z"}]]},"git":{"createdTime":1790071903000,"updatedTime":1790244845000,"contributors":[{"name":"cql","username":"cql","email":"cql.ql@qq.com","commits":3,"url":"https://github.com/cql"}]},"readingTime":{"minutes":20.4,"words":6119},"filePathRelative":"项目/面试助手/模拟面试第二版-联调与冒烟清单.md","autoDesc":true}`),a={name:`模拟面试第二版-联调与冒烟清单.md`};function o(r,i,a,o,s,c){return e(),n(`div`,null,[...i[0]||=[t(`<h1 id="模拟面试-v2-联调与冒烟清单-step-1-2-3-4" tabindex="-1"><a class="header-anchor" href="#模拟面试-v2-联调与冒烟清单-step-1-2-3-4"><span>模拟面试 V2：联调与冒烟清单（Step 1 / 2 / 3 / 4）</span></a></h1><blockquote><p><strong>用途</strong>：交付与回归时照着做一遍，就能确认 V2 的四条链路没坏 —— ① 文字作答（Step 1）、② 独立识别通道 <code>/ws/asr</code>（Step 2）、③ 治理：配额 / 指标 / 埋点 / 日志（Step 3）、 ④ <strong>识别通道的使用资格与回收</strong>（Step 4：必须有一场进行中的面试；面试结束即回收）。</p><p><strong>与实现文档的分工</strong>：方案文档讲「为什么这么设计」，本清单讲「怎么验、看到什么才算对、坏了怎么定位」。 相关：后端方案 §4.1 / §4.2 / §12.3–12.5 / <strong>§12.8</strong>；前端方案 §6；<code>AGENTS.md</code> §3.7 / §3.8 / §6.8。</p><p><strong>一句话结论</strong>：自动化脚本覆盖协议层（0 依赖，两分钟）， 识别准确率 / 回答质量 / 交互手感必须人来做（第 2 节）。</p></blockquote><hr><h2 id="_0-前置准备-5-分钟" tabindex="-1"><a class="header-anchor" href="#_0-前置准备-5-分钟"><span>0. 前置准备（5 分钟）</span></a></h2><h3 id="_0-1-环境" tabindex="-1"><a class="header-anchor" href="#_0-1-环境"><span>0.1 环境</span></a></h3><table><thead><tr><th style="text-align:left;">项</th><th style="text-align:left;">默认值（线上口径）</th><th style="text-align:left;">说明</th></tr></thead><tbody><tr><td style="text-align:left;">后端 HTTP</td><td style="text-align:left;"><code>http://localhost:8080</code></td><td style="text-align:left;">控制器统一挂在 <code>/api/**</code>（<code>WebConfig</code> 加前缀）</td></tr><tr><td style="text-align:left;">后端 WebSocket</td><td style="text-align:left;"><code>ws://localhost:8080</code></td><td style="text-align:left;"><code>/ws/audio</code>、<code>/ws/asr</code> <strong>不在</strong> <code>/api</code> 下</td></tr><tr><td style="text-align:left;">指标端点</td><td style="text-align:left;"><code>http://localhost:8009/actuator/prometheus</code></td><td style="text-align:left;"><code>management.server.port: 8009</code></td></tr><tr><td style="text-align:left;">Node</td><td style="text-align:left;">≥ 22（推荐 24）</td><td style="text-align:left;">冒烟脚本用全局 <code>fetch</code> + <code>WebSocket</code>，<strong>零依赖</strong></td></tr><tr><td style="text-align:left;">小程序</td><td style="text-align:left;">开发者工具打开 V2 页面</td><td style="text-align:left;"><code>src/pages/mock-interview-v2</code>（文字作答版）</td></tr></tbody></table><blockquote><p><strong>本机联调实例（2026-09-22 实测，和默认值不一样）</strong>：profile <code>dev</code>，API 在 <strong>7005</strong>、 管理端口在 <strong>7009</strong>（<code>application-local.yml</code> / <code>docker/.env</code> 的 <code>SERVER_PORT</code>），日志落在 <code>logs/app-dev.log</code>（profile 是 <code>dev</code>，<code>logging.level.root: debug</code>）。 所以本地跑脚本要显式指定：</p><div class="language-bash line-numbers-mode" data-highlighter="shiki" data-ext="bash" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-bash"><span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">BASE_URL</span><span style="--shiki-light:#383A42;--shiki-dark:#56B6C2;">=</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">http://localhost:7005</span><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;"> WS_URL</span><span style="--shiki-light:#383A42;--shiki-dark:#56B6C2;">=</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">ws://localhost:7005</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">ACTUATOR_URL=http://localhost:7009 </span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">SMOKE_LOG_FILE=logs/app-dev.log</span><span style="--shiki-light:#0184BC;--shiki-dark:#56B6C2;"> \\</span></span>
<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">TOKEN=&lt;accessToken&gt; </span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">node</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> scripts/smoke-v2.mjs</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>端口对不上时的定位方式：<code>Get-NetTCPConnection -State Listen | ? { $_.LocalPort -in 7005,7009,8080,8009 }</code> 配合 <code>Get-CimInstance Win32_Process -Filter &quot;Name=&#39;java.exe&#39;&quot;</code> 看哪个是 <code>PassUpApplication</code>。</p></blockquote><h3 id="_0-2-拿-accesstoken-脚本与手工联调都要" tabindex="-1"><a class="header-anchor" href="#_0-2-拿-accesstoken-脚本与手工联调都要"><span>0.2 拿 accessToken（脚本与手工联调都要）</span></a></h3><p><strong>最快路径（推荐）</strong>：小程序开发者工具打开 V2 页面 → <code>Network</code> 面板 → 点任一请求 → 复制请求头 <code>Authorization: Bearer &lt;xxx&gt;</code> 里的 <code>xxx</code>，它就是 accessToken。</p><p>脚本用法：<code>TOKEN=&lt;xxx&gt; node scripts/smoke-v2.mjs</code></p><blockquote><p>为什么不做&quot;脚本自动登录&quot;：小程序登录依赖 <code>wx.login</code> 的 code，脱离运行时拿不到； PC 端的 <code>/api/web/auth/login</code> 需要用户名 + 密码（微信注册的用户没有密码）。 硬造 token 只会让脚本看起来能跑、实际验了个假身份。</p></blockquote><h3 id="_0-3-账号要求-最容易踩的一条" tabindex="-1"><a class="header-anchor" href="#_0-3-账号要求-最容易踩的一条"><span>0.3 账号要求（最容易踩的一条）</span></a></h3><table><thead><tr><th style="text-align:left;">要求</th><th style="text-align:left;">不满足会怎样</th></tr></thead><tbody><tr><td style="text-align:left;"><strong>账号有面试时长余额</strong>（<code>INTERVIEW_SECONDS</code> &gt; 0 或无限资格）</td><td style="text-align:left;"><code>/ws/audio</code> 建连被拒：<code>INSUFFICIENT_BALANCE</code>（关闭码 4001）</td></tr><tr><td style="text-align:left;">余额别太少</td><td style="text-align:left;"><code>MOCK</code> 文字会话<strong>同样按时长计费</strong>，联调一轮会走掉几十秒；<strong>冒烟脚本为了让识别通道可用还会开一条&quot;资格载体&quot;会话并保活</strong>，同样计时</td></tr><tr><td style="text-align:left;"><strong>识别通道只能在面试进行中使用</strong>（Step 4）</td><td style="text-align:left;">没有进行中的面试时点话筒 → <code>ASR_NO_ACTIVE_SESSION</code>（关闭码 <strong>4007</strong>）：先开始一场面试再用语音</td></tr><tr><td style="text-align:left;">建议：用专门的联调账号</td><td style="text-align:left;">人工用例里会反复建连 / 暂停 / 重连</td></tr></tbody></table><blockquote><p>注意：<strong>面试会话与识别通道的准入是两套闸门</strong> —— 面试会话看&quot;余额 &gt; 0&quot;（<code>INSUFFICIENT_BALANCE</code>），识别通道看&quot;<strong>当前有没有进行中的面试</strong>&quot;（4007）； 每日识别配额只作用于 <code>/ws/asr</code>，不影响面试会话。 也别把 4007 当故障：它正是 Step 4 设的成本护栏（豆包识别按音频时长计费， 不能让&quot;只要登录&quot;就能白刷）。</p></blockquote><p><strong>（强烈建议）本机独享一个 Redis</strong>（2026-09-24 加，原因见 §5 第一条）： 如果同机 / 同网还有别的后端实例（VirtualBox VM、devcontainer、另一台机器）也在跑这套服务， 它们会连<strong>同一个</strong> Redis，各自的计费调度器会把&quot;不在自己内存注册表里&quot;的会话判成孤儿删掉 —— 表现为<strong>面试进行中点话筒被 4007 拒</strong>，而且计费静默停止。隔离只需一步（服务已在 <code>docker-compose.dev.yml</code> 里备好）：</p><div class="language-bash line-numbers-mode" data-highlighter="shiki" data-ext="bash" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-bash"><span class="line"><span style="--shiki-light:#0184BC;--shiki-dark:#56B6C2;">cd</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> docker</span></span>
<span class="line"><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">docker</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> compose</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;"> --env-file</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> .env</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;"> -f</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> docker-compose.base.yml</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;"> -f</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> docker-compose.dev.yml</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> up</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;"> -d</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> redis-local</span></span>
<span class="line"><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;"># 只绑 127.0.0.1:6380 —— 别的机器探不到（可自测：Test-NetConnection 172.16.0.222 -Port 6380 → False）</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>启动本机后端时加两个环境变量（<strong>不要改 <code>docker/.env</code></strong>：那份 env 会被 devcontainer / 其他实例一起读，改了等于把所有人一起搬过来，隔离就白做了）：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>SPRING_DATA_REDIS_HOST=127.0.0.1</span></span>
<span class="line"><span>SPRING_DATA_REDIS_PORT=6380</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><blockquote><p><strong>Postgres 仍是共用的</strong>（<code>jdbc:postgresql://172.16.0.222:5432/pass_up_dev</code>）：多实例在数据层仍会互相干扰 （扣同一份 <code>user_asset</code>、回收同一批 <code>interview</code> 行、共享 <code>minio</code> 的文件）。 要彻底隔离就再单独建一个库（如 <code>pass_up_local</code>）并把 <code>SPRING_DATASOURCE_URL</code> 指过去 —— 代价是本地数据从零开始（Flyway 会自动建表，admin 账号会自动创建，体验模板会自动灌）。</p></blockquote><h3 id="_0-4-两分钟自检-先跑这个" tabindex="-1"><a class="header-anchor" href="#_0-4-两分钟自检-先跑这个"><span>0.4 两分钟自检（先跑这个）</span></a></h3><div class="language-bash line-numbers-mode" data-highlighter="shiki" data-ext="bash" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-bash"><span class="line"><span style="--shiki-light:#0184BC;--shiki-dark:#56B6C2;">cd</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> pass-up.backend</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">TOKEN</span><span style="--shiki-light:#383A42;--shiki-dark:#56B6C2;">=</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">&lt;</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">accessToken</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">&gt; </span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">node</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> scripts/smoke-v2.mjs</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><table><thead><tr><th style="text-align:left;">退出码</th><th style="text-align:left;">含义</th></tr></thead><tbody><tr><td style="text-align:left;"><code>0</code></td><td style="text-align:left;">协议层全通过（SKIP / WARN 不算失败）</td></tr><tr><td style="text-align:left;"><code>1</code></td><td style="text-align:left;">有 FAIL，脚本末尾列出失败项</td></tr><tr><td style="text-align:left;"><code>2</code></td><td style="text-align:left;">前置缺失（没给 TOKEN / Node 版本过低）</td></tr><tr><td style="text-align:left;"><code>3</code></td><td style="text-align:left;">脚本自身异常（把它当成一个 bug 报出来）</td></tr></tbody></table><p>可选环境变量：</p><table><thead><tr><th style="text-align:left;">变量</th><th style="text-align:left;">默认</th><th style="text-align:left;">用途</th></tr></thead><tbody><tr><td style="text-align:left;"><code>TEMPLATE_UID</code></td><td style="text-align:left;">空</td><td style="text-align:left;">指定体验模板（如 <code>exp-java-backend-3y</code>），避免依赖账号的默认简历 / 岗位</td></tr><tr><td style="text-align:left;"><code>SMOKE_LOG_FILE</code></td><td style="text-align:left;">空</td><td style="text-align:left;">后端日志文件路径 → 自动校验「日志里没有作答原文」（Step 3）</td></tr><tr><td style="text-align:left;"><code>SMOKE_QUOTA_TOTAL</code></td><td style="text-align:left;"><code>0</code></td><td style="text-align:left;">把 <code>realtime.asr.daily-sessions-per-user</code> 临时调小后，验证 4006 配额拒绝</td></tr><tr><td style="text-align:left;"><code>SMOKE_LLM_TIMEOUT_MS</code></td><td style="text-align:left;"><code>240000</code></td><td style="text-align:left;">LLM 慢时放宽。<strong>首问整轮可能要数十秒到数分钟</strong>（实测 9 秒 ~ 2 分钟），脚本总时长基本由它决定</td></tr></tbody></table><p><strong>看到 <code>SKIP</code> 不一定是坏事</strong>，但要读懂它在说什么：</p><table><thead><tr><th style="text-align:left;">SKIP 文案</th><th style="text-align:left;">含义</th></tr></thead><tbody><tr><td style="text-align:left;"><code>本机未配豆包 ASR Key</code></td><td style="text-align:left;">建连与推帧链路已走通，但识别文本验不了（要有 Key + 真人 / 真实音频）</td></tr><tr><td style="text-align:left;"><code>先收到 ASR_UNAVAILABLE</code></td><td style="text-align:left;">audio 模式的「拒绝文字作答」分支没覆盖到，因为会话先因 ASR 不可用结束了</td></tr><tr><td style="text-align:left;"><code>未复现 TURN_BUSY</code></td><td style="text-align:left;">见 WARN：那一轮回复瞬间结束，没造出并发窗口</td></tr></tbody></table><hr><h2 id="_1-自动化冒烟覆盖了什么-脚本用例对照" tabindex="-1"><a class="header-anchor" href="#_1-自动化冒烟覆盖了什么-脚本用例对照"><span>1. 自动化冒烟覆盖了什么（脚本用例对照）</span></a></h2><table><thead><tr><th style="text-align:left;">用例</th><th style="text-align:left;">验的是</th><th style="text-align:left;">判定依据（脚本断言）</th></tr></thead><tbody><tr><td style="text-align:left;"><code>S0-1</code></td><td style="text-align:left;">凭证链路</td><td style="text-align:left;"><code>GET /api/ws/ticket</code> 返回非空 <code>data</code>（顺带证明 token 有效）</td></tr><tr><td style="text-align:left;"><code>S1-1</code></td><td style="text-align:left;"><strong>会话就绪点迁移</strong>（Step 1 的关键回归）</td><td style="text-align:left;"><code>mode=MOCK&amp;input=text</code> 建连后收到 <code>THINKING</code> —— 说明不靠 ASR 就绪也能起算计费 + 发首问</td></tr><tr><td style="text-align:left;"><code>S1-2</code></td><td style="text-align:left;"><strong>回合串行化</strong></td><td style="text-align:left;">首问还在生成时发 <code>ANSWER</code> → <code>ERROR{TURN_BUSY}</code>（未被接受、不并发跑两轮）</td></tr><tr><td style="text-align:left;"><code>S1-3</code></td><td style="text-align:left;"><strong>「回答结束」信号</strong>（哨兵用例）</td><td style="text-align:left;">收到 <code>LLM_REPLY{final:true}</code>，<code>replyId</code> 与首问 <code>THINKING</code> 一致；打印 final 分片条数与是否带正文（正常：<strong>1 条且带正文</strong>）</td></tr><tr><td style="text-align:left;"><code>S1-4</code></td><td style="text-align:left;">文字作答 → 回合</td><td style="text-align:left;">发 <code>ANSWER</code> 后新增一条 <code>THINKING</code>，且随后的 <code>LLM_REPLY.replyId</code> 与之<strong>一致</strong></td></tr><tr><td style="text-align:left;"><code>S1-5</code></td><td style="text-align:left;">空作答</td><td style="text-align:left;"><code>ERROR{INVALID_ANSWER}</code>，且 <code>THINKING</code> 数量不变（不消耗回合）</td></tr><tr><td style="text-align:left;"><code>S1-6</code></td><td style="text-align:left;">超长作答</td><td style="text-align:left;">2001 字 → <code>ERROR{ANSWER_TOO_LONG}</code>（<strong>拒绝而非截断</strong>）</td></tr><tr><td style="text-align:left;"><code>S1-7</code></td><td style="text-align:left;">协议严格</td><td style="text-align:left;">多传未知字段（<code>source</code>）→ <strong>整条丢弃</strong>：无 <code>THINKING</code>、无 <code>ERROR</code>、静默窗口内 0 条新消息</td></tr><tr><td style="text-align:left;"><code>S1-8</code></td><td style="text-align:left;">正常收尾</td><td style="text-align:left;">客户端关闭 → 连接正常收敛（触发结束事件与计费收尾）</td></tr><tr><td style="text-align:left;"><code>S1-9</code></td><td style="text-align:left;">非法参数组合</td><td style="text-align:left;"><code>ASSIST + input=text</code> → <code>ERROR{INTERVIEW_PREPARE_FAILED}</code> + 关闭码 <strong>4002</strong>（非法组合当场暴露，不静默降级成 audio）</td></tr><tr><td style="text-align:left;"><code>S1-10</code></td><td style="text-align:left;">模式与能力对应</td><td style="text-align:left;">audio 模式发 <code>ANSWER</code> → <code>ERROR{INVALID_INPUT_MODE}</code>，且不产生回合</td></tr><tr><td style="text-align:left;"><code>S2-1</code></td><td style="text-align:left;">识别通道推帧（含资格放行）</td><td style="text-align:left;">先确保<strong>有进行中的面试</strong>（<code>ensureCarrier</code>），再 <code>/ws/asr</code> 建连 + 推 PCM 帧后<strong>无 ERROR</strong>（有 Key 时另看 <code>ASR_RESULT</code>）</td></tr><tr><td style="text-align:left;"><code>S2-2</code></td><td style="text-align:left;">识别并发闸门</td><td style="text-align:left;">第二条 → <code>ERROR{ASR_LIMIT_EXCEEDED}</code> + 关闭码 <strong>4004</strong></td></tr><tr><td style="text-align:left;"><code>S2-3</code></td><td style="text-align:left;">识别鉴权</td><td style="text-align:left;">不带 ticket → <code>ERROR{AUTH_FAILED}</code> + 关闭码 <strong>4001</strong></td></tr><tr><td style="text-align:left;"><code>S3-1</code></td><td style="text-align:left;">指标暴露</td><td style="text-align:left;"><code>/actuator/prometheus</code> 里有 4 个 <code>asr_stream_*</code> 指标，并打印 accepted / rejected_limit / <strong>rejected_no_session</strong> 计数</td></tr><tr><td style="text-align:left;"><code>S3-2</code></td><td style="text-align:left;">每日配额（可选）</td><td style="text-align:left;">建连到超限 → <code>ERROR{ASR_QUOTA_EXCEEDED}</code> + 关闭码 <strong>4006</strong></td></tr><tr><td style="text-align:left;"><code>S3-3</code></td><td style="text-align:left;">日志脱敏（可选）</td><td style="text-align:left;">①正向：日志里能看到 <code>收到文本作答 … text=len=N</code> 的摘要；②反向：带随机标记的作答原文<strong>不出现在 INFO+ 级别</strong>（DEBUG 与 AI 复述行不算泄漏，见下）</td></tr><tr><td style="text-align:left;"><code>S4-1</code></td><td style="text-align:left;"><strong>识别资格闸门</strong>（Step 4）</td><td style="text-align:left;"><strong>没有进行中的面试</strong>时建连 → <code>ERROR{ASR_NO_ACTIVE_SESSION}</code> + 关闭码 <strong>4007</strong>（脚本先开一条会话再关掉，确定性制造&quot;没有面试&quot;；带小重试窗口，避免服务端收尾慢导致的假失败）</td></tr><tr><td style="text-align:left;"><code>S4-2</code></td><td style="text-align:left;">资格不误杀</td><td style="text-align:left;">面试进行中建连 → <strong>放行</strong>（无 ERROR）；本机没配豆包 Key 时是 <code>ASR_UNAVAILABLE</code>，那同样证明资格已放行（否则会是 4007）</td></tr><tr><td style="text-align:left;"><code>S4-3</code></td><td style="text-align:left;"><strong>面试结束即回收</strong>（Step 4）</td><td style="text-align:left;">识别连接已建好 → 关闭面试会话 → 该识别连接被服务端<strong>静默关闭</strong>：<code>code=1000</code>、<code>reason=INTERVIEW_ENDED</code>，且<strong>全程没有 ERROR</strong></td></tr><tr><td style="text-align:left;"><code>S5-1</code></td><td style="text-align:left;"><strong>上游输入空闲释放</strong>（2026-09-24，可选、默认跳过）</td><td style="text-align:left;">建连后<strong>不推任何帧</strong> → 约 &lt;阈值&gt; 后被静默释放：<code>code=1000</code>、<strong><code>reason=ASR_INPUT_IDLE</code></strong>、无 ERROR，且耗时在「阈值 ~ 阈值+检查周期」内。需把后端 <code>realtime.asr.input-idle-timeout-ms</code> 临时调小（如 3000）并重启，再带上 <code>SMOKE_ASR_IDLE_TIMEOUT_MS=&lt;同一个值&gt;</code></td></tr></tbody></table><hr><h2 id="_2-手工联调清单-脚本替代不了的部分" tabindex="-1"><a class="header-anchor" href="#_2-手工联调清单-脚本替代不了的部分"><span>2. 手工联调清单（脚本替代不了的部分）</span></a></h2><h3 id="a-文字作答链路-step-1" tabindex="-1"><a class="header-anchor" href="#a-文字作答链路-step-1"><span>A. 文字作答链路（Step 1）</span></a></h3><table><thead><tr><th style="text-align:left;">#</th><th style="text-align:left;">操作</th><th style="text-align:left;">期望</th><th style="text-align:left;">不满足时先看</th></tr></thead><tbody><tr><td style="text-align:left;">A-1</td><td style="text-align:left;">从设置页以 <code>?mode=text</code> 进入 V2 页面，建连</td><td style="text-align:left;">建连成功；AI 面试官<strong>主动发首问</strong>（不需要用户先说话）</td><td style="text-align:left;">前端建连 URL 是否真带 <code>input=text</code>；后端是否 <code>MOCK</code>（<code>ASSIST</code> 无首问）</td></tr><tr><td style="text-align:left;">A-2</td><td style="text-align:left;">在输入框打字 → 发送</td><td style="text-align:left;">出现&quot;思考中&quot;→ 逐字流式输出；<strong>输入框在回复期间禁用</strong></td><td style="text-align:left;">前端是否消费 <code>TURN_BUSY</code>（回到 S1-4）</td></tr><tr><td style="text-align:left;">A-3</td><td style="text-align:left;">回复中再点发送</td><td style="text-align:left;">提示&quot;AI 正在回复中，请稍候&quot;，且<strong>没有</strong>第二条回答</td><td style="text-align:left;">前端本地没拦 → 允许发，但后端必须回 <code>TURN_BUSY</code></td></tr><tr><td style="text-align:left;">A-4</td><td style="text-align:left;">发送空白 / 纯空格 / 纯换行</td><td style="text-align:left;">提示内容不能为空，不产生回答</td><td style="text-align:left;"><code>INVALID_ANSWER</code></td></tr><tr><td style="text-align:left;">A-5</td><td style="text-align:left;">粘贴一段超长文本（&gt;2000 字）发送</td><td style="text-align:left;">明确提示超长；<strong>不要</strong>发出去被截断</td><td style="text-align:left;"><code>ANSWER_TOO_LONG</code></td></tr><tr><td style="text-align:left;">A-6</td><td style="text-align:left;"><strong>点话筒说话</strong>（语音输入辅助）</td><td style="text-align:left;">识别文本进入输入框（草稿可被后续分片覆盖，<code>final</code> 后落定），<strong>不触发回答</strong></td><td style="text-align:left;">识别通道不触发回合是设计如此（Step 2 的核心解耦）</td></tr><tr><td style="text-align:left;">A-7</td><td style="text-align:left;">暂停 → 继续面试（重连）</td><td style="text-align:left;">会话恢复；<strong>输入框仍可文字作答</strong>，且<strong>不会凭空开始走语音会话</strong>。V2 入口：头部「暂停」（带确认）→ 底部暂停条「继续面试」</td><td style="text-align:left;"><code>interview.input_mode</code> 是否落库 / <code>LogicalSessionRebuilder</code> 是否还原（见 A-9）</td></tr><tr><td style="text-align:left;">A-8</td><td style="text-align:left;">提示词一致性：问一个只有简历里才有的细节</td><td style="text-align:left;">回答里用到了简历/岗位内容</td><td style="text-align:left;">快照裁剪与 <code>system_prompt</code> 落库</td></tr><tr><td style="text-align:left;">A-9</td><td style="text-align:left;"><strong>数据层铁证</strong>（三层判据）</td><td style="text-align:left;">①现象：发 <code>ANSWER</code> 有回答（audio 模式会被拒）；②<code>select mode, input_mode from interview order by id desc limit 1</code> → <code>text</code>；③重连后仍能文字作答</td><td style="text-align:left;"><code>input_mode</code> 没落库 → 重连后会退化成 audio（重连即启动 ASR，且 <code>ANSWER</code> 被判模式不符）</td></tr><tr><td style="text-align:left;">A-10</td><td style="text-align:left;"><strong>V1 / PC 正式面试回归</strong>（最重要的一条）</td><td style="text-align:left;">PC 端（<code>mode=ASSIST</code>，不带 <code>input</code>）一切照旧：识别 → 自动回答 → 计费 / 暂停 / 结束</td><td style="text-align:left;"><code>audio</code> 路径必须与改动前<strong>行为等价</strong>（后端方案 §5.2 的 5 条回归）</td></tr><tr><td style="text-align:left;">A-11</td><td style="text-align:left;">结束面试 → 收尾跳转（对齐 V1）</td><td style="text-align:left;">点底部「结束面试」→ 提示「面试已结束」→ 用<strong>通用模板</strong>的面试进「体验完成」引导页，没用模板则返回上一页；模板分支用 <code>redirectTo</code>，<strong>返回键不该再回到面试页</strong>。暂停态点结束同样可退出（此时后端由暂停保留时长兜底收尾，<code>end_reason</code> 是超时口径）</td><td style="text-align:left;">「是否用模板」在<strong>建连成功时定格</strong>（<code>usedTemplateRef</code>）—— 别改成收尾时再读 storage，那时选中态可能已被清掉</td></tr></tbody></table><h3 id="b-独立识别通道-step-2" tabindex="-1"><a class="header-anchor" href="#b-独立识别通道-step-2"><span>B. 独立识别通道（Step 2）</span></a></h3><table><thead><tr><th style="text-align:left;">#</th><th style="text-align:left;">操作</th><th style="text-align:left;">期望</th></tr></thead><tbody><tr><td style="text-align:left;">B-1</td><td style="text-align:left;">面试会话进行中，点话筒 → 说一句话 → 停止</td><td style="text-align:left;">识别文本进入输入框；<strong>面试会话不受任何影响</strong>（没有额外回答、没有计费异常）</td></tr><tr><td style="text-align:left;">B-2</td><td style="text-align:left;">说话后什么都不做</td><td style="text-align:left;">⚠️ 2026-09-24 后<strong>这条已经验不到绝对上限了</strong>：前端 8 秒空闲释放 / 后端 25 秒上游空闲会先把它收掉（见 B-9）。真要验绝对上限：把后端 <code>input-idle-timeout-ms</code> 设为 0、临时把前端 <code>IDLE_RELEASE_DELAY_MS</code> 调大，再等满 <code>session-max-duration-ms</code>（300 秒）→ 应收 <code>ASR_TIMEOUT</code> / 4005，面试继续</td></tr><tr><td style="text-align:left;">B-3</td><td style="text-align:left;">在已有识别连接时再点一次话筒</td><td style="text-align:left;">旧连接先关 / 或收到 <code>ASR_LIMIT_EXCEEDED</code>(4004)；<strong>不要静默重连</strong></td></tr><tr><td style="text-align:left;">B-4</td><td style="text-align:left;">断网 → 恢复网络</td><td style="text-align:left;">识别连接失败只提示&quot;改用打字&quot;，面试会话按既有心跳规则处理（两者互不牵连）</td></tr><tr><td style="text-align:left;">B-5</td><td style="text-align:left;">移动网络下同时维持会话 + 识别两条连接</td><td style="text-align:left;">双连接稳定（这是 Step 2 的主要风险项，必须在真机 + 移动网络验一次）</td></tr><tr><td style="text-align:left;">B-6</td><td style="text-align:left;">当天识别次数用完后点话筒</td><td style="text-align:left;">提示&quot;今日语音识别次数已用完&quot;（4006），<strong>不自动重连</strong>；文字作答仍然可用</td></tr><tr><td style="text-align:left;">B-7</td><td style="text-align:left;"><strong>没有面试时点话筒</strong>（Step 4）</td><td style="text-align:left;">在&quot;未开始面试 / 面试已结束 / 面试已暂停&quot;的页面上点话筒 → 提示「请先开始面试后再使用语音输入」（4007）；<strong>不打开麦克风、不自动重连</strong></td></tr><tr><td style="text-align:left;">B-8</td><td style="text-align:left;"><strong>说话中直接结束面试</strong>（Step 4）</td><td style="text-align:left;">识别连接被服务端<strong>静默</strong>收掉（1000，页面不弹任何错误），麦克风随之释放、&quot;正在聆听&quot;提示消失</td></tr><tr><td style="text-align:left;">B-9</td><td style="text-align:left;"><strong>停止说话后不再操作</strong>（2026-09-24）</td><td style="text-align:left;">点话筒说一段 → 点话筒停止 → 什么都不做：约 <strong>8 秒后</strong>识别连接自动关闭（开发者工具 Network 里 WS 关闭）；期间最后一句识别结果仍会落进输入框；再点话筒能正常开始（<strong>不能</strong>出现&quot;点了没反应&quot;）</td></tr><tr><td style="text-align:left;">B-10</td><td style="text-align:left;"><strong>单次上限 180 秒</strong>（2026-09-24）</td><td style="text-align:left;">说满 <strong>180 秒</strong> → 微信自动停录音 + 提示「已到单次语音上限，可再点话筒继续说」，<strong>已识别文字仍在输入框里</strong>；随后再点话筒能正常开始（新连接、新窗口）。界面<strong>不显示</strong>已用时长（2026-09-24 决定）</td></tr></tbody></table><h3 id="c-治理-step-3" tabindex="-1"><a class="header-anchor" href="#c-治理-step-3"><span>C. 治理（Step 3）</span></a></h3><table><thead><tr><th style="text-align:left;">#</th><th style="text-align:left;">操作</th><th style="text-align:left;">期望</th></tr></thead><tbody><tr><td style="text-align:left;">C-1</td><td style="text-align:left;">跑一轮 B-1 后看指标</td><td style="text-align:left;"><code>asr_stream_sessions_active</code> 回到 0；<code>asr_stream_connections_total{result=&quot;accepted&quot;}</code> +1；<code>asr_stream_session_duration_seconds{end=&quot;client_close&quot;}</code> 有一次样本；<code>asr_stream_audio_bytes_total</code> 按说话时长增长（≈32000 字节/秒）</td></tr><tr><td style="text-align:left;">C-2</td><td style="text-align:left;">Prometheus 查询</td><td style="text-align:left;"><code>rate(asr_stream_audio_bytes_total[5m]) / 32000</code> ≈ 每秒识别音频时长 → 识别成本；<code>asr_stream_session_duration_seconds_count{end!=&quot;client_close&quot;}</code> / <code>..._count</code> = 失败率</td></tr><tr><td style="text-align:left;">C-3</td><td style="text-align:left;">日志抽查（Step 3 的核心验收）</td><td style="text-align:left;"><strong>INFO 及以上</strong>搜不到作答原文与识别转写；能看到 <code>len=42</code> 这类摘要。命令：搜你自己的作答内容，再看它命中行的级别</td></tr><tr><td style="text-align:left;">C-4</td><td style="text-align:left;">埋点核对</td><td style="text-align:left;">分析平台查 <code>interview_session_started</code> / <code>interview_session_ended</code>，看到 \`props.input_mode=AUDIO</td></tr><tr><td style="text-align:left;">C-5</td><td style="text-align:left;">配额验证</td><td style="text-align:left;">临时把 <code>realtime.asr.daily-sessions-per-user</code> 改成 1 → 重启 → 第二次点话筒必回 4006（并在 Redis 里看到 <code>interview:asr:quota:&lt;userId&gt;:&lt;北京日期&gt;</code>）</td></tr><tr><td style="text-align:left;">C-6</td><td style="text-align:left;">资格与回收的指标（Step 4）</td><td style="text-align:left;"><code>asr_stream_connections_total{result=&quot;rejected_no_session&quot;}</code> 只在&quot;没在面试里点话筒&quot;时增长；被回收的连接记在 <code>asr_stream_session_duration_seconds{end=&quot;server_close&quot;}</code>（<strong>正常收尾，不是故障</strong>）；<strong>客户端该关没关</strong>的兜底记在 <code>end=&quot;input_idle&quot;</code>（同样不是故障，但它持续增长说明有客户端没走&quot;停止后 ~8 秒自己释放&quot;那条正路，值得看一眼）</td></tr></tbody></table><blockquote><p>C-1 / C-2 的读法：<strong>失败率只看 <code>end=&quot;asr_unavailable&quot;</code> 的占比</strong>（豆包挂了 / 没配 Key）。 另外三类都<strong>不是</strong>故障，别混进去：<code>client_close</code>（用户说完就关）、 <code>server_close</code>（<strong>面试结束回收</strong>，Step 4）是正常收尾，<code>timeout</code>（点了话筒忘记说话）是用户误触 —— 混在一起会把&quot;用户误触 / 正常回收&quot;误判成&quot;服务不稳定&quot;。</p></blockquote><hr><h2 id="_3-每次发版必跑的回归基线" tabindex="-1"><a class="header-anchor" href="#_3-每次发版必跑的回归基线"><span>3. 每次发版必跑的回归基线</span></a></h2><ol><li><strong>后端全量单测</strong>：<code>./mvnw test</code>（<strong>实测 268 通过 / 0 失败</strong>；Step 1/2/3/4 的新用例、输入空闲兜底 2 例与 §12.6 的两个修复用例都在里面）。</li><li><strong>协议冒烟</strong>：<code>TOKEN=&lt;token&gt; node scripts/smoke-v2.mjs</code> → 退出码 0（实测 <strong><code>PASS=19 / FAIL=0 / SKIP=1</code></strong>； 带上 <code>SMOKE_ASR_IDLE_TIMEOUT_MS=&lt;后端实际阈值&gt;</code>（并把后端 <code>realtime.asr.input-idle-timeout-ms</code> 临时调小）则是 <strong><code>PASS=20 / FAIL=0 / SKIP=1</code></strong> —— 多出来的那条是 <code>S5-1</code>）。 跑之前先按 §0.3 把本机 Redis 隔离开，否则会被同机的其他实例干扰成红。</li><li><strong>人工最小集</strong>：A-1、A-2、A-7、A-9、A-10（PC 正式面试）、B-1、B-5、B-7、B-8、B-9、C-3。 <blockquote><p>A-10 与 B-5 是<strong>回归红线</strong>：一个保护老用户（PC 正式面试），一个保护新链路在移动网络的可用性。 B-7 / B-8 是 Step 4 的红线：<strong>识别通道的资格与回收</strong>（没有面试不许用、面试结束必须回收）。</p></blockquote></li><li><strong>易回归的三点</strong>：<code>S1-3</code>（「回答结束」= 末分片带 <code>finish_reason</code>，一轮一条； <strong>不要&quot;顺手补一条空 final&quot;</strong>）、<code>S3-3</code>（INFO 日志仍无作答原文）、 <code>S4-1</code> + <code>S4-3</code>（<strong>资格闸门与结束回收</strong>：漏了就是豆包成本失控，而且失败是静默的）。 <blockquote><p><code>S4-2</code> / <code>S4-3</code> / <code>S5-1</code> 都是&quot;1000 关闭、无 ERROR&quot;，<strong>只靠 <code>reason</code> 区分</strong> （<code>INTERVIEW_ENDED</code> / <code>ASR_SESSION_END</code> / <code>ASR_INPUT_IDLE</code>）—— 别为了&quot;少断言&quot;把 reason 那两条删掉， 删了就分不清&quot;资格未放行 / 回收没生效 / 空闲兜底&quot;。 另外这三条用例在观察窗口内都会<strong>持续补帧</strong>（<code>observeAsrWithFrames</code>），就是为了在 <code>input-idle-timeout-ms</code> 被临时调小时不被空闲兜底抢先把连接关掉。</p></blockquote></li><li><strong>发版后</strong>：看一眼 <code>asr_stream_connections_total{result=~&quot;rejected_.*&quot;}</code> 与失败率，确认没有系统性拒绝。</li></ol><hr><h2 id="_4-常见故障对照表-现象-→-根因-→-去哪里看" tabindex="-1"><a class="header-anchor" href="#_4-常见故障对照表-现象-→-根因-→-去哪里看"><span>4. 常见故障对照表（现象 → 根因 → 去哪里看）</span></a></h2><table><thead><tr><th style="text-align:left;">现象</th><th style="text-align:left;">最可能的原因</th><th style="text-align:left;">排查入口</th></tr></thead><tbody><tr><td style="text-align:left;">建连立刻断开，收到 <code>INSUFFICIENT_BALANCE</code></td><td style="text-align:left;">账号没有面试时长余额</td><td style="text-align:left;"><code>TicketHandshakeInterceptor</code>（余额准入）；给账号充值 / 用无限资格账号</td></tr><tr><td style="text-align:left;">建连立刻断开，收到 <code>MISSING_TICKET</code> / <code>INVALID_TICKET</code></td><td style="text-align:left;">没带 ticket / ticket 已过期（30 秒）/ 被复用（GETDEL 一次性）</td><td style="text-align:left;">前端是否&quot;先取 ticket 再立即建连&quot;；<code>WebSocketTicketService</code></td></tr><tr><td style="text-align:left;">点话筒没反应，无任何提示</td><td style="text-align:left;">识别通道建连失败且前端未处理</td><td style="text-align:left;">开发者工具 Network 里的 WS 帧；<code>AsrWebSocketHandler</code> 是否发了 <code>ERROR</code></td></tr><tr><td style="text-align:left;">点话筒提示「请先开始面试」（<code>ASR_NO_ACTIVE_SESSION</code> / 4007）</td><td style="text-align:left;">该用户当前<strong>没有进行中的面试</strong>（未开始 / 已结束 / 已暂停）—— 这是 Step 4 刻意设的成本护栏，不是故障</td><td style="text-align:left;">是否真在面试中：Redis <code>interview:session:user:&lt;userId&gt;</code> 是否存在且未 <code>paused</code>；<code>AsrUsageGuard#decide</code></td></tr><tr><td style="text-align:left;">面试结束后识别连接还活着（豆包还在计费）</td><td style="text-align:left;">回收没生效：<code>InterviewEndedEvent</code> 没人监听</td><td style="text-align:left;"><code>AsrSessionReclaimer</code> 是否装配；事件是否真的发布（<code>AudioStreamSession#publishEnded</code> / <code>evictExpiredPausedSessions</code>）</td></tr><tr><td style="text-align:left;"><strong>停止说话后识别连接还挂着</strong>（豆包还在喂静音、按时长计费）</td><td style="text-align:left;">前端空闲释放没跑（定时器被清 / 小程序切后台）<strong>且</strong>后端兜底没生效</td><td style="text-align:left;">前端 <code>WsAsrTransport#stop</code> 的 <code>IDLE_RELEASE_DELAY_MS</code>（8 秒）；后端 <code>realtime.asr.input-idle-timeout-ms</code>（25 秒，0 = 关掉了）；日志搜 <code>上游输入空闲已达上限</code>，指标看 <code>end=&quot;input_idle&quot;</code></td></tr><tr><td style="text-align:left;"><strong>面试进行中点话筒却提示「请先开始面试」（4007）</strong></td><td style="text-align:left;">① <strong>最常见：同一个 Redis 上还有别的后端实例</strong>（VM / devcontainer / 另一台机器），它的计费调度器把本机这条会话当孤儿会话删掉了（见 §5）；② 本机自身也可能有&quot;迟到起算 / 起算竞态&quot;（已修，见 §5）</td><td style="text-align:left;">先确认本机后端连的是<strong>独占的</strong> Redis（§0.3：<code>redis-local:6380</code>）；再搜日志 <code>忽略迟到的「会话就绪」</code> 与 <code>计费会话归属不是本次会话</code>；对照 <code>InterviewBillingService#startBilling</code>、<code>AudioStreamSession#markSessionReady</code></td></tr><tr><td style="text-align:left;">频繁 <code>TURN_BUSY</code></td><td style="text-align:left;">前端没有在等回复时禁用输入框；或用户连点</td><td style="text-align:left;">前端消费 <code>THINKING</code> / <code>LLM_REPLY{isFinal}</code> 的状态机</td></tr><tr><td style="text-align:left;">text 会话重连后开始走语音</td><td style="text-align:left;"><code>input_mode</code> 没落库 / 重建时没还原</td><td style="text-align:left;"><code>Interview.input_mode</code>（V26）/ <code>LogicalSessionRebuilder</code></td></tr><tr><td style="text-align:left;">识别通了但文字进不了输入框</td><td style="text-align:left;">前端 <code>base + draft</code> 规则没按 <code>final</code> 落定</td><td style="text-align:left;">前端方案 §6.1；<code>ASR_RESULT.final</code></td></tr><tr><td style="text-align:left;"><code>ANSWER</code> 发了却什么都没发生</td><td style="text-align:left;">多传了字段（如 <code>source</code>）→ 整条丢弃</td><td style="text-align:left;">上行报文与 <code>ControlMessage</code> 字段一一对应；见 S1-7</td></tr><tr><td style="text-align:left;">指标一条都不涨</td><td style="text-align:left;">该分支没被走到 / 抓的是另一个实例 / actuator 端口不对</td><td style="text-align:left;"><code>curl localhost:8009/actuator/prometheus | grep asr_stream</code></td></tr><tr><td style="text-align:left;">埋点里没有 <code>input_mode</code></td><td style="text-align:left;"><code>analytics.enabled=false</code>（默认关闭）；或事件未上报</td><td style="text-align:left;"><code>AnalyticsTracker</code> 日志；AGENTS.md §6.8</td></tr><tr><td style="text-align:left;">指标里 <code>rejected_quota</code> 持续增长</td><td style="text-align:left;">配额上限对真实使用偏低，或账号被刷</td><td style="text-align:left;"><code>interview:asr:quota:*</code>；调 <code>daily-sessions-per-user</code></td></tr><tr><td style="text-align:left;">前端收不到「回答结束」，输入框一直禁用</td><td style="text-align:left;"><code>final</code> 分片没到（上游末分片不再带 <code>finish_reason</code>），或消费方把字段判成了 <code>isFinal</code></td><td style="text-align:left;">回归看 <code>S1-3</code>；字段名以实际 JSON 为准（§12.6.1）</td></tr><tr><td style="text-align:left;">日志里出现作答原文或识别转写</td><td style="text-align:left;">仍有 INFO 级在用 <code>truncate</code>（保留前 512 字<strong>内容</strong>）的行；已修 <code>[回合] 触发LLM回答</code> 一处</td><td style="text-align:left;">回归看 <code>S3-3</code>；<code>LogTextUtils#describe</code></td></tr><tr><td style="text-align:left;">dev 下日志里能看到完整 LLM 上下文 / 简历内容</td><td style="text-align:left;"><strong>预期行为</strong>：dev profile 是 <code>root: debug</code>，这些是 DEBUG 行；生产 <code>root=INFO</code> 不输出</td><td style="text-align:left;">看命中行的级别再判断，别按&quot;全文件搜不到&quot;验收</td></tr></tbody></table><blockquote><p><strong>日志脱敏的验收标准（别搞错）</strong>：按<strong>级别</strong>判定 —— <code>INFO</code> 及以上不得出现用户输入原文（作答、识别转写）， <code>DEBUG</code> 允许（dev 排查用）。另外两类&quot;命中&quot;不是泄漏：AI 回答里的复述（<code>[回合] LLM回答完成</code>，回答本就落库）、 以及 dev 的调试日志。冒烟 <code>S3-3</code> 已按这套语义实现。</p><p><strong>「回答结束」的验收标准</strong>：<code>LLM_REPLY{final:true}</code> 一轮<strong>恰好一条</strong>、<code>replyId</code> 与触发它的 <code>THINKING</code> 一致；它<strong>带最后一段正文</strong>，前端按<strong>追加</strong>处理（不要整段替换）。它是 V2 输入框恢复的唯一依据 —— 这条信号没了，输入框会一直禁用。</p></blockquote><hr><h2 id="_5-已知边界-现在验不了-不属于本轮" tabindex="-1"><a class="header-anchor" href="#_5-已知边界-现在验不了-不属于本轮"><span>5. 已知边界（现在验不了 / 不属于本轮）</span></a></h2><table><thead><tr><th style="text-align:left;">项</th><th style="text-align:left;">说明</th></tr></thead><tbody><tr><td style="text-align:left;">识别准确率 / 回答质量</td><td style="text-align:left;">必须真人说话 + 真实音频；脚本只能验协议与管道</td></tr><tr><td style="text-align:left;">文本模式「失联 = 结束」还是「失联 = 暂停」</td><td style="text-align:left;">✅ <strong>已定稿</strong>：失联 = 结束（≠ 暂停），文字作答阈值 180 秒（后端方案 §4.1.6 / §8.1）</td></tr><tr><td style="text-align:left;">「无操作自动暂停」</td><td style="text-align:left;">✅ <strong>已定稿</strong>：V2 不引入，保持显式 <code>PAUSE</code>（后端方案 §8 P0-1）；文本模式下用户静默思考照样按时长计费</td></tr><tr><td style="text-align:left;">前端埋点 <code>source</code>(voice/text)</td><td style="text-align:left;">后端拿不到（打字与语音转写在协议上无法区分），由前端埋点承担</td></tr><tr><td style="text-align:left;">多端登录顶号 × V2 会话</td><td style="text-align:left;">沿用既有顶号语义，V2 未改动，也不在本次冒烟范围</td></tr><tr><td style="text-align:left;"><strong>多实例共享 Redis 顶掉活会话的计费会话</strong>（2026-09-24 冒烟实跑定位；<strong>不是代码缺陷，是本地环境</strong>）</td><td style="text-align:left;"><strong>现象</strong>：<code>/ws/asr</code> 在&quot;确实进行中的面试&quot;里被判无资格（4007），并且计费静默停止（用户白嫖）。<br><strong>根因</strong>：同机还有别的实例（当时是 3 台 VirtualBox VM）跑同一套后端，且<strong>连的是同一个 Redis</strong>（<code>172.16.0.222:6379</code>）—— 它们各自的 <code>InterviewBillingScheduler</code> 会 <code>SCAN interview:session:user:*</code>，把&quot;不在自己内存注册表里&quot;的会话当孤儿会话 <code>stopBilling</code> 删键。<br><strong>定位证据</strong>：① Redis <code>MONITOR</code> 抓到一条 <code>DEL</code>，但应用日志里<strong>没有任何对应记录</strong>（同步 appender，不存在丢行）；② 给两处删键点（<code>stopBilling</code> / <code>forceTerminateActiveSession</code>）加带调用栈的临时日志后，<strong>删键那一刻两者都没被调用</strong> ⇒ 不是本进程；③ <code>CLIENT LIST</code> 里有一条 age≈6.6 小时的 <code>Lettuce</code> 客户端在跑 <code>SCAN</code>（本机实例只有几分钟大）。<br><strong>解决</strong>：本机独享一个 Redis（<code>docker-compose.dev.yml</code> 已加 <code>redis-local</code>，<strong>只绑 <code>127.0.0.1:6380</code></strong>），后端用 <code>SPRING_DATA_REDIS_HOST/PORT</code> 指过去，做法见 §0.3。隔离后冒烟连续两轮 <code>PASS=19 / FAIL=0</code>。<br><strong>注意</strong>：Postgres（<code>pass_up_dev</code>）<strong>仍是共用的</strong> —— 多实例之间的数据级干扰（扣同一份 <code>user_asset</code>、回收同一批 <code>interview</code> 行）不会因此消失</td></tr><tr><td style="text-align:left;"><strong>迟到起算 / 起算竞态</strong>（2026-09-24 排障过程中证实并<strong>已修</strong>；与上一条环境问题独立）</td><td style="text-align:left;">① <code>AudioStreamSession#markSessionReady()</code> <strong>不校验会话是否已结束</strong> ⇒ 被顶号 / 已关闭的 audio 会话，其<strong>迟到的 <code>AsrEvent.Ready</code></strong> 仍会走完&quot;起算&quot;（实测：连接 35.479 已收尾，36.144 还在为它起算）；② <code>InterviewBillingService#startBilling()</code> 的&quot;已存在就跳过&quot;是 <strong>check-then-act</strong>（同一 JVM 内实测<strong>同一毫秒两条</strong> <code>开始计费</code>），两个会话都可能写同一个键，键最终可能归属<strong>已死会话</strong>。<br><strong>已修</strong>：前者加&quot;已结束 / 已暂停即返回&quot;守卫（日志会打 <code>忽略迟到的「会话就绪」</code>）；后者改为<strong>只在同一 <code>logicalSessionId</code></strong> 时跳过、属于别的会话则<strong>覆盖并打 ERROR</strong>。两者都有单测（<code>AudioStreamSessionAsrBaselineTest</code> / <code>InterviewBillingServiceTest</code>）。</td></tr></tbody></table>`,48)]])}var s=r(a,[[`render`,o]]);export{i as _pageData,s as default};