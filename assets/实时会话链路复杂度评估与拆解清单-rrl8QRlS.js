import{A as e,D as t,L as n,f as r,m as i,p as a,s as o,u as s}from"./runtime-core.esm-bundler-BHJ3o1LK.js";import{t as c}from"./plugin-vue_export-helper-BDNMzG2s.js";var l=JSON.parse(`{"path":"/%E9%A1%B9%E7%9B%AE/%E9%9D%A2%E8%AF%95%E5%8A%A9%E6%89%8B/%E6%9E%B6%E6%9E%84%E8%AE%BE%E8%AE%A1/%E5%AE%9E%E6%97%B6%E4%BC%9A%E8%AF%9D%E9%93%BE%E8%B7%AF%E5%A4%8D%E6%9D%82%E5%BA%A6%E8%AF%84%E4%BC%B0%E4%B8%8E%E6%8B%86%E8%A7%A3%E6%B8%85%E5%8D%95.html","title":"实时会话链路复杂度评估与拆解清单","lang":"zh-CN","frontmatter":{"title":"实时会话链路复杂度评估与拆解清单","icon":"mdi:puzzle-outline","sort":130,"description":"实时会话链路：复杂度评估与拆解清单 文档定位：现状评估（不评价历史决策）+ 可执行的拆解清单。 状态：已定稿（2026-09-22，两轮评审）—— 可按 §7 的顺序开始实施，不再扩展架构。 这是本目录关于实时会话的第三篇，三篇分工如下： 本文不推翻前两篇的结论，恰恰相反：其中一条原则（统一 cleanup）已经在文档里定死， 但代码结构上尚未收敛 —...","head":[["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"实时会话链路复杂度评估与拆解清单\\",\\"image\\":[\\"\\"],\\"dateModified\\":\\"2026-09-24T02:24:02.000Z\\",\\"author\\":[{\\"@type\\":\\"Person\\",\\"name\\":\\"桥黎\\",\\"url\\":\\"\\"}]}"],["meta",{"property":"og:url","content":"https://docs.cqlql.top/%E9%A1%B9%E7%9B%AE/%E9%9D%A2%E8%AF%95%E5%8A%A9%E6%89%8B/%E6%9E%B6%E6%9E%84%E8%AE%BE%E8%AE%A1/%E5%AE%9E%E6%97%B6%E4%BC%9A%E8%AF%9D%E9%93%BE%E8%B7%AF%E5%A4%8D%E6%9D%82%E5%BA%A6%E8%AF%84%E4%BC%B0%E4%B8%8E%E6%8B%86%E8%A7%A3%E6%B8%85%E5%8D%95.html"}],["meta",{"property":"og:site_name","content":"开发笔记"}],["meta",{"property":"og:title","content":"实时会话链路复杂度评估与拆解清单"}],["meta",{"property":"og:description","content":"实时会话链路：复杂度评估与拆解清单 文档定位：现状评估（不评价历史决策）+ 可执行的拆解清单。 状态：已定稿（2026-09-22，两轮评审）—— 可按 §7 的顺序开始实施，不再扩展架构。 这是本目录关于实时会话的第三篇，三篇分工如下： 本文不推翻前两篇的结论，恰恰相反：其中一条原则（统一 cleanup）已经在文档里定死， 但代码结构上尚未收敛 —..."}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:locale","content":"zh-CN"}],["meta",{"property":"og:updated_time","content":"2026-09-24T02:24:02.000Z"}],["meta",{"property":"article:modified_time","content":"2026-09-24T02:24:02.000Z"}]]},"git":{"createdTime":1790071903000,"updatedTime":1790216642000,"contributors":[{"name":"cql","username":"cql","email":"cql.ql@qq.com","commits":2,"url":"https://github.com/cql"}]},"readingTime":{"minutes":35.69,"words":10706},"filePathRelative":"项目/面试助手/架构设计/实时会话链路复杂度评估与拆解清单.md","autoDesc":true}`),u={name:`实时会话链路复杂度评估与拆解清单.md`},d={style:{"text-align":`left`}},f={style:{"text-align":`left`}};function p(c,l,u,p,m,h){let g=e(`RouteLink`);return t(),s(`div`,null,[l[16]||=o(`h1`,{id:`实时会话链路-复杂度评估与拆解清单`,tabindex:`-1`},[o(`a`,{class:`header-anchor`,href:`#实时会话链路-复杂度评估与拆解清单`},[o(`span`,null,`实时会话链路：复杂度评估与拆解清单`)])],-1),l[17]||=o(`blockquote`,null,[o(`p`,null,[o(`strong`,null,`文档定位：现状评估（不评价历史决策）+ 可执行的拆解清单。`),o(`strong`,null,`状态：已定稿（2026-09-22，两轮评审）—— 可按 §7 的顺序开始实施，不再扩展架构。`)]),o(`p`,null,`这是本目录关于实时会话的第三篇，三篇分工如下：`)],-1),o(`table`,null,[l[5]||=o(`thead`,null,[o(`tr`,null,[o(`th`,{style:{"text-align":`left`}},`文档`),o(`th`,{style:{"text-align":`left`}},`处理的事`)])],-1),o(`tbody`,null,[o(`tr`,null,[o(`td`,d,[i(g,{to:`/%E9%A1%B9%E7%9B%AE/%E9%9D%A2%E8%AF%95%E5%8A%A9%E6%89%8B/%E6%9E%B6%E6%9E%84%E8%AE%BE%E8%AE%A1/%E5%AE%9E%E6%97%B6%E9%9D%A2%E8%AF%95%E4%BC%9A%E8%AF%9D%E9%87%8D%E6%9E%84-%E9%A2%86%E5%9F%9F%E4%BA%8B%E4%BB%B6%E8%A7%A3%E8%80%A6.html`},{default:n(()=>[...l[0]||=[a(`实时面试会话重构-领域事件解耦`,-1)]]),_:1})]),l[1]||=o(`td`,{style:{"text-align":`left`}},[a(`用领域事件把计费 / 持久化从实时链路摘出去（✅ 已落地）；其 §8「计费投影」仍是提案（代码里没有 `),o(`code`,null,`BillingStateProjection`),a(`）`)],-1)]),o(`tr`,null,[o(`td`,f,[i(g,{to:`/%E9%A1%B9%E7%9B%AE/%E9%9D%A2%E8%AF%95%E5%8A%A9%E6%89%8B/%E6%9E%B6%E6%9E%84%E8%AE%BE%E8%AE%A1/%E5%AE%9E%E6%97%B6%E4%BC%9A%E8%AF%9D%E5%BF%83%E8%B7%B3%E4%B8%8E%E6%9A%82%E5%81%9C%E6%81%A2%E5%A4%8D-%E5%81%A5%E5%A3%AE%E6%80%A7%E6%94%B9%E8%BF%9B%E6%96%B9%E6%A1%88.html`},{default:n(()=>[...l[2]||=[a(`实时会话心跳与暂停恢复-健壮性改进方案`,-1)]]),_:1})]),l[3]||=o(`td`,{style:{"text-align":`left`}},`独立心跳、死连接检测、暂停恢复、跨进程重建、参数配置化（✅ 全部闭环），并定了三条健壮性原则`,-1)]),l[4]||=o(`tr`,null,[o(`td`,{style:{"text-align":`left`}},[o(`strong`,null,`本文`)]),o(`td`,{style:{"text-align":`left`}},[a(`前两篇之后`),o(`strong`,null,`仍然留在`),a(`这两个类里的复杂度：量化 → 病根 → 拆解清单 → 验收方式`)])],-1)])]),o(`blockquote`,null,[l[9]||=o(`p`,null,[a(`本文不推翻前两篇的结论，恰恰相反：其中一条原则（`),o(`strong`,null,`统一 cleanup`),a(`）已经在文档里定死， 但`),o(`strong`,null,`代码结构上尚未收敛`),a(` —— 这会是本文要处理的重点之一（§4.4）。`)],-1),o(`p`,null,[l[7]||=a(`相关：后端方案`,-1),i(g,{to:`/%E9%A1%B9%E7%9B%AE/%E9%9D%A2%E8%AF%95%E5%8A%A9%E6%89%8B/%E5%90%8E%E7%AB%AF%E5%AE%9E%E7%8E%B0/%E6%A8%A1%E6%8B%9F%E9%9D%A2%E8%AF%95%E7%AC%AC%E4%BA%8C%E7%89%88-%E8%AF%AD%E9%9F%B3%E8%AF%86%E5%88%AB%E4%B8%8ELLM%E5%9B%9E%E5%90%88%E8%A7%A3%E8%80%A6%E6%96%B9%E6%A1%88.html`},{default:n(()=>[...l[6]||=[a(`《模拟面试第二版-语音识别与LLM回合解耦方案》`,-1)]]),_:1}),l[8]||=a(` —— 本文 §6 的 P0 三项即它的「步骤零」。`,-1)])]),l[18]||=r(`<h2 id="_1-结论" tabindex="-1"><a class="header-anchor" href="#_1-结论"><span>1. 结论</span></a></h2><ol><li><strong>复杂度是真的，但不是「写坏了」，而是「新能力持续落在同两个类上」。</strong> 领域事件解耦时 <code>AudioStreamSession</code> 约 730 行，现在是 <strong>812 行</strong> —— 拆过一次又长回来了。 暂停恢复、独立心跳、ASR 自愈、错误归一这些后续能力，全都默认落在它身上。</li><li><strong>「容易出错」的准确口径</strong>：单点改动有测试兜着（本模块约 62 个用例）， <strong>长尾组合场景没有防线</strong>（暂停中重连 + ASR 重连 + 计费扫描 + 心跳踢出 + 顶号并发）。</li><li><strong>病根不是行数</strong>，而是三类可复现的东西：职责叠加、隐式不变量、跨模块隐式耦合。</li><li><strong>处方</strong>：先补测试接缝（P0-0）→ 三次抽取（ASR 运行期 / 回合编排 / 连接判据接口）→ ID 语义区分 → cleanup 收敛与不变量结构化。全部是行为等价的机械重构，不新增能力。 <strong>V2 的 <code>input=text</code> 与 <code>/ws/asr</code> 应该在这些之后加</strong>（见 §7）。 <blockquote><p><strong>测试不是最后一步，而是第一步</strong>：P0-0 是 P0-1 / P0-2 的前置条件（§4.5 的三个盲区正好是要动的块）。</p></blockquote></li><li><strong>完成标准不是&quot;行数下降&quot;</strong>，而是<strong>变化边界</strong>：<code>AudioStreamSession</code> 不再认识 ASR 运行机制、 LLM 客户端与上下文裁剪；且满足 §8 的四条约束（新增输入源不改 runner、新增通道不复制 runner、 新增回合输入不复制 turn runner、新增退出路径只能进统一 cleanup）。</li></ol><h2 id="_2-量化现状" tabindex="-1"><a class="header-anchor" href="#_2-量化现状"><span>2. 量化现状</span></a></h2><blockquote><p>下表是<strong>改造前</strong>的基线（2026-09-22）。P0 每完成一项在此记录变化，便于回看。</p></blockquote><table><thead><tr><th style="text-align:left;">类</th><th style="text-align:right;">行数</th><th style="text-align:left;">位置</th></tr></thead><tbody><tr><td style="text-align:left;"><code>AudioStreamSession</code></td><td style="text-align:right;"><strong>812</strong></td><td style="text-align:left;"><code>modules/realtime/session</code></td></tr><tr><td style="text-align:left;"><code>AudioStreamingService</code></td><td style="text-align:right;"><strong>625</strong></td><td style="text-align:left;"><code>modules/realtime/streaming</code></td></tr><tr><td style="text-align:left;"><code>SystemPromptBuilder</code></td><td style="text-align:right;">261</td><td style="text-align:left;">（对照：模块内第三大的业务类）</td></tr><tr><td style="text-align:left;"><code>LogicalSessionRebuilder</code></td><td style="text-align:right;">214</td><td style="text-align:left;"><code>modules/realtime/session</code></td></tr><tr><td style="text-align:left;"><code>InterviewSessionState</code></td><td style="text-align:right;">208</td><td style="text-align:left;"><code>modules/realtime/session</code></td></tr><tr><td style="text-align:left;"><code>InterviewPrepareService</code></td><td style="text-align:right;">183</td><td style="text-align:left;"><code>modules/realtime/interview</code></td></tr></tbody></table><p>前两个类合计 <strong>1437 行</strong>，且都是<strong>每次改动都要经过</strong>的核心路径（V1、V2、PC 正式面试共用）。</p><p><strong>进度</strong>：P0-1 之后 <code>AudioStreamSession</code> → <strong>676 行</strong>（-136），ASR 运行期独立为 <code>AsrStreamRunner</code>（289 行，含注释）； P0-2 之后 → <strong>550 行</strong>（-126），回合执行独立为 <code>ConversationTurnRunner</code>（206 行）。两个核心类合计 1437 → <strong>756 行</strong>； P0-4 为纯改名，行数不变；P0-3 之后 <code>AudioStreamingService</code> → <strong>560 行</strong>（注册表独立为 <code>InMemorySessionRegistry</code> 116 行）， <code>AudioStreamSession</code> 因实现 <code>SessionHandle</code> 增加 3 个适配方法 → 567 行； P1-1 之后 <code>AudioStreamingService</code> → <strong>646 行</strong>（<strong>净增</strong> 86 行：把 7 条退出路径的时序差异与取舍写进了 javadoc， 结构收益不在行数上）；P1-2 之后 <code>AudioStreamingService</code> → 650 行、<code>AudioStreamSession</code> 567 → <strong>668 行</strong> （同样是 javadoc 为主：把&quot;启动事务&quot;的四条不变量写在了代码里）。</p><h2 id="_3-职责清单-这两个类现在到底在干什么" tabindex="-1"><a class="header-anchor" href="#_3-职责清单-这两个类现在到底在干什么"><span>3. 职责清单：这两个类现在到底在干什么</span></a></h2><h3 id="_3-1-audiostreamsession-10-个角色" tabindex="-1"><a class="header-anchor" href="#_3-1-audiostreamsession-10-个角色"><span>3.1 <code>AudioStreamSession</code>（10 个角色）</span></a></h3><p>WebSocket 持有者（含发送队列 + <code>sendLoop</code>）｜ASR 运行器（<code>consumeLoop</code>、重连退避、<code>pumpAudio</code>、残留帧丢弃、<code>lastAsrId</code> 轮换、<code>asrUnavailable</code>）｜上行协议适配（binary / text / heartbeat / PAUSE）｜<strong>回合编排</strong>（<code>triggerLlmReply</code>、<code>triggerInitialQuestion</code>）｜对话上下文管理（<code>context</code>、<code>trimContext</code>、<code>buildMessages</code>、<code>MAX_CONTEXT</code>）｜落库协调（<code>conversationService</code>）｜领域事件发布（Started / Paused / Resumed / Ended）｜连接存活状态（两套时间戳 + <code>heartbeatCapable</code> + <code>isAlive</code>）｜暂停与结束语义（<code>paused</code>、<code>markPaused</code>、<code>onClose</code> 分支）｜ASR 消息 id 轮换与去重语义</p><h3 id="_3-2-audiostreamingservice-8-个角色" tabindex="-1"><a class="header-anchor" href="#_3-2-audiostreamingservice-8-个角色"><span>3.2 <code>AudioStreamingService</code>（8 个角色）</span></a></h3><p>握手准备与失败回滚｜两张注册表的所有者（<code>streamSessions</code> / <code>logicalSessions</code>）｜恢复与跨进程重建协调｜心跳扫描与踢出｜三种回收（暂停 / 悬挂 / pending）｜<strong>连接存活的唯一判据来源</strong>｜顶号与强制关闭｜主动 / 被动关闭的仲裁</p><blockquote><p>这不是&quot;作者没拆&quot;，而是<strong>新能力没有落脚点</strong>：加一个&quot;文字作答&quot;要动它、加一个&quot;识别通道&quot;也要动它。 上一轮讨论 V2 时&quot;专门写一套会不会更好&quot;的直觉，正是被这个类逼出来的（结论见 V2 方案 §10.2：新写一套更贵）。</p></blockquote><h2 id="_4-真正的病根" tabindex="-1"><a class="header-anchor" href="#_4-真正的病根"><span>4. 真正的病根</span></a></h2><h3 id="_4-1-隐式不变量只能靠注释维持" tabindex="-1"><a class="header-anchor" href="#_4-1-隐式不变量只能靠注释维持"><span>4.1 隐式不变量只能靠注释维持</span></a></h3><p>编译器管不了、只能靠人记住的规则：</p><table><thead><tr><th style="text-align:left;">#</th><th style="text-align:left;">不变量</th><th style="text-align:left;">出处</th><th style="text-align:left;">忘了会怎样</th></tr></thead><tbody><tr><td style="text-align:left;">1</td><td style="text-align:left;">构造器有副作用（<code>markActive</code> + <code>bindConnection</code> + 起 2 个线程 + 发事件），失败必须<strong>调用方手工回滚</strong></td><td style="text-align:left;"><code>AudioStreamingService#onOpen</code> 的 <code>catch (RuntimeException)</code> 里手工 <code>resumeState.markPaused()</code> / <code>endInterview()</code></td><td style="text-align:left;">留下&quot;无物理连接的 ACTIVE&quot;僵尸，两条回收路径都扫不到（历史上真实发生过，见前一篇 P0.1）</td></tr><tr><td style="text-align:left;">2</td><td style="text-align:left;">顶号时<strong>只请求关闭，不能抢先摘注册表</strong></td><td style="text-align:left;"><code>onOpen</code> 内注释</td><td style="text-align:left;">心跳扫描再也扫不到该连接，逻辑会话永久残留</td></tr><tr><td style="text-align:left;">3</td><td style="text-align:left;"><code>streamSessions.remove()</code> 的返回值同时承担「移除」与「幂等守卫」</td><td style="text-align:left;">前一篇 §4 原则一</td><td style="text-align:left;">&quot;移除&quot;与&quot;清理&quot;变成两件事，静默出错</td></tr><tr><td style="text-align:left;">4</td><td style="text-align:left;">ASR 重连前必须 <code>dropStaleAudio()</code> + 轮换 <code>lastAsrId</code></td><td style="text-align:left;"><code>consumeLoop</code></td><td style="text-align:left;">旧帧喂给新连接（过期转写）、新旧句子在前端串成一条</td></tr><tr><td style="text-align:left;">5</td><td style="text-align:left;">不要无脑 <code>forceClose()</code></td><td style="text-align:left;"><code>DoubaoAsrClient</code> 注释</td><td style="text-align:left;"><code>abort</code> + <code>shutdownNow</code> 以异常回调，日志出现误导性故障栈</td></tr><tr><td style="text-align:left;">6</td><td style="text-align:left;">计费起算必须由 <code>startedPublished</code> 守卫</td><td style="text-align:left;"><code>onAsrReady</code></td><td style="text-align:left;">重连重复 <code>startBilling</code>，<strong>覆盖起算点、已用时长归零</strong></td></tr><tr><td style="text-align:left;">7</td><td style="text-align:left;">ASR 故障必须走 <code>onAsrSessionError</code> 而非 <code>onAsrSessionEnd</code></td><td style="text-align:left;"><code>consumeLoop</code> finally + <code>asrUnavailable</code></td><td style="text-align:left;">结束原因误记为 <code>USER_ENDED</code>，统计与客诉都失真</td></tr></tbody></table><p>前一篇（心跳与暂停恢复）已经修过一批同类问题，但<strong>新写法仍在重复同一模式</strong>： 横向关注点默认寄生在业务回调里 —— 这次 V2 要拆的 ASR→LLM 耦合，就是它的第二次发作。</p><h3 id="_4-2-跨模块的隐式耦合-谁在依赖注册表" tabindex="-1"><a class="header-anchor" href="#_4-2-跨模块的隐式耦合-谁在依赖注册表"><span>4.2 跨模块的隐式耦合：谁在依赖注册表</span></a></h3><p>4 处「按物理连接定位会话」的调用点直接依赖 <code>AudioStreamingService</code> 的 <code>streamSessions</code>：</p><div class="language-java line-numbers-mode" data-highlighter="shiki" data-ext="java" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-java"><span class="line"><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">// InterviewBillingScheduler#validateSessionState（每 10 秒跑一次）</span></span>
<span class="line"><span style="--shiki-light:#C18401;--shiki-dark:#E5C07B;">String</span><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;"> activeConnectionId </span><span style="--shiki-light:#383A42;--shiki-dark:#56B6C2;">=</span><span style="--shiki-light:#E45649;--shiki-dark:#E5C07B;"> streamingService</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">resolveActiveConnectionId</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">(sessionId);</span></span>
<span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">if</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;"> (activeConnectionId </span><span style="--shiki-light:#383A42;--shiki-dark:#56B6C2;">==</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;"> null</span><span style="--shiki-light:#383A42;--shiki-dark:#56B6C2;"> ||</span><span style="--shiki-light:#383A42;--shiki-dark:#56B6C2;"> !</span><span style="--shiki-light:#E45649;--shiki-dark:#E5C07B;">streamingService</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">hasActiveStreamSession</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">(activeConnectionId)</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">) {</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E5C07B;">    billingService</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">stopBilling</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">(userId, sessionId);</span><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">   // ← 找不到就视为孤儿，停计费</span></span>
<span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">    return</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;"> false</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">;</span></span>
<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">}</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><table><thead><tr><th style="text-align:left;">调用点</th><th style="text-align:left;">依赖</th><th style="text-align:left;">漏改后果</th></tr></thead><tbody><tr><td style="text-align:left;"><code>InterviewBillingScheduler#validateSessionState</code></td><td style="text-align:left;"><code>isConnectionLost</code> / <code>resolveActiveConnectionId</code> / <code>hasActiveStreamSession</code></td><td style="text-align:left;">新链路的会话若不在注册表里 → <strong>静默停止扣费</strong>，<code>TIME_UPDATE</code> 停发</td></tr><tr><td style="text-align:left;"><code>InterviewBillingScheduler#terminateSession</code></td><td style="text-align:left;">同上 + <code>forceCloseSession</code></td><td style="text-align:left;">余额耗尽时关不掉会话</td></tr><tr><td style="text-align:left;"><code>WsHeartbeatScheduler</code> → <code>evictDeadSessions</code></td><td style="text-align:left;">只遍历 <code>streamSessions</code></td><td style="text-align:left;">死连接不回收</td></tr><tr><td style="text-align:left;"><code>AudioStreamingService#onOpen</code>（顶号）</td><td style="text-align:left;"><code>forceTerminateActiveSession</code> → <code>resolveActiveConnectionId</code> → <code>streamSessions.get</code></td><td style="text-align:left;">顶号失效，同用户两条会话并行计费</td></tr></tbody></table><p>这就是 V2「识别通道」方案里建议<strong>不碰注册表</strong>的原因，也是 §6 P0-3 要接口化的动机。</p><h3 id="_4-3-命名与语义漂移" tabindex="-1"><a class="header-anchor" href="#_4-3-命名与语义漂移"><span>4.3 命名与语义漂移</span></a></h3><table><thead><tr><th style="text-align:left;">漂移</th><th style="text-align:left;">具体表现</th><th style="text-align:left;">危害</th></tr></thead><tbody><tr><td style="text-align:left;">两个 <code>sessionId</code></td><td style="text-align:left;"><code>InterviewContext#sessionId</code> 是<strong>物理连接 ID</strong>，<code>InterviewSessionState#sessionId</code> 是<strong>逻辑会话 ID</strong>，后者靠 <code>getLogicalSessionId()</code> 别名纠偏</td><td style="text-align:left;"><code>InterviewAsyncService#runSafely</code> 的告警日志打的是 <code>context.getSessionId()</code>（物理 ID），排查时极易看错</td></tr><tr><td style="text-align:left;"><code>stopBilling</code> 参数名与注释不符</td><td style="text-align:left;">形参 <code>currentSessionId</code>，javadoc 写&quot;当前正在关闭的 <strong>WebSocket 会话 ID</strong>&quot;；但三处调用方全部传<strong>逻辑会话 ID</strong>，内部也是拿它比 <code>sessionInfo.getSessionId()</code>（<code>startBilling</code> 用逻辑 ID 写入）</td><td style="text-align:left;">按注释传物理连接 ID，会静默走&quot;跳过清理&quot;分支 → <strong>计费不结算、Redis key 残留</strong></td></tr><tr><td style="text-align:left;"><code>Audio*</code> 前缀</td><td style="text-align:left;"><code>AudioStreamSession</code> / <code>AudioWebSocketHandler</code> 现在还要承载文本（V2 之后更明显）</td><td style="text-align:left;">误导阅读者以为只处理音频</td></tr><tr><td style="text-align:left;">文档与代码相反</td><td style="text-align:left;"><code>AGENTS.md</code> §6.5.4 末尾称 <code>PausedMessage</code> 经 <code>sendQueue</code> 异步入队&quot;若需强一致可改为同步发送&quot;，代码早已是 <code>sendMessageNow</code>；§8.6 把&quot;补实时会话单测&quot;列为待做，实际已有约 62 个用例</td><td style="text-align:left;">下一个人按文档改代码</td></tr><tr><td style="text-align:left;">错误码两端未登记</td><td style="text-align:left;">后端下发 <code>ASR_UNAVAILABLE</code> + 关闭码 4003，PC 的 <code>protocol.ts</code> 与小程序 <code>ws.ts</code> 白名单里<strong>都没有这个码</strong></td><td style="text-align:left;">PC 端把消息整条丢弃（用户看不到原因），小程序兜底归为 <code>BILLING_ERROR</code></td></tr></tbody></table><h3 id="_4-4-已定原则与代码结构的落差" tabindex="-1"><a class="header-anchor" href="#_4-4-已定原则与代码结构的落差"><span>4.4 已定原则与代码结构的落差</span></a></h3><p>前一篇 §4 定了三条原则，其中<strong>原则一（任何物理连接退出路径最终都必须进入统一 cleanup）在结构上还没收敛</strong>：</p><blockquote><p><strong>状态</strong>：这条已在 <strong>P1-1</strong> 收敛（连接退出的「通知/关闭」与「收尾」各成一个入口，见 §6 P1-1 进度）。 下面保留问题描述作为改造动机；未进 <code>cleanup</code> 的两条逻辑终结路径（暂停超时 / DB 兜底）见 P1-1 说明。</p></blockquote><ul><li><strong>触发收尾的入口有 7 处</strong>：Spring <code>afterConnectionClosed</code>（间接承接 <code>handleAsrSessionEnd</code> / <code>handleAsrSessionError</code> / <code>handlePauseRequested</code> / <code>handleTransportError</code> / <code>forceCloseSession</code> 五个主动 <code>close()</code> 路径） + 心跳踢出 <code>evictDeadSessions</code> 里<strong>自己拼的</strong>「发 ERROR → <code>onError</code> → 虚拟线程 sleep 1500ms → <code>onClose</code>」。 后者的时序（发错误通知 + 宽限 1.5 秒）是心跳踢出独有的，与其他路径不一致。</li><li><strong>「发结束事件」没有单一入口</strong>：<code>AudioStreamSession#onClose</code> 发 <code>InterviewEndedEvent</code>； <code>evictExpiredPausedSessions</code> 自己 <code>logicalSessions.remove()</code> + 发事件； <code>evictExpiredPausedInterviews</code>（DB 兜底）<strong>刻意不发事件</strong>（有理由，但正是&quot;每个路径各自决定&quot;的体现）。</li><li><strong>登记与移除不对称</strong>：逻辑会话登记有 3 个分支（新会话/重建态/不登记），移除有 2 处，各有条件。</li></ul><p>结论：原则写在文档里，代码里仍是&quot;每条退出路径各自拼一遍收尾&quot;。新增输入源或新端点时，这里最容易漏。</p><h3 id="_4-5-测试覆盖的不对称-最需要正视的一点" tabindex="-1"><a class="header-anchor" href="#_4-5-测试覆盖的不对称-最需要正视的一点"><span>4.5 测试覆盖的不对称（最需要正视的一点）</span></a></h3><p>本模块测试其实是全仓最厚的：<code>AudioStreamingServiceTest</code>（32 个用例）、<code>LogicalSessionRebuilderTest</code>（14）、 <code>AudioStreamSessionHeartbeatTest</code>（6）、<code>AudioStreamingWebSocketIntegrationTest</code>（6）、<code>RealtimePropertiesTest</code>（4），合计约 62 个。</p><p>但盲区恰好落在<strong>最常改、最该动</strong>的地方：</p><table><thead><tr><th style="text-align:left;">块</th><th style="text-align:left;">覆盖</th></tr></thead><tbody><tr><td style="text-align:left;">注册表 / 恢复 / 心跳判据 / 暂停回收</td><td style="text-align:left;">✅ 覆盖较好</td></tr><tr><td style="text-align:left;"><strong>ASR 消费循环与重连预算</strong></td><td style="text-align:left;">❌ 无（依赖豆包客户端）</td></tr><tr><td style="text-align:left;"><strong>回合流水线（LLM 交互、上下文裁剪、落库顺序）</strong></td><td style="text-align:left;">❌ 无</td></tr><tr><td style="text-align:left;"><strong>计费调度器的三条判据</strong></td><td style="text-align:left;">❌ 无</td></tr></tbody></table><p>→ 含义：§6 的 P0-1 / P0-2 正好落进盲区，<strong>所以 P0-0（测试接缝与行为基线）是它们的前置条件</strong> —— 先给 <code>DoubaoAsrClient</code> 与 LLM 客户端做注入点 + fake，把两组不变量测试按<strong>现状</strong>跑绿，再开始抽取； 否则拆完只是&quot;看起来没坏&quot;。</p><h2 id="_5-出错的历史证据-都可在仓库内查到" tabindex="-1"><a class="header-anchor" href="#_5-出错的历史证据-都可在仓库内查到"><span>5. 出错的历史证据（都可在仓库内查到）</span></a></h2><table><thead><tr><th style="text-align:left;">症状</th><th style="text-align:left;">根因</th><th style="text-align:left;">现状</th></tr></thead><tbody><tr><td style="text-align:left;">计费的&quot;失联防线&quot;从未生效，半开连接可一直计费</td><td style="text-align:left;">拿本任务每轮自己刷新的 <code>lastHeartbeatTime</code> 当判据，恒不超时</td><td style="text-align:left;">✅ 已改为向会话层询问 <code>isConnectionLost</code>（前一篇 P2）</td></tr><tr><td style="text-align:left;">计费&quot;失联&quot;阈值 30 秒</td><td style="text-align:left;">把&quot;用户静默&quot;当&quot;失联&quot;，语义错误</td><td style="text-align:left;">✅ 已删除该阈值</td></tr><tr><td style="text-align:left;">心跳寄生在业务消息上（<code>receiveAudio</code> / <code>receiveText</code> 顺带刷新）</td><td style="text-align:left;">横向关注点寄生在业务回调</td><td style="text-align:left;">✅ 已拆成独立 heartbeat（前一篇 P2）；<strong>同类病根在 ASR→LLM 上又出现一次</strong>（V2 要拆）</td></tr><tr><td style="text-align:left;">重连失败留下「无物理连接的 ACTIVE」僵尸</td><td style="text-align:left;">构造器已 <code>markActive</code>/<code>bindConnection</code>，失败未回退</td><td style="text-align:left;">✅ 已修（前一篇 P0.1）+ 单测</td></tr><tr><td style="text-align:left;">结束原因混记（ASR 故障记成用户主动结束）</td><td style="text-align:left;">ASR 故障走自然收尾路径</td><td style="text-align:left;">✅ 已引入 <code>ASR_ERROR</code> + 关闭码 4003</td></tr><tr><td style="text-align:left;"><code>ASR_UNAVAILABLE</code> 两端认不出</td><td style="text-align:left;">新增错误码未同步前端协议文件</td><td style="text-align:left;">❌ <strong>仍未修</strong></td></tr><tr><td style="text-align:left;">文档与代码相反（<code>PausedMessage</code> 发送方式、单测现状）</td><td style="text-align:left;">文档没跟着代码走</td><td style="text-align:left;">❌ 仍未同步</td></tr></tbody></table><h2 id="_6-拆解清单" tabindex="-1"><a class="header-anchor" href="#_6-拆解清单"><span>6. 拆解清单</span></a></h2><p>原则：<strong>每项都是行为等价的机械重构，独立提交、独立可回滚、不夹带新功能。</strong></p><blockquote><p><strong>完成定义（每项都适用）：代码抽取完成 + 行为基线测试通过 + 原链路集成测试通过。</strong> 没有测试就不算完成 —— 机械抽取不等于低风险：<code>ASR 回调 → 会话状态 → 回合 → 计费 → WebSocket 推送</code> 之间存在<strong>隐式时序</strong>，只有测试能证明它没变。</p></blockquote><h3 id="p0-0-测试接缝与行为基线-先做-是-p0-1-p0-2-的前置条件" tabindex="-1"><a class="header-anchor" href="#p0-0-测试接缝与行为基线-先做-是-p0-1-p0-2-的前置条件"><span>P0-0 测试接缝与行为基线（先做，是 P0-1 / P0-2 的前置条件）</span></a></h3><blockquote><p><strong>进度：已落地（2026-09-22）。</strong> 注入点用两个工厂接口实现（<code>AsrClientFactory</code> / <code>LlmClientFactory</code>， 默认走豆包实现，放在 <code>AudioStreamSessionDependencies</code> 上），测试脚手架为 <code>AudioStreamSessionTestHarness</code>，两组基线共 7 个用例（<code>AudioStreamSessionAsrBaselineTest</code> 3 个、 <code>AudioStreamSessionTurnBaselineTest</code> 4 个）。</p><p><strong>验证结果：71 个测试全绿</strong> = realtime 单元测试 63（含新增 7）+ WebSocket 端到端集成 5 + 计费集成 3。 端到端集成测试需要本地存在 <code>pass_up_test</code> 库（<code>application-test.yml</code> 指向它；库不存在会以 「上下文加载失败」的形式报错，表现为 5 个 Error，与本类改动无关）： <code>docker exec postgres psql -U postgres -c &quot;CREATE DATABASE pass_up_test ENCODING &#39;UTF8&#39; TEMPLATE template0&quot;</code>。</p></blockquote><p>不是&quot;补覆盖率&quot;，而是<strong>先把现状钉住</strong>：</p><ol><li><p><strong>注入点</strong>：<code>DoubaoAsrClient</code>（工厂或接口）、LLM 客户端（现在每轮 <code>new DoubaoLlmClient</code>，加一个 <code>LlmClientFactory</code> 即可 fake）。</p></li><li><p><strong>不变量测试</strong>（两组，分别对应 P0-1 / P0-2 的抽取对象）：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>ASR     ready × reconnect → startBilling 只能一次</span></span>
<span class="line"><span>        reconnect         → 残留音频 = 0</span></span>
<span class="line"><span>        new ASR           → asrId != 旧 asrId</span></span>
<span class="line"><span></span></span>
<span class="line"><span>回合     final text → 恰好一轮 LLM</span></span>
<span class="line"><span>        非 final   → 零轮</span></span>
<span class="line"><span>        空 final   → 零轮</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></li><li><p>这两组测试<strong>先在现有实现上跑绿</strong>（即把现状钉住），再开始抽取。</p></li></ol><blockquote><p><strong>cleanup 的基线不在这一步</strong>：它对应 P1-1 的收敛对象，放在 P1-1 一起做（见该节「行为基线」）。 这样划分是为了让&quot;测试接缝&quot;只服务于它真正要保护的那两次抽取，不提前把 P1 的对象拉进来。</p></blockquote><h3 id="p0-1-抽-asrstreamrunner-asr-runtime" tabindex="-1"><a class="header-anchor" href="#p0-1-抽-asrstreamrunner-asr-runtime"><span>P0-1 抽 <code>AsrStreamRunner</code>：ASR Runtime</span></a></h3><blockquote><p><strong>进度：已落地（2026-09-22）。</strong> 产出 <code>AsrStreamRunner</code> + <code>AsrEvent</code>（sealed：<code>Ready</code> / <code>Transcript</code> / <code>Ended</code> / <code>Unavailable</code>）</p><ul><li><code>AsrEventHandler</code>；会话侧改为 <code>onAsrEvent(...)</code> 消费事件，<code>stop()</code> 由 <code>markPaused()</code> / <code>onClose()</code> 显式调用 （不再让运行期去读会话的 paused / WebSocket 状态）。 <strong>判据已核验</strong>：会话里不再出现重连 / 退避 / <code>asrId</code> / 残留帧（812 → 676 行）； 验收 = P0-0 的 7 个基线 + 64 个既有单元/集成测试，共 <strong>71 个全绿</strong>。</li></ul></blockquote><ul><li><p><strong>职责一句话</strong>：从「收到音频字节」到「产出 ASR 事件」，含连接重建。</p></li><li><p><strong>对外只有三个动作 + 一个事件入口</strong>：</p><div class="language-java line-numbers-mode" data-highlighter="shiki" data-ext="java" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-java"><span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">interface</span><span style="--shiki-light:#C18401;--shiki-dark:#E5C07B;"> AsrEventHandler</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;"> {</span><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;"> void</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;"> onEvent</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">(</span><span style="--shiki-light:#C18401;--shiki-dark:#E5C07B;">AsrEvent</span><span style="--shiki-light:#383A42;--shiki-light-font-style:inherit;--shiki-dark:#E06C75;--shiki-dark-font-style:italic;"> event</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">);</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;"> }</span></span>
<span class="line"><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">// AsrReady / AsrFinalText / AsrSessionEnded / AsrSessionError</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">class</span><span style="--shiki-light:#C18401;--shiki-dark:#E5C07B;"> AsrStreamRunner</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;"> {</span></span>
<span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">    void</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;"> start</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">();</span></span>
<span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">    void</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;"> submitAudio</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">(</span><span style="--shiki-light:#C18401;--shiki-dark:#C678DD;">byte</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">[] </span><span style="--shiki-light:#383A42;--shiki-light-font-style:inherit;--shiki-dark:#E06C75;--shiki-dark-font-style:italic;">chunk</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">);</span></span>
<span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">    void</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;"> stop</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">();</span></span>
<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">}</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><blockquote><p><strong>修订</strong>（原方案留了 <code>onReady</code> / <code>onText</code> / <code>onEnd</code> / <code>onError</code> 四个回调）：<strong>统一成一个事件入口</strong>。 四个回调在第二个使用点（<code>/ws/asr</code>）就会立刻分叉 —— 换 API 比换一个 handler 实现贵得多。</p></blockquote></li><li><p><strong>判据</strong>：抽取完成后，<code>AudioStreamSession</code> <strong>不应再出现</strong>重连、退避、<code>asrId</code>、残留帧这些概念。</p></li><li><p><strong>搬走</strong>：<code>consumeLoop</code>、<code>pumpAudio</code>、<code>AsrRun</code>、<code>reconnectBackoffMillis</code>、<code>dropStaleAudio</code>、<code>shouldStopRetrying</code>、<code>lastAsrId</code> 轮换、<code>asrUnavailable</code>。</p></li><li><p><strong>附带收益</strong>：V2 的 <code>/ws/asr</code> 识别通道复用同一个 runner（V2 方案 §4.2.1 的依赖项）。</p></li><li><p><strong>验收</strong>：P0-0 的 ASR 组 + 集成测试 + 手工弱网回归（断网 / 杀连接后自动恢复）。</p></li></ul><h3 id="p0-2-抽-conversationturnrunner-一个输入-一个回合" tabindex="-1"><a class="header-anchor" href="#p0-2-抽-conversationturnrunner-一个输入-一个回合"><span>P0-2 抽 <code>ConversationTurnRunner</code>：一个输入 = 一个回合</span></a></h3><blockquote><p><strong>进度：已落地（2026-09-22）。</strong> 产出 <code>ConversationTurnRunner</code>（<code>submit(UserTurnInput)</code> / <code>submitOpening()</code> / <code>cancel()</code> + <code>TurnOutbound</code> 输出口）；会话侧只剩「输入源适配 + 下发」，<code>context</code> / <code>MAX_CONTEXT</code> / 上下文裁剪 / LLM 客户端 / 持久化调用全部移出（<code>LogicalSessionRebuilder</code> 的窗口引用同步改到 <code>ConversationTurnRunner#MAX_CONTEXT</code>）。 <strong>判据已核验</strong>：会话里不再出现 <code>buildMessages</code> / <code>trimContext</code> / <code>MAX_CONTEXT</code> / <code>streamChat</code> / <code>saveUserMessage</code>（812 → 550 行）；71 个单元 / 集成测试全绿，<strong>并通过真实端到端冒烟 11/11</strong>。</p></blockquote><ul><li><p><strong>职责一句话</strong>：<strong>一个输入，执行一个完整回合。</strong></p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>UserTurnInput → 构建上下文 → 调 LLM → 输出 assistant token → 持久化 user/assistant → TurnResult</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div></li><li><p><strong>搬走</strong>：<code>submitUserTurn</code>（= 现 <code>triggerLlmReply</code> 与 <code>triggerInitialQuestion</code> 的共同部分）、<code>buildMessages</code>、<code>trimContext</code>、<code>MAX_CONTEXT</code>、回合串行化。</p></li><li><p><strong>留下</strong>：输入源适配（ASR final / 客户端文本）。</p></li><li><p><strong>明确不做</strong>（防止换皮大类，逐条写死）：不碰 WebSocket、ASR、billing、session lifecycle、pause、close、connection、retry。</p></li><li><p><strong>Context 是它使用的内部协作对象</strong>：<code>buildMessages</code> / <code>trimContext</code> / <code>MAX_CONTEXT</code> 属于&quot;如何构造 LLM 输入&quot;， 是回合执行用到的一个组件，不是与之并列的第二个大类。</p></li><li><p><strong>继续拆的条件（不提前拆）</strong>：只有出现<strong>第二个独立使用场景</strong>，或该职责形成<strong>明显独立的变化轴</strong>时， 才考虑从它里面再拆 <code>TurnContext</code> / <code>TurnPersistence</code> 之类的东西。<strong>不预先规定行数阈值</strong> —— 否则容易从「一个大类」滑向「Session / Runner / Context / Persistence 四个小类」的为拆而拆。</p></li><li><p><strong>附带收益</strong>：V2 新增 <code>ANSWER</code> 上行时只需加一个&quot;喂输入&quot;的分支（V2 方案 §4.1.2）。</p></li><li><p><strong>验收</strong>：P0-0 的回合组 + 上下文裁剪窗口不变 + 落库顺序（user → assistant）不变。</p></li></ul><h3 id="p0-3-连接存活判据接口化-只做查询-判据" tabindex="-1"><a class="header-anchor" href="#p0-3-连接存活判据接口化-只做查询-判据"><span>P0-3 连接存活判据接口化（只做查询 / 判据）</span></a></h3><blockquote><p><strong>进度：已落地（2026-09-22）。</strong> 实际形状与下面的草图有四处偏差，都有理由：</p><table><thead><tr><th style="text-align:left;">项</th><th style="text-align:left;">落地结论</th></tr></thead><tbody><tr><td style="text-align:left;">查询方法</td><td style="text-align:left;"><code>findActive(logicalSessionId) → Optional&lt;SessionHandle&gt;</code> + <code>contains(connectionId)</code> + <code>hasConnections()</code>。草图里的 <code>isAlive(connectionId)</code> <strong>没做</strong> —— 存活判据挂在 <code>SessionHandle</code> 上（<code>findActive(...).get().isAlive()</code>），再开一个按连接 ID 的入口只会多一条重复语义</td></tr><tr><td style="text-align:left;"><code>SessionHandle</code></td><td style="text-align:left;"><strong>做了</strong>，且只有 4 个方法：<code>connectionId()</code> / <code>isTransportOpen()</code> / <code>isAlive()</code> / <code>idleMillis()</code>；<code>AudioStreamSession implements SessionHandle</code>（3 个适配方法）</td></tr><tr><td style="text-align:left;">注册表存在哪</td><td style="text-align:left;">两张 map <strong>搬进</strong> <code>InMemorySessionRegistry</code>（不再是 <code>AudioStreamingService</code> 的私有字段），服务改为委托 —— 否则只是&quot;给容器开了个窗口&quot;，谈不上隔离</td></tr><tr><td style="text-align:left;">变更 API 归属</td><td style="text-align:left;"><code>registerConnection / removeConnection / activeConnection / registerLogicalSession / logicalSessionIds / …</code> <strong>只放在实现类上、不进接口</strong>；实时链路注入实现类，外围注入 <code>SessionRegistry</code> 接口</td></tr></tbody></table><p>顺带删除 <code>AudioStreamingService</code> 的 5 个查询方法：<code>resolveActiveConnectionId</code> / <code>isConnectionLost</code> / <code>connectionIdleMillis</code> / <code>hasActiveStreamSession</code> / <code>isEmpty</code>（外围全部改调注册表）。 <code>AudioStreamingService</code> 625 → <strong>560 行</strong>；新增 <code>InMemorySessionRegistry</code>（116 行）+ <code>SessionRegistry</code> / <code>SessionHandle</code> 两个接口。</p><p><strong>测试（本次重点，补的正是 §4.5 点名的盲区）</strong>：</p><ul><li>新增 <code>InMemorySessionRegistryTest</code>（10 用例）：逻辑会话不存在 / 已暂停 / 已结束 / 连接尚未登记 → <code>findActive</code> 一律为空； 重连后按新连接 ID 命中；<code>contains</code> / <code>hasConnections</code> / 快照语义。</li><li>新增 <code>InterviewBillingSchedulerTest</code>（5 用例）：三条判据逐条固定，含<strong>「找不到活跃会话 ⇒ 安全结算并停计费」</strong>这条兜底。 为此把 <code>validateSessionState</code> 从 <code>private</code> 放宽为包级可见（同包直调，比为了可测性再造接口便宜）。</li><li><code>AudioStreamingServiceTest</code>：4 个 <code>isConnectionLost</code> 用例迁走；反射写 map 的 helper 改为直接调注册表 API（测试不再依赖私有字段名）。</li><li>合计 <strong>82 个</strong>用例全绿（71 → 82），真实端到端冒烟 <strong>11/11</strong>。</li></ul><p><strong>过程中踩到的两个坑（值得记一笔）</strong>：</p><ol><li><strong>又一次「grep 被截断 + 只看增量编译」</strong>：<code>handlePauseRequested</code> 里还剩一处 <code>streamSessions.get(...)</code>， 我的 grep 输出被截断而漏改 —— 这次是 <code>clean package</code> 直接报「找不到符号」才抓到。与 P0-4 的教训同源： <strong>每步都要 clean 全量构建</strong>，别信增量编译的&quot;成功&quot;。</li><li><strong>测试里拿 mock 当 handle</strong>：<code>findActive</code> 返回的 <code>AudioStreamSession</code> 是 mock，<code>handle.connectionId()</code> 没打桩就返回 <code>null</code>， 于是服务里&quot;解析物理连接 ID&quot;这一步静默变 <code>null</code>（生产代码没问题，是测试装配失真）。 顺手把服务内部的取用改为注册表的 <code>activeConnection(logicalSessionId)</code>（直接给会话对象）： 既省掉一次重复查找，也不再让链路内部绕窄视图。</li></ol></blockquote><blockquote><p><strong>修订</strong>：原方案写成「解析物理连接 / 判存活 / 查活跃 / <strong>强关</strong>」，把&quot;注册表抽象&quot;和&quot;会话控制服务&quot;混在了一起， 会让 <code>SessionRegistry</code> 长成新的上帝接口。<strong>本次只抽查询与判据：</strong></p></blockquote><div class="language-java line-numbers-mode" data-highlighter="shiki" data-ext="java" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-java"><span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">interface</span><span style="--shiki-light:#C18401;--shiki-dark:#E5C07B;"> SessionRegistry</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;"> {</span></span>
<span class="line"><span style="--shiki-light:#C18401;--shiki-dark:#E5C07B;">    Optional</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">&lt;</span><span style="--shiki-light:#C18401;--shiki-dark:#E5C07B;">SessionHandle</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">&gt;</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;"> findActive</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">(</span><span style="--shiki-light:#C18401;--shiki-dark:#E5C07B;">String</span><span style="--shiki-light:#383A42;--shiki-light-font-style:inherit;--shiki-dark:#E06C75;--shiki-dark-font-style:italic;"> logicalSessionId</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">);</span></span>
<span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">    boolean</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;"> isAlive</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">(</span><span style="--shiki-light:#C18401;--shiki-dark:#E5C07B;">String</span><span style="--shiki-light:#383A42;--shiki-light-font-style:inherit;--shiki-dark:#E06C75;--shiki-dark-font-style:italic;"> connectionId</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">);</span></span>
<span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">    boolean</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;"> contains</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">(</span><span style="--shiki-light:#C18401;--shiki-dark:#E5C07B;">String</span><span style="--shiki-light:#383A42;--shiki-light-font-style:inherit;--shiki-dark:#E06C75;--shiki-dark-font-style:italic;"> connectionId</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">);</span></span>
<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">}</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><ul><li><code>forceClose</code> / <code>closeWithNotice</code> <strong>暂留 <code>AudioStreamingService</code></strong>，等 P1-1 收敛后再定归属（现在先不猜）。</li><li>命名纪律：不叫 <code>SessionController</code> / <code>SessionLifecycleManager</code> —— 这类名字会自己长大。</li><li><strong>现状</strong>：§4.2 的 4 处调用点直接依赖 <code>AudioStreamingService</code> 的 <code>streamSessions</code>。</li><li><strong>收益</strong>：隔离注册表实现，避免计费 / 心跳等外围模块直接依赖实时连接容器；新链路不必碰注册表， 也不会误伤计费判据。未来若连接状态的管理方式变化，影响面集中在注册表实现内部。 <blockquote><p>注意：抽接口<strong>不等于</strong>解决跨实例问题 —— 把 <code>ConcurrentHashMap</code> 换成 <code>SessionRegistry</code> 并不会让 实例 B 看到实例 A 的连接。多实例部署是<strong>另一个议题</strong>，不要把它算作本次抽取的收益。</p></blockquote></li><li><strong>验收</strong>：<code>InterviewBillingScheduler</code> 三条判据行为不变 + 「找不到会话 ⇒ 停计费」这条兜底有单测。</li></ul><h3 id="p0-4-id-语义区分-从-p1-提前到-p0" tabindex="-1"><a class="header-anchor" href="#p0-4-id-语义区分-从-p1-提前到-p0"><span>P0-4 ID 语义区分（从 P1 提前到 P0）</span></a></h3><blockquote><p><strong>进度：已落地（2026-09-22）。</strong> 改名范围：<code>InterviewContext#sessionId → connectionId</code>（删掉手工别名 getter， 由 Lombok 生成）、<code>InterviewSessionState#sessionId → logicalSessionId</code>（同样删别名）、 <code>InterviewBillingService#startBilling / stopBilling</code> 形参 → <code>logicalSessionId</code>（javadoc 一并纠正）、 <code>AudioStreamingService</code> 中按物理连接索引的 5 个方法形参 → <code>connectionId</code>、 <code>InterviewAsyncService</code> 的日志字段 → <code>connectionId</code>。</p><p><strong>判据已核验</strong>：实时链路的<strong>字段与形参</strong>中已无裸 <code>sessionId</code>。<strong>刻意排除</strong>（各有理由，不是遗漏）： ① <code>InterviewSessionInfo.sessionId</code>（Redis JSON 的 key，改名会让部署中的在飞会话读取失败）； ② <code>Interview.sessionId</code> + <code>InterviewRepository.findBySessionId...</code>（实体字段绑 DB 列 <code>session_id</code> 与派生查询）； ③ <code>PausedMessage.sessionId</code>（下行协议字段，两端前端都在读）； ④ 日志标签 <code>sessionId={}</code>（保留旧标签以免破坏既有排查 grep；其参数已全部是连接 ID）。 <code>InterviewBillingScheduler</code> 内部的局部变量名留到 P0-3 顺手改（那里本来就要动同一批调用点）。</p><p><strong>本次最有价值的一条教训</strong>：<code>InterviewConversationEventHandler</code> 里还有一处 <code>.sessionId(...)</code>， 因我第一次 grep 结果被截断而漏改；更隐蔽的是 <strong>Maven 增量编译跳过了这个文件</strong>， 于是 <code>package</code> 成功、71 个测试全绿，<strong>却把一个「Unresolved compilation problem」的坏 class 打进了 jar</strong> （VS Code 的 JDT 增量编译写进 <code>target/classes</code> 的产物）。真实链路一连 <code>resume</code> 就以 1011 断开，才把它暴露出来。 → <strong>每一步的验收都要 <code>clean package</code> + 真实冒烟，不能只看 BUILD SUCCESS</strong>； 同理，grep 结果被截断时要缩小范围重搜，别据此判断&quot;改完了&quot;。</p></blockquote><ul><li>问题性质是<strong>类型语义</strong>（primitive obsession），不是结构问题，<strong>越早改越便宜</strong>。</li><li>最小成本版（<strong>不引入 value object</strong>）—— 字段 / 形参统一为 <code>connectionId</code> 与 <code>logicalSessionId</code>，禁止裸用 <code>sessionId</code>： <ul><li><code>InterviewContext#sessionId</code> → <code>connectionId</code></li><li><code>InterviewSessionState#sessionId</code> → <code>logicalSessionId</code></li><li><code>InterviewBillingService#stopBilling(userId, currentSessionId)</code>：形参名与 javadoc 改为逻辑会话 ID （现在注释写的是&quot;WebSocket 会话 ID&quot;，照它传会静默跳过结算）</li></ul></li><li>为什么不做 value object：本项目的实用主义取向；<code>String connectionId</code> 已能消除绝大多数误用。</li><li><strong>验收</strong>：<strong>实时链路边界内</strong>禁止出现无法区分物理连接与逻辑会话的裸 <code>sessionId</code>； 所有跨类调用明确使用 <code>connectionId</code> 或 <code>logicalSessionId</code>；<code>stopBilling</code> 的调用方与注释一致。 （不要求&quot;全局搜不到 <code>sessionId</code>&quot; —— 其他业务对象自带的 <code>sessionId</code> 语义本就明确，不需要跟着改。）</li></ul><h3 id="p1-1-关闭路径收敛-cleanup-reason-的边界" tabindex="-1"><a class="header-anchor" href="#p1-1-关闭路径收敛-cleanup-reason-的边界"><span>P1-1 关闭路径收敛：<code>cleanup(reason)</code> 的边界</span></a></h3><blockquote><p><strong>进度：已落地（2026-09-22）。</strong> 落法不是&quot;抽一个新类&quot;，而是<strong>把收尾抽成两个函数</strong>：</p><table><thead><tr><th style="text-align:left;">入口</th><th style="text-align:left;">职责</th><th style="text-align:left;">调用方</th></tr></thead><tbody><tr><td style="text-align:left;"><code>cleanup(session, reason)</code></td><td style="text-align:left;"><strong>唯一收尾入口（幂等）</strong>：摘除物理连接（幂等的锚点）→ <code>AudioStreamSession#onClose()</code> → 已结束则摘除逻辑会话</td><td style="text-align:left;">Spring 关闭回调；心跳踢出 / 主动关闭后的兜底</td></tr><tr><td style="text-align:left;"><code>requestClose(session, reason, status, notice)</code></td><td style="text-align:left;"><strong>唯一退出出口</strong>：按需同步发通知 → 关闭传输层。<strong>不做收尾</strong>（收尾统一走回调）</td><td style="text-align:left;">7 条路径全部改调它</td></tr></tbody></table><p>7 条路径改写后各自只剩一行：顶号 / 传输层错误 / 计费终止 / 心跳踢出 / ASR 自然结束 / ASR 不可用 / 用户暂停。 新退出路径只需给出 <code>CleanupReason</code> + 关闭码 +（可选）通知。新增 <code>CleanupReason</code>（<code>lifecycle</code> 包）， <strong>与 <code>InterviewEndReason</code> 刻意分开</strong>：连接退出 ≠ 面试结束（暂停、顶号、心跳踢出都只退出连接）。</p><p><strong>幂等的锚点是「注册表摘除成功者唯一」</strong>：<code>cleanup</code> 里 <code>removeConnection</code> 返回 <code>null</code> 即直接返回， 于是 <code>session.onClose()</code> 至多执行一次 → <code>InterviewEndedEvent</code> 至多一条 → 停计费至多一次（§10 第 8 条）。 心跳踢出的「兜底补一刀」因此天然安全，不必再自己防重。</p><p><strong>刻意保持现状项（累计，都已写进代码注释）</strong>：</p><ol><li>心跳踢出的宽限期仍是「先关连接、等 1.5 秒再兜底收尾」—— 于是&quot;给前端留时间把残存数据发完&quot;这个初衷 其实没落到实处（连接那时已经关了）。改成「先等宽限、再关闭」是行为变更，需单独决策。</li><li>通知的发送方式按路径区分：ASR 不可用走<strong>同步写</strong>（保证&quot;先收到原因、再收到关闭帧&quot;，与握手错误一致）； 心跳踢出走<strong>会话发送队列</strong>（扫描线程与发送线程并发写同一 socket 会交错）。</li><li><s><strong>结束原因的映射口径</strong>（P1-2）：现在只有 ASR 故障记 <code>ASR_ERROR</code>，心跳超时 / 计费终止 / 顶号都记 <code>USER_ENDED</code>。</s> ✅ <strong>已收口（2026-09-23）</strong>：新增 <code>DISCONNECTED</code>（心跳超时·顶号·传输错误），并把 <code>BILLING_TERMINATED</code> <strong>拆成</strong> <code>BILLING_EXHAUSTED</code> / <code>BILLING_ERROR</code> （后者映射到此前空置的 <code>SYSTEM_ERROR</code>）—— 拆而非改名，因为 <code>InterviewBillingScheduler#terminateSession</code> 本来就有&quot;余额耗尽&quot;与&quot;计费异常&quot;两个调用点。 映射表见《模拟面试第二版》§8 P0-2，单测 <code>AudioStreamSessionEndReasonTest</code> 钉住。 <strong>历史数据不回溯</strong>，报表按发布日分界。</li></ol><p><strong>测试（§10 第 8 条基线落地）</strong>：</p><ul><li><code>cleanup</code>：重复调用只收尾一次；逻辑会话未结束 → <strong>保留</strong>（暂停可重连）、已结束 → 摘除；未知连接静默跳过。</li><li>心跳踢出 3 个用例：失联 → 通知（走队列）+ 请求关闭 + 宽限后兜底收尾（用 Mockito <code>timeout(3000)</code> 等异步结果）； 存活不踢；已标记踢出不重复踢。<strong>这条路径冒烟脚本覆盖不到</strong>（脚本一直发心跳、永远不会被判失联），只能靠单测锁住。</li><li>合计 <strong>89 个</strong>用例全绿（82 → 89），真实端到端冒烟仍 <strong>11/11</strong>（本次主代码改动后未复跑的必要性见 §10）。</li></ul><p><strong>本阶段只收敛「物理连接退出」</strong>：<code>evictExpiredPausedSessions</code>（暂停超时 → 终结逻辑会话）与 <code>evictExpiredPausedInterviews</code>（DB 兜底）<strong>刻意不进 <code>cleanup</code></strong> —— 它们没有物理连接要收， 属于&quot;面试为什么结束&quot;的判定（各有其既有理由，见 §4.4）。 这正是上面那句&quot;定义收窄&quot;的含义：<code>cleanup</code> 不被拉成万能方法。</p><p>定义收窄为：<strong>物理连接退出的唯一收尾入口</strong>，而<strong>不是</strong>&quot;所有会话结束都要调它&quot;。</p></blockquote><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>7 个物理连接退出入口</span></span>
<span class="line"><span>        ↓</span></span>
<span class="line"><span>cleanup(connection, reason)     ← 连接资源收敛 + 注册表移除（幂等）</span></span>
<span class="line"><span>        ↓</span></span>
<span class="line"><span>   逻辑会话是否结束？</span></span>
<span class="line"><span>     ├─ 否 → pause（保留 TTL 待重连）</span></span>
<span class="line"><span>     └─ 是 → ended（发 Ended 事件）</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><ul><li><p><code>cleanup</code> <strong>不做</strong>&quot;面试是否结束&quot;的判断 —— 那是逻辑生命周期（pause / resume / end）的事。 两者一旦混在一起，<code>cleanup</code> 就会变成万能方法，把&quot;连接关闭&quot;和&quot;面试结束&quot;再次搅在一起。</p></li><li><p>心跳踢出的&quot;发错误 + 宽限期&quot;变成 cleanup 的参数（如 <code>gracefulNotice(code, message, graceMillis)</code>）， 而不是每条退出路径各写一遍 —— 这正是 §4.4 里 7 个入口时序不一致的根因。</p></li><li><p><strong>验收</strong>：前一篇原则一在结构上成立；新增一条退出路径不再复制收尾代码。</p></li><li><p><strong>行为基线（本阶段落地为测试）</strong>：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>cleanup() × N    → remove 只一次</span></span>
<span class="line"><span>                 → Ended 事件只一次</span></span>
<span class="line"><span>                 → 停计费只一次</span></span>
<span class="line"><span></span></span>
<span class="line"><span>非当前连接的事件  → 不影响活跃连接与逻辑会话（见 P1-3 的&quot;迟到事件&quot;场景）</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><blockquote><p>这组基线归 P1-1 而不归 P0-0：P0-0 只保护 P0-1 / P0-2 那两次抽取， 而 cleanup 是本阶段才收敛的对象，测试与重构同批交付更清晰。</p></blockquote></li></ul><h3 id="p1-2-生命周期不变量结构化" tabindex="-1"><a class="header-anchor" href="#p1-2-生命周期不变量结构化"><span>P1-2 生命周期不变量结构化</span></a></h3><blockquote><p><strong>进度：已落地（2026-09-22）。</strong> 四项逐条：</p><table><thead><tr><th style="text-align:left;">项</th><th style="text-align:left;">落地</th></tr></thead><tbody><tr><td style="text-align:left;">① 构造器无副作用</td><td style="text-align:left;"><code>new AudioStreamSession(...)</code> 只赋字段 + 建两个 runner；不再 <code>markActive</code> / <code>bindConnection</code> / <code>submit(sendLoop)</code> / <code>asrRunner.start()</code> / <code>publishResumed()</code></td></tr><tr><td style="text-align:left;">② <code>start()</code> 是启动事务边界</td><td style="text-align:left;"><code>start()</code>：重连 → <code>markActive</code> + <code>bindConnection</code> → <code>submit(sendLoop)</code> → <code>asrRunner.start()</code> → <code>publishResumed()</code>；任一步失败 → <code>rollbackStart()</code> → 继续抛</td></tr><tr><td style="text-align:left;">③ 退出原因统一</td><td style="text-align:left;">删掉 <code>asrUnavailable</code> 布尔，改为会话上的 <code>exitReason</code>（<code>CleanupReason</code>）：<code>requestClose</code> 时记录、收尾时由 <code>onClose(reason)</code> 消费</td></tr><tr><td style="text-align:left;">④ 两类原因不混用</td><td style="text-align:left;"><code>CleanupReason</code>（连接为什么退出）经 <code>toEndReason(reason)</code> <strong>唯一一次</strong>映射为 <code>InterviewEndReason</code>（面试为什么结束）</td></tr></tbody></table><p><strong>③ 的关键点</strong>：退出原因必须在<strong>请求关闭时</strong>记到会话上。因为收尾走的是容器关闭回调， 回调只带&quot;连接断了&quot;（<code>CONNECTION_CLOSED</code>），真实原因（ASR 故障 / 暂停 / 顶号 / 计费终止）会丢 —— 原先那个 <code>asrUnavailable</code> 布尔正是为了跨过这次回调。现在换成通用的 <code>exitReason</code>，<strong>任何</strong>退出路径都能带上原因。</p><p><strong>③ 与 ④ 的口径冲突（已按 ④ 处理）</strong>：任务描述里&quot;<code>InterviewEndReason</code> 扩展为 <code>CleanupReason</code>&quot;与 &quot;两类原因不混用&quot;互相矛盾。落地为<strong>两个枚举都保留</strong>：<code>CleanupReason</code> 是入参（连接维度）， <code>InterviewEndReason</code> 是出参（面试维度），中间只在一处映射。 映射现状<strong>刻意不变</strong>：只有 ASR 故障记 <code>ASR_ERROR</code>，心跳超时 / 计费终止 / 顶号仍记 <code>USER_ENDED</code> （改它会动到已落库的统计口径）—— 见待确认项。</p><p><strong>回滚责任转移</strong>：<code>rollbackStart()</code> 就地把逻辑会话恢复成&quot;未启动&quot;的样子（重连 → PAUSED 并解绑连接； 新会话 → 标记 ENDED），因为不这么做就会留下「无物理连接的 ACTIVE」僵尸（心跳扫描扫不到、暂停回收又不认它）。 <code>AudioStreamingService</code> 因此不再手工 <code>resumeState.markPaused()</code>，只保留新会话失败时的 <code>endInterview</code>（DB 不归会话管）。 另外<strong>「面试开始」事件仍在 ASR 就绪时发</strong>（<code>handleAsrReady</code>），本次没动 —— 计费起算点必须与&quot;识别真的可用&quot;对齐。</p><p><strong>测试</strong>：</p><ul><li>新增 <code>AudioStreamSessionLifecycleTest</code>（4 个用例，真实会话 + fake ASR）：构造不启动 ASR / 不发事件； <code>start()</code> 启动 ASR；重连成功迁 ACTIVE + 绑定新连接 + 恢复事件只发一次； <strong>重连中途失败就地回滚为 PAUSED 且解绑连接</strong>。</li><li><code>AudioStreamingServiceTest</code>：原「重连失败退回 PAUSED」用例改为断言服务侧剩余责任（不登记连接、不结束 DB 记录）； 新增「新会话启动失败 → 结束 DB 记录」。</li><li>合计 <strong>94 个</strong>用例全绿（89 → 94），真实端到端冒烟 <strong>11/11</strong>。</li></ul><p><strong>顺带修掉一个长期假绿的集成测试用例（值得记一笔）</strong>：<code>AudioStreamingWebSocketIntegrationTest</code> 的 SessionFactory 桩挂在<strong>三参</strong>重载上，而服务调用的是<strong>四参</strong>重载 → 服务拿到 <code>null</code> 会话 → <code>onOpen</code> 里 NPE → 连接被 1011 关掉。于是「相同用户重复连接时后续连接被拒绝」这条用例把 &quot;任何关闭&quot;当成了&quot;被拒绝&quot;而假绿，那 5 个用例其实都没走到建连/收尾逻辑。 修正桩之后（四参 + 真实 <code>getState()</code> + 空壳也走收尾），改为断言本配置下的真实行为。</p><p>四个点属于同一件事（&quot;生命周期状态只能由一处写，且写的时候是原子的&quot;），因此<strong>是一个任务，不拆成四个</strong>：</p></blockquote><ol><li><strong>构造器无副作用</strong>：<code>new AudioStreamSession(...)</code> 只创建对象 —— 不 <code>markActive</code>、不绑连接、不起线程、不发事件。</li><li><strong><code>start()</code> 是启动事务边界</strong>：markActive → bindConnection → start runner → publish started， 任一步失败就地回滚（不再让 <code>AudioStreamingService</code> 手工补偿 —— 那正是 §4.1 第 1 条不变量的来源）。</li><li><strong>退出原因统一</strong>：<code>InterviewEndReason</code> 扩展为 <code>CleanupReason</code>，替代布尔 + 多处 if 判断。</li><li><strong>两类原因的边界写清</strong>：<code>CleanupReason</code>（连接为什么退出：用户断开 / 心跳超时 / ASR 故障 / 计费终止 / 暂停） 与 <code>InterviewEndReason</code>（面试为什么结束：用户结束 / 暂停超时 / 余额耗尽）<strong>不混用</strong> —— 连接退出 ≠ 面试结束（见 P1-1 的时序图）。</li></ol><p>（ID 区分已提前到 P0-4。）</p><h3 id="p1-3-组合场景测试-长尾" tabindex="-1"><a class="header-anchor" href="#p1-3-组合场景测试-长尾"><span>P1-3 组合场景测试（长尾）</span></a></h3><blockquote><p><strong>进度：已落地（2026-09-22），并且这一步抓到了真 bug。</strong></p><p>场景 2（旧连接迟到事件）<strong>不是&quot;补个测试就好&quot;</strong> —— 写出来立刻暴露出： <code>AudioStreamSession#onClose</code> 对<strong>共享的逻辑会话</strong>是无条件写状态的，于是 「A 掉线（无 PAUSE）→ 客户端带 resume 重连成 B → A 的收尾才到达」这条时序下， A 的收尾会把 B 正在进行的面试<strong>结束掉</strong>：状态标记 <code>ENDED</code> → 发 <code>InterviewEndedEvent</code> （→ 停计费 + 结束面试记录），上层随后还把逻辑会话摘出注册表 —— 计费扫描从此找不到它，表现为<strong>&quot;面试还在跑、时长却不扣了&quot;</strong>。这是既有 bug，与本次重构无关。</p><p><strong>防护（已落地）</strong>：<code>onClose(reason)</code> 先 <code>releaseResources()</code>（只收敛本连接资源），然后两道早退：</p><ol><li>逻辑会话已 <code>ENDED</code> → 直接返回（不重复发结束事件，§10.8 的第二道防线）；</li><li><code>state.getCurrentConnectionId()</code> 非空且 ≠ 本连接 → <strong>迟到收尾</strong>，只清自己的资源，绝不碰逻辑会话。 （暂停态 <code>currentConnectionId</code> 为 <code>null</code>，因此正常暂停收尾不会被误判成迟到 ✓ 冒烟已实测。）</li></ol><p><strong>测试</strong>（新增 <code>AudioStreamSessionLongTailTest</code>，真实会话 + fake ASR）：</p><ul><li><code>重复收尾_Ended事件只发一次</code>：端点重复回调 / 兜底补刀也不能重复发事件（否则重复停计费、重复结束记录）；</li><li><code>迟到收尾_已被新连接接替_不影响逻辑会话</code>：不得结束面试、不得改暂停、不得解绑新连接、不得发结束事件；</li><li><code>暂停收尾_保留逻辑会话并发暂停事件</code>：暂停的正例（反向保证第 2 条不会误伤暂停路径）。</li></ul><p>合计 <strong>97 个</strong>用例全绿（94 → 97），真实端到端冒烟 <strong>11/11</strong>。</p><p><strong>尚未覆盖（留作后续）</strong>：</p><ul><li>场景 1 的&quot;真并发&quot;（暂停中重连 + ASR 重连 + 计费扫描 + 心跳踢出同刻发生）目前只覆盖了<strong>确定性交错</strong> （迟到收尾 / 重复收尾），没有多线程随机交错压测 —— 随机交错容易产出偶发红灯，价值不如确定性用例； 如需补，建议用 <code>AudioStreamSessionTestHarness#blockAsrClientClose</code> 构造确定性窗口，而不是加 sleep。</li><li>场景 3（异步落库丢失窗口）未做：<code>InterviewAsyncService</code> 的 <code>@PreDestroy</code> 有 30 秒排空等待， 可以补一条&quot;shutdown 会等待在途落库任务&quot;的用例，但那是&quot;进程优雅退出&quot;的语义，与本次长尾关系较远。</li></ul><p>P0-0 / P1-1 锁的是单点不变量，这里补组合：</p></blockquote><ol><li><p>暂停中重连 + ASR 重连 + 计费扫描 + 心跳踢出并发。</p></li><li><p><strong>旧连接迟到事件不得影响新连接</strong>（前一篇 §4 原则三的强化版，比&quot;旧 <code>onClose</code> 晚于新连接建立&quot;更具体）：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>旧连接 A 断开 → 新连接 B 建立并成为 active → A 的 onClose / ASR error / cleanup 才到达</span></span>
<span class="line"><span>     ↓</span></span>
<span class="line"><span>允许：只清理 A 自身（A 的资源、A 在注册表中的条目）</span></span>
<span class="line"><span>禁止：移除 B / 结束逻辑会话 / 停掉 B 的计费 / 清掉 B 的注册表条目</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>这是最容易&quot;看起来修好了、实际偶发踩中&quot;的一类竞态，必须用测试钉住。</p></li><li><p>异步落库丢失窗口（前一篇 §7 假设 5）。</p></li></ol><h3 id="p2-1-命名与文档对齐" tabindex="-1"><a class="header-anchor" href="#p2-1-命名与文档对齐"><span>P2-1 命名与文档对齐</span></a></h3><p><code>Audio*</code> → <code>Interview*</code>（<strong>路径 <code>/ws/audio</code> 不动</strong>，两个前端都写死了）；<code>AGENTS.md</code> §3.6 / §6.5.4 / §8.6 与代码同步； 两端错误码白名单补齐（<code>protocol.ts</code>、<code>ws.ts</code>）。</p><h3 id="归属前一篇、尚未落地的提案" tabindex="-1"><a class="header-anchor" href="#归属前一篇、尚未落地的提案"><span>归属前一篇、尚未落地的提案</span></a></h3>`,73),o(`p`,null,[i(g,{to:`/%E9%A1%B9%E7%9B%AE/%E9%9D%A2%E8%AF%95%E5%8A%A9%E6%89%8B/%E6%9E%B6%E6%9E%84%E8%AE%BE%E8%AE%A1/%E5%AE%9E%E6%97%B6%E9%9D%A2%E8%AF%95%E4%BC%9A%E8%AF%9D%E9%87%8D%E6%9E%84-%E9%A2%86%E5%9F%9F%E4%BA%8B%E4%BB%B6%E8%A7%A3%E8%80%A6.html`},{default:n(()=>[...l[10]||=[a(`领域事件解耦`,-1)]]),_:1}),l[11]||=a(` §8 的 `,-1),l[12]||=o(`code`,null,`BillingStateProjection`,-1),l[13]||=a(`（状态机成为写计费的唯一入口）： 代码里`,-1),l[14]||=o(`strong`,null,`没有这个类`,-1),l[15]||=a(`，属"提案未落地"。是否做、何时做由计费侧决定；若做，与本文 P1-2 有交集 （都指向"状态迁移成为唯一写入口"）。`,-1)]),l[19]||=r(`<h2 id="_7-与-v2-的关系-顺序" tabindex="-1"><a class="header-anchor" href="#_7-与-v2-的关系-顺序"><span>7. 与 V2 的关系（顺序）</span></a></h2><p>按 ①~⑨ 串行实施（⑤~⑧ 可与 V2 并行）：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>① P0-0  测试接缝 + 行为基线（ASR fake / LLM fake）</span></span>
<span class="line"><span>② P0-1  AsrStreamRunner</span></span>
<span class="line"><span>③ P0-2  ConversationTurnRunner</span></span>
<span class="line"><span>④ P0-4  connectionId / logicalSessionId</span></span>
<span class="line"><span>        ★ V2 门槛到此满足（+ 原链路集成测试通过）→ 可以开始 V2，不必等 ⑤~⑧</span></span>
<span class="line"><span>⑤ P0-3  SessionRegistry（只做查询 / 判据）</span></span>
<span class="line"><span>⑥ P1-1  物理连接 cleanup 收敛</span></span>
<span class="line"><span>⑦ P1-2  生命周期不变量结构化</span></span>
<span class="line"><span>⑧ P1-3  长尾组合场景</span></span>
<span class="line"><span>⑨ V2    input=text → /ws/asr（可与 ⑤~⑧ 并行）</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><ul><li><p>V2 要加的两个入口正好是这次抽象的<strong>验收用例</strong>：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>ASR final ──┐</span></span>
<span class="line"><span>            ├─→ ConversationTurnRunner</span></span>
<span class="line"><span>TEXT answer ┘</span></span>
<span class="line"><span></span></span>
<span class="line"><span>/ws/audio ──┐</span></span>
<span class="line"><span>            ├─→ AsrStreamRunner</span></span>
<span class="line"><span>/ws/asr   ──┘</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>如果这两个入口接不进去，说明抽象不对 —— 这也是&quot;先拆后加&quot;的真正价值。</p></li><li><p><strong>不需要等所有 P1 才做 V2</strong>：门槛是 —— P0-0 基线绿 <strong>+</strong> P0-1 / P0-2 抽取完成且原链路集成测试通过 <strong>+</strong> P0-4 合并。 达到这个门槛，V2 就已不会继续污染 <code>AudioStreamSession</code>。</p><ul><li>P0-3 尚未合并<strong>不阻塞 V2 步骤一</strong>（文本作答不碰注册表），但 V2 步骤二（新增 <code>/ws/asr</code>）之前建议完成。</li><li>P1-1 / P1-2 / P1-3 可与 V2 并行或排在其后。</li></ul></li><li><p>反过来先加分支的后果：<code>if (audio) { … } else if (text) { … }</code> 会继续落在最脆的地方， V2 之后这个类会到 900 行以上且无人敢动。</p></li></ul><h2 id="_8-拆解完成的结构性验收-不看行数" tabindex="-1"><a class="header-anchor" href="#_8-拆解完成的结构性验收-不看行数"><span>8. 拆解完成的结构性验收（不看行数）</span></a></h2><h3 id="audiostreamsession-的目标形态" tabindex="-1"><a class="header-anchor" href="#audiostreamsession-的目标形态"><span><code>AudioStreamSession</code> 的目标形态</span></a></h3><ul><li>只应出现：WebSocket 输入输出、<strong>连接生命周期协调</strong>、<code>InterviewSessionState</code> 引用、少量编排。 <blockquote><p>措辞用「协调」而不是「连接生命周期」：P1-1 之后真正的 close / cleanup / 注册表移除应收敛到统一入口， 会话本身只负责&quot;触发并协调&quot;，不负责自己再实现一遍收尾。</p></blockquote></li><li><strong>不应出现</strong>：<code>DoubaoAsrClient</code>、重连 / 退避 / 残留帧、LLM 客户端、上下文裁剪、计费调度、清理细节。</li></ul><h3 id="audiostreamingservice-的目标形态" tabindex="-1"><a class="header-anchor" href="#audiostreamingservice-的目标形态"><span><code>AudioStreamingService</code> 的目标形态</span></a></h3><ul><li>只应出现：连接建立、连接注册、恢复协调、生命周期协调。</li><li><strong>不应出现</strong>：ASR runtime、回合执行、心跳判据细节。</li></ul><h3 id="拆解的目标是「变化边界」-不是「代码变短」" tabindex="-1"><a class="header-anchor" href="#拆解的目标是「变化边界」-不是「代码变短」"><span>拆解的目标是「变化边界」，不是「代码变短」</span></a></h3><p>这份方案的核心不是把 812 行拆成几个小类，而是把实时链路后续演进的<strong>变化方向固定下来</strong>：</p><blockquote><p><strong>拆解不是为了减少代码行数，而是为了建立稳定的变化边界：</strong></p><table><thead><tr><th style="text-align:left;">变化</th><th style="text-align:left;">约束</th></tr></thead><tbody><tr><td style="text-align:left;">输入源变化（新增一种&quot;用户输入&quot;）</td><td style="text-align:left;">不修改 <code>AsrStreamRunner</code></td></tr><tr><td style="text-align:left;">识别通道变化（<code>/ws/audio</code> ↔ <code>/ws/asr</code>）</td><td style="text-align:left;">不复制 <code>AsrStreamRunner</code></td></tr><tr><td style="text-align:left;">回合输入变化（ASR final / TEXT answer / 首问 / 未来的&quot;重新生成&quot;）</td><td style="text-align:left;">不复制 <code>ConversationTurnRunner</code></td></tr><tr><td style="text-align:left;">连接退出路径变化（新增一种断开原因）</td><td style="text-align:left;">只能进入统一 <code>cleanup</code>，不再新增收尾入口</td></tr></tbody></table></blockquote><p>落到日常开发，就是一句可操作的对照：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>新输入      → 找输入适配点</span></span>
<span class="line"><span>新识别通道  → 复用 ASR Runner</span></span>
<span class="line"><span>新回合输入  → 复用 Turn Runner</span></span>
<span class="line"><span>新退出路径  → 进入统一 cleanup</span></span>
<span class="line"><span>新连接状态  → 不直接碰外围注册表</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>行数只是副产品（预计 <code>AudioStreamSession</code> 回落到 300~400 行），<strong>不是目标本身</strong>。</p><h2 id="_9-明确不做的事" tabindex="-1"><a class="header-anchor" href="#_9-明确不做的事"><span>9. 明确不做的事</span></a></h2><ul><li>不重写实时链路、不引入 Actor / 响应式框架：现有约 62 个用例是资产，重写等于清零。</li><li><strong>不把实时热路径事件化</strong>：沿用前一篇 §3 的边界 —— 实时流处理（音频 / ASR token / LLM token）直接调用， 只有业务生命周期（Started / Paused / Resumed / Ended）才走领域事件。<strong>本次不扩大领域事件的范围。</strong></li><li>不为 V2 新写一套会话实现（代价见 V2 方案 §10.2）。</li><li>不做一次性大重构：每项独立提交、独立回滚，P0 与 P1 之间不留长分支。</li><li>不在这些类上继续叠&quot;顺手写的&quot;新能力：新能力先找落脚点（runner / handler），找不到就先设计。</li><li><strong>不再新增 Runner / Manager / Context / Projection 这类抽象</strong>：本清单写明的抽取对象就是全部， 实施过程中不得临时&quot;顺手再抽一个&quot; —— 这是本次重构最大的风险（换文件名 ≠ 降复杂度）。</li></ul><blockquote><p><strong>本次实施要守住的一句话：</strong><strong>先把已经存在的变化边界拆出来，而不是提前为未来所有可能的变化设计架构。</strong> 与项目整体&quot;实用主义 DDD、不过度设计&quot;的方向一致。</p></blockquote><h2 id="_10-拆解完成的行为基线" tabindex="-1"><a class="header-anchor" href="#_10-拆解完成的行为基线"><span>10. 拆解完成的行为基线</span></a></h2><blockquote><p><strong>真实验证入口</strong>：<code>pass-up.backend/tools/realtime-session-smoke.mjs</code>（零依赖 Node 脚本）—— 登录 → 取 WS 凭证 → <code>/ws/audio</code> 建连(MOCK) → 心跳 + 静音音频上行 → 面试官首问 → 跨 70 秒验证「音频不是心跳」的严格判据 → PAUSE → resume 重连（校验不重复首问）→ 结束，共 11 条断言。 P0-2 落地后实测 <strong>11/11 通过</strong>；P0-4 后复测同样 <strong>11/11 通过</strong>（含 <code>resume</code> 重连与计费恢复）。 P0-3 后复测仍 <strong>11/11 通过</strong> —— 注册表抽取对上面这些行为是<strong>等价</strong>的。 P1-1 后复测仍 <strong>11/11 通过</strong>；第 8 条（清理幂等）由 7 个新单测锁住， 其中「心跳踢出」这条冒烟够不到的路径（脚本一直发心跳）也一并覆盖。 P1-2 后复测仍 <strong>11/11 通过</strong>（启动路径改成&quot;构造 + <code>start()</code> 事务&quot;后，首问 / 恢复 / 计费起算全在）。 DB 侧 <code>interview</code> 记录为 ended、首问消息已落库。</p><p>⚠️ <strong>跑冒烟前先看测试账号余额</strong>：脚本每轮会真实消耗 <code>admin</code>(user 1) 约 85 秒「面试时长」， 连跑几轮就会扣到 0，表现为跑到一半收到 <code>BALANCE_EXHAUSTED</code> 并以 1008 关闭 —— <strong>那不是回归</strong>。 充值（dev 库在 docker 里）：</p><div class="language-bash line-numbers-mode" data-highlighter="shiki" data-ext="bash" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-bash"><span class="line"><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">docker</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> exec</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;"> -e</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> PGPASSWORD=</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;">123456</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;"> -i</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> postgres</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> psql</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;"> -U</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> postgres</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;"> -d</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> pass_up_dev</span><span style="--shiki-light:#0184BC;--shiki-dark:#56B6C2;"> \\</span></span>
<span class="line"><span style="--shiki-light:#986801;--shiki-dark:#D19A66;">  -c</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> &quot;UPDATE user_asset SET balance = 3600, updated_at = now()</span></span>
<span class="line"><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">      WHERE user_id = 1 AND asset_type = &#39;INTERVIEW_SECONDS&#39;;&quot;</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></blockquote><p>这些基线会分批落成测试（不留在文档里）：第 1~3 条随 <strong>P0-0</strong>，第 4~7 条由既有测试与集成测试覆盖，第 8 条随 <strong>P1-1</strong>。</p><ol><li>ASR final → <strong>一次</strong> LLM 回合；非 final 与空文本不触发。</li><li>MOCK 首问只发一次；计费起算只发一次（重连、暂停恢复都不重复）。</li><li>ASR 中断自动重建：退避 500ms → 1s → 2s、最多 3 次、单连接存活 ≥30s 重置预算；重连前丢弃残留音频并轮换 asrId。</li><li>暂停 → 保留逻辑会话 15 分钟；恢复时复用 <code>systemPrompt</code> / 上下文 / <code>interviewId</code>，不重复开场。</li><li>心跳判据：发过心跳的客户端 60 秒，老客户端音频 5 分钟；计费与心跳共用同一判据来源。</li><li>余额耗尽 → 下发 <code>BALANCE_EXHAUSTED</code> + 停计费 + 关连接；顶号 → 旧连接以 <code>POLICY_VIOLATION</code> 关闭。</li><li>端到端：PC 正式面试（会议音频）与小程序模拟面试各跑通一次完整流程（含暂停 / 继续）。</li><li>清理幂等：同一次会话的 remove / Ended 事件 / 停计费各只发生一次。</li></ol><blockquote><p>工作量参考：P0 四项以「移动 + 新接口」为主、净增代码少，但<strong>风险不在代码量</strong>（隐式时序）， 主要成本在 P0-0 的测试接缝与 P1-1 的幂等验证。<strong>没有测试就不算完成。</strong></p></blockquote>`,23)])}var m=c(u,[[`render`,p]]);export{l as _pageData,m as default};