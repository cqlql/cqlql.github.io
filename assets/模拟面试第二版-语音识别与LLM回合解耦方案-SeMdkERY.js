import{A as e,D as t,L as n,f as r,m as i,p as a,s as o,u as s}from"./runtime-core.esm-bundler-BHJ3o1LK.js";import{t as c}from"./plugin-vue_export-helper-BDNMzG2s.js";var l=JSON.parse(`{"path":"/%E9%A1%B9%E7%9B%AE/%E9%9D%A2%E8%AF%95%E5%8A%A9%E6%89%8B/%E5%90%8E%E7%AB%AF%E5%AE%9E%E7%8E%B0/%E6%A8%A1%E6%8B%9F%E9%9D%A2%E8%AF%95%E7%AC%AC%E4%BA%8C%E7%89%88-%E8%AF%AD%E9%9F%B3%E8%AF%86%E5%88%AB%E4%B8%8ELLM%E5%9B%9E%E5%90%88%E8%A7%A3%E8%80%A6%E6%96%B9%E6%A1%88.html","title":"模拟面试第二版-语音识别与LLM回合解耦方案","lang":"zh-CN","frontmatter":{"title":"模拟面试第二版-语音识别与LLM回合解耦方案","icon":"mdi:source-branch","sort":2,"description":"模拟面试第二版：语音识别与 LLM 回合解耦方案 文档定位：后端方案（Step 1 / Step 2 / Step 3 均已交付；前置的实时会话重构 P0-0…P1-3 已完成）。 即：本文档依赖的&quot;接缝&quot;（ASR 运行独立、回合执行独立、连接退出收敛、启动事务边界、注册表查询化） 都已落地，并且 §4.1 的「文字作答」、§4.2...","head":[["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"模拟面试第二版-语音识别与LLM回合解耦方案\\",\\"image\\":[\\"\\"],\\"dateModified\\":\\"2026-09-22T10:11:43.000Z\\",\\"author\\":[{\\"@type\\":\\"Person\\",\\"name\\":\\"桥黎\\",\\"url\\":\\"\\"}]}"],["meta",{"property":"og:url","content":"https://docs.cqlql.top/%E9%A1%B9%E7%9B%AE/%E9%9D%A2%E8%AF%95%E5%8A%A9%E6%89%8B/%E5%90%8E%E7%AB%AF%E5%AE%9E%E7%8E%B0/%E6%A8%A1%E6%8B%9F%E9%9D%A2%E8%AF%95%E7%AC%AC%E4%BA%8C%E7%89%88-%E8%AF%AD%E9%9F%B3%E8%AF%86%E5%88%AB%E4%B8%8ELLM%E5%9B%9E%E5%90%88%E8%A7%A3%E8%80%A6%E6%96%B9%E6%A1%88.html"}],["meta",{"property":"og:site_name","content":"开发笔记"}],["meta",{"property":"og:title","content":"模拟面试第二版-语音识别与LLM回合解耦方案"}],["meta",{"property":"og:description","content":"模拟面试第二版：语音识别与 LLM 回合解耦方案 文档定位：后端方案（Step 1 / Step 2 / Step 3 均已交付；前置的实时会话重构 P0-0…P1-3 已完成）。 即：本文档依赖的&quot;接缝&quot;（ASR 运行独立、回合执行独立、连接退出收敛、启动事务边界、注册表查询化） 都已落地，并且 §4.1 的「文字作答」、§4.2..."}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:locale","content":"zh-CN"}],["meta",{"property":"og:updated_time","content":"2026-09-22T10:11:43.000Z"}],["meta",{"property":"article:modified_time","content":"2026-09-22T10:11:43.000Z"}]]},"git":{"createdTime":1790071903000,"updatedTime":1790071903000,"contributors":[{"name":"cql","username":"cql","email":"cql.ql@qq.com","commits":1,"url":"https://github.com/cql"}]},"readingTime":{"minutes":66.36,"words":19907},"filePathRelative":"项目/面试助手/后端实现/模拟面试第二版-语音识别与LLM回合解耦方案.md","autoDesc":true}`),u={name:`模拟面试第二版-语音识别与LLM回合解耦方案.md`};function d(c,l,u,d,f,p){let m=e(`RouteLink`);return t(),s(`div`,null,[l[12]||=r(`<h1 id="模拟面试第二版-语音识别与-llm-回合解耦方案" tabindex="-1"><a class="header-anchor" href="#模拟面试第二版-语音识别与-llm-回合解耦方案"><span>模拟面试第二版：语音识别与 LLM 回合解耦方案</span></a></h1><blockquote><p><strong>文档定位：后端方案（Step 1 / Step 2 / Step 3 均已交付；前置的实时会话重构 P0-0…P1-3 已完成）。</strong> 即：本文档依赖的&quot;接缝&quot;（ASR 运行独立、回合执行独立、连接退出收敛、启动事务边界、注册表查询化） 都已落地，并且 <strong>§4.1 的「文字作答」、§4.2 的 <code>/ws/asr</code>、以及 Step 3 的治理（配额 / 指标 / 埋点口径 / 日志脱敏） 都已经在代码里跑通</strong>（含单测 + 全量回归，2026-09-22）；识别运行本体复用 <code>AsrStreamRunner</code>（P0-1），未重写消费循环。 落地记录见 §12.3 / §12.4 / §12.5 —— <strong>改这三块代码前先看那三节</strong>； 联调记录见 <strong>§12.6</strong>：一个真缺陷（INFO 日志泄漏作答原文，已修）与一个已撤销的误判 （把 <code>final</code> 字段判成 <code>isFinal</code>，误以为「回答结束」信号不存在）。 回答一个问题：现在「ASR 出文本」与「触发 LLM 回答」是同一根链条上的两段，V2 要 「打字作答 + 话筒当输入法」两种新玩法，这条链条该怎么拆。</p><p>前置文档：小程序方案《模拟面试第二版-文字作答与实时语音输入方案》（第 5、6 节）。 另一个消费者是 PC 端 <code>pass-up.frontend/apps/user</code> 的 <code>features/realtime-voice</code>（正式面试，无独立笔记）， 本次不动它，但方案必须保证不打扰它 —— 见 §2.3。</p></blockquote><h2 id="_1-结论先行" tabindex="-1"><a class="header-anchor" href="#_1-结论先行"><span>1. 结论先行</span></a></h2><p><strong>核心结论：</strong></p><ol><li><strong>需要拆，但不是把 ASR 和 LLM 拆成两个服务，而是把「输入源」和「回合执行」拆开。</strong> 现在只有一个输入源（ASR final），它硬编码了「出文本 ⇒ 立刻跑一轮 LLM」。V2 要多一个输入源（客户端文本），且 ASR 要有一种「只出文本、不跑回合」的用法。</li><li><strong>面试会话继续复用 <code>/ws/audio</code></strong>（计时扣费、心跳、暂停恢复、顶号、记录落库、LLM 流式都在那儿），只新增一条文本上行消息；<strong>不另起 HTTP 文本接口</strong> —— 否则等于把 <code>InterviewSessionState</code> + 计费 + 逻辑会话恢复那套在 HTTP 上重建一遍。</li><li><strong>语音识别独立成一条轻量通道 <code>/ws/asr</code></strong>，只做识别：不落库、不计费、不触发 LLM、不进逻辑会话。理由是它的失败语义与生命周期跟面试会话完全不同 —— 话筒识别失败不该把整场面试以 <code>ASR_UNAVAILABLE(4003)</code> 关掉（现状就会）。</li></ol><p><strong>以及一条硬边界：正式面试（PC 端，<code>mode=ASSIST</code>）本次一行不改，仍然是「识别后立即 LLM」。</strong> 它采集的是<strong>会议音频</strong>（<code>getDisplayMedia</code> 共享会议窗口的声音），AI 扮演求职者给出回答思路 —— 「听清问题就立刻回答」正是它的全部价值，既不需要文字作答，也不会用话筒当输入法。 所有新增能力都由 <code>input=text</code> 参数与独立识别通道承载：PC 端不传新参数、不连新通道， 行为与今天一致（见 §2.3 与 §5）。</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>① 文字作答   /ws/audio（面试会话）  上行 {&quot;type&quot;:&quot;ANSWER&quot;,&quot;text&quot;:&quot;...&quot;} → LLM 回合 → 流式下行（协议不变）</span></span>
<span class="line"><span>② 语音输入   /ws/asr  （新，纯识别）上行 PCM 帧 → 下行 {&quot;type&quot;:&quot;ASR_RESULT&quot;,...} → 前端填输入框</span></span>
<span class="line"><span>③ 会话本身   心跳 / 计时扣费 / 暂停恢复 / 落库 / 顶号 —— 全部沿用现状，零改动</span></span>
<span class="line"><span>④ 正式面试   /ws/audio（PC，mode=ASSIST，不传 input）→ ASR final 即触发 LLM —— 一行不改</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>当前进度（2026-09-22，以仓库为准）</strong>：</p><table><thead><tr><th style="text-align:left;">形态</th><th style="text-align:left;">状态</th></tr></thead><tbody><tr><td style="text-align:left;">① 文字作答（<code>input=text</code> + V26 + <code>ANSWER</code> + 回合串行化/<code>SubmitResult</code> + 就绪点迁移）</td><td style="text-align:left;">✅ <strong>已交付</strong>（Step 1；全量测试 182 通过；两端协议文件已登记）</td></tr><tr><td style="text-align:left;">② 语音识别独立通道 <code>/ws/asr</code></td><td style="text-align:left;">✅ <strong>已交付</strong>（Step 2：路由 + 轻量鉴权 + 时长/并发上限 + 错误语义；识别运行本体复用 <code>AsrStreamRunner</code>）</td></tr><tr><td style="text-align:left;">③ 会话本身（心跳 / 计时扣费 / 暂停恢复 / 落库 / 顶号）</td><td style="text-align:left;">✅ 未改动，行为等价（回归见 §5.2）</td></tr><tr><td style="text-align:left;">④ 正式面试（PC，<code>mode=ASSIST</code>，不传 <code>input</code>）</td><td style="text-align:left;">✅ 一行不改，<code>audio</code> 路径按「等价改写」验收</td></tr></tbody></table><p><strong>Step 3（治理）也已交付</strong>：每日识别配额（限流）、识别通道指标、埋点口径（<code>input_mode</code>）、 日志脱敏（只记长度不记内容）—— 见 §4.2.4 与 §12.5。后端侧 V2 到此<strong>功能与治理都齐了</strong>。</p><h2 id="_2-现状-耦合点与它带来的三个连带约束" tabindex="-1"><a class="header-anchor" href="#_2-现状-耦合点与它带来的三个连带约束"><span>2. 现状：耦合点与它带来的三个连带约束</span></a></h2><h3 id="_2-1-耦合点-代码事实" tabindex="-1"><a class="header-anchor" href="#_2-1-耦合点-代码事实"><span>2.1 耦合点（代码事实）</span></a></h3><blockquote><p><strong>本节的&quot;代码事实&quot;已按 P0-1 / P0-2 / P1-1 / P1-2 落地后的代码复核更新（2026-09-22）。</strong> 变化对本文档的影响是<strong>小了好几个量级</strong> —— 原先要&quot;啃&quot;的两处结构问题，现在都已经是现成的接缝：</p><table><thead><tr><th style="text-align:left;">现在的样子</th><th style="text-align:left;">对 V2 的意义</th></tr></thead><tbody><tr><td style="text-align:left;">ASR 消费/重连已独立为 <code>modules/realtime/session/AsrStreamRunner.java</code>（P0-1，含退避、残留帧丢弃、<code>asrId</code> 轮换）</td><td style="text-align:left;"><code>/ws/asr</code> <strong>直接复用它</strong>，不要再抽第二个（§4.2 的&quot;抽公共组件&quot;已完成）</td></tr><tr><td style="text-align:left;">回合执行已独立为 <code>ConversationTurnRunner</code>（P0-2）：<code>submit(UserTurnInput)</code> / <code>submitOpening()</code> / <code>cancel()</code></td><td style="text-align:left;">步骤一不用再&quot;抽出 <code>submitUserTurn</code>&quot;，只需在文本分支调 <code>turnRunner.submit(...)</code>（§4.1.3 已改写）</td></tr><tr><td style="text-align:left;">连接退出已收敛为 <code>requestClose(...)</code> + <code>cleanup(reason)</code> 两个入口，退出原因用 <code>CleanupReason</code>（P1-1/P1-2）</td><td style="text-align:left;">约束 3（ASR 故障不该杀面试）现在<strong>只需保证识别通道不接这条入口</strong>；<code>/ws/audio</code> 内 <code>ASR_UNAVAILABLE</code> 语义未变</td></tr><tr><td style="text-align:left;">会话启动收敛为 <code>start()</code> 事务边界，构造器无副作用；迟到收尾有防护（P1-2/P1-3）</td><td style="text-align:left;">text 模式的起算/开场应挂在「<strong>会话就绪</strong>」= <code>start()</code> 成功完成之后（§4.1.4），不是 <code>onOpen</code></td></tr></tbody></table><p>也就是说：<strong>V2 现在主要是在既有接缝上加分支与新通道，而不是先重构再改造。</strong></p></blockquote><p>当初的耦合点（保留了代码位置的历史痕迹，便于对照）：</p><div class="language-java line-numbers-mode" data-highlighter="shiki" data-ext="java" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-java"><span class="line"><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">// 原 onAsrText（ASR 每几百毫秒一帧回调）—— 这段现在属于 AsrStreamRunner 的事件产出 + AudioStreamSession 的事件分支</span></span>
<span class="line"><span style="--shiki-light:#C18401;--shiki-dark:#C678DD;">var</span><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;"> asrResponse </span><span style="--shiki-light:#383A42;--shiki-dark:#56B6C2;">=</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;"> sendTranscriptMessage</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">(segment)</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">;</span><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">   // ① 下行 ASR_RESULT</span></span>
<span class="line"><span style="--shiki-light:#C18401;--shiki-dark:#C678DD;">var</span><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;"> replyParam </span><span style="--shiki-light:#383A42;--shiki-dark:#56B6C2;">=</span><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;"> new</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;"> LlmRequestContext</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">()</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">;</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E5C07B;">replyParam</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">setReplyId</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">(</span><span style="--shiki-light:#E45649;--shiki-dark:#E5C07B;">asrResponse</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">getId</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">());</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E5C07B;">replyParam</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">setText</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">(</span><span style="--shiki-light:#E45649;--shiki-dark:#E5C07B;">asrResponse</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">getText</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">());</span></span>
<span class="line"><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">triggerLlmReply</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">(replyParam)</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">;</span><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">                        // ② final 且非空 ⇒ 立刻跑 LLM 回合</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>triggerLlmReply</code> 里的三件事全绑在一条线上：<code>handleUserMessage</code>（写上下文 + 落库）→ <code>sendThinkingMessage</code> → <code>DoubaoLlmClient.streamChat</code>。</p><p>另一侧，上行文本的入口只有控制指令，没有任何业务文本：</p><div class="language-java line-numbers-mode" data-highlighter="shiki" data-ext="java" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-java"><span class="line"><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">// AudioStreamSession#receiveText：只认 heartbeat 与 PAUSE，其余一律落到 &quot;非控制指令&quot; 的 debug 日志</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><h3 id="_2-2-三个连带约束" tabindex="-1"><a class="header-anchor" href="#_2-2-三个连带约束"><span>2.2 三个连带约束</span></a></h3><table><thead><tr><th style="text-align:left;">#</th><th style="text-align:left;">约束</th><th style="text-align:left;">后果</th></tr></thead><tbody><tr><td style="text-align:left;">1</td><td style="text-align:left;">有音频就有 LLM 回合（final 即触发）</td><td style="text-align:left;">话筒没法当「输入法」用：说一句 AI 就抢答一轮，V2 要求识别结果只填输入框</td></tr><tr><td style="text-align:left;">2</td><td style="text-align:left;">没有文本作答入口</td><td style="text-align:left;">V2 的核心玩法（打字）后端根本没有路径，必须新增</td></tr><tr><td style="text-align:left;">3</td><td style="text-align:left;">ASR 故障 = 会话故障</td><td style="text-align:left;">重连耗尽 → <code>AsrEvent.Unavailable</code> → <code>closeListener.onAsrSessionError</code> → <code>requestClose(ASR_UNAVAILABLE)</code> → 下发 <code>ASR_UNAVAILABLE</code> + 关闭码 <code>4003</code> 整场结束（<code>asrUnavailable</code> 布尔已在 P1-2 换成 <code>CleanupReason</code>，语义未变）。对「输入辅助」来说这是灾难：用户只是想用语音打个字，面试被终止了</td></tr></tbody></table><p>约束 3 是「识别必须离开面试会话」的决定性理由 —— 不是洁癖，而是失败语义与生命周期确实不同：</p><table><thead><tr><th style="text-align:left;">维度</th><th style="text-align:left;">面试会话（<code>/ws/audio</code>）</th><th style="text-align:left;">输入法式识别</th></tr></thead><tbody><tr><td style="text-align:left;">生存期</td><td style="text-align:left;">整场面试（分钟级），可暂停/恢复</td><td style="text-align:left;">一次说话（秒级），说完即弃</td></tr><tr><td style="text-align:left;">失败后果</td><td style="text-align:left;">会话结束、停计费、落库收尾</td><td style="text-align:left;">提示「改用打字」，其余不受影响</td></tr><tr><td style="text-align:left;">计费</td><td style="text-align:left;">按会话时长扣 <code>INTERVIEW_SECONDS</code></td><td style="text-align:left;">不扣面试时长</td></tr><tr><td style="text-align:left;">是否需要落库</td><td style="text-align:left;">是（<code>interview_message</code>）</td><td style="text-align:left;">否（识别原文不落库，隐私上更干净）</td></tr><tr><td style="text-align:left;">是否需要跨重连存活</td><td style="text-align:left;">是（<code>InterviewSessionState</code>）</td><td style="text-align:left;">否</td></tr></tbody></table><h3 id="_2-3-另一个消费者-pc-端「正式面试助手」-本次不动-但要保证不被打扰" tabindex="-1"><a class="header-anchor" href="#_2-3-另一个消费者-pc-端「正式面试助手」-本次不动-但要保证不被打扰"><span>2.3 另一个消费者：PC 端「正式面试助手」（本次不动，但要保证不被打扰）</span></a></h3><p><code>pass-up.frontend/apps/user</code>（Web 端）的 <code>features/realtime-voice</code> 与小程序<strong>共用同一条 <code>/ws/audio</code></strong>， 改动必须按「一个后端同时服务两个客户端」来评估：</p><table><thead><tr><th style="text-align:left;">维度</th><th style="text-align:left;">现状</th><th style="text-align:left;">说明</th></tr></thead><tbody><tr><td style="text-align:left;">建连</td><td style="text-align:left;"><code>useVoiceWebSocket#connect</code> 拼 <code>/ws/audio?ticket=...</code>（暂停重连才追加 <code>&amp;resume=</code>）</td><td style="text-align:left;"><strong>不传 <code>mode</code></strong> ⇒ 后端 <code>InterviewMode.from(null)</code> 回退 <code>ASSIST</code>，即正式面试</td></tr><tr><td style="text-align:left;">音频来源</td><td style="text-align:left;"><code>useDisplayAudio</code> 的 <code>getDisplayMedia</code>（共享会议窗口 / 标签页声音）</td><td style="text-align:left;"><strong>不是麦克风</strong>；<code>usePCMProcessor</code> 转 16k PCM 后上行</td></tr><tr><td style="text-align:left;">上行</td><td style="text-align:left;">二进制 PCM + <code>{&quot;type&quot;:&quot;PAUSE&quot;}</code> + <code>{&quot;type&quot;:&quot;heartbeat&quot;}</code>（<code>CLIENT_CONTROL</code>）</td><td style="text-align:left;">没有任何业务文本上行</td></tr><tr><td style="text-align:left;">下行消费</td><td style="text-align:left;"><code>hooks/protocol.ts#parseServerMessage</code>：<strong>逐字段运行时校验</strong>，未知 type / 字段畸形 → 返回 null → <code>console.warn</code> 后忽略</td><td style="text-align:left;">新增下行消息类型对老客户端是安全的（不会崩，但也不会被理解）</td></tr><tr><td style="text-align:left;">会话起点</td><td style="text-align:left;">首次收到<strong>任意</strong>后端消息即视为 started</td><td style="text-align:left;">依赖「ASR 就绪后立刻有下行」，与 <code>onAsrReady</code> 触发首问/起算相配</td></tr><tr><td style="text-align:left;">展示逻辑</td><td style="text-align:left;"><code>ASR_RESULT</code> 建/更新转写气泡；<code>LLM_REPLY.replyId</code> 回指 ASR id，把回答插在该转写之后</td><td style="text-align:left;">这条「转写 ↔ 回答」配对是 PC 的核心体验</td></tr></tbody></table><p><strong>结论：PC 端不在本次改造范围内。</strong> 方案里所有行为变化都收在 <code>input=text</code> 与 <code>/ws/asr</code> 两个新开关后面： PC 端不传新参数、不连新通道、不发 <code>ANSWER</code>，因此不受影响；但改动仍必须做回归（见 §5.2）。</p><p>顺带记一处<strong>既有协议漂移</strong>（本次不修，但别重蹈）：后端在 ASR 重连耗尽时会下发 <code>ErrorMessage(&quot;ASR_UNAVAILABLE&quot;)</code> + 关闭码 <code>4003</code>，而两端的错误码白名单里<strong>都没有这个码</strong>：</p><ul><li>PC：<code>protocol.ts</code> 的 <code>SERVER_ERROR_CODES</code> 未包含 → <code>parseServerMessage</code> 直接丢弃 → 只剩一条 console 告警， <strong>用户看不到任何原因</strong>，只能感知「监听已结束」；</li><li>小程序：<code>features/interview/ws.ts#resolveServerErrorCode</code> 未包含 → 兜底归为 <code>BILLING_ERROR</code> （文案用服务端 message，表现尚可但不精确）。</li></ul><p>→ <strong>新增 / 调整任何错误码或下行消息类型时，必须同步更新两端协议文件</strong> （<code>apps/user/src/features/realtime-voice/hooks/protocol.ts</code>、<code>src/features/interview/ws.ts</code>）。 V2 的识别通道沿用 <code>ASR_RESULT</code> 时无需动协议；若新增 type，则两端都要登记。</p><h2 id="_3-方案对比-识别通道放在哪" tabindex="-1"><a class="header-anchor" href="#_3-方案对比-识别通道放在哪"><span>3. 方案对比：识别通道放在哪</span></a></h2><table><thead><tr><th style="text-align:left;">维度</th><th style="text-align:left;"><strong>A. 独立识别通道（推荐）</strong></th><th style="text-align:left;">B. 留在面试会话内按需开关</th><th style="text-align:left;">C. 文本作答走 HTTP SSE（<code>/api/qna/chat</code> 同构）</th></tr></thead><tbody><tr><td style="text-align:left;">连接数</td><td style="text-align:left;">2 条（会话 + 识别，识别短命）</td><td style="text-align:left;">1 条</td><td style="text-align:left;">2 条（会话 + HTTP），仍要 1 条 WS 承载计时</td></tr><tr><td style="text-align:left;">文本作答</td><td style="text-align:left;"><code>/ws/audio</code> 新增 <code>ANSWER</code> 上行</td><td style="text-align:left;">同 A</td><td style="text-align:left;">新接口 <code>POST /api/interviews/{id}/messages</code></td></tr><tr><td style="text-align:left;">ASR 失败影响</td><td style="text-align:left;"><strong>仅识别通道</strong>，面试不受影响</td><td style="text-align:left;">需为 text 模式单独拆掉 <code>4003</code> 分支，否则话筒故障杀面试</td><td style="text-align:left;">同 A</td></tr><tr><td style="text-align:left;">复用现有 ASR 逻辑</td><td style="text-align:left;">复用 <code>DoubaoAsrClient</code>（含 8 秒空闲保活）+ 抽取消费循环</td><td style="text-align:left;">复用 <code>consumeLoop</code>，但它是「长跑重连」结构，要改成可启停</td><td style="text-align:left;">同 A</td></tr><tr><td style="text-align:left;">计费/暂停/恢复/顶号</td><td style="text-align:left;">全部沿用</td><td style="text-align:left;">全部沿用</td><td style="text-align:left;"><strong>全部要重建</strong>（HTTP 无连接，<code>isConnectionLost</code> 失效，暂停恢复无从下手）</td></tr><tr><td style="text-align:left;">语义清晰度</td><td style="text-align:left;">高：识别就是识别</td><td style="text-align:left;">低：同一个 <code>ASR_RESULT</code> 在两种模式下含义不同</td><td style="text-align:left;">中</td></tr><tr><td style="text-align:left;">可复用性</td><td style="text-align:left;">高：AI 问答、简历诊断等页面都能用同一条识别通道</td><td style="text-align:left;">低：只能服务面试</td><td style="text-align:left;">中</td></tr><tr><td style="text-align:left;">主要代价</td><td style="text-align:left;">多一条 WS 的鉴权/心跳/限流/监控；需要抽公共 ASR 运行器</td><td style="text-align:left;">改动集中在 <code>AudioStreamSession</code>，看似省事，实则要按模式分支它的错误路径与生命周期</td><td style="text-align:left;">会话层重建成本最高</td></tr></tbody></table><p><strong>结论：A。</strong> B 作为「先不新增端点」的降级备选保留（见 §10），C 不建议。</p><h2 id="_4-推荐方案细化" tabindex="-1"><a class="header-anchor" href="#_4-推荐方案细化"><span>4. 推荐方案细化</span></a></h2><h3 id="_4-1-面试会话侧-新增「文本作答」输入源-✅-已交付-step-1" tabindex="-1"><a class="header-anchor" href="#_4-1-面试会话侧-新增「文本作答」输入源-✅-已交付-step-1"><span>4.1 面试会话侧：新增「文本作答」输入源（✅ 已交付，Step 1）</span></a></h3><blockquote><p><strong>实现状态（2026-09-22）：本节已全部落地。</strong> 关键落点： <code>InterviewInputMode</code> + <code>converter/InterviewInputModeConverter</code>（V26 <code>interview.input_mode</code>）、 <code>ControlMessage.TYPE_ANSWER</code>、<code>AudioStreamSession#handleAnswer</code>、<code>ConversationTurnRunner.SubmitResult</code>、 <code>AudioStreamSession#markSessionReady()</code>。 与本节描述<strong>只有三处细节差异</strong>（实现比方案更严/更简），已记在 §12.3，改代码前先看那三条。</p></blockquote><h4 id="_4-1-1-建连参数与持久化" tabindex="-1"><a class="header-anchor" href="#_4-1-1-建连参数与持久化"><span>4.1.1 建连参数与持久化</span></a></h4><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>/ws/audio?ticket=xxx&amp;mode=MOCK&amp;input=text&amp;language=zh&amp;...</span></span>
<span class="line"><span>                                    ^^^^^^^^^^ 新增，取值 audio | text，默认 audio</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><ul><li><p>默认 <code>audio</code> ⇒ <strong>老前端不传即保持现状</strong>（ASR 自动提交回合、ASR 故障 4003 全不变）。PC 端与 V1 小程序都走这条分支。</p></li><li><p><strong><code>input=text</code> 只对 <code>mode=MOCK</code> 成立</strong>：<code>ASSIST</code>（正式面试）+ <code>text</code> 视为非法参数，直接回 <code>INTERVIEW_PREPARE_FAILED</code> / 握手错误，不静默降级为 audio。 理由与「传了无效参数就失败、不静默换一个」的既有原则一致：正式面试若被误配成打字， 用户会觉得「AI 不回答了」，而这是个配置错误，必须当场暴露。将来若真要做「PC 端文字提问」，再放开。</p></li><li><p><code>input</code> 需要落库：<code>interview.input_mode</code>（Flyway <code>V26</code>；<strong>已复核(2026-09-22)：当前最新仍是 <code>V25</code></strong>）， 否则进程重启后 <code>LogicalSessionRebuilder</code> 从 DB 重建逻辑会话时无法还原作答方式。 <strong>列定义用 <code>varchar(...) NOT NULL DEFAULT &#39;audio&#39;</code>，不要用可空列</strong>：</p><div class="language-sql line-numbers-mode" data-highlighter="shiki" data-ext="sql" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-sql"><span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">ALTER</span><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;"> TABLE</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;"> interview </span><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">ADD</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;"> COLUMN input_mode </span><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">varchar</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">(</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;">16</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">) </span><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">NOT NULL</span><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;"> DEFAULT</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> &#39;audio&#39;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">;</span></span>
<span class="line"><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">-- 历史行因此自动落到 &#39;audio&#39;（未设置 / 老数据 / 默认值 三者不再需要区分）</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>理由：这个字段是<strong>会话行为的一部分</strong>（决定这条连接起不起 ASR、ACK 什么上行），不是可选业务属性。 留成可空会让 <code>NULL</code> 同时承担三种含义（老数据 / 未设置 / 默认 audio）， 而代码层又是&quot;默认 audio&quot; —— 两边语义一错位，重建逻辑会话时就可能把 text 会话还原成 audio。 <strong>约定：代码层默认 <code>audio</code>，数据库层最终统一 <code>NOT NULL + DEFAULT &#39;audio&#39;</code>，两者一致。</strong></p><p>Java 侧<strong>不要散落字符串</strong>，用枚举（与项目既有枚举风格一致），DB 仍是 <code>varchar(16)</code>（不上 PostgreSQL ENUM）：</p><div class="language-java line-numbers-mode" data-highlighter="shiki" data-ext="java" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-java"><span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">public</span><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;"> enum</span><span style="--shiki-light:#C18401;--shiki-dark:#E5C07B;"> InterviewInputMode</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;"> { </span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;">AUDIO</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">,</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;"> TEXT</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;"> }   </span><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">// 默认 AUDIO</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>落库/回读都经 <code>InterviewInputMode.from(String)</code>：<strong><code>null</code> / blank → <code>AUDIO</code>（历史数据与缺省）， 但未知值（<code>&quot;voive&quot;</code> / <code>&quot;TEXTT&quot;</code> 这类）抛异常，不静默降级</strong>。 理由：这个字段是<strong>行为型</strong>的（决定起不起 ASR、收不收 <code>ANSWER</code>、怎么触发会话就绪）， 把脏数据悄悄变成另一种业务行为，比当场报错危险得多。<code>null</code> 与&quot;未知值&quot;语义完全不同，不能合并处理。 <strong>SQL 默认值与代码缺省值保持一致，并明确区分&quot;缺省值&quot;与&quot;非法值&quot;</strong>（前者静默取默认，后者必须报错）。 注意这是&quot;两个默认、语义一致&quot;，不是&quot;一处定义&quot;：DB 的 <code>DEFAULT &#39;audio&#39;</code> 与代码的 <code>null → AUDIO</code> 各存在一份， 改动时要两边一起看 —— 别把它当成唯一的默认值来源。</p></li><li><p>传导链：<code>TicketHandshakeInterceptor</code>（透传）→ <code>InterviewPrepareService.ConnectionRequest</code>（新增字段） → <code>Interview</code>（落库）→ <code>InterviewContext</code>（新增字段）→ <code>InterviewSessionState</code>（新增字段）。</p></li></ul><h4 id="_4-1-2-回合执行-复用-conversationturnrunner-p0-2-已完成-本次不再抽取" tabindex="-1"><a class="header-anchor" href="#_4-1-2-回合执行-复用-conversationturnrunner-p0-2-已完成-本次不再抽取"><span>4.1.2 回合执行：复用 <code>ConversationTurnRunner</code>（P0-2 已完成，本次不再抽取）</span></a></h4><p>把「用户说了什么 → AI 回什么」抽成一个方法，任何输入源都汇入它：</p><div class="language-java line-numbers-mode" data-highlighter="shiki" data-ext="java" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-java"><span class="line"><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">// 已存在（P0-2 抽出）：一个输入 = 一个完整回合（写上下文 + 落库 + THINKING + LLM 流式 + 落库）</span></span>
<span class="line"><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">// com.xiaodingtie.passup.modules.realtime.session.ConversationTurnRunner</span></span>
<span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">public</span><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;"> void</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;"> submit</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">(</span><span style="--shiki-light:#C18401;--shiki-dark:#E5C07B;">UserTurnInput</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;"> input)</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">;</span><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">   // record UserTurnInput(String inputId, String text)</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><ul><li>ASR 路径（不变）：<code>AsrEvent.Transcript(final)</code> → 下行 <code>ASR_RESULT</code> → <code>inputMode == AUDIO &amp;&amp; final &amp;&amp; 非空</code> ⇒ <code>turnRunner.submit(new UserTurnInput(segment.id(), segment.text()))</code></li><li>文本路径（新增）：<code>receiveText</code> 的 <code>ANSWER</code> 分支 ⇒ <code>turnRunner.submit(new UserTurnInput(后端生成 inputId, text))</code></li></ul><blockquote><p><strong><code>UserTurnInput</code> 的 ID 语义（建议随本次一起改名）</strong>：现有 record 是 <code>UserTurnInput(String replyId, String text)</code> —— 字段叫 <code>replyId</code>，但它其实承载的是<strong>输入侧</strong>的标识 （ASR 用 segment id、文本输入没有 transcript），只是&quot;顺手&quot;被复用成了回复的关联 ID （前端据此把 <code>LLM_REPLY</code> 配到对应的 <code>ASR_RESULT</code> 转写气泡上）。建议改为：</p><div class="language-java line-numbers-mode" data-highlighter="shiki" data-ext="java" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-java"><span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">record</span><span style="--shiki-light:#C18401;--shiki-dark:#E5C07B;"> UserTurnInput</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">(</span><span style="--shiki-light:#C18401;--shiki-dark:#E5C07B;">String</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;"> inputId</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">,</span><span style="--shiki-light:#C18401;--shiki-dark:#E5C07B;"> String</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;"> text)   </span><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">// ASR: segment id；TEXT: 后端生成</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>理由：以后还会有 <code>paste</code> / 快捷回复 / 断线补发等输入源，不该让它们全都叫 <code>transcript</code> / <code>reply</code>。 顺带把 §4.1.3 里&quot;UUID&quot;这件事写清：<strong>ID 由后端生成</strong>（<code>UUID.randomUUID()</code> 只是实现细节， 不要在方案里把它当成契约；前端不需要自己造 ID，也不该依赖 ID 的格式）。</p><p><strong>协议边界（写死）</strong>：<code>ANSWER</code> <strong>仅允许 <code>inputMode=text</code></strong>。 audio 模式收到 <code>ANSWER</code> ⇒ 回 <code>ErrorMessage(&quot;INVALID_INPUT_MODE&quot;)</code>、<strong>不触发任何回合</strong>、不改会话状态。 理由：否则&quot;<code>ControlMessage.TYPE_ANSWER</code> 存在&quot;很容易被读成&quot;audio 模式也能直接发文字&quot;。 这与「<code>ASSIST</code> + <code>input=text</code> 直接拒绝」是同一条原则 —— <strong>模式与能力一一对应，不做静默兼容</strong>。</p></blockquote><ul><li><strong>原来的&quot;抽 <code>submitUserTurn</code>&quot;这一步已由 P0-2 完成</strong>，本方案不再需要它； 唯一新增的是「谁把文本喂给 runner」这一处分支（外加 <code>inputMode</code> 判定）。</li><li>注意：回合串行化（<code>turnInProgress</code>）在 <code>ConversationTurnRunner</code> 内部（它自己持 <code>cancel()</code> 语义）， 文本模式不需要在会话层再实现一遍；但<strong>前端连点</strong>仍可能出现，见 §4.1.5。</li></ul><blockquote><p>刻意<strong>不做</strong>的事：不引入「Turn/Message 领域模型」、不改 <code>DoubaoLlmClient</code> 的调用方式（仍是每轮 <code>new</code>）。 这次重构只需要一个入口方法，把顺手的重写压到最小 —— 旧链路的稳定性优先。</p></blockquote><h4 id="_4-1-3-上行协议新增-answer" tabindex="-1"><a class="header-anchor" href="#_4-1-3-上行协议新增-answer"><span>4.1.3 上行协议新增 ANSWER</span></a></h4><div class="language-json line-numbers-mode" data-highlighter="shiki" data-ext="json" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-json"><span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">{ </span><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">&quot;type&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: </span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">&quot;ANSWER&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">, </span><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">&quot;text&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: </span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">&quot;用户的回答文本&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;"> }</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><ul><li>校验（✅ 已实现）：剔除控制字符（<strong>保留换行与制表符</strong>，多行作答是正常输入）、去首尾空白后必须<strong>非空</strong> 且不超过 <strong>2000 字</strong>；违规分别回 <code>ErrorMessage</code>： <ul><li><code>INVALID_ANSWER</code>（空）—— 明确报错而不是静默丢弃，否则前端会以为&quot;发了没反应&quot;；</li><li><code>ANSWER_TOO_LONG</code>（超长）—— <strong>拒绝而不是截断</strong>，截断会让 AI 收到残缺语义而用户无感知。</li></ul></li><li><strong>不做幂等，也不加 <code>answerId</code> / <code>clientMsgId</code></strong>（2026-09-22 定稿；评审里&quot;给 <code>ANSWER</code> 加客户端幂等 ID&quot;的 建议<strong>已撤回</strong>）：前端发送后即禁用输入（等回复），没有重发场景；将来若真要「失败重试」， 再加 <code>clientMsgId</code> 并由<strong>后端</strong>做去重。</li><li><strong>加字段不是&quot;可选增强&quot;，而是协议变更 —— 现在单边加会把整条消息废掉</strong>： 上行解析走 <code>Jsons.parse</code>（Jackson 默认 <code>FAIL_ON_UNKNOWN_PROPERTIES=true</code>），而 <code>ControlMessage</code> 只声明了 <code>type</code> / <code>text</code>，多传一个字段会让整条 <code>ANSWER</code> 被当解析失败丢弃（只剩一条 debug 日志）。 要加必须两端同版本一起改（<code>ControlMessage</code> 加字段 + 显式放开未知字段）。</li><li>下行协议<strong>完全不变</strong>（<code>THINKING</code> / <code>LLM_REPLY</code> / <code>TIME_UPDATE</code> / <code>ERROR</code> / <code>PAUSED</code>）： 文本作答没有 ASR id，<code>replyId</code> 由后端生成 UUID，前端仍按 <code>onThinking(replyId)</code> 建占位气泡、 按 <code>LLM_REPLY.replyId</code> 填充 —— 前端零协议改动。</li></ul><blockquote><p><strong>协议事实（2026-09-22 联调确认）</strong>：<code>LLM_REPLY{final:true}</code> 就是豆包流式响应的<strong>末分片</strong> （<code>finish_reason != null</code>），<strong>它同时带最后一段正文</strong> —— 前端对它是<strong>追加</strong>语义、不要整段替换， 一轮回复只应有一条。它是 V2「等 <code>final</code> 再恢复输入框」的唯一依据。 字段名以实际 JSON 为准：是 <code>final</code> 而不是 <code>isFinal</code>（Jackson 对 <code>isXxx()</code> 会去掉前缀）。 细节与一次相关误判见 §12.6.1。</p></blockquote><h4 id="_4-1-4-会话起点的迁移-容易被漏掉的一处" tabindex="-1"><a class="header-anchor" href="#_4-1-4-会话起点的迁移-容易被漏掉的一处"><span>4.1.4 会话起点的迁移（容易被漏掉的一处）</span></a></h4><p>现状（<strong>P0-1/P0-2/P1-2 之后仍然成立</strong>）：<strong>计费起算与 MOCK 首问都挂在 ASR 就绪上</strong> （<code>AudioStreamSession#handleAsrReady()</code> → <code>publishStarted()</code> / <code>turnRunner.submitOpening()</code>）。 <code>start()</code> 里<strong>刻意没有</strong>发「面试开始」事件（P1-2 明确保留了&quot;起算点必须与识别真的可用对齐&quot;这一语义）， 所以 text 模式不启 ASR 时，这两个回调一样永远不会来 —— 会话会既不扣费也不开场。</p><table><thead><tr><th style="text-align:left;">触发点</th><th style="text-align:left;">audio 模式</th><th style="text-align:left;">text 模式（新增）</th><th style="text-align:left;">幂等守卫（不变）</th></tr></thead><tbody><tr><td style="text-align:left;">计费起算 <code>publishStarted()</code></td><td style="text-align:left;"><code>handleAsrReady()</code></td><td style="text-align:left;"><strong>会话就绪：<code>start()</code> 成功完成之后</strong></td><td style="text-align:left;"><code>InterviewSessionState#startedPublished</code></td></tr><tr><td style="text-align:left;">MOCK 首问 <code>turnRunner.submitOpening()</code></td><td style="text-align:left;"><code>handleAsrReady()</code></td><td style="text-align:left;"><strong>同上</strong></td><td style="text-align:left;"><code>InterviewSessionState#initialQuestionSent</code></td></tr></tbody></table><ul><li>用同一对 <code>AtomicBoolean</code> 守卫，保证 ASR 内部重连、暂停后重连都不重复起算/重复开场。</li><li>实现建议：把 <code>handleAsrReady()</code> 的两段逻辑抽成一个「会话就绪」入口 —— <strong>✅ 已按此实现，方法就叫 <code>markSessionReady()</code></strong>：audio 由 <code>AsrEvent.Ready</code> 触发、text 由「<strong>会话就绪</strong>」触发， 且<strong>未改 audio 的触发时机</strong>（那会是行为变更）。</li></ul><blockquote><p><strong>「WebSocket 建立成功」≠「面试开始」</strong>（这一条要在实现时写死，别让它漂）： <code>onOpen</code> 里在建连之后还有 prepare、<code>new Session</code>、<code>start()</code>；其中 <code>start()</code> 才是生命周期事务边界 （状态迁移、绑连接、起 runner），它<strong>可能失败并就地回滚</strong>（P1-2）。 如果 text 模式把起算点挂在泛化的 <code>onOpen</code> 上，就会出现「连接建立了、会话其实没准备好，却已经开始计费」。 因此 text 模式的触发点是：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>WebSocket onOpen → prepare（pending → active）→ new Session → session.start() 成功</span></span>
<span class="line"><span>                                                                   ↓</span></span>
<span class="line"><span>                                                            【会话就绪】</span></span>
<span class="line"><span>                                                                   ↓</span></span>
<span class="line"><span>                                       markSessionReady(): publishStarted() + submitOpening()</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>落地方式（二选一，建议后者）：</p><ol><li><code>AudioStreamingService.onOpen</code> 在 <code>streamSession.start()</code> 返回后调用一次 <code>streamSession.markSessionReady()</code>（text 分支）；</li><li>或让 <code>start()</code> 接收「就绪来源」（<code>ASR_READY</code> / <code>CONNECT_READY</code>），由内部统一决定何时 <code>markSessionReady()</code> —— 好处是&quot;就绪&quot;这件事只有一个写入口，不会出现两处各调一次（也就不会重复起算）。 无论选哪个，<strong>幂等仍由 <code>startedPublished</code> / <code>initialQuestionSent</code> 这对 <code>AtomicBoolean</code> 保证</strong>。</li></ol></blockquote><ul><li>text 模式下不启动 ASR 运行期（不创建 <code>AsrStreamRunner</code>，或创建但不 <code>start()</code>）⇒ 也就没有 <code>AsrEvent.Ended → closeListener.onAsrSessionEnd</code> 这条「ASR 结束即关闭物理连接」的路径。</li><li><code>receiveAudio</code> 在 text 模式下收到音频帧：<strong>忽略并 warn</strong>（防御性，前端不该推），不要静默喂给不存在的 ASR。</li></ul><h4 id="_4-1-5-回合串行化-text-模式必须做" tabindex="-1"><a class="header-anchor" href="#_4-1-5-回合串行化-text-模式必须做"><span>4.1.5 回合串行化（text 模式必须做）</span></a></h4><p>现状的 ASR final 天然串行（同一回调线程），但 LLM 是流式异步的，用户连续说两句本来就可能并发两个回合； 改为打字后「连点发送」会变得非常容易，直接并发会打乱上下文顺序。</p><p><strong>归属原则（定死，别把复杂度塞回 Session）</strong>：</p><blockquote><p><strong>「回合是否正在执行」以及「同一时间允不允许再提交一个回合」，全部由 <code>ConversationTurnRunner</code> 负责； Session 只负责把不同输入源转换成 <code>UserTurnInput</code>。</strong></p></blockquote><p>这一点 <code>ConversationTurnRunner</code> 的类注释里已经写好（&quot;回合串行化是回合执行的语义，落在这里而不是会话里&quot;）， <strong>不要在会话里再放 <code>AtomicBoolean turnInProgress</code></strong> —— 否则会出现两层回合状态：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>需要避免的形态：</span></span>
<span class="line"><span>AudioStreamSession</span></span>
<span class="line"><span>  ├── inputMode</span></span>
<span class="line"><span>  ├── turnInProgress      ← 与 Runner 内部状态重复</span></span>
<span class="line"><span>  └── ConversationTurnRunner</span></span>
<span class="line"><span>        └── （自己也在判断忙/闲）</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>落地方式：给 Runner 的提交入口加返回值，让&quot;忙时拒绝&quot;成为 Runner 的能力：</p><div class="language-java line-numbers-mode" data-highlighter="shiki" data-ext="java" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-java"><span class="line"><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">// modules/realtime/session/ConversationTurnRunner</span></span>
<span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">public</span><span style="--shiki-light:#C18401;--shiki-dark:#E5C07B;"> SubmitResult</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;"> submit</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">(</span><span style="--shiki-light:#C18401;--shiki-dark:#E5C07B;">UserTurnInput</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;"> input)</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">;</span><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">   // ACCEPTED / BUSY / REJECTED</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">// 会话侧只做输入源适配与结果转译：</span></span>
<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">ANSWER → </span><span style="--shiki-light:#E45649;--shiki-dark:#E5C07B;">turnRunner</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">submit</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">(</span><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">new</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;"> UserTurnInput</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">(inputId, text))</span></span>
<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">            ├─ ACCEPTED → 无需额外动作</span></span>
<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">            ├─ BUSY     → 回 </span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">ErrorMessage</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">(</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">&quot;TURN_BUSY&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">)（前端等待期间禁用输入框）</span></span>
<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">            └─ REJECTED → 忽略（会话已结束</span><span style="--shiki-light:#383A42;--shiki-dark:#56B6C2;">/</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">暂停，前端本就该断开）</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>三种状态的语义（写死，别让实现者自行发挥）</strong>：</p><table><thead><tr><th style="text-align:left;">值</th><th style="text-align:left;">含义</th><th style="text-align:left;">典型场景</th><th style="text-align:left;">前端应对</th></tr></thead><tbody><tr><td style="text-align:left;"><code>ACCEPTED</code></td><td style="text-align:left;">已进入回合执行，可以等 <code>THINKING</code> / <code>LLM_REPLY</code></td><td style="text-align:left;">正常提交</td><td style="text-align:left;">正常等待</td></tr><tr><td style="text-align:left;"><code>BUSY</code></td><td style="text-align:left;"><strong>已有回合在执行</strong>，本次输入未被接受</td><td style="text-align:left;">用户连点 / 上一轮还在流式输出</td><td style="text-align:left;">禁用输入框并提示&quot;AI 正在回复&quot;</td></tr><tr><td style="text-align:left;"><code>REJECTED</code></td><td style="text-align:left;"><strong>Runner 当前不允许创建新回合</strong>（具体原因 —— ENDED / PAUSED / CLOSING / CANCELLED —— 由 Runner 内部判定，<strong>不暴露给会话层</strong>）</td><td style="text-align:left;">断线残包、已结束/已取消会话上的提交</td><td style="text-align:left;">走断线 / 结束态</td></tr></tbody></table><p><strong><code>BUSY ≠ REJECTED</code></strong>：前者是&quot;现在不行、等下可以&quot;，后者是&quot;这场面试已经不能答了&quot;。两者前端体验不同，不要合成一个。</p><p><strong><code>submitOpening()</code> 与 <code>submit()</code> 的关系</strong>：<strong>共享同一把回合状态，但不必共用同一个 public API</strong>。 Opening 是系统触发的特殊回合（没有用户输入，文案来自 <code>OPENING_INSTRUCTION</code>）， 为了&quot;统一 API&quot;把它伪装成一个 <code>UserTurnInput</code> 反而更脏；正确形态是：</p><div class="language-java line-numbers-mode" data-highlighter="shiki" data-ext="java" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-java"><span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">public</span><span style="--shiki-light:#C18401;--shiki-dark:#E5C07B;"> SubmitResult</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;"> submitOpening</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">()</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">;</span><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">            // 系统触发，走同一把&quot;回合状态&quot;</span></span>
<span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">public</span><span style="--shiki-light:#C18401;--shiki-dark:#E5C07B;"> SubmitResult</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;"> submit</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">(</span><span style="--shiki-light:#C18401;--shiki-dark:#E5C07B;">UserTurnInput</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;"> input)</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">;</span><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;"> // 用户输入</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><blockquote><p><strong>实现核对（2026-09-22，已落地）</strong>：串行化已按本节实现，但有 <strong>两处与原先的&quot;现状核对&quot;不同</strong>， 别再按旧理解改代码：</p><ol><li><strong><code>submit</code> 不是同步阻塞的</strong>。原先记录为&quot;会一直执行到整轮流式结束才返回&quot;是<strong>误读</strong>： <code>DoubaoLlmClient#streamChat</code> 是 <code>executor.submit(...)</code> 之后<strong>立即返回</strong>（异步）。 因此&quot;<code>submit</code> 返回即释放占用&quot;根本兜不住连点 —— <strong>占用必须覆盖整轮流式输出</strong>： 在进入回合之前取占用，在本轮<strong>终态</strong>（<code>onComplete</code> / <code>onError</code> / <code>onCancel</code>）释放， 另加&quot;提交本身失败&quot;（建客户端 / 提交任务抛异常）的兜底释放，避免永久卡在 <code>BUSY</code>。</li><li><strong><code>cancel()</code> 不清理占用标志</strong>（即 §12.1 的&quot;第二种做法&quot;）：另设一个 <code>accepting</code> 标志表示 &quot;本 Runner 还接不接受新回合&quot;，<code>cancel()</code> 只关它 + 打断当前请求；占用一律由<strong>拥有该回合的提交路径</strong>释放。 于是&quot;取消后再提交&quot;得到的是 <code>REJECTED</code>（不是 <code>BUSY</code>），也不存在&quot;旧回合收尾误清新一轮占用&quot;。</li></ol><p>由此仍然成立的硬约束只有一条：<strong>占用必须是 CAS 标志</strong>（<code>AtomicBoolean.compareAndSet</code>）—— ASR 路径的 <code>submit</code> 来自 ASR 事件线程、文本路径来自 WS 处理线程，两者可能并发。</p><p><strong><code>BUSY</code> 仍必须在进入回合之前判断</strong>：不能靠&quot;同一连接顺序处理、反正不会并发&quot;来兜 —— 那样用户看到的是<strong>发了没反应</strong>，比直接回 <code>TURN_BUSY</code> 更糟。</p></blockquote><blockquote><p><strong>提交状态是统一的（不变）</strong>：<code>ConversationTurnRunner</code> 的串行化状态对 opening 与用户回合<strong>共用一把</strong>， 因此 audio 路径也一样受它约束（ASR final 与 opening 不会并发）。 不要理解成&quot;audio 绕过 BUSY、text 使用 BUSY&quot; —— 那会变成两套语义。audio 的并发隐患（多句 ASR final 抢跑） 仍另开治理项，但<strong>入口判断只有一处</strong>。 注意：<code>submitOpening()</code> 也走同一个入口判断，否则「首问还没答完用户就打字」会并发两个回合。</p></blockquote><h4 id="_4-1-6-心跳仍是唯一存活判据-——-text-模式下更关键" tabindex="-1"><a class="header-anchor" href="#_4-1-6-心跳仍是唯一存活判据-——-text-模式下更关键"><span>4.1.6 心跳仍是唯一存活判据 —— text 模式下更关键</span></a></h4><p>text 模式<strong>没有音频帧</strong>，而「未声明心跳的老客户端」判据是音频活跃度 5 分钟： 用户写一段长回答超过 5 分钟不提交，就会在旧判据下被判失联。 因此前端接入 V2 会话时<strong>必须发应用层心跳</strong>（建连后立即一次，随后 25 秒一次，随连接启停）， 一旦发过心跳就切到 60 秒严格判据，音频空窗不再影响。这条是前端方案的硬约束，后端不做兜底特例 （不把「收到 ANSWER」当心跳，否则又会回到「业务消息寄生心跳」的老问题）。</p><p><strong>状态语义（写死；评审里那条「失联 ≠ 暂停」就是这一节）</strong>：</p><table><thead><tr><th style="text-align:left;">事件</th><th style="text-align:left;">后端行为（均为既有实现，V2 未改）</th><th style="text-align:left;">会话状态</th></tr></thead><tbody><tr><td style="text-align:left;">用户在思考，长时间不发 <code>ANSWER</code></td><td style="text-align:left;">无任何特判：只要心跳在就一直是 <code>ACTIVE</code>。<strong>思考时间算面试时间</strong>，按整场时长计费</td><td style="text-align:left;"><code>ACTIVE</code></td></tr><tr><td style="text-align:left;">收到 <code>{&quot;type&quot;:&quot;heartbeat&quot;}</code></td><td style="text-align:left;">只刷新&quot;连接存活&quot;，<strong>不代表&quot;用户正在操作&quot;</strong>，也不刷新计费活跃度</td><td style="text-align:left;"><code>ACTIVE</code></td></tr><tr><td style="text-align:left;">超过失联阈值没收到心跳（语音 60 秒 / <strong>文字作答 180 秒</strong>）</td><td style="text-align:left;">判连接失活 → <strong>踢出并结束面试</strong>（停计费、落库收尾）；<strong>不是暂停</strong></td><td style="text-align:left;"><code>ENDED</code></td></tr><tr><td style="text-align:left;">客户端显式发 <code>{&quot;type&quot;:&quot;PAUSE&quot;}</code></td><td style="text-align:left;">断开物理连接、保留逻辑会话，等 <code>resume=&lt;逻辑会话ID&gt;</code> 重连</td><td style="text-align:left;"><code>PAUSED</code></td></tr><tr><td style="text-align:left;"><code>PAUSED</code> 超过保留时长（默认 15 分钟）未重连</td><td style="text-align:left;"><code>SESSION_EXPIRED</code> 结束</td><td style="text-align:left;"><code>ENDED</code></td></tr></tbody></table><p>三个要点：</p><ol><li><strong>没有&quot;无操作自动暂停&quot;</strong>：只有显式 <code>PAUSE</code> 才进 <code>PAUSED</code>；<strong>用户思考 1 分钟仍是 <code>ACTIVE</code></strong>。</li><li><strong>失联是&quot;结束&quot;不是&quot;暂停&quot;</strong>（✅ 2026-09-22 定稿，不再变更）：小程序切后台 / 计时器被系统挂起 超过阈值，用户回来时<strong>面试已经结束</strong> —— 这是既定行为，V2 联调时不要当 bug 报。</li><li><strong>但文字作答的失联阈值放宽到 180 秒</strong>（<code>realtime.text-heartbeat-timeout-ms</code>，语音仍 60 秒）： text 模式是&quot;看题 → 思考 → 打字&quot;，<strong>切后台/锁屏想一会儿再回来是常态</strong> （挂起的是 JS，心跳随之停，但 TCP 连接往往还在），沿用 60 秒会把回来的人判成失联。 阈值的作用点只有一个：<code>AudioStreamSession#effectiveTimeoutMillis()</code>（三档：老客户端音频 5 分钟 / 语音心跳 60 秒 / 文字心跳 180 秒），<strong>没有新增状态</strong>。 阈值上限由计费约束：判定期间会话仍是 <code>ACTIVE</code>、<strong>照常计时计费</strong>，所以 <code>阈值 = 真的一去不回时最多多扣多少秒</code>。</li></ol><blockquote><p><strong>为什么不引入 <code>DISCONNECTED</code> 状态或&quot;失联可恢复&quot;</strong>：连接真的断了时，前端<strong>没有自动重连</strong> （当前唯一的恢复路径是&quot;显式 <code>PAUSE</code> → 手动 <code>resume</code>&quot;），后端留多久都不会有人来接； 而 TCP 未断（只是 JS 挂起）的场景，放宽阈值就能自动续上，<strong>零前端改动</strong>。 要真做&quot;失联可恢复&quot;，得引入新状态 + DB 语义 + 前端自动 resume，属独立议题，收益远小于成本。 另外，把失联映射成 <code>PAUSED</code> 会让 <code>PAUSED</code> 同时表示&quot;主动暂停 / 锁屏 / 断网 / 杀进程 / 故意断网&quot;， 「用户主动暂停」这个最有产品价值的信号会被冲掉 —— 这正是&quot;两套语义分开&quot;要守住的东西。</p></blockquote><h3 id="_4-2-识别通道侧-ws-asr-纯识别-✅-已交付-step-2" tabindex="-1"><a class="header-anchor" href="#_4-2-识别通道侧-ws-asr-纯识别-✅-已交付-step-2"><span>4.2 识别通道侧：<code>/ws/asr</code>（纯识别）（✅ 已交付，Step 2）</span></a></h3><blockquote><p><strong>实现状态（2026-09-22）：本节已落地。</strong> 关键落点：<code>WebSocketConfig</code>（注册 <code>/ws/asr</code>）、 <code>AsrHandshakeInterceptor</code>（只验票）、<code>AsrWebSocketHandler</code>、<code>modules/realtime/asr/AsrStreamingService</code> 与 <code>AsrStreamSession</code>；识别运行本体直接复用 <code>AsrStreamRunner</code>（P0-1），<strong>没有重写消费循环</strong>。 与本节描述有三处实现细节差异（含一处<strong>必须补的协议字段</strong> <code>final</code>），见 §4.2.2 与 §12.4。 前端据此可以开始实现 <code>AsrTransport</code>（协议契约见 §4.2.2）。</p></blockquote><blockquote><p><strong>本次只服务 V2 小程序</strong>：PC 端采集的是会议音频、没有「用户对着电脑说话」的场景，不接这条通道。 但通道本身按通用能力设计（只依赖 ticket 鉴权、语义就是「音频 → 文本」）， 将来 PC 端要做「手动语音提问」时可直接复用（PC 已有 16k PCM 转换管线 <code>usePCMProcessor</code>）， 不需要再为它改一次后端 —— 这也是 §4.2.3 建议把「鉴权」与「面试余额准入」分开的原因。</p></blockquote><h4 id="_4-2-1-组成" tabindex="-1"><a class="header-anchor" href="#_4-2-1-组成"><span>4.2.1 组成</span></a></h4><table><thead><tr><th style="text-align:left;">组件</th><th style="text-align:left;">作用</th><th style="text-align:left;">复用情况</th></tr></thead><tbody><tr><td style="text-align:left;"><code>WebSocketConfig</code></td><td style="text-align:left;">注册 <code>/ws/asr</code></td><td style="text-align:left;">改（加一行注册）</td></tr><tr><td style="text-align:left;">握手拦截器</td><td style="text-align:left;">只校验 ticket（<strong>不校验余额</strong>）</td><td style="text-align:left;">新增轻量实现，见 4.2.3</td></tr><tr><td style="text-align:left;"><code>AsrWebSocketHandler</code></td><td style="text-align:left;">二进制上行 / 文本下行 + 关闭收尾</td><td style="text-align:left;">新增（结构对齐 <code>AudioWebSocketHandler</code>，去掉业务分支）</td></tr><tr><td style="text-align:left;"><code>AsrStreamingService</code></td><td style="text-align:left;">连接注册表 + 单用户并发限制 + 时长上限</td><td style="text-align:left;">新增</td></tr><tr><td style="text-align:left;"><code>AsrStreamSession</code></td><td style="text-align:left;">一次识别的运行单元：推帧、收文本、生命周期</td><td style="text-align:left;">新增（<strong>很薄</strong>：只管连接 / 时长上限 / 单用户并发，识别运行本体复用下面的 Runner）</td></tr><tr><td style="text-align:left;"><code>DoubaoAsrClient</code></td><td style="text-align:left;">豆包实时识别</td><td style="text-align:left;"><strong>原样复用</strong>（含静音保活、错误归一、<code>release()</code> 幂等）</td></tr><tr><td style="text-align:left;">消费/重连循环</td><td style="text-align:left;"><code>pumpAudio</code> + 退避重连 + 残留音频丢弃 + <code>asrId</code> 轮换</td><td style="text-align:left;">✅ <strong>已就绪（P0-1）</strong>：<code>modules/realtime/session/AsrStreamRunner.java</code>（<code>/ws/audio</code> 已在用，重连预算与 <code>realtime.asr.*</code> 属性都在它内部）→ <code>/ws/asr</code> <strong>直接复用，禁止复制</strong></td></tr></tbody></table><blockquote><p><strong>原「抽公共组件」这一步已完成（P0-1）</strong>，所以本方案不再包含&quot;动旧代码&quot;的重构点 —— 新增通道是纯增量。回归已在 P0-1 做过（重连预算 / 退避 / <code>reconnect-reset-ms</code> / 丢弃残留音频行为等价）。</p><p><strong>两条边界要守住</strong>：</p><ol><li>识别通道<strong>不接 <code>SessionRegistry</code>、也不走 <code>requestClose/cleanup</code></strong> —— 那是面试会话的收尾入口（P0-3 / P1-1）； 识别连接没有逻辑会话，它的收尾只有自己的资源（关豆包客户端 + 摘自己的注册表）， 因此也不存在 P1-3 那类「迟到收尾把新连接面试搞掉」的问题（无共享逻辑会话可写）。</li><li><code>/ws/asr</code> 的鉴权沿用 ticket，但<strong>不进面试会话的两级注册表、不计费、不落库</strong>（现有结论不变）。</li><li><strong><code>AsrStreamSession</code> 不承载任何面试会话语义</strong>：不拥有 <code>InterviewSessionState</code>、不谈逻辑会话、 不碰计费/暂停恢复/顶号/<code>CleanupReason</code>。它只是&quot;一次识别&quot;的运行单元。 否则很容易慢慢长成第二个 <code>AudioStreamSession</code>，把刚拆出来的边界又糊回去。</li></ol><p>理想结构就是一条直线，复杂度全部压在最后一步的一个组件里：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>AsrWebSocketHandler → AsrStreamingService → AsrStreamSession → AsrStreamRunner → DoubaoAsrClient</span></span>
<span class="line"><span>                                                       ↑</span></span>
<span class="line"><span>                                       （重连/退避/残留帧/自愈 全在这里，P0-1 已完成）</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>判断是否长歪的一句话自检：<strong>&quot;识别失败时会不会有人想动面试状态？&quot;</strong> 只要答案是&quot;不会&quot;， 这条通道就没跑偏（对照 §4.2.1 边界 1）。</p><p><strong><code>AsrStreamRunner</code> 依赖核对（2026-09-22 实测，不是&quot;声明&quot;）</strong>：它的 import 只有 <code>AsrSegment</code> / <code>DoubaoAsrClient</code> / <code>DoubaoAsrProperties</code> / <code>RealtimeProperties</code> 与 <code>java.util.concurrent.*</code>， <strong>没有</strong> <code>AudioStreamSession</code> / <code>InterviewSessionState</code> / <code>CleanupReason</code> / <code>SessionRegistry</code> / 计费 / <code>sendMessage</code> / <code>closeListener</code>；事件出口是 <code>AsrEventHandler</code> + <code>AsrEvent</code>（Ready / Transcript / Ended / Unavailable）， 入口是 PCM 帧 —— 即&quot;声音进、事件出&quot;，符合公共 Runner 的形态。 唯一与面试沾边的是 <code>RealtimeProperties</code>（<code>realtime.asr.*</code> 的重连预算），那是<strong>配置而非会话状态</strong>，可接受； 新增消费者时不要往里塞任何&quot;面试状态&quot;。</p></blockquote><h4 id="_4-2-2-协议-与面试链路对齐-前端才能复用同一套合并规则" tabindex="-1"><a class="header-anchor" href="#_4-2-2-协议-与面试链路对齐-前端才能复用同一套合并规则"><span>4.2.2 协议（与面试链路对齐，前端才能复用同一套合并规则）</span></a></h4><ul><li>上行：二进制 PCM 帧（16kHz / 16bit / 单声道），与现有前端采集参数一致，后端无需转码。</li><li>上行控制：<strong>不使用应用层 heartbeat</strong>。识别连接是短命的（点话筒 → 说话 → 关闭）， 复用面试那套心跳语义只会让实现者反复追问&quot;到底需不需要&quot;。连接存活由<strong>最大识别时长</strong>兜底 （落地为 <code>realtime.asr.session-max-duration-ms</code>，默认 120 秒，见 §4.2.4）。 <blockquote><p>注意：<strong>没有&quot;帧空闲超时&quot;这一条</strong>。前端用本地 VAD，只在说话时推帧，&quot;几十秒没帧&quot;是正常态 （用户在想），所以按帧空闲判死会误杀 —— 这条在实现时被明确去掉，与 <code>AGENTS.md §3.7</code> 「音频不是心跳」是同一条理由。</p></blockquote></li><li>上行文本：<strong>不使用</strong>（没有心跳、没有控制指令）。收到文本只记 debug 日志，不回错误、不改状态。</li><li>下行：复用 <code>AsrResultMessage</code>，并<strong>补上 <code>final</code> 字段</strong>（✅ 实现时新增，见下方&quot;实现修正&quot;）： <code>{&quot;type&quot;:&quot;ASR_RESULT&quot;,&quot;id&quot;:&quot;...&quot;,&quot;text&quot;:&quot;...&quot;,&quot;final&quot;:false,&quot;serverNow&quot;:...}</code>。 保持现有 id 语义：<strong>同一句的中间帧与 final 帧共享 id，final 之后轮换</strong>。</li><li>错误与关闭码（<strong>识别通道自己的语义空间</strong>，不复用面试链路的业务错误）：</li></ul><table><thead><tr><th style="text-align:left;">场景</th><th style="text-align:left;">下行 <code>ERROR.code</code></th><th style="text-align:left;">关闭码</th><th style="text-align:left;">前端应对</th></tr></thead><tbody><tr><td style="text-align:left;">凭证缺失 / 无效</td><td style="text-align:left;"><code>AUTH_FAILED</code></td><td style="text-align:left;">4001</td><td style="text-align:left;">重新取凭证；提示重新登录</td></tr><tr><td style="text-align:left;">单用户并发识别连接超限</td><td style="text-align:left;"><code>ASR_LIMIT_EXCEEDED</code></td><td style="text-align:left;">4004</td><td style="text-align:left;">先关掉自己那条旧识别连接再重试（不要静默重连）</td></tr><tr><td style="text-align:left;"><strong>当日识别次数已用完</strong>（Step 3）</td><td style="text-align:left;"><code>ASR_QUOTA_EXCEEDED</code></td><td style="text-align:left;">4006</td><td style="text-align:left;">提示「今日语音识别次数已用完，请明天再试或手动输入」；<strong>重试无用</strong>，不要自动重连</td></tr><tr><td style="text-align:left;">识别服务不可用（重连耗尽 / 未预期异常 / 未配置 Key）</td><td style="text-align:left;"><code>ASR_UNAVAILABLE</code></td><td style="text-align:left;">4003</td><td style="text-align:left;">提示「语音识别不可用，请手动输入」，<strong>不影响面试</strong></td></tr><tr><td style="text-align:left;">单次识别达到时长上限（兜底）</td><td style="text-align:left;"><code>ASR_TIMEOUT</code></td><td style="text-align:left;">4005</td><td style="text-align:left;">当作&quot;本次语音输入已结束&quot;，可再次点话筒</td></tr><tr><td style="text-align:left;">正常结束（用户停止 / 服务端正常收尾）</td><td style="text-align:left;">无</td><td style="text-align:left;">1000</td><td style="text-align:left;">静默收尾，无需提示</td></tr></tbody></table><blockquote><p><strong>三道准入闸门的顺序（实现里写死，前端可依赖）</strong>：凭证 → <strong>并发</strong>（可即时自救）→ <strong>日配额</strong>（重试无用）。 被前两道拒绝的连接<strong>不消耗</strong>当日配额。</p></blockquote><blockquote><p><strong>实现修正（2026-09-22，必须知道）</strong>：本节原文写&quot;复用 <code>AsrResultMessage</code>&quot;时<strong>没有 <code>final</code> 字段</strong>， 但前端 <code>useSpeechInput</code> 的「中间态覆盖草稿 / 定稿追加」<strong>必须有它</strong>（&quot;id 轮换&quot;只能表达&quot;下一句开始了&quot;， 无法表达&quot;最后一段已定稿&quot;）。实现时给 <code>AsrResultMessage</code> 加了 <code>isFinal</code>（序列化名 <code>final</code>， 与 <code>LLM_REPLY</code> 一致），并<strong>两条通道都填</strong>：<code>/ws/audio</code> 也照带， 避免&quot;同一个消息类型在两条通道上语义不一致&quot;。 这是<strong>协议只增不减</strong>的增量：两端老客户端只读 <code>id</code> / <code>text</code>，多一个字段被忽略，行为不变（见 §5.1）。</p></blockquote><ul><li>结束方式：<strong>先同步下发 <code>ERROR</code> 再关闭</strong>（不能走发送队列 —— 队列是异步的，关闭帧可能先到，</li></ul><blockquote><p>用户看到的就是&quot;识别莫名结束&quot;而没有原因）。</p></blockquote><h4 id="_4-2-3-鉴权-复用-ticket-但不复用「余额校验」" tabindex="-1"><a class="header-anchor" href="#_4-2-3-鉴权-复用-ticket-但不复用「余额校验」"><span>4.2.3 鉴权：复用 ticket，但不复用「余额校验」</span></a></h4><p><code>TicketHandshakeInterceptor</code> 现在做两件事：验 ticket + <code>billingService.canConsume(INTERVIEW_SECONDS)</code>。 第二件事只对 <code>/ws/audio</code> 成立 —— 识别通道不应该因为「面试时长余额为 0」而拒绝连接 （且将来 AI 问答等页面复用识别通道时更没有面试余额这个概念）。</p><p>建议：把「纯鉴权」与「业务准入」分开。<strong>最小改法</strong>是为 <code>/ws/asr</code> 新增一个只做 <code>WebSocketTicketService#verifyAndConsume</code> + 参数透传的拦截器；<strong>更干净的做法</strong>是把现有拦截器 拆成 <code>TicketAuthInterceptor</code>（鉴权 + 属性透传）与 <code>/ws/audio</code> 专用的余额准入，二者组合注册。 前者改动小、不动旧链路，建议先做前者，后者作为后续治理。</p><p>凭证获取不需要新接口：复用 <code>GET /api/ws/ticket</code>（TTL 30 秒、<code>GETDEL</code> 一次性）， 点话筒时取一次即可，与「点话筒即建连」的时间窗完全匹配。</p><blockquote><p><strong>实施前必须确认的一条（2026-09-22 已查代码，结论：当前架构满足要求）</strong>： <strong>ticket 的签发过程本身不得承担 <code>INTERVIEW_SECONDS</code> 余额准入</strong>，余额检查只能属于 <code>/ws/audio</code> 的业务握手准入。 否则 <code>/ws/asr</code> 会被&quot;面试余额&quot;间接限制，&quot;识别通道与面试解耦&quot;就是假象。 实测现状：余额检查在 <code>TicketHandshakeInterceptor#beforeHandshake</code> 里 （<code>verifyAndConsume(ticket)</code> 之后调 <code>billingService.canConsume(userId, INTERVIEW_SECONDS)</code>）， <strong>不在签发侧</strong> —— 所以新增一个只做 <code>verifyAndConsume</code> + 属性透传的轻量拦截器<strong>就够了</strong>，不需要先拆签发逻辑。 实施时若发现签发侧也加了余额判断，必须先把该职责移回拦截器，再加新通道。</p></blockquote><blockquote><p><strong>留一个扩展点，但现在不改 Ticket 系统</strong>：如果 <code>/ws/asr</code> 将来成为通用识别能力 （AI 问答、简历诊断等页面复用），ticket 最好能表达<strong>用途</strong>，服务端验证时可区分 <code>INTERVIEW</code> / <code>ASR_INPUT</code>， 避免&quot;给面试会话开的 ticket 被拿去建任意 ASR 连接&quot;。短期没有实际风险（都要登录、都短 TTL、都一次性）， 所以本次<strong>仅记录该方向</strong>：先在 ticket 载体（或属性透传）里预留 <code>scene/purpose</code> 的位置， 真正收紧放在「识别通道被第二个页面复用」时再做 —— 属于本文档之外的一次独立改动。</p></blockquote><h4 id="_4-2-4-生命周期与成本治理" tabindex="-1"><a class="header-anchor" href="#_4-2-4-生命周期与成本治理"><span>4.2.4 生命周期与成本治理</span></a></h4><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>点话筒 → 取 ticket → 建连 → 前端开始推 PCM 帧 → 边说边收 ASR_RESULT → 用户停止 / 录音达上限 / 静默</span></span>
<span class="line"><span>        → 前端关闭连接（或后端在时长上限触发关闭）→ 释放 DoubaoAsrClient</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><ul><li>连接是<strong>短命</strong>的：不做暂停恢复、不进 <code>logicalSessions</code>、不参与心跳踢出扫描。✅ <strong>已落地</strong>： 识别连接的收尾只有自己的资源（停 <code>AsrStreamRunner</code> + 清发送队列 + 摘并发闸门）。</li><li>单次识别上限 ✅ <strong>已落地</strong>：<code>realtime.asr.session-max-duration-ms</code>（默认 <strong>120 秒</strong>，前端录音 60 秒）。 到点由<strong>会话自己的计时器</strong>主动关闭并回 <code>ASR_TIMEOUT</code>（关闭码 4005）。 <blockquote><p>为什么用&quot;会话内计时器&quot;而不是加一个全局扫描任务：它<strong>不依赖任何回调</strong>， 连接悬挂（网络黑洞 / 前端崩溃）时照样会到期收尾，于是识别通道不需要第二个 <code>WsHeartbeatScheduler</code>。</p></blockquote></li><li>单用户并发识别连接上限 ✅ <strong>已落地</strong>：<code>realtime.asr.max-connections-per-user</code>（默认 <strong>1</strong>）。 超限的<strong>拒绝</strong>（<code>ASR_LIMIT_EXCEEDED</code> / 4004），不做顶号 —— 顶号会把&quot;前端两条连接竞态&quot;变成静默互踢。 另有一条自愈：建连时先摘掉&quot;传输已关闭但回调还没到&quot;的死连接登记，否则死连接会把用户锁住最长一个时长上限。</li><li>埋点/指标 ✅ <strong>已落地（Step 3）</strong>：走 <code>MeterRegistry</code>，出口是已暴露的 <code>/actuator/prometheus</code>： <ul><li><code>asr_stream_sessions_active</code>（Gauge）：当前活跃识别连接数；</li><li><code>asr_stream_connections_total{result}</code>：建连结果（<code>accepted</code> / <code>rejected_limit</code> / <code>rejected_quota</code> / <code>rejected_auth</code> / <code>rejected_error</code>）；</li><li><code>asr_stream_session_duration_seconds{end}</code>：单次识别时长，<code>end</code> = <code>client_close</code> / <code>asr_unavailable</code> / <code>timeout</code> —— <strong>失败率 = 非 <code>client_close</code> 的占比</strong>，同时给出时长分布（一次收尾只上报一次）；</li><li><code>asr_stream_audio_bytes_total</code>：上行音频字节数，<strong>识别成本的代理量</strong>（16k/16bit/单声道 = 32000 B/s）。</li></ul><blockquote><p>刻意不做&quot;每次识别打一行成本日志&quot;：字节数换算成音频时长就够了，日志里只留长度（见下一条）。</p></blockquote></li><li>每日配额 ✅ <strong>已落地（Step 3）</strong>：<code>realtime.asr.daily-sessions-per-user</code>（默认 <strong>300</strong>，0 = 不限制）。 补的是&quot;不停建连 → 说话 → 关闭&quot;这种循环刷量（单用户并发 1 + 单次 120 秒挡不住它）； 超限回 <code>ASR_QUOTA_EXCEEDED</code> / 4006。三条性质：<strong>按北京时间自然日滚动</strong>（JVM 跑 UTC， 按 UTC 切会让用户在北京时间早 8 点被重置）、<strong>只有被接受的连接才计数</strong>、<strong>Redis 异常时放行</strong>（失败开放）。</li><li>日志脱敏 ✅ <strong>已落地（Step 3）</strong>：用户内容只记长度、不记原文 —— <code>LogTextUtils#describe()</code> 返回 <code>len=42</code> 这类摘要，用在 V2 新增的三处日志（收到 <code>ANSWER</code>、 识别结果未触发回合、识别通道收到上行文本）。 <blockquote><p>与既有的 <code>truncate()</code> 分工：截断仍会保留前 512 字符的<strong>内容</strong>，只适合&quot;不是隐私但要防刷屏&quot;的文本； 用户作答 / 识别转写这类内容一律用 <code>describe()</code>。</p></blockquote></li><li>音频不落盘、识别原文不落库：只存在于内存与下行消息里。✅ 已落地。</li></ul><h3 id="_4-3-文字作答的完整时序" tabindex="-1"><a class="header-anchor" href="#_4-3-文字作答的完整时序"><span>4.3 文字作答的完整时序</span></a></h3><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>前端                                后端</span></span>
<span class="line"><span> |  建连 /ws/audio?...&amp;input=text     |</span></span>
<span class="line"><span> |---------------------------------&gt;| prepare（快照 + 提示词）→ pending→active</span></span>
<span class="line"><span> |                                    | inputMode=text ⇒ 不起 ASR</span></span>
<span class="line"><span> |                                    | session.start() 成功 → 【会话就绪】markSessionReady()</span></span>
<span class="line"><span> |                                    |   → publishStarted() + submitOpening()（均按逻辑会话幂等）</span></span>
<span class="line"><span> |&lt;----------------- THINKING + LLM_REPLY(流式) ---- AI 面试官开场</span></span>
<span class="line"><span> |</span></span>
<span class="line"><span> |  用户打字 / 话筒识别后编辑</span></span>
<span class="line"><span> |  {&quot;type&quot;:&quot;ANSWER&quot;,&quot;text&quot;:&quot;...&quot;}   |</span></span>
<span class="line"><span> |---------------------------------&gt;| 校验（inputMode / 非空）→ turnRunner.submit(input)</span></span>
<span class="line"><span> |                                    |   写上下文 + 落库(user) + THINKING</span></span>
<span class="line"><span> |&lt;----------------- THINKING + LLM_REPLY(流式) ----+</span></span>
<span class="line"><span> |                                    |   落库(assistant)（忙/闲状态由 Runner 自持，会话不参与）</span></span>
<span class="line"><span> |</span></span>
<span class="line"><span> |  （每 25 秒）{&quot;type&quot;:&quot;heartbeat&quot;}   |→ 刷新连接心跳（唯一存活判据）</span></span>
<span class="line"><span> |  （可选）另一条 /ws/asr 做语音输入   |→ 只回 ASR_RESULT，不触发任何回合</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="_5-兼容与灰度" tabindex="-1"><a class="header-anchor" href="#_5-兼容与灰度"><span>5. 兼容与灰度</span></a></h2><table><thead><tr><th style="text-align:left;">场景</th><th style="text-align:left;">行为</th></tr></thead><tbody><tr><td style="text-align:left;"><strong>PC 正式面试</strong>（<code>pass-up.frontend/apps/user</code>，不传 <code>mode</code> / 不传 <code>input</code>）</td><td style="text-align:left;"><code>ASSIST</code> + <code>audio</code>，<strong>对用户可见行为保持不变</strong>：ASR final 即触发 LLM、ASR 故障仍是 4003、计费起算仍挂 ASR 就绪（协议、时序语义、计费口径一致；重构后日志/内部时序可能有细微差异，不作为验收标准）</td></tr><tr><td style="text-align:left;">小程序 V1（<code>input</code> 不传）</td><td style="text-align:left;">等价 <code>input=audio</code>，行为与今天完全一致（含 ASR 自动提交、<code>4003</code> 关码）</td></tr><tr><td style="text-align:left;">小程序 V1 显式传 <code>input=audio</code></td><td style="text-align:left;">与不传等价</td></tr><tr><td style="text-align:left;">V2（<code>mode=MOCK</code> + <code>input=text</code>）</td><td style="text-align:left;">文本作答；不起 ASR；ASR 相关错误路径不可达</td></tr><tr><td style="text-align:left;"><code>ASSIST</code> + <code>input=text</code></td><td style="text-align:left;"><strong>拒绝</strong>（非法参数），不静默降级</td></tr><tr><td style="text-align:left;">text 模式收到音频帧</td><td style="text-align:left;">忽略 + warn（防御）</td></tr><tr><td style="text-align:left;">重连（暂停后）</td><td style="text-align:left;"><code>inputMode</code> 从 <code>InterviewSessionState</code> / DB 还原，重连后仍是 text，不误启 ASR</td></tr><tr><td style="text-align:left;">跨进程重建</td><td style="text-align:left;"><code>LogicalSessionRebuilder</code> 从 <code>interview.input_mode</code> 还原</td></tr><tr><td style="text-align:left;">客户端发版顺序</td><td style="text-align:left;"><strong>现有 PC 端与 V1 小程序无需因后端兼容性而发版</strong>（协议只增不减、新增能力全在不传参数的分支之外）；<strong>V2 小程序按正常功能节奏单独发版</strong>（<code>input=text</code> / <code>ANSWER</code> / <code>/ws/asr</code> / <code>useSpeechInput</code> 都是新能力）。<strong>后端可以先发布，不要求等待 V2 客户端</strong></td></tr></tbody></table><p>灰度上不需要开关：<code>inputMode</code> 本身就是入口级开关，前端选择走 V1 还是 V2 即完成分流。</p><h3 id="_5-1-本次改动的「影响面矩阵」-改前逐项确认" tabindex="-1"><a class="header-anchor" href="#_5-1-本次改动的「影响面矩阵」-改前逐项确认"><span>5.1 本次改动的「影响面矩阵」（改前逐项确认）</span></a></h3><table><thead><tr><th style="text-align:left;">改动</th><th style="text-align:left;">PC 正式面试</th><th style="text-align:left;">小程序 V1</th><th style="text-align:left;">小程序 V2</th></tr></thead><tbody><tr><td style="text-align:left;">新增 <code>input</code> 查询参数（默认 audio）</td><td style="text-align:left;">无影响（不传）</td><td style="text-align:left;">无影响（不传）</td><td style="text-align:left;">生效</td></tr><tr><td style="text-align:left;"><code>V26</code> 新增 <code>interview.input_mode</code> 列（<code>NOT NULL DEFAULT &#39;audio&#39;</code>）</td><td style="text-align:left;">无影响（历史行自动为 <code>audio</code>）</td><td style="text-align:left;">无影响</td><td style="text-align:left;">生效</td></tr><tr><td style="text-align:left;"><code>ControlMessage</code> / 上行新增 <code>ANSWER</code></td><td style="text-align:left;">无影响（不发）</td><td style="text-align:left;">无影响（不发）</td><td style="text-align:left;">生效</td></tr><tr><td style="text-align:left;">回合执行 <code>ConversationTurnRunner</code></td><td style="text-align:left;">✅ <strong>已完成（P0-2），本次直接复用</strong>；V2 仅新增 <code>SubmitResult</code>（<code>ACCEPTED</code>/<code>BUSY</code>/<code>REJECTED</code>）与 <code>submitOpening()</code> 共享串行化状态</td><td style="text-align:left;">同左</td><td style="text-align:left;">生效（只加提交治理，不重构）</td></tr><tr><td style="text-align:left;">「计费起算 / MOCK 首问」触发点迁移</td><td style="text-align:left;">无影响（仍走 ASR 就绪）</td><td style="text-align:left;">无影响（audio 分支不变）</td><td style="text-align:left;">生效（改为<strong>会话就绪</strong>触发：<code>start()</code> 成功后 <code>markSessionReady()</code>）</td></tr><tr><td style="text-align:left;">新增 <code>/ws/asr</code> 通道</td><td style="text-align:left;">无影响（不连）</td><td style="text-align:left;">无影响（V1 不用）</td><td style="text-align:left;">✅ 已交付</td></tr><tr><td style="text-align:left;"><code>TicketHandshakeInterceptor</code> 拆分鉴权与余额校验</td><td style="text-align:left;">无影响（<code>/ws/audio</code> 的余额准入保留）</td><td style="text-align:left;">无影响</td><td style="text-align:left;">✅ 已交付（新增轻量拦截器，原拦截器一行未改；余额准入仍在 <code>/ws/audio</code>）</td></tr><tr><td style="text-align:left;"><code>AsrResultMessage</code> 新增 <code>final</code> 字段（Step 2）</td><td style="text-align:left;">无影响：老客户端逐字段校验只认 <code>id</code>/<code>text</code>，多余字段被忽略</td><td style="text-align:left;">同左</td><td style="text-align:left;">生效（语音输入靠它分「中间态/定稿」）</td></tr><tr><td style="text-align:left;"><code>ConversationTurnRunner</code> 触发日志改只记长度（§12.6 的真缺陷修复）</td><td style="text-align:left;"><strong>行为变化（正面的）</strong>：日志里不再有用户作答原文</td><td style="text-align:left;">同左</td><td style="text-align:left;">生效（INFO 级脱敏，生产同样受益）</td></tr><tr><td style="text-align:left;"><code>AsrStreamRunner</code> 从 <code>AudioStreamSession</code> 抽取</td><td style="text-align:left;">✅ <strong>已完成（P0-1）</strong>，行为等价已回归；本次是&quot;复用&quot;而非&quot;抽取&quot;</td><td style="text-align:left;">同左</td><td style="text-align:left;">不适用</td></tr></tbody></table><blockquote><p><strong>矩阵状态（2026-09-22）</strong>：<code>input</code> 参数 / V26 / <code>ANSWER</code> / 回合串行化 / 触发点迁移<strong>已在 Step 1 落地</strong>； <code>/ws/asr</code> 与「拦截器拆分」<strong>尚未动工</strong>（Step 2）。</p></blockquote><h3 id="_5-2-回归验证清单-按客户端" tabindex="-1"><a class="header-anchor" href="#_5-2-回归验证清单-按客户端"><span>5.2 回归验证清单（按客户端）</span></a></h3><p><strong>PC 正式面试（<code>pass-up.frontend</code>，必须先跑）</strong></p><ol><li>共享会议音频 → 开始监听 → 转写气泡按 <code>ASR_RESULT</code> 生成、AI 回答插在对应转写之后（<code>replyId</code> 配对正确）。</li><li>静默 ≥ 60 秒：连接不被误杀（心跳生效），期间无音频、无转写。</li><li>暂停 → <code>PAUSED</code> → 继续监听（带 <code>resume</code>）→ 上下文与转写不丢、不重复开场。</li><li>余额耗尽 / 心跳超时 / ASR 故障三条中断路径的提示与关闭行为与改造前一致 （其中 <code>ASR_UNAVAILABLE</code> 的既有漂移见 §2.3，可顺带修，但不要混在本次改动里）。</li><li>结束监听 → <code>interview</code> 记录落库、历史列表可见。</li></ol><p><strong>小程序 V1</strong>：同上（语音整场跑通）。</p><p><strong>小程序 V2</strong>：建连即收到首问（<code>input=text</code>）、发 <code>ANSWER</code> 收到流式追问、 心跳维持 60 秒判据、暂停/恢复后仍是 text 模式、识别通道独立开关且失败不影响会话。</p><h2 id="_6-改动清单-文件级" tabindex="-1"><a class="header-anchor" href="#_6-改动清单-文件级"><span>6. 改动清单（文件级）</span></a></h2><blockquote><p>标记：✅ = Step 1 已交付；⬜ = 尚未动工（Step 2）。</p></blockquote><p>新增：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>modules/realtime/InterviewInputMode.java                    # ✅ 枚举 + from() 严格解析（+ converter/InterviewInputModeConverter.java）</span></span>
<span class="line"><span>src/main/resources/db/migration/V26__interview_input_mode.sql   # ✅ input_mode NOT NULL DEFAULT &#39;audio&#39;</span></span>
<span class="line"><span>infrastructure/websocket/AsrWebSocketHandler.java           # ✅ /ws/asr 入口</span></span>
<span class="line"><span>infrastructure/websocket/AsrHandshakeInterceptor.java       # ✅ 只校验 ticket（复用 WebSocketTicketService）</span></span>
<span class="line"><span>modules/realtime/asr/AsrStreamingService.java               # ✅ 识别连接登记 + 单用户并发闸门 + 错误语义 + 建连指标</span></span>
<span class="line"><span>modules/realtime/asr/AsrStreamSession.java                  # ✅ 一次识别的运行单元（含时长兜底计时器 + 时长/音频量指标）</span></span>
<span class="line"><span>modules/realtime/asr/AsrConnectionQuota.java                # ✅ 每日识别次数配额（Step 3；Redis 计数、失败开放）</span></span>
<span class="line"><span>modules/realtime/session/AsrStreamRunner.java               # ✅ 已就绪（P0-1）：ASR 消费/重连循环；本方案只复用，不重写</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>修改：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>infrastructure/websocket/TicketHandshakeInterceptor.java    # ✅ 透传 input 参数</span></span>
<span class="line"><span>modules/realtime/model/ControlMessage.java                  # ✅ 新增 TYPE_ANSWER + text 字段</span></span>
<span class="line"><span>modules/realtime/session/AudioStreamSession.java            # ✅ ANSWER 分支 + inputMode + text 模式不启 ASR + markSessionReady()</span></span>
<span class="line"><span>modules/realtime/session/ConversationTurnRunner.java        # ✅ SubmitResult 三态 + 回合串行化（ACCEPTED/BUSY/REJECTED）</span></span>
<span class="line"><span>modules/realtime/streaming/AudioStreamingService.java       # ✅ inputMode 传导（查询参数 → InterviewContext）+ 重连沿用</span></span>
<span class="line"><span>modules/realtime/interview/InterviewService.java            # ✅ createPending / InterviewContext 增加 inputMode + 严格校验组合</span></span>
<span class="line"><span>modules/realtime/interview/InterviewPrepareService.java     # ✅ ConnectionRequest 增加 inputMode + 组合校验 + 落库</span></span>
<span class="line"><span>modules/realtime/interview/entity/Interview.java            # ✅ input_mode 列</span></span>
<span class="line"><span>modules/realtime/session/LogicalSessionRebuilder.java       # ✅ 从 DB 还原 inputMode</span></span>
<span class="line"><span>infrastructure/websocket/WebSocketConfig.java               # ✅ 注册 /ws/asr</span></span>
<span class="line"><span>modules/realtime/model/AsrResultMessage.java                # ✅ 新增 final 字段（两条通道都填，见 §4.2.2 的实现修正）</span></span>
<span class="line"><span>modules/realtime/config/RealtimeProperties.java             # ✅ 新增 asr.session-max-duration-ms / max-connections-per-user / daily-sessions-per-user</span></span>
<span class="line"><span>common/util/LogTextUtils.java                               # ✅ 新增 describe()：用户内容只记长度（Step 3 日志脱敏）</span></span>
<span class="line"><span>modules/realtime/lifecycle/InterviewStartedEvent.java       # ✅ 带 inputMode（Step 3 埋点口径）</span></span>
<span class="line"><span>modules/realtime/lifecycle/InterviewEndedEvent.java         # ✅ 带 inputMode（同上）</span></span>
<span class="line"><span>modules/realtime/analytics/InterviewAnalyticsEventHandler.java # ✅ props 增 input_mode（同上）</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><blockquote><p><strong>与方案原文的两处实现差异（不要照原文找文件）</strong>：</p><ol><li><strong><code>InterviewSessionState</code> 不再单独存 <code>inputMode</code> 字段</strong> —— 它委托给持有的 <code>InterviewContext</code> （<code>getInputMode()</code>）。理由是避免&quot;两个地方都能改&quot;：重连时一旦漏同步，text 会话会被还原成 audio 且用户无感知。</li><li><strong><code>AudioStreamSessionFactory</code> 无需改动</strong> —— <code>inputMode</code> 随 <code>InterviewContext</code> 一起传进来，工厂不参与。</li></ol><p>另外两端协议文件也已同步（详见 §9 的表）。</p></blockquote><p>可选的后续治理（不在本次范围）：<code>AudioStreamSession</code> 改名为 <code>InterviewStreamSession</code>（现在它要承载文本，名字已名不副实）， 但改名会牵动大量引用，建议等 V2 稳定后单独一次提交。</p><hr><h2 id="_7-风险与代价" tabindex="-1"><a class="header-anchor" href="#_7-风险与代价"><span>7. 风险与代价</span></a></h2><table><thead><tr><th style="text-align:left;">风险</th><th style="text-align:left;">说明</th><th style="text-align:left;">对策</th></tr></thead><tbody><tr><td style="text-align:left;"><strong>波及 PC 正式面试</strong>（本次最大风险）</td><td style="text-align:left;">两条前端共用 <code>/ws/audio</code>，改的是会话内部（回合抽取、参数透传、触发点），一旦写歪就会同时影响 ASSIST</td><td style="text-align:left;">所有变化都收在 <code>input=text</code> / 新通道分支里，<code>audio</code> 路径按「等价改写」标准验收；PC 端零代码改动 + §5.2 的 5 条回归必须先跑</td></tr><tr><td style="text-align:left;">双连接</td><td style="text-align:left;">移动网络下同时维持会话连接 + 识别连接</td><td style="text-align:left;">识别连接按需建立、用完即关，不留空闲长连</td></tr><tr><td style="text-align:left;"><strong><code>AsrStreamRunner</code> 跨场景复用风险</strong></td><td style="text-align:left;"><code>/ws/audio</code> 已在用它，V2 新增 <code>/ws/asr</code> 后它成为<strong>两个消费者共用的公共件</strong></td><td style="text-align:left;">已实测核对（见 §4.2.1）：Runner 不依赖 <code>AudioStreamSession</code> / <code>InterviewSessionState</code> / <code>CleanupReason</code> / <code>SessionRegistry</code> / 计费，只吃 PCM、只吐 <code>AsrEvent</code>。新增消费者时<strong>保持这条边界</strong>，不要为 <code>/ws/asr</code> 往 Runner 里加面试会话语义</td></tr><tr><td style="text-align:left;">新成本入口</td><td style="text-align:left;">识别通道可被脚本刷（豆包按音频时长计费）</td><td style="text-align:left;">ticket 一次性鉴权 + 单用户并发 1 + 单次时长上限 + 指标观测</td></tr><tr><td style="text-align:left;">会话起点迁移漏改</td><td style="text-align:left;">text 模式不启 ASR，若忘迁「起算 / 首问」触发点，会既不扣费也不开场</td><td style="text-align:left;">§4.1.4 的两处触发点必须与 <code>input</code> 参数同一次提交完成</td></tr><tr><td style="text-align:left;">计费口径</td><td style="text-align:left;">文本模式仍按会话时长计费，用户静默思考照样扣费</td><td style="text-align:left;">属产品决策，需与产品确认是否引入「无操作自动暂停」（现有暂停机制可直接复用）</td></tr></tbody></table><h2 id="_8-待确认问题" tabindex="-1"><a class="header-anchor" href="#_8-待确认问题"><span>8. 待确认问题</span></a></h2><blockquote><p><strong>分两类，别让后置项阻塞开工</strong>：</p><table><thead><tr><th style="text-align:left;">类别</th><th style="text-align:left;">事项</th><th style="text-align:left;">状态</th></tr></thead><tbody><tr><td style="text-align:left;"><strong>Step 1 前必须确认</strong></td><td style="text-align:left;"><code>input</code> 参数命名（<code>input=audio|text</code>）</td><td style="text-align:left;">倾向已定：<code>input</code></td></tr><tr><td style="text-align:left;"></td><td style="text-align:left;">超长回答：拒绝 or 截断</td><td style="text-align:left;">已定为<strong>拒绝</strong>（截断会让 AI 收到残缺语义）</td></tr><tr><td style="text-align:left;"></td><td style="text-align:left;"><code>ASSIST + input=text</code> 是否拒绝</td><td style="text-align:left;">已定为<strong>拒绝</strong>（非法参数当场暴露，不静默降级）</td></tr><tr><td style="text-align:left;"><strong>可边实施边定</strong></td><td style="text-align:left;"><code>source</code> 是否埋点 / 埋点字段</td><td style="text-align:left;">后置（Step 3）</td></tr><tr><td style="text-align:left;"></td><td style="text-align:left;"><code>/ws/asr</code> 单次时长上限</td><td style="text-align:left;">✅ 已定：<strong>120 秒</strong>（<code>realtime.asr.session-max-duration-ms</code>，前端录音 60 秒）</td></tr><tr><td style="text-align:left;"></td><td style="text-align:left;">单用户并发识别连接数</td><td style="text-align:left;">✅ 已定：<strong>1</strong>（<code>realtime.asr.max-connections-per-user</code>，超限拒绝而非顶号）</td></tr><tr><td style="text-align:left;"></td><td style="text-align:left;">是否引入&quot;无操作自动暂停&quot;</td><td style="text-align:left;">后置（产品决策）</td></tr><tr><td style="text-align:left;"></td><td style="text-align:left;"><strong>文本模式「失联 = 结束」是否改成「失联 = 暂停」，或给 text 模式更长的失联阈值</strong></td><td style="text-align:left;">✅ 已定（2026-09-22）：<strong>保持「失联 = 结束」</strong>，文字作答阈值放宽到 <strong>180 秒</strong>（见 §4.1.6 与 §8-8）</td></tr><tr><td style="text-align:left;"></td><td style="text-align:left;"><strong>失联结束是否单独记一个 <code>InterviewEndReason</code></strong></td><td style="text-align:left;">待产品/数据拍板（见 §8-9）：现状记成 <code>USER_ENDED</code>，埋点分不出&quot;失联&quot;与&quot;主动结束&quot;</td></tr><tr><td style="text-align:left;"></td><td style="text-align:left;"><code>/ws/asr</code> 是否给其他页面复用</td><td style="text-align:left;">后置（决定 ticket 何时加 <code>scene/purpose</code>）</td></tr></tbody></table><p>上面标&quot;已定&quot;的三项实际上已是<strong>定稿约束</strong>（见 §4.1.1），保留在此仅为记录决策过程。</p></blockquote><ol><li><p><strong><code>input</code> 参数命名</strong>：<code>input=audio|text</code> 还是 <code>answerMode</code> / <code>inputMode</code>？（倾向 <code>input</code>，短且与 <code>mode</code> 不撞）</p></li><li><p><s><strong>文本作答是否落 <code>source</code>（voice / text）</strong>：建议只做埋点，不加列。</s> ✅ 已定（Step 3）：<strong>不加列，且后端不做这个口径</strong> —— 作答来源只有前端知道 （用户打的字与「点话筒识别后填进输入框」的字，到后端都是同一条 <code>ANSWER</code>；后端也刻意不让前端多传字段， 见 §4.1.3：多一个未知字段会把整条作答丢成解析失败）。该口径由<strong>前端埋点</strong>承担； 后端能提供的是 <code>input_mode</code>（这条会话是 V1 语音形态还是 V2 文字形态，已进 <code>interview_session_started/ended</code>）。</p></li><li><p><s><strong>单次识别时长上限</strong>、单用户并发识别连接数上限的取值。</s> ✅ 已定（Step 2）：120 秒 / 每用户 1 条；两者都在 <code>RealtimeProperties.Asr</code> 里，默认值有 <code>RealtimePropertiesTest</code> 钉住。</p></li><li><p><strong>是否需要「无操作自动暂停」</strong>：文本模式下用户思考/离开都不会断连，按现有规则会一直扣时长； 若产品要求，可复用 <code>PAUSE</code> 链路由前端在无操作 N 分钟后主动暂停。</p></li><li><p><strong>识别通道是否给其他页面复用</strong>（AI 问答输入框、简历诊断）：若是，ticket 与拦截器要按「与面试无关」的通用能力设计， §4.2.3 的拆分就从「最小改法」升级为必做。</p></li><li><p><strong>超长回答的处理</strong>：截断还是拒绝（建议拒绝 + 明确提示，避免 AI 收到不完整语义）。</p></li><li><p><strong>正式面试（<code>ASSIST</code>）是否永久不支持文本作答</strong>：本方案按「拒绝」处理，若产品后续要做 「PC 端手动打字提问」，需要同时决定它是否也算面试时长、是否复用同一会话。</p></li><li><p><s><strong>文本模式的「失联」语义</strong></s> ✅ <strong>已定稿（2026-09-22）</strong>：<strong>不改「失联 → 暂停」</strong>，保持&quot;失联 = 结束&quot;； <strong>只把文字作答的失联阈值放宽到 180 秒</strong>（<code>realtime.text-heartbeat-timeout-ms</code>；语音仍 60 秒）， 作用点 <code>AudioStreamSession#effectiveTimeoutMillis()</code>，<strong>不新增状态、不做&quot;失联可恢复&quot;</strong>。 不变量：<strong>「思考」永远不等于「暂停」</strong>，不做&quot;无操作自动暂停&quot;，「主动暂停」只由显式 <code>PAUSE</code> 触发。 理由与边界见 §4.1.6。</p></li><li><p><strong>失联结束是否单独记一个 <code>InterviewEndReason</code>（新，2026-09-22 联调后提出）</strong>： 现状 <code>toEndReason()</code> 只把 ASR 故障映射成 <code>ASR_ERROR</code>，<strong>其余（心跳超时 / 计费终止 / 顶号…）全记 <code>USER_ENDED</code></strong> —— 于是&quot;切后台失联&quot;与&quot;用户主动挂断&quot;在落库与埋点 （<code>interview_session_ended</code> 的 <code>end_reason</code>）里同码，<strong>分不出来</strong>。 与&quot;语义干净&quot;的诉求直接冲突，而 V2 会显著放大这类样本（文字模式更容易切后台）。 建议新增 <code>DISCONNECTED</code>（心跳超时/顶号等连接层原因）并在埋点区分。 <strong>代价</strong>：历史数据口径分界（此前 <code>USER_ENDED</code> 里混着失联），统计口径需说明；故需数据/产品确认后实施。</p></li></ol><h2 id="_9-与前端方案笔记的差异-✅-已于-2026-09-22-同步修订" tabindex="-1"><a class="header-anchor" href="#_9-与前端方案笔记的差异-✅-已于-2026-09-22-同步修订"><span>9. 与前端方案笔记的差异（✅ 已于 2026-09-22 同步修订）</span></a></h2><blockquote><p>前端有两份相关材料：小程序方案笔记《模拟面试第二版-文字作答与实时语音输入方案》， 以及 PC 端既有实现 <code>pass-up.frontend/apps/user/src/features/realtime-voice</code>（无独立笔记）。</p></blockquote><p>小程序笔记第 6.1 节把「提交回答 + 收追问」建议为 「对齐 <code>qnaApi.streamChat</code> 的流式 HTTP 用法」。按本方案应修正为：</p><blockquote><p>文本作答走<strong>面试会话 WebSocket 的 <code>ANSWER</code> 上行消息</strong>，下行事件仍是 <code>THINKING</code> / <code>LLM_REPLY</code>； 不使用 HTTP 流式接口 —— 会话计时、心跳、暂停恢复、顶号、余额推送都在同一条 WS 上， 另开 HTTP 会把一场面试拆成两条通道，前端要同时维护两者并额外校验「会话是否仍在进行」。</p></blockquote><p>前端笔记第 6.2 / 5.x 的「识别通道」部分与本方案 §4.2 一致（独立通道、<code>ASR_RESULT</code> 的 id 语义、 点话筒才建连），无需修改；但要补两点：</p><ul><li><strong>建连 URL 用 <code>input=text</code></strong>（且 <code>mode=mock</code>）。注意别和页面入口的 <code>?mode=text</code> 混了 —— 后者是前端自己的&quot;页面形态&quot;参数，<strong>不能透传给后端</strong>（<code>InterviewMode.from(&#39;text&#39;)</code> 会静默回退成 <code>ASSIST</code>， 然后被 <code>assist + text</code> 校验拒掉）。</li><li><strong>进度口径以本文档为准</strong>：前端笔记曾写「方案 A 的后端侧已就绪」——<strong>不成立</strong>。 已就绪的只有可复用的 <code>AsrStreamRunner</code>，<code>/ws/asr</code> 通道本体属 §11 的 <strong>Step 2，尚未动工</strong>； 前端 <code>AsrTransport</code> 继续留桩（该处措辞已于 2026-09-22 同步修订）。</li></ul><blockquote><p><strong>协议事实只有一个来源：本文档 + 两端协议文件。</strong> 前端笔记只描述&quot;前端怎么消费/怎么实现&quot;， 不再维护第二份协议定义 —— 这轮已经在这里发生过一次漂移（前端说&quot;后端已就绪&quot;、这里说&quot;Step 2 未开始&quot;）。</p></blockquote><p>另外三处「文档/协议真相源」已一并更新：</p><table><thead><tr><th style="text-align:left;">位置</th><th style="text-align:left;">写清的内容</th><th style="text-align:left;">状态</th></tr></thead><tbody><tr><td style="text-align:left;"><code>pass-up.backend/AGENTS.md</code> §3.6 / §3.6.1</td><td style="text-align:left;"><code>input</code> 参数、<code>ANSWER</code> 上行、4 个错误码、回合串行化语义</td><td style="text-align:left;">✅ 已更新</td></tr><tr><td style="text-align:left;">小程序 <code>src/features/interview/ws.ts</code></td><td style="text-align:left;"><code>InterviewInputMode</code> + <code>InterviewConfig.input</code> + URL <code>&amp;input=</code>；<code>sendAnswer(text)</code>；4 个错误码（含文案，且<strong>不进 <code>FATAL_CODES</code></strong>，因为它们不终止会话）</td><td style="text-align:left;">✅ 已登记</td></tr><tr><td style="text-align:left;">PC <code>apps/user</code> 的 <code>hooks/protocol.ts</code></td><td style="text-align:left;">只扩了错误码白名单（PC 是 audio 会话、收不到这 4 个码；登记只为白名单完整性）；<strong><code>ANSWER</code> 不进 <code>CLIENT_CONTROL</code></strong> —— PC 永远不发它</td><td style="text-align:left;">✅ 已更新</td></tr><tr><td style="text-align:left;"><code>/ws/asr</code> 的下行协议</td><td style="text-align:left;">沿用 <code>ASR_RESULT</code>，<strong>不新增消息类型</strong>，但<strong>新增 <code>final</code> 字段</strong>（协议只增不减，老客户端忽略）；错误码与关闭码见 §4.2.2 的表</td><td style="text-align:left;">✅ 后端已交付；<strong>前端在实现 <code>AsrTransport</code> 时登记</strong>（别塞进 <code>ws.ts</code> 的面试错误码空间）</td></tr></tbody></table><p>PC 端（<code>features/realtime-voice</code>）本身<strong>本次零改动</strong>，只需在 PR 描述与回归清单里显式声明「正式面试未受影响」（见 §5.2）。</p><h2 id="_10-备选方案与「为什么不新写一套」" tabindex="-1"><a class="header-anchor" href="#_10-备选方案与「为什么不新写一套」"><span>10. 备选方案与「为什么不新写一套」</span></a></h2><h3 id="_10-1-识别留在面试会话内-方案-b-的适用条件" tabindex="-1"><a class="header-anchor" href="#_10-1-识别留在面试会话内-方案-b-的适用条件"><span>10.1 识别留在面试会话内（方案 B）的适用条件</span></a></h3><p>如果短期内不想新增端点，B 也能满足功能，但需要接受并处理以下改造：</p><ol><li><code>input=text</code> 时，ASR 变为<strong>按需启停</strong>：新增 <code>{&quot;type&quot;:&quot;ASR_START&quot;}</code> / <code>{&quot;type&quot;:&quot;ASR_STOP&quot;}</code>， 识别结果只下行不提交回合（即「不自动触发 LLM」）。</li><li><code>consumeLoop</code> 从「长跑重连」改为「一次识别片段内重连」：启停、重连预算、<code>asrUnavailable</code> 标记 都要按片段重定义，且必须在 text 模式下把「重连耗尽 ⇒ 4003 结束面试」改成「只通知、不结束」。</li><li>同一个 <code>ASR_RESULT</code> 在两种模式下含义不同（自动作答 vs 输入辅助），协议语义开始漂移。</li></ol><p>判断标准：<strong>只要 V2 的语音输入要在面试中使用，就必须处理第 2 点</strong>（否则用户的识别故障会终止面试）； 既然要处理，就没有理由不把它挪到独立通道（第 2 点的复杂度 &gt; 新端点的复杂度）。</p><h3 id="_10-2-专门为-v2-新写一条会话链路-——-不建议" tabindex="-1"><a class="header-anchor" href="#_10-2-专门为-v2-新写一条会话链路-——-不建议"><span>10.2 专门为 V2 新写一条会话链路？—— 不建议</span></a></h3><p>先分清「专门写一套」的三档，代价差别很大：</p><table><thead><tr><th style="text-align:left;">档位</th><th style="text-align:left;">做法</th><th style="text-align:left;">代价</th></tr></thead><tbody><tr><td style="text-align:left;">① 新端点 + 新 session 类，但注册进同一个 <code>AudioStreamingService</code></td><td style="text-align:left;">需要先抽 <code>InterviewSession</code> 接口（<code>isAlive</code> / <code>connectionIdleMillis</code> / <code>effectiveTimeoutMillis</code> / <code>isHeartbeatCapable</code> / <code>markClosing</code> / <code>markPaused</code> / <code>isPaused</code> / <code>getState</code> / <code>onClose</code> / <code>receiveText</code> / <code>sendMessage</code> ≈ 12 个方法），再把回合执行抽成公共件</td><td style="text-align:left;">diff ≈ 复用方案的 2~3 倍，且两条链路都要回归</td></tr><tr><td style="text-align:left;">② 完全独立（新 handler + 新 service + 新注册表）</td><td style="text-align:left;">除 ① 的开销外，还必须同时改 4 处「按物理连接定位会话」的调用点</td><td style="text-align:left;"><strong>最大，且有隐蔽故障</strong>，见下</td></tr><tr><td style="text-align:left;">③ 纯 HTTP（SSE 文本对话，<code>/api/qna/chat</code> 同构）</td><td style="text-align:left;">等于放弃「实时会话」这一整套（心跳、暂停恢复、顶号、<code>TIME_UPDATE</code>）</td><td style="text-align:left;">只有产品语义也变成「异步聊天」时才成立</td></tr></tbody></table><p><strong>② 为什么危险</strong>：这套「按连接定位会话」的调用点散在三个类里，全部直接依赖 <code>AudioStreamingService</code> 的 <code>streamSessions</code> 注册表 ——</p><div class="language-java line-numbers-mode" data-highlighter="shiki" data-ext="java" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-java"><span class="line"><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">// InterviewBillingScheduler#validateSessionState（每 10 秒跑一次）</span></span>
<span class="line"><span style="--shiki-light:#C18401;--shiki-dark:#E5C07B;">String</span><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;"> activeConnectionId </span><span style="--shiki-light:#383A42;--shiki-dark:#56B6C2;">=</span><span style="--shiki-light:#E45649;--shiki-dark:#E5C07B;"> streamingService</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">resolveActiveConnectionId</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">(sessionId);</span></span>
<span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">if</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;"> (activeConnectionId </span><span style="--shiki-light:#383A42;--shiki-dark:#56B6C2;">==</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;"> null</span><span style="--shiki-light:#383A42;--shiki-dark:#56B6C2;"> ||</span><span style="--shiki-light:#383A42;--shiki-dark:#56B6C2;"> !</span><span style="--shiki-light:#E45649;--shiki-dark:#E5C07B;">streamingService</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">hasActiveStreamSession</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">(activeConnectionId)</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">) {</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E5C07B;">    billingService</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">stopBilling</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">(userId, sessionId);</span><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">   // ← 文本会话若不在注册表里，会被这里静默停掉计费</span></span>
<span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">    return</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;"> false</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">;</span></span>
<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">}</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><table><thead><tr><th style="text-align:left;">调用点</th><th style="text-align:left;">依赖</th><th style="text-align:left;">漏改后果</th></tr></thead><tbody><tr><td style="text-align:left;"><code>InterviewBillingScheduler#validateSessionState</code></td><td style="text-align:left;"><code>isConnectionLost</code> / <code>resolveActiveConnectionId</code> / <code>hasActiveStreamSession</code></td><td style="text-align:left;">文本面试<strong>静默停止扣费</strong>（用户白嫖），<code>TIME_UPDATE</code> 也不再下发</td></tr><tr><td style="text-align:left;"><code>InterviewBillingScheduler#terminateSession</code></td><td style="text-align:left;">同上 + <code>forceCloseSession</code></td><td style="text-align:left;">余额耗尽时关不掉会话</td></tr><tr><td style="text-align:left;"><code>WsHeartbeatScheduler</code> → <code>evictDeadSessions</code></td><td style="text-align:left;">只遍历 <code>streamSessions</code></td><td style="text-align:left;">文本会话的死连接不会被回收</td></tr><tr><td style="text-align:left;"><code>AudioStreamingService#onOpen</code>（新连接顶号）</td><td style="text-align:left;"><code>forceTerminateActiveSession</code> → <code>resolveActiveConnectionId</code> → <code>streamSessions.get</code></td><td style="text-align:left;">顶号失效，同一用户两条会话并行计费</td></tr><tr><td style="text-align:left;"><code>evictExpiredPausedSessions</code> / <code>LogicalSessionRebuilder</code></td><td style="text-align:left;">按 <code>interview.session_id</code> 维护逻辑会话</td><td style="text-align:left;">暂停回收与跨进程重建对文本会话失效</td></tr></tbody></table><p>漏改任一处都<strong>不是编译错误</strong>，而是「不扣费 / 不回收 / 顶号失效」这类要上线后才发现的故障。 另外 <code>/ws/audio</code> 这个<strong>路径不能改</strong>（2026-09-22 再次确认；评审里&quot;改名 <code>/ws/interview</code>&quot;的提议<strong>已否决</strong>）： PC 端 <code>useVoiceWebSocket</code> 与小程序 <code>ws.ts</code> 都把它写死了，换路径等于两个前端同时发版。 名字现在确实名不副实（它承载的是&quot;面试会话&quot;而非&quot;音频&quot;），但正确做法是<strong>在文档与代码注释里写明它是面试会话通道</strong>； 将来真要改，也必须&quot;新旧双路径并存 + 客户端版本判定&quot;单独开一次改动。</p><p>由此得到判据：</p><blockquote><p><strong>判断标准不是「改动大小」，而是「会话语义是否相同」。</strong> 语义相同（有时长、心跳、暂停恢复、顶号、落库）就复用同一条会话与注册表，输入源不同只是分支； 语义真的不同（异步、不需要保持连接、按条计费）时才值得另起一套 —— 而那种情况下连 WebSocket 都不该用（见档位 ③）。</p></blockquote><h3 id="_10-3-本方案实际采用的结构-不是-建议再抽一次" tabindex="-1"><a class="header-anchor" href="#_10-3-本方案实际采用的结构-不是-建议再抽一次"><span>10.3 本方案实际采用的结构（不是&quot;建议再抽一次&quot;）</span></a></h3><blockquote><p><strong>这一节原写作&quot;折中建议&quot;，但那些抽取动作已经完成（P0-1 / P0-2），因此改写为结构说明</strong> —— 免得后续读者以为 V2 Step 1 还要再做一次回合抽取。</p></blockquote><p>本方案最终采用：</p><ul><li><code>/ws/audio</code> 继续承载<strong>面试会话</strong>：不改变既有会话生命周期、计费、心跳、暂停恢复、顶号与落库的<strong>语义</strong> （代码会随参数透传与触发点迁移而动，改动标准是&quot;对用户可见行为保持不变&quot;）；</li><li><code>ConversationTurnRunner</code>（P0-2）已负责<strong>回合执行</strong>：上下文维护 + LLM 流式 + 落库 + 串行化；</li><li><code>AsrStreamRunner</code>（P0-1）已负责 <strong>ASR 消费与重连</strong>；</li><li><code>AudioStreamSession</code> 只负责<strong>连接生命周期 + 输入源适配</strong>。</li></ul><p>因此 <strong>V2 不再进行任何&quot;抽取&quot;重构</strong>，只在已有边界上增加两样东西：</p><ul><li>一个 <strong>TEXT 输入分支</strong>（<code>ANSWER</code> → <code>ConversationTurnRunner#submit</code>）；</li><li>一个 <strong><code>/ws/asr</code> 消费者</strong>（复用 <code>AsrStreamRunner</code>）。</li></ul><p>V2 对 <code>ConversationTurnRunner</code> 的唯一新增是<strong>提交治理</strong>：<code>SubmitResult submit(...)</code> （<code>ACCEPTED</code> / <code>BUSY</code> / <code>REJECTED</code>）与 <code>submitOpening()</code> 走同一把回合状态。 将来若再加第三种输入源（粘贴 / 快捷回复 / 断线补发），仍是<strong>加一个分支</strong>，而不是再写一套会话。</p><p>可选收尾（不影响功能）：<code>AudioStreamSession</code> 与 <code>AudioWebSocketHandler</code> 的名字已名不副实 （现在要承载文本），可在 V2 稳定后单独一次提交改名为 <code>InterviewStreamSession</code> / <code>InterviewWebSocketHandler</code> （<strong>路径 <code>/ws/audio</code> 保持不动</strong>）。</p>`,158),o(`blockquote`,null,[o(`p`,null,[l[1]||=o(`code`,null,`ConversationTurnRunner`,-1),l[2]||=a(` / `,-1),l[3]||=o(`code`,null,`AsrStreamRunner`,-1),l[4]||=a(` / 注册表接口化这三项抽取的动机、优先级与验收方式， 另见`,-1),i(m,{to:`/%E9%A1%B9%E7%9B%AE/%E9%9D%A2%E8%AF%95%E5%8A%A9%E6%89%8B/%E6%9E%B6%E6%9E%84%E8%AE%BE%E8%AE%A1/%E5%AE%9E%E6%97%B6%E4%BC%9A%E8%AF%9D%E9%93%BE%E8%B7%AF%E5%A4%8D%E6%9D%82%E5%BA%A6%E8%AF%84%E4%BC%B0%E4%B8%8E%E6%8B%86%E8%A7%A3%E6%B8%85%E5%8D%95.html`},{default:n(()=>[...l[0]||=[a(`《实时会话链路复杂度评估与拆解清单》`,-1)]]),_:1}),l[5]||=a(` §6 —— 本文只交代 V2 为什么需要它们。`,-1)])]),l[13]||=r(`<h2 id="_11-建议实施顺序" tabindex="-1"><a class="header-anchor" href="#_11-建议实施顺序"><span>11. 建议实施顺序</span></a></h2><blockquote><p><strong>前置基线：已完成，不再作为实施步骤。</strong></p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>P0-0 → P0-1 / P0-2 / P0-4 → P0-3 → P1-1 → P1-2 → P1-3     ✅ 全部完成（含单测与真实端到端冒烟）</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>因此本节下面的「步骤零：先拆职责 + 补测试接缝」以及 P0/P1 各项<strong>属于历史记录</strong>； <strong>Step 1 / Step 2 / Step 3 都已交付</strong>：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>Step 1  文字作答            ✅ 已交付（2026-09-22）：input=text + V26 + ANSWER + 会话就绪点迁移</span></span>
<span class="line"><span>                               + text 模式不启 ASR + 回合串行化/SubmitResult</span></span>
<span class="line"><span>Step 2  独立识别通道        ✅ 已交付（2026-09-22）：/ws/asr + 复用 AsrStreamRunner + 轻量鉴权拦截器</span></span>
<span class="line"><span>                               + 单用户并发闸门 + 单次时长上限 + 错误/关闭码语义</span></span>
<span class="line"><span>Step 3  治理                ✅ 已交付（2026-09-22）：每日识别配额（限流）、识别通道指标、</span></span>
<span class="line"><span>                               埋点口径（input_mode）、日志脱敏（只记长度）</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>各步的准入门槛与&quot;不许顺手做的事&quot;见文末《定稿与实施纪律》（§12）。 落地记录见 <strong>§12.3（Step 1）/ §12.4（Step 2）/ §12.5（Step 3）</strong> —— 改代码前先看那三节。</p></blockquote><blockquote><p>各步的价值：<strong>Step 1 独立可发布</strong>，已经交付；<strong>Step 2 是语音输入的前置</strong>，现在也齐了； <strong>Step 3 让这条新成本入口可观测、可限额</strong>。也就是说「文字作答 + 话筒当输入法」这套产品形态 在<strong>后端侧功能与治理都已完整</strong>。</p><p>当前剩下的两个缺口都在<strong>前端</strong>（后端已无阻塞）：</p><ol><li><strong>面试链路接线</strong>：建连带 <code>input=text</code>、发 <code>ANSWER</code>、消费 <code>TURN_BUSY</code> 等错误码；</li><li><strong>识别通道接线</strong>：实现 <code>AsrTransport</code>（协议见 §4.2.2 —— 含 <code>final</code> 与 5 个错误码）。</li></ol></blockquote><hr><h2 id="_12-定稿与实施纪律-2026-09-22" tabindex="-1"><a class="header-anchor" href="#_12-定稿与实施纪律-2026-09-22"><span>12. 定稿与实施纪律（2026-09-22）</span></a></h2><p><strong>本方案已定稿</strong>：架构主线不再调整，已进入实施。<strong>Step 1 已完成</strong> —— 下面 4 点里 ①②④ 已落进代码，③ 是 Step 2 的约束（届时必须遵守）：</p><table><thead><tr><th style="text-align:left;">#</th><th style="text-align:left;">位置</th><th style="text-align:left;">改成</th><th style="text-align:left;">状态</th></tr></thead><tbody><tr><td style="text-align:left;">①</td><td style="text-align:left;">§4.1.4</td><td style="text-align:left;">触发点从&quot;建连成功（<code>onOpen</code>）&quot;改为「<strong>会话就绪：<code>start()</code> 成功完成后</strong>」，并写死&quot;WS 建立成功 ≠ 面试开始&quot;</td><td style="text-align:left;">✅ 已落地（<code>markSessionReady()</code>）</td></tr><tr><td style="text-align:left;">②</td><td style="text-align:left;">§4.1.5</td><td style="text-align:left;">回合串行化<strong>全部归 <code>ConversationTurnRunner</code></strong>（新增 <code>SubmitResult submit(...)</code>）；会话里<strong>不出现</strong> <code>turnInProgress</code></td><td style="text-align:left;">✅ 已落地（<code>SubmitResult</code>）</td></tr><tr><td style="text-align:left;">③</td><td style="text-align:left;">§4.2.1</td><td style="text-align:left;">写死：<code>AsrStreamSession</code> <strong>不承载任何面试会话语义</strong></td><td style="text-align:left;">⬜ Step 2 落地时写死</td></tr><tr><td style="text-align:left;">④</td><td style="text-align:left;">§4.1.1 / §5.1</td><td style="text-align:left;"><code>interview.input_mode</code> 用 <code>NOT NULL DEFAULT &#39;audio&#39;</code>（<code>null</code>/blank → <code>AUDIO</code>；<strong>未知值抛异常</strong>），<strong>不用可空列</strong></td><td style="text-align:left;">✅ 已落地（V26）</td></tr></tbody></table><p>随本次一起收敛（均已在本文档内改写）：§4.2.3 的 ticket 用途字段只<strong>留扩展点、不实现</strong>； §4.1.3 的 <code>UserTurnInput</code> ID 语义（<code>replyId</code> → <code>inputId</code>，<strong>ID 由后端生成</strong>，前端不依赖其格式）。</p><h3 id="_12-1-conversationturnrunner-的提交契约-step-1-要新增的部分" tabindex="-1"><a class="header-anchor" href="#_12-1-conversationturnrunner-的提交契约-step-1-要新增的部分"><span>12.1 <code>ConversationTurnRunner</code> 的提交契约（Step 1 要新增的部分）</span></a></h3><p><strong>对外契约只有三态</strong>，内部原因<strong>不扩散到 <code>AudioStreamSession</code></strong>：</p><div class="language-java line-numbers-mode" data-highlighter="shiki" data-ext="java" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-java"><span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">public</span><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;"> enum</span><span style="--shiki-light:#C18401;--shiki-dark:#E5C07B;"> SubmitResult</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;"> { </span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;">ACCEPTED</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">,</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;"> BUSY</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">,</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;"> REJECTED</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;"> }</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><ul><li><code>ACCEPTED</code>：已进入回合执行；</li><li><code>BUSY</code>：<strong>已有回合在执行</strong>，本次输入不被接受（&quot;现在不行、等下可以&quot;→ 前端禁用输入框）；</li><li><code>REJECTED</code>：<strong>Runner 当前不允许创建新回合</strong>（&quot;这场面试已经不能答了&quot; → 前端走断线/结束态）。 <strong>具体原因（ENDED / PAUSED / CLOSING / CANCELLED）由 Runner 内部判定，不暴露给会话层</strong> —— 尤其 <code>paused</code> 之类语义是否等于&quot;不能提交&quot;，取决于 Runner 自己的生命周期约束，会话不需要知道。</li></ul><p>实现骨架（<strong>最终形态，2026-09-22 已落地</strong> —— 与最初设想的 <code>try/finally</code> 不同，原因见下）：</p><div class="language-java line-numbers-mode" data-highlighter="shiki" data-ext="java" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-java"><span class="line"><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">// 进入回合前先过两道闸</span></span>
<span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">if</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;"> (</span><span style="--shiki-light:#383A42;--shiki-dark:#56B6C2;">!</span><span style="--shiki-light:#E45649;--shiki-dark:#E5C07B;">accepting</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">get</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">()</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">)                                   </span><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">return</span><span style="--shiki-light:#E45649;--shiki-dark:#E5C07B;"> SubmitResult</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#E45649;--shiki-dark:#E5C07B;">REJECTED</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">;</span><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">  // 本 Runner 已取消</span></span>
<span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">if</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;"> (</span><span style="--shiki-light:#383A42;--shiki-dark:#56B6C2;">!</span><span style="--shiki-light:#E45649;--shiki-dark:#E5C07B;">turnInProgress</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">compareAndSet</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">(</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;">false</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">, </span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;">true</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">)</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">)          </span><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">return</span><span style="--shiki-light:#E45649;--shiki-dark:#E5C07B;"> SubmitResult</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#E45649;--shiki-dark:#E5C07B;">BUSY</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">;</span><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">      // 必须在进入 streamTurn 之前判断</span></span>
<span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">try</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;"> {</span></span>
<span class="line"><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">    handleUserMessage</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">(text)</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">;</span></span>
<span class="line"><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">    streamTurn</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">(inputId</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">,</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;"> buildMessages</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">())</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">;</span><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">   // 内部是异步的：交给豆包客户端后立即返回</span></span>
<span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">    return</span><span style="--shiki-light:#E45649;--shiki-dark:#E5C07B;"> SubmitResult</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#E45649;--shiki-dark:#E5C07B;">ACCEPTED</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">;</span></span>
<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">} </span><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">catch</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;"> (</span><span style="--shiki-light:#C18401;--shiki-dark:#E5C07B;">RuntimeException</span><span style="--shiki-light:#383A42;--shiki-light-font-style:inherit;--shiki-dark:#E06C75;--shiki-dark-font-style:italic;"> e</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">) {</span></span>
<span class="line"><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">    endTurn</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">()</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">;</span><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">                              // 提交本身失败（建客户端 / 提交任务抛异常）：占用必须当场释放</span></span>
<span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">    throw</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;"> e</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">;</span></span>
<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">}</span></span>
<span class="line"><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">// 占用的释放不在这里 —— 在本轮终态 onComplete / onError / onCancel（见 §4.1.5 的实现核对）</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><blockquote><ul><li>为什么必须 CAS：ASR 路径来自 ASR 事件线程、文本路径来自 WS 处理线程，两者可能并发。</li><li><strong>为什么占用必须覆盖整轮流式输出</strong>：<code>DoubaoLlmClient#streamChat</code> 是异步的（<code>executor.submit</code> 后立即返回）， &quot;<code>submit</code> 返回即释放&quot;会让用户连点直接并发两轮回合，上下文顺序被打乱。</li><li>为什么释放放在终态回调：那是&quot;这一轮真的结束了&quot;的唯一可靠信号； <code>onCancel</code>（被 <code>cancel()</code> 打断）也是终态，必须释放，否则暂停/关闭后这轮永远占着 <code>BUSY</code>。</li></ul></blockquote><blockquote><p>⚠️ <strong><code>cancel()</code> 的并发硬约束（实现级，别简化）</strong>：<code>cancel()</code> <strong>不能无条件 <code>turnInProgress.set(false)</code></strong>。 反例：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>A submit → turnInProgress=true</span></span>
<span class="line"><span>cancel()  → turnInProgress=false</span></span>
<span class="line"><span>B submit → turnInProgress=true</span></span>
<span class="line"><span>A 的终态回调 → turnInProgress=false   ← 把 B 的占用清掉了，B 还没跑完就又能被提交</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>正确做法二选一：</p><ol><li><strong>给在途回合一个身份</strong>（generation / token），只释放&quot;自己那一代&quot;的占用；</li><li>或让 <code>cancel()</code> <strong>只做取消动作、不碰占用标志</strong>，占用一律由<strong>拥有该回合的提交路径</strong>释放。</li></ol><p><strong>本次采用的是第 2 种</strong>：<code>cancel()</code> 只关 <code>accepting</code> + <code>cancelToken.cancel()</code>。 于是&quot;取消后再提交&quot;得到的是 <code>REJECTED</code>（而不是 <code>BUSY</code>）， 并从结构上排除了&quot;旧回合收尾误清新一轮占用&quot;—— 不需要额外的 generation 机制。 验收要求（下表最后两行）已由 <code>ConversationTurnRunnerTest</code> 覆盖。</p></blockquote><p><strong>Step 1 必测清单（Runner 契约，不是架构改动）—— ✅ 已全部覆盖（<code>ConversationTurnRunnerTest</code>，8 例）</strong>：</p><table><thead><tr><th style="text-align:left;">场景</th><th style="text-align:left;">期望</th></tr></thead><tbody><tr><td style="text-align:left;"><code>submit</code> → <code>submit</code>（第一轮未结束）</td><td style="text-align:left;">第二次 <code>BUSY</code>，且<strong>不落库</strong>、不发 <code>THINKING</code></td></tr><tr><td style="text-align:left;"><code>submit</code> → <code>submit</code>（第一轮已结束）</td><td style="text-align:left;">第二次 <code>ACCEPTED</code></td></tr><tr><td style="text-align:left;"><code>submit</code> 内部抛异常</td><td style="text-align:left;">状态复位，后续 <code>submit</code> 仍能 <code>ACCEPTED</code>（不能被卡死）</td></tr><tr><td style="text-align:left;"><code>opening</code> → <code>submit</code>（首问在途）</td><td style="text-align:left;"><code>BUSY</code>（两者共享同一把状态）</td></tr><tr><td style="text-align:left;"><code>submit</code> → <code>session close</code> / <code>cancel()</code></td><td style="text-align:left;">关闭后再 <code>submit</code> → <code>REJECTED</code>（而不是 <code>BUSY</code>）</td></tr><tr><td style="text-align:left;"><code>cancel()</code> 与在途回合的终态回调并发</td><td style="text-align:left;">不被二次清理（幂等），且不会把新一轮误判为 BUSY</td></tr></tbody></table><blockquote><p>&quot;本轮结束&quot;在测试里由 fake LLM 的 <code>onComplete</code> / <code>onError</code> / <code>onCancel</code> 显式驱动 —— 真实链路里这是豆包客户端的终态回调，单测必须自己模拟，否则占用永远不释放。</p></blockquote><h3 id="_12-2-一条硬约束-本次最大的风险" tabindex="-1"><a class="header-anchor" href="#_12-2-一条硬约束-本次最大的风险"><span>12.2 一条硬约束（本次最大的风险）</span></a></h3><p><strong>不要因为 V2 又开始顺手治理旧代码。</strong> 明确不在本次范围内：</p><ul><li><code>AudioStreamSession</code> 大规模改名（等 V2 稳定后单独一次提交）</li><li>Ticket 系统全面重构（只留 <code>scene/purpose</code> 扩展点）</li><li>Turn / Message 领域模型重构</li><li><code>/ws/audio</code> 协议重新设计</li><li><code>ASSIST</code> 模式改造</li><li>audio 模式的并发治理（并发隐患另开治理项）</li><li>计费系统重新设计</li><li>为 <code>/ws/asr</code> 再抽一层 <code>AsrRunner</code> 接口 / Strategy / Factory（<code>AsrStreamRunner</code> 已是公共件，够用）</li></ul><p>一句话：V2 的需求是<strong>&quot;增加两种输入方式&quot;</strong>，别让它重新变成一次实时会话架构重构。</p><h3 id="_12-3-step-1-落地记录-实现与本文档的-3-处差异-已验证项" tabindex="-1"><a class="header-anchor" href="#_12-3-step-1-落地记录-实现与本文档的-3-处差异-已验证项"><span>12.3 Step 1 落地记录（实现与本文档的 3 处差异 + 已验证项）</span></a></h3><blockquote><p>2026-09-22。<strong>改代码前先看这三条</strong>：它们不是&quot;方案里的另一种写法&quot;，而是已经跑在代码里的最终形态， 照方案原文的字面写法改回去会引入缺陷。</p></blockquote><table><thead><tr><th style="text-align:left;">#</th><th style="text-align:left;">本文档原文</th><th style="text-align:left;">实现（最终形态）</th><th style="text-align:left;">为什么</th></tr></thead><tbody><tr><td style="text-align:left;">1</td><td style="text-align:left;">§4.1.5 / §12.1 按&quot;<code>submit</code> 同步阻塞、占用在 <code>submit</code> 的 <code>finally</code> 释放&quot;写</td><td style="text-align:left;"><strong>占用覆盖整轮流式输出</strong>：进入回合前 CAS 取得，本轮终态（<code>onComplete</code> / <code>onError</code> / <code>onCancel</code>）释放，加上&quot;提交本身失败&quot;的兜底释放；<code>cancel()</code> 只关 <code>accepting</code>、不碰占用</td><td style="text-align:left;"><code>DoubaoLlmClient#streamChat</code> 是异步的（<code>executor.submit</code> 后立即返回），按字面写会导致&quot;连点即并发两轮&quot;；<code>cancel()</code> 不碰占用则从结构上排除了误清</td></tr><tr><td style="text-align:left;">2</td><td style="text-align:left;">§4.1.1 传导链写 <code>… → InterviewContext（新增字段）→ InterviewSessionState（新增字段）</code></td><td style="text-align:left;"><strong>只在 <code>InterviewContext</code> 上存一份</strong>；<code>InterviewSessionState#getInputMode()</code> 委托给持有的上下文；<code>AudioStreamSessionFactory</code> 无需改动</td><td style="text-align:left;">避免&quot;两处都能改&quot;：重连时漏同步会让 text 会话被还原成 audio，且用户无感知（单一来源优先于形态对称）</td></tr><tr><td style="text-align:left;">3</td><td style="text-align:left;">§4.1.1 只写了&quot;DB 用小写 <code>DEFAULT &#39;audio&#39;</code>、Java 用枚举&quot;</td><td style="text-align:left;">落库经 <strong><code>AttributeConverter</code></strong>（<code>InterviewInputMode.getCode()</code> 小写 ↔ <code>InterviewInputMode.from()</code>），<strong>不用 <code>@Enumerated(EnumType.STRING)</code></strong></td><td style="text-align:left;"><code>@Enumerated</code> 会写枚举名 <code>AUDIO</code>，与 V26 的 <code>DEFAULT &#39;audio&#39;</code> 和历史行的小写值不一致 —— 读历史行会直接解析失败</td></tr></tbody></table><p><strong>几处方案里没细写、实现时补齐的边界</strong>（都属&quot;方案边界&quot;而非架构变更）：</p><ul><li>空作答回 <code>INVALID_ANSWER</code>（<strong>明确报错</strong>，不静默丢弃）；</li><li><code>ANSWER</code> 解析失败 / 未知 type：只记 debug 日志并忽略，<strong>不改变会话状态</strong>（与既有控制指令一致）；</li><li>text 模式收到音频帧：忽略 + <strong>只 warn 一次</strong>（音频是高频输入，逐帧 warn 会淹没日志）；</li><li>text 模式下 <code>AudioStreamSession#receiveAudio</code> 直接返回，不把帧喂给&quot;创建但未启动&quot;的 ASR 运行期。</li></ul><p><strong>已验证（2026-09-22）</strong>：</p><ul><li>全量测试 <strong>182 通过 / 0 失败</strong>，含 <code>ConversationTurnRunnerTest</code>(8)、text 模式会话行为(9)、 <code>InterviewInputMode</code> 解析(5)、建连参数校验(3)，以及既有 audio 基线（ASR 运行期 / 回合触发 / 心跳 / 长尾收尾）；</li><li>V26 与实体映射：在独立库上跑完整 Flyway 迁移链 + <code>ddl-auto=validate</code> + 真实 WebSocket 端到端用例， 产出列为 <code>character varying(16) NOT NULL DEFAULT &#39;audio&#39;</code>；</li><li>两端协议登记已落地（见 §9 的表）。</li></ul><h3 id="_12-4-step-2-落地记录-ws-asr-实现与本文档的-3-处差异-已验证项" tabindex="-1"><a class="header-anchor" href="#_12-4-step-2-落地记录-ws-asr-实现与本文档的-3-处差异-已验证项"><span>12.4 Step 2 落地记录（<code>/ws/asr</code> 实现与本文档的 3 处差异 + 已验证项）</span></a></h3><blockquote><p>2026-09-22。<strong>改代码前先看这三条。</strong></p></blockquote><table><thead><tr><th style="text-align:left;">#</th><th style="text-align:left;">本文档原文</th><th style="text-align:left;">实现（最终形态）</th><th style="text-align:left;">为什么</th></tr></thead><tbody><tr><td style="text-align:left;">1</td><td style="text-align:left;">§4.2.2「复用 <code>AsrResultMessage</code>」未提 <code>final</code></td><td style="text-align:left;"><code>AsrResultMessage</code> <strong>新增 <code>isFinal</code></strong>（序列化名 <code>final</code>），<code>/ws/asr</code> 与 <code>/ws/audio</code> <strong>都填</strong></td><td style="text-align:left;">前端「中间态覆盖草稿 / 定稿追加」需要显式终态 —— 只靠 id 轮换无法表达&quot;最后一段已定稿&quot;。两条通道共用同一消息类型，语义必须一致（否则就是文档反复警惕的&quot;协议语义漂移&quot;）</td></tr><tr><td style="text-align:left;">2</td><td style="text-align:left;">§4.2.2 写「连接存活由<strong>音频帧活跃度</strong> + 最大识别时长控制」</td><td style="text-align:left;"><strong>只保留最大时长</strong>（120 秒），<strong>没有&quot;帧空闲超时&quot;</strong></td><td style="text-align:left;">前端用本地 VAD，只在说话时推帧，&quot;几十秒没帧&quot;是正常态（用户在想）；按帧空闲判死会误杀用户 —— 与 AGENTS §3.7「音频不是心跳」同一条理由</td></tr><tr><td style="text-align:left;">3</td><td style="text-align:left;">§4.2.4「（或只做识别连接超时兜底）」</td><td style="text-align:left;">兜底做在<strong>会话自己的计时器</strong>里，<strong>没有新增全局扫描任务</strong></td><td style="text-align:left;">计时器不依赖任何回调，连接悬挂（网络黑洞 / 前端崩溃）时照样到期收尾；识别通道因此不必挂到 <code>WsHeartbeatScheduler</code> 上，保持了&quot;不接面试注册表/调度&quot;的边界</td></tr></tbody></table><p><strong>文档没写、实现时补的边界</strong>（都属方案边界，不是架构变更）：</p><ul><li><strong>单用户并发超限是&quot;拒绝&quot;而不是&quot;顶号&quot;</strong>：回 <code>ASR_LIMIT_EXCEEDED</code> / 4004。顶号会把&quot;前端两条连接竞态&quot; 变成静默互踢；拒绝至少是明确的，前端可以先关掉自己那条旧连接再重试。</li><li><strong>死连接自愈</strong>：建连时先摘掉&quot;传输已关闭、但容器 <code>onClose</code> 还没到&quot;的登记， 否则死连接会把用户锁住最长 120 秒（这条有单测）。</li><li><strong>不收上行文本</strong>：收到文本只记 debug 日志，不回错误、不改状态 —— 这条通道的能力就是&quot;音频进、文本出&quot;。</li><li><strong>连接→会话的定位</strong>用 <code>WebSocketSession#getAttributes</code>（不再维护第二张按连接索引的表）； 并发闸门只用一张 <code>userId → 会话</code> 的表，<code>remove(userId, session)</code> 两参删除保证不误摘新连接。</li></ul><p><strong>已验证（2026-09-22）</strong>：全量 <strong>198 通过 / 0 失败</strong>，其中 Step 2 新增/扩展： <code>AsrStreamingServiceTest</code>(9：<code>ASR_RESULT</code> + <code>final</code>、并发拒绝、关闭后放行、死连接自愈、 <code>ASR_UNAVAILABLE</code>、<code>ASR_TIMEOUT</code>、缺 <code>userId</code>)、 <code>AsrHandshakeInterceptorTest</code>(4：缺票 / 无效票 / 有效票 / <strong>不做余额准入</strong>)、 <code>WebSocketConfigTest</code>(1：两条路径<strong>各挂自己的拦截器</strong>，按调用顺序断言)、 <code>RealtimePropertiesTest</code>(+2：两个上限的默认值 + &quot;后端上限必须比前端录音上限宽松&quot;)。</p><h3 id="_12-5-step-3-落地记录-治理-配额-指标-埋点口径-日志脱敏" tabindex="-1"><a class="header-anchor" href="#_12-5-step-3-落地记录-治理-配额-指标-埋点口径-日志脱敏"><span>12.5 Step 3 落地记录（治理：配额 / 指标 / 埋点口径 / 日志脱敏）</span></a></h3><blockquote><p>2026-09-22。这一步<strong>没有新功能</strong>，全是&quot;让这条新链路可观测、可限额、不留隐私痕迹&quot;。</p></blockquote><table><thead><tr><th style="text-align:left;">#</th><th style="text-align:left;">事项</th><th style="text-align:left;">落地形态</th><th style="text-align:left;">为什么是这个形态</th></tr></thead><tbody><tr><td style="text-align:left;">1</td><td style="text-align:left;">限流</td><td style="text-align:left;"><code>AsrConnectionQuota</code>：Redis 计数 + <strong>北京时间自然日</strong>滚动；<code>realtime.asr.daily-sessions-per-user</code> 默认 <strong>300</strong>（0 = 不限制）；超限 <code>ASR_QUOTA_EXCEEDED</code> / 4006</td><td style="text-align:left;">并发 1 + 单次 120 秒挡不住&quot;建连 → 说话 → 关闭&quot;的循环刷量；按 UTC 切日会让用户在北京时间早 8 点&quot;配额莫名回血&quot;，且日志对不上</td></tr><tr><td style="text-align:left;">2</td><td style="text-align:left;">失败开放</td><td style="text-align:left;">Redis 读/写异常一律放行 + WARN</td><td style="text-align:left;">识别只是输入辅助，缓存抖动不该让用户连字都打不了（与&quot;埋点采集故障绝不阻塞业务&quot;同源）</td></tr><tr><td style="text-align:left;">3</td><td style="text-align:left;">配额计数时机</td><td style="text-align:left;">只在连接<strong>被接受</strong>之后 <code>consume</code>；并发 / 配额 / 鉴权拒绝都不计数</td><td style="text-align:left;">用户&quot;手滑点两下话筒&quot;不该白掉当天额度（那两次并没有产生识别成本）</td></tr><tr><td style="text-align:left;">4</td><td style="text-align:left;">指标</td><td style="text-align:left;">4 个 Micrometer 指标（清单见 §4.2.4），出口是<strong>已暴露的</strong> <code>/actuator/prometheus</code></td><td style="text-align:left;">沿用项目既有做法（<code>FileCleanupService</code> / <code>MinioStorageService</code> 同风格）；不引入新监控栈、不加埋点事件名（平台事件字典未登记的新事件有风险）</td></tr><tr><td style="text-align:left;">5</td><td style="text-align:left;">埋点口径</td><td style="text-align:left;"><code>InterviewStartedEvent</code> / <code>InterviewEndedEvent</code> 增 <code>inputMode</code> → props 里的 <code>input_mode</code></td><td style="text-align:left;">V2 唯一能量化收益的口径（渗透率 / 时长 / 结束原因）；<code>source(voice/text)</code> 后端拿不到，明确交给前端上报</td></tr><tr><td style="text-align:left;">6</td><td style="text-align:left;">日志脱敏</td><td style="text-align:left;"><code>LogTextUtils#describe()</code> = <code>len=42</code>，替换 V2 新增的 3 处日志</td><td style="text-align:left;">用户作答 / 识别转写是<strong>内容</strong>而不是诊断信息；截断仍会保留前 512 字符原文，用来脱敏不够</td></tr></tbody></table><p><strong>闸门顺序（实现里写死，别调换）</strong>：凭证 → <strong>并发</strong>（可即时自救：关掉旧连接即可）→ <strong>日配额</strong>（重试无用）。 调换的话，用户会先看到&quot;次数已用完&quot;，而真实原因只是旧连接没关干净 —— 一个能自救、一个不能， 错误码给错会让前端给出错误的引导。</p>`,40),o(`p`,null,[l[7]||=o(`strong`,null,`联调与回归`,-1),l[8]||=a(`：协议层冒烟脚本 `,-1),l[9]||=o(`code`,null,`pass-up.backend/scripts/smoke-v2.mjs`,-1),l[10]||=a(`（零依赖 Node，用法与用例编号见 《`,-1),i(m,{to:`/%E9%A1%B9%E7%9B%AE/%E9%9D%A2%E8%AF%95%E5%8A%A9%E6%89%8B/%E6%A8%A1%E6%8B%9F%E9%9D%A2%E8%AF%95%E7%AC%AC%E4%BA%8C%E7%89%88-%E8%81%94%E8%B0%83%E4%B8%8E%E5%86%92%E7%83%9F%E6%B8%85%E5%8D%95.html`},{default:n(()=>[...l[6]||=[a(`模拟面试第二版-联调与冒烟清单`,-1)]]),_:1}),l[11]||=a(`》）；人工验收项与故障对照表也在那份清单里。`,-1)]),l[14]||=r(`<p><strong>已验证（2026-09-22）</strong>：全量 <strong>224 通过 / 0 失败</strong>，其中 Step 3 新增 / 扩展： <code>AsrConnectionQuotaTest</code>(10：放行 / 达上限 / 0 = 不限制 / Redis 读异常与写异常 / 首次设 TTL / 键按用户与日期隔离)、 <code>LogTextUtilsTest</code>(3：只暴露长度、不含原文、<code>truncate</code> 行为不变)、 <code>InterviewAnalyticsEventHandlerTest</code>(4：<code>input_mode</code> 上报、props 字符串化、读不到时长留空、缺失不猜)、 <code>AsrStreamingServiceTest</code>(+6：配额用完 4006、被拒连接不消耗配额、活跃数 / 建连结果 / 时长 tag / 音频字节)。</p><h3 id="_12-6-联调记录-一个真缺陷、一个误判-2026-09-22" tabindex="-1"><a class="header-anchor" href="#_12-6-联调记录-一个真缺陷、一个误判-2026-09-22"><span>12.6 联调记录：一个真缺陷、一个误判（2026-09-22）</span></a></h3><blockquote><p>实跑协议冒烟（<code>scripts/smoke-v2.mjs</code>）暴露出两件事。<strong>误判那条也写下来</strong>： 它的成因（字段名判定错）比结论本身更值得记住，否则下一个人会再踩一遍。</p></blockquote><table><thead><tr><th style="text-align:left;">#</th><th style="text-align:left;">结论</th><th style="text-align:left;">依据</th><th style="text-align:left;">处理</th></tr></thead><tbody><tr><td style="text-align:left;">1</td><td style="text-align:left;"><strong>误判 → 已撤销改动</strong>：曾判定&quot;<code>LLM_REPLY</code> 从来没有 <code>final:true</code>，需在回合层补发一条空文本结束分片&quot;</td><td style="text-align:left;">冒烟脚本只判 JSON 里的 <code>isFinal</code> 字段，而实际字段名是 <strong><code>final</code></strong>（见 §12.6.1）→&quot;70 个分片 0 条 final&quot;是<strong>假象</strong>；改用兼容判定后实测为 <strong>1 条/轮，且带正文</strong></td><td style="text-align:left;">撤除补发的结束分片（它只是多余消息）；改为在单测里钉住&quot;末分片原样透传&quot;</td></tr><tr><td style="text-align:left;">2</td><td style="text-align:left;"><strong>真缺陷（已修）</strong>：作答原文进了 <strong>INFO</strong> 日志（<strong>生产同样落盘</strong>）</td><td style="text-align:left;"><code>ConversationTurnRunner#submit</code> 的 <code>[回合] 触发LLM回答 … text=</code> 一直用 <code>LogTextUtils.truncate</code>（仍保留前 512 字<strong>内容</strong>）；Step 3 只改了 V2 新增的三处日志</td><td style="text-align:left;">改用 <code>LogTextUtils#describe</code>（<code>len=39</code> 这种摘要）</td></tr></tbody></table><h4 id="_12-6-1-协议事实-llm-reply-final-到底是什么-写死-两端遵守" tabindex="-1"><a class="header-anchor" href="#_12-6-1-协议事实-llm-reply-final-到底是什么-写死-两端遵守"><span>12.6.1 协议事实：<code>LLM_REPLY.final</code> 到底是什么（写死，两端遵守）</span></a></h4><ul><li><code>final:true</code> 就是<strong>豆包流式响应的末分片</strong>（<code>finish_reason != null</code>），<strong>它同时带最后一段正文</strong>， 不是&quot;空标记&quot;。因此： <ul><li>前端对 <code>final</code> 分片的 <code>text</code> 仍然是<strong>追加</strong>语义（<code>prev + msg.text</code>）， <strong>不能</strong>改成&quot;用 final 的文本整段替换&quot;；</li><li>一轮回复<strong>只应有一条</strong> final 分片（前端据此恢复输入框、结束 <code>loading</code>）；</li><li>它是 V2 文字作答&quot;等 <code>final</code> 再恢复输入框&quot;的<strong>唯一依据</strong>：上游哪天不再把 <code>finish_reason</code> 挂在末分片上（改成独立空分片或干脆不发），前端就会收不到 <code>onLlmDone</code>、输入框卡死。 <strong>冒烟用例 <code>S1-3</code> 就是这条的哨兵</strong>（同时打印 <code>final</code> 分片条数与是否带正文）。</li></ul></li><li><strong>字段名陷阱（本次误判的根因）</strong>：Java 侧字段是 <code>isFinal</code>（<code>@Data</code> 生成 <code>isFinal()</code>）， Jackson 对 <code>isXxx()</code> 布尔 getter 会<strong>去掉 <code>is</code> 前缀</strong>，所以下行 JSON 里叫 <strong><code>final</code></strong>。 消费方必须两个都认 —— 前端 <code>ws.ts</code> 是这么写的（<code>parsed.final === true || parsed.isFinal === true</code>）， 而冒烟脚本最初只判 <code>isFinal</code>，于是得出了&quot;信号不存在&quot;的假结论。 <blockquote><p>一般化：<strong>协议字段名以实际 JSON 为准，不要按 Java 字段名猜</strong>；拿不准时两端都兼容。</p></blockquote></li></ul><h4 id="_12-6-2-日志口径-step-3-的补充规则-按级别分" tabindex="-1"><a class="header-anchor" href="#_12-6-2-日志口径-step-3-的补充规则-按级别分"><span>12.6.2 日志口径（Step 3 的补充规则，按级别分）</span></a></h4><table><thead><tr><th style="text-align:left;">内容</th><th style="text-align:left;">级别</th><th style="text-align:left;">处理</th></tr></thead><tbody><tr><td style="text-align:left;">用户输入（作答原文、识别转写）</td><td style="text-align:left;">INFO 及以上</td><td style="text-align:left;"><strong>只记长度</strong>（<code>describe</code> → <code>len=42</code>）</td></tr><tr><td style="text-align:left;">AI 回答（<code>[回合] LLM回答完成</code>）</td><td style="text-align:left;">INFO</td><td style="text-align:left;"><strong>保留截断</strong>：它是&quot;AI 到底说了什么&quot;的唯一排查线索，且回答本来就落库（<code>interview_message</code>），不算新增暴露面</td></tr><tr><td style="text-align:left;">任意内容</td><td style="text-align:left;">DEBUG</td><td style="text-align:left;">可保留截断（dev 用来排查；生产 <code>root=INFO</code> 不输出）</td></tr></tbody></table><blockquote><p>冒烟用例 <code>S3-3</code> 按<strong>级别</strong>判定，而不是&quot;全文件搜不到原文&quot;：dev profile 是 <code>root: debug</code>， 会把完整 LLM 上下文打成 DEBUG —— 那是调试用途，不该被误报成泄漏。 该用例还有一个正向断言（日志里必须能看到 <code>收到文本作答 … text=len=N</code>）， 否则&quot;没搜到原文&quot;可能只是<strong>压根没记</strong>。</p></blockquote><p><strong>验证（2026-09-22）</strong>：</p><ul><li>全量回归 <strong>225 通过 / 0 失败</strong>；<code>AudioStreamSessionTurnBaselineTest</code> 新增 <code>末分片带finish_reason时_原样透传为final分片</code>（钉住&quot;末分片带正文 + 一轮一条 + replyId 配对&quot;， 防的正是&quot;再顺手补一条空 final&quot;这类动作）。</li><li>冒烟实跑：<strong>16 PASS / 0 FAIL / 1 SKIP</strong>（首次跑出 <code>final 分片 2 条（空文本 1，带正文 1）</code>， 即&quot;补发版&quot;的两条；撤销补发后应为 1 条带正文）。</li><li>日志侧：<code>logs/app-dev.log</code> 中修复前（17:25 / 17:37）各有一次 INFO 泄漏；修复后（17:50 起的新会话） 触发日志已变成 <code>text=len=39</code>，INFO 中不再出现作答原文。</li></ul>`,11)])}var f=c(u,[[`render`,d]]);export{l as _pageData,f as default};