---
title: 镜像加速与代理配置
icon: devicon:docker
sort: 3
---

## 镜像加速

新增 daemon 文件，这是Docker 守护进程的配置文件。

```bash
sudo nano /etc/docker/daemon.json
```

加入以下内容：（[目前国内可用Docker镜像源汇总](https://github.com/dongyubin/DockerHub?tab=readme-ov-file)）

```json
{ 
  "registry-mirrors": [
    "https://docker.xpg666.xyz",
    "https://docker.1ms.run",
    "https://dytt.online"
   ]
}
```

重新加载 docker

```bash
sudo systemctl daemon-reexec
sudo systemctl reload docker
```

查看是否配置成功

```bash
sudo docker info
```

如果成功，会多出 `Registry Mirrors:`字段，并可以从中找到你设置的值



测试镜像拉取。最后一行会注明拉取地址

```bash
docker pull hello-world
# hello-world 一般都能拉取成功，显示拉取地址是 docker.io/library/hello-world:latest，这个地址似乎没有被墙
# 或者直接拉取 kibana，将从 https://registry-1.docker.io/v2/ 拉取，一般会失败
docker pull kibana
```

## 代理

Docker **不会自动使用系统代理**。

需要显式配置：

```
sudo mkdir -p /etc/systemd/system/docker.service.d
sudo nano /etc/systemd/system/docker.service.d/http-proxy.conf
```

```plain
[Service]
Environment="HTTP_PROXY=http://172.16.0.222:7890"
Environment="HTTPS_PROXY=http://172.16.0.222:7890"
Environment="NO_PROXY=localhost,127.0.0.1"
```

重启：

```plain
sudo systemctl daemon-reload
sudo systemctl restart docker
```

## 离线导入镜像

如果无法直连 `ghcr.io` 等镜像仓库，可以在能科学上网的机器上拉取并保存镜像，然后导入到目标服务器的容器运行时中：

```bash
# 在可通网络的机器上导出
docker pull ghcr.io/kube-vip/kube-vip:v1.2.2
docker save ghcr.io/kube-vip/kube-vip:v1.2.2 -o kube-vip.tar
```

将 `kube-vip.tar` 传到目标服务器后导入：

- **K3s 内置 containerd**（`k3s ctr`）：

  ```bash
  sudo k3s ctr image import kube-vip.tar
  ```

- **Docker**：

  ```bash
  docker load -i kube-vip.tar
  ```

- **独立 containerd（含 k8s）**：

  ```bash
  ctr image import kube-vip.tar
  # 或使用 crictl（K8s CRI 层）
  ctr -n k8s.io image import kube-vip.tar
  ```