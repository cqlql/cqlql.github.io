---
title: K3s 方案控制台（方案）
icon: mdi:application-cog
sort: 15
---

# K3s 方案控制台（把已验证的方案产品化）

> **文档定位：方案定稿 + 实施记录。**
> P0~P2 已落地（见附录 v5~v7）；配置分层与控制台改配置已落地（见 v9）；
> v8 的 P4 只有决策、未实施。
> 上游两份方案都已实机验证：
> - [K3s HA 一键部署方案](./K3s HA 一键部署方案.md) —— `cluster-infra/k3s-ha/`，`verify` 全绿、VIP 真实漂移
> - [可观测性栈 k3s 部署方案](./可观测性栈 k3s 部署方案.md) —— `cluster-infra/monitoring/`，17 条规则 + 端到端钉钉链路
>
> 本文回答的是**下一个问题**：脚本已经跑通了，为什么还需要一个控制台、以及它到底该做什么。
>
> **定位**（注意不是「部署控制台」）：
>
> > **K3s 方案控制台** —— 把已验证的 K3s HA、VIP、Traefik、Registry、监控等基础设施方案，
> > 固化为**可配置、可校验、可执行、可观测**的操作系统。
>
> 「部署控制台」容易让人理解成「一个带网页的部署脚本」，而这个项目的实际设计不是那个。
> 它真正要回答的不是「如何执行 `deploy.sh`」，而是：
>
> > **我怎样确定这套基础设施方案现在处于正确状态？**
>
> 这也解释了为什么已经有 `./deploy.sh all`，仍然会觉得「不踏实」。
> 它要闭合的是一个环：
>
> ```text
> 准备一次方案配置 → 自动校验 → 看到执行计划 → 确认影响面
>        → 执行 → 实时日志 → 16 项验证
>        → 明确告诉你：这套集群现在到底对不对
> ```
>
> 第一版不追求功能多。**这个环跑通，就已经解决了最初那个痛点**——
> 不是让我少敲几个命令，而是让我不用一直担心「是不是哪里忘了、是不是哪个参数错了」。

## 一、起点：脚本减少了「手工操作」，但没有减少「人的不确定性」

三份手工文档收敛成 `./deploy.sh all` 之后，复杂度并没有等比下降。它分三层：

```text
操作复杂度   要敲多少条命令、按什么顺序        →  已消灭（main() 阶段编排）
决策复杂度   每个值该填什么、为什么是这个值     →  已转移（config.env 逐行写明理由）
验证复杂度   我现在到底在不在一个有效状态      →  部分自动化，但输出是给人看的
```

真正让人不踏实的不是第一层，是**第三层**：脚本说「完成」了，我怎么知道它真的对？

这一点在两份方案里其实已经被反复印证——看《K3s HA 一键部署方案》§四「比手工文档多固化的八件事」
的「漏掉的后果」一列，出现频率最高的词组是同一个：

> **「看起来一切正常」**

- 污点丢了 → 不报错，只是少一个节点通告 VIP
- `curl -k` 通了 → 证书缺 SAN 被掩盖，直到 `kubectl` 访问 apiserver 才炸 `x509`
- servicelb 没禁 → `EXTERNAL-IP` 是节点物理 IP，看着像正常

所以「控制台」要解决的，本质上是**把「看起来正常」和「真的正常」之间的缝补上，并且让它可见**。

## 二、先划边界：哪些已经在脚本里了

在动手之前必须承认一件事：**三层里有两层已经存在，而且质量不低。**

| 层 | 现状 | 证据（`cluster-infra/`） |
| :--- | :--- | :--- |
| 输入 | `config.env` 是唯一事实来源，每行都写了「为什么是这个值 / 改它要连带改什么」 | `k3s-ha/config.env` 11 KB、`monitoring/config.env` 8.8 KB |
| 校验 | `preflight` 已经在**重算拓扑**，不是检查配置有没有填 | `check_traefik_replicas`、VIP 冲突检测、代理真下载一次安装脚本、渲染残留 `__XXX__` 检测 |
| 执行 | 阶段幂等，且区分「配置幂等」与「无副作用」 | `main()` 的 `PREFLIGHT_REPLICAS_SOFT` 分流 |
| 状态 | `verify` 16 项，**查链路不查配置** | 控制面走到 `/readyz` + 证书 SAN；业务面逐节点 curl |
| 输出 | 三种前缀 + 退出码 | `ok ✓` / `warn !` / `die [错误]`；`phase_verify` 末尾 `return "$fail"` |

**也就是说：校验层和状态层已经写好了，缺的只是「机器可读」和「人类可读」之间那一层薄壳。**

这决定了整个项目的性质——**它不是从零做一个运维平台，是给一个已经正确的判定逻辑做视图。**

## 三、架构与边界：GUI 里不许复制「方案判定逻辑」

### 3.1 目标架构

```text
                    Browser
                       │
                       ▼
              ┌─────────────────┐
              │  React + antd   │
              │  状态 / 配置 /   │
              │  任务 / 日志     │
              └────────┬────────┘
                       │ HTTP / SSE
                       ▼
              ┌─────────────────┐
              │    Go API       │
              │  Task Manager   │
              │  Auth           │
              │  Stream         │
              │  JSON Adapter   │
              └────────┬────────┘
                       │ exec
                       ▼
              ┌─────────────────┐
              │   deploy.sh     │
              │  preflight      │
              │  prepare        │
              │  server-first   │
              │  join           │
              │  kube-vip       │
              │  traefik        │
              │  verify         │
              └────────┬────────┘
                       │ SSH
          ┌────────────┼────────────┐
          ▼            ▼            ▼
         k1           k2           k3
```

**Kubernetes API / Prometheus 暂时放在虚线区（P3）**，在 P2 之前不进 Go API：

```text
     ┌───────────────┐        ┌───────────────┐
     │ Kubernetes API│        │  Prometheus   │
     │      P3       │        │      P3       │
     └───────┬───────┘        └───────┬───────┘
             └──────────┬─────────────┘
                        ▼
                     Go API
```

### 3.2 边界怎么划：区分「方案判定」和「控制台自身校验」

这是本文最重要的一条，也是最容易在实施时被违反的一条。

初稿写的是「**校验逻辑必须留在脚本里，一行都不许搬进后端**」——**原则对，但表述过死**。
因为 Go 后端必然会有自己的校验，例如：

```text
任务是否正在运行
用户是否有权限执行
参数是否完整
是否允许同时执行两个部署任务
当前任务是否可以取消
```

这些**不是 K3s 方案校验**，不能也不该塞进 Bash。准确的说法是：

> **K3s 集群的事实判定与方案校验，以 `deploy.sh` 为唯一事实来源，Go 不复制这些判定逻辑；
> Go 可以拥有控制台自身的任务、权限、状态和参数校验。**

```text
Go
│
├── 控制台校验（Go 自己的领域）
│   ├── 参数完整性
│   ├── 任务状态
│   ├── 并发控制
│   └── 权限
│
├── 控制台自己的写操作（Go 的领域，v9 新增）
│   └── config-edit：编辑 config.local.env
│       └── 但「这份值合不合法」整包交给 ↓ validate-config
│
└── 调用 Bash
       │
       └── K3s 事实校验（deploy.sh 是唯一事实来源）
           ├── VIP 冲突 / 同网段 / 空闲
           ├── kube-vip 两套 DaemonSet
           ├── Traefik 副本数 vs eligible nodes
           ├── TLS SAN
           ├── Registry 可达性
           └── …
```

判据很简单：

> **如果判定的是「集群是否符合本方案要求」，由 Bash 负责；
> 如果判定的是「控制台是否允许执行某个操作」，由 Go 负责。**

这条比「要不要 SSH」更本质。因为以后必然会出现这种情形：

```text
Go 调用 verify --json
        ↓
Go 根据结果决定：
  · 是否允许显示「执行」
  · 是否要求二次确认
  · 是否允许继续下一阶段
```

这**仍然是 Go 的控制流程**，而不是 Go 在**重新判断 K3s 是否正确**。
两者容易混淆，区别在于：前者消费 `verify` 的结论，后者自己产出一个结论。

#### v9 补的第三类：控制台自己的写操作

上面的二分（K3s 判定 → Bash / 控制台自身校验 → Go）在 v9 不够用了。
`config-edit`（在控制台里改配置）**既不是 K3s 判定，也不是「校验」**——
它是控制台**自己写文件**。归在 Go 这一侧，但它带来的诱惑很具体：

```text
要校验「VIP 能不能填这个值」
    ✗ 错的做法：Go 里写 if !isValidIP(vip) { ... }
    ✓ 对的做法：把候选值写成一份配置，跑 deploy.sh validate-config，看它怎么说
```

错的做法看起来更「顺手」，而且**单看代码完全合理**——它只是把一个 IPv4 正则放在 Go 里。
但那样一来，「什么算合法 VIP」就有了两份定义（Go 一份、`deploy.sh` 一份），
两边必然在某个边界条件下给出不同答案，而那时候**不知道该信谁**。

所以硬约束是：**控制台连「VIP 不能等于节点 IP」这条规则都不认识**。
它只负责把候选值递过去、把结论显示出来。实测里那条 `VIP_CP=192.168.1.201`
被拦下的判定项，文案与 `fix` 全部原样来自 `deploy.sh`，Go 一行判断都没有。

#### 为什么方案判定不能复制到 Go

理由不是「懒得搬」，而是三条都不可妥协的：

1. **离线测试已经压在这套 bash 上了。** `k3s-ha/tests/selftest.sh`（79 项，秒级）+ `integration.sh`（88 项）。
   把 `check_traefik_replicas` 翻译成 Go，等于把这 167 项断言的保护范围砍掉一半。
2. **两份事实来源必然漂移。** 一旦 Go 里也有一份「副本数 vs eligible」的判断，
   两边就会在某个边界条件下给出不同答案——而那时候你**不知道该信谁**。
   这恰恰是控制台要消灭的「不可确认」，它会亲手把它造回来。
3. **校验的价值在于「每次执行都重算」。** 方案 §四 第 5~7 条的原话是
   「不只是写进清单，而是写成了**每次执行都会重算的校验**」。搬到 Go 就变回静态配置了。

### 3.3 推论：Go 后端第一版可以薄到不可思议

```text
POST /api/tasks            →  os/exec 起一个 bash deploy.sh <phase>
GET  /api/tasks/{id}/logs  →  读 stdout/stderr，按行推 SSE/WebSocket
GET  /api/state            →  跑 verify --json，返回结构化结果
```

没有 ORM、没有数据库、没有 K8s 客户端。这些不是「第一版可以省略」，而是
**在 P2 之前都不应该出现**。加上 §3.2 划出来的那四类控制台校验，就是第一版 Go 的全部内容。

## 四、动作的「影响面」必须是一等公民

这是现有脚本已经想清楚、但 GUI 最容易做丢的一件事。

`deploy.sh` 的头部注释明说：

> 幂等 = 「配置幂等」，而不是「执行无副作用」……**`traefik` 阶段会触发一次 Traefik 滚动重启**
> ——所以不能把 `all` 当成「零影响重复执行」。只想「看看现状」请用 `./deploy.sh verify`。

对照成控制台的动作表：

| 动作 | 改集群吗 | 影响面 | GUI 该怎么表现 |
| :--- | :--- | :--- | :--- |
| `verify` | 否，只读 | 无 | 绿色，随时可点，结果即首页 |
| `preflight` | 否，但会 SSH 到每台 + 渲染清单到临时目录 | 无 | 绿色，带「会连 3 台机器」提示 |
| `render` | 否 | 无 | 绿色，输出可下载的渲染产物 |
| `taint` / `kube-vip` / `traefik` | 是 | **`traefik` 会滚动重启入口** | 黄色，需二次确认 |
| `all` | 是 | 包含上面全部 + 可能的 k3s 重启 | 黄色，**不许做成无脑大按钮** |
| `uninstall` | 是 | 需 `CONFIRM_UNINSTALL=yes`，且**≠ 恢复出厂** | 红色，必须逐条列出「不会恢复什么」 |
| **改配置**（`config-edit`，v9 新增） | 否，**但改的是「之后每一次部署」** | 比跑一条命令的后果更长远 | 黄色，**两步**：先预览（校验 + diff），再确认写入 |

最后一行的「否，但」值得说清。它不碰集群，看起来最无害；但它改变的是
**后面每一次 `deploy.sh` 会读到什么**——这个后果比跑一条命令长远得多。
所以它在界面上不是绿色，而且走的是唯一一个「两步」的写路径：

```text
① 预览：候选值 → 跑 deploy.sh validate-config → 显示校验结论 + 会变的行（**不写盘**）
② 写入：重新校验一遍 → 写文件 → 留 .bak → 记审计
```

第 ① 步是**不写盘**的，所以它在有任务跑的时候也能用（只读）；
第 ② 步要独占，因为部署脚本启动时就把配置读进内存了 ——
这期间改配置会让「脚本看到的值」和「文件里的值」不一致，事后极难解释。

确认令牌绑的是「键 + **改前全文** + 改后全文」，不只是键和值。
这样文件在预览之后被别人动过（哪怕动的是别的键），令牌就失效，强制重新预览 ——
**把「确认」绑到具体内容上，是它唯一有意义的实现方式。**

最后一行值得单独说。`uninstall` 的语义在方案 §八 里被刻意写清楚了：

> **`uninstall` = 移除本方案管理的 kube-vip / Traefik 定制资源；K3s 本身及 `servicelb` 的禁用状态不自动恢复。**

如果 GUI 把它渲染成一个「删除集群」按钮，用户点完会以为回到了干净状态，实际不是。
**这类「边界语义」正是控制台最该显式表达的东西**——脚本用文档写了，控制台要把它变成界面的一部分。

## 五、技术栈

### 5.1 前端：React + TypeScript + Ant Design（对齐 `pass-up.frontend`）

**不引入第二套心智**——直接沿用已有的 React 基线。基线取自 `pass-up.frontend`：

| 项 | 取值 | 出处 |
| :--- | :--- | :--- |
| 框架 | React 19.2 + react-dom 19.2 | `pnpm-workspace.yaml` 的 `catalog:` |
| 语言 | TypeScript ~6.0 | 同上 |
| 构建 | Vite 8.1 | 同上 |
| 组件库 | **antd 6.5** + `@ant-design/icons` 6.3 | `apps/admin`、`packages/shared` 的 dependencies |
| 路由 | react-router-dom 7.18 | `apps/admin` |
| 状态 | zustand 5.0 | `catalog:` |
| 请求 / 时间 | axios 1.18 + dayjs | `apps/admin` |
| Lint | oxlint 1.76 + eslint 10 | 顶层 `devDependencies` |

**工程结构照抄 `pass-up.frontend` 的形状**：pnpm workspace + `apps/*` + `packages/*`，
依赖版本走 `catalog:` 集中管理，`packages/` 下复用 `shared` / `vite-config` / `tsconfig` / `eslint-config`
四个基础包。这样两个项目的依赖升级、lint 规则、tsconfig 是同一套心智，不用维护两份。

> 一个提醒：`pass-up.frontend` 用了 `@ant-design/x`（antd 的 AI 对话组件库）。
> 控制台不需要它，别顺手带上。

#### 明确不用 Tailwind CSS

`pass-up.frontend` 目前带着 `tailwindcss@4.3`，但**控制台项目不带**。

理由不只是「可读性」——在这个项目上它还有一层更硬的问题：

1. **可读性**：控制台的 UI 是**大量表单 + 状态表格 + 日志流 + 步骤条 + 拓扑图**，
   这些几乎全是 antd 组件；真正需要自定义样式的只有布局外壳和少数状态色。
   用 utility class 表达这些，会把「一个组件长什么样」拆散到十几个 class 字符串里。
2. **优先级打架（这条才是关键）**：antd 6 用的是 **CSS-in-JS**，样式注入顺序不受你控制。
   想覆盖 antd 组件内部结构时，Tailwind 的 utility 经常压不住，最后要靠 `!important` 兜底
   ——**这是维护成本，不是审美问题**。
3. **antd 已经把视觉规范收敛在 theme token 里了**，再叠一层 utility 等于两套规范并存。

**替代方案**（按推荐顺序）：

| 场景 | 做法 |
| :--- | :--- |
| 布局 | antd 的 `Layout` / `Grid` / `Space` / `Flex`，**不写自定义 CSS** |
| 需要微调 | **CSS Modules**（`xxx.module.css`）——类名有语义、可跳转、可被 lint |
| 全局主题 | `ConfigProvider` + theme token（`components` 覆盖），而不是用 class 盖 antd 内部结构 |
| 状态色（通过 / 警告 / 失败） | 定义**一组语义化 CSS 变量**，禁止散落的 `text-green-600` 这类写法 |

> 状态色那一条对本项目尤其重要：`verify` 的三态（`✓ 通过` / `! 警告` / `[错误]` 失败）
> 是整个界面的核心语义，它必须是**一处定义、全局引用**的 token，
> 而不是每个组件各写一遍颜色类名。

### 5.2 后端：Go，但理由不是「别和 PassUp 绑在一起」

「避免技术栈耦合」是对的，但太弱了。真正的理由是**部署形态**：

| 维度 | Go | Java |
| :--- | :--- | :--- |
| 产物 | 单静态二进制，`./k3s-console` 即跑 | JAR + JVM |
| 运行时资源占用 | **低**，适合部署在资源受限的 Linux 管理机上 | JVM 基线本身就不小 |
| 进程/流编排 | `os/exec` + goroutine 天然顺手 | 需线程池 + 流处理框架 |
| SSH | `golang.org/x/crypto/ssh` 一等公民 | 需 JSch / sshd 库 |

> 「资源占用低」是**定性描述**，不写具体 RSS 数字。
> 实际占用取决于 Go runtime、HTTP server、SSH 连接数、子进程、日志缓存、并发任务、
> 以及以后是否引入 K8s client —— 把某个数字写成选型依据，它会很快变成一个过时且误导的硬指标。
> 真正的技术理由就是上表那三条稳定的：**单静态二进制 / `os/exec` + goroutine / SSH 生态**。

**最关键的一条是内存**：这个控制台大概率要和被它管理的集群跑在同一批机器上，
而那三台节点是 **7.7 GiB / 台**——正是这个数字，逼得 PassUp 后端的 `limits.memory`
从 8Gi 收到 2Gi（`-XX:MaxRAMPercentage=70` 按 limit 算堆，8Gi limit → 5.6Gi 堆）。
**在一个刚因为内存紧张而调整过 JVM 参数的集群旁边，再放一个 JVM 控制台，是自相矛盾的。**

### 5.3 关于 `client-go`：它是 P3 的事，而且给不了你一半的状态

那份建议把「接 Kubernetes API」放在 V3，方向对，但**理由说反了**——它给人的印象是
「K8s API 比较高级所以放后面」。真实原因是：

> **`verify` 的 16 项里，有一半是「节点本地事实」，`client-go` 永远拿不到。**

逐项拆开看：

| 判定项 | 数据来源 |
| :--- | :--- |
| 节点状态 / 两套 DaemonSet / 业务 VIP / `externalTrafficPolicy` / 副本分布 / 副本数-vs-eligible / 节点污点 | **k8s API**（7 项） |
| 控制面 VIP 是否落在网卡（`/32`）/ 6443 TCP / `/readyz` / **证书 SAN** / 业务连通 / **逐节点 VIP 通告** / **`registries.yaml` 逐字节一致** | **SSH 到节点**（7 项） |
| 本机直连 / 版本固定提醒 | 本地（2 项） |

那 7 项 SSH 项里有几条是**整份验收里最值钱的**：

- **证书 SAN**：`curl -k` 会跳过校验，只有 `openssl s_client` 才能发现漏写 `tls-san`
  （本环境真实踩过，用 VIP 访问 apiserver 报 `x509`）；
- **逐节点 VIP 通告**：`externalTrafficPolicy: Local` 下每个承载 Traefik 的节点各自通告 VIP，
  「某台没在通告」是**最隐蔽的一类故障**；
- **`registries.yaml` 一致**：`cmp` 逐字节比对，容器拉镜像行为是否和脚本产物一致。

**结论**：控制台的状态层**不是**一个「接 K8s API」的问题，而是一个
**「把 verify 链路跑一遍并结构化」**的问题。`client-go` 只能覆盖 7/16，
而且是最容易用 `kubectl` 拿的那 7 项。

所以正确顺序是：**先把 `verify` 变成机器可读，再谈 `client-go`。**

#### 而且 `client-go` 可以不急着做

这一点值得单独写下来，因为它是**优先级**问题，不只是**顺序**问题。

本项目最核心的价值不是「查看 Pod / Deployment / Node」——那些 Headlamp 已经做得很好，
再做一个不会更好。真正的核心链路是：

```text
deploy.sh → verify → 方案判定 → 控制台展示
```

所以 P3 接 `client-go` 的定位是：

> **为了补充交互和实时状态，而不是为了替代 `verify`。**

一旦这条原则写清楚，`client-go` 就从「必须做的 V3」变成了「**有明确增量才做的可选项**」。
如果某天发现「节点 CPU / 内存实时曲线」确实需要它，再加；
如果 `kubectl` 的输出已经够用，就不加。**不做也是一种合法结论。**

### 5.4 图表：P3 再定，但候选已经收窄

antd 自身不带图表。本项目需要图的地方有两处，**优先用非图表的表达**：

| 需求 | 首选 | 理由 |
| :--- | :--- | :--- |
| 拓扑关系（节点 → VIP → Traefik → 业务） | **手写 SVG / 纯 CSS**，不用图库 | 拓扑是**固定形状**（3 节点 + 2 个 VIP），不是数据驱动的图。引图库反而更绕 |
| 时序指标（CPU / 内存 / targets） | 到 P3 再选：`@ant-design/charts` 或 ECharts | 前者与 antd 同源、主题自动一致；后者生态更大、运维场景更常见 |

**不要在 P1 引入任何图表库**——P1 的全部内容是「一张状态卡 + 若干表格」，
表格能表达的东西不要画成图。

## 六、控制台自己跑在哪：一台 Linux 管理机（P0 就要定）

**结论：跑在一台 Linux 管理机上，控制台自己不进被它管理的集群。**

这不是个可以往后拖的问题——后面的 SSH 私钥、`kubectl`、`bash`、`docker`、网络、`kubeconfig`、
任务日志全都依赖它。所以它必须在 P0/P1 就定下来。

### 6.1 为什么不能跑在 Windows

目标是「**让这个东西本身比手工执行脚本更可靠**」。如果控制台自己依赖
Git Bash、`cygpath`、Windows 版 SSH、本机 Docker，那它**重新制造了一层不确定性**——
而这层不确定性还是你自己要维护的：

- `cygpath -m`（Windows 原生 kubectl 读不了 Git Bash 的 `/c/...` 路径）；
- MSYS 会把「看着像 Unix 绝对路径」的环境变量值改写掉（`VITE_BASE_PATH=/admin` 被转成
  `<PortableGit>/admin`，导致 admin 整页白屏而 HTTP 全 200）；
- 构建还会被沙箱的删除保护 shim 拦下，要额外带 `CODEBUDDY_SAFE_DELETE_ENABLED=0`。

这些都是真实踩过的坑，但它们**是开发环境的问题，不该成为运行时的依赖**。

### 6.2 为什么也不建议跑在集群节点上

三个选项对照：

| 选项 | 满足 | 问题 |
| :--- | :--- | :--- |
| **A. Windows 本机** | SSH、本机 kubectl | 依赖 Git Bash / cygpath / 本机 Docker（见 §6.1） |
| **B. 集群内某个节点** | SSH、kubectl、bash 全有 | ① 节点是 **7.7 GiB**，控制台要和业务抢内存；② 控制台管的就是这个集群，**集群挂了控制台也没了**，等于把「看门狗」和「被看护对象」放一起；③ `build-image` 用的是**本机 docker**，节点上没有 |
| **C. 集群外一台 Linux 管理机** | 全部满足 | 需要一台机器 + 一次部署——**这是唯一可接受的成本** |

选项 B 的第 ② 条是决定性的：控制台的价值是「集群不对时告诉你哪里不对」，
它**必须在集群之外**才能履行这个职责。

### 6.3 部署形态

```text
                   Windows
                  Browser
                     │
                     ▼
             ┌───────────────┐
             │ Linux 管理机  │
             │               │
             │ K3s Console   │
             │ Go + React    │
             │               │
             │ deploy.sh     │
             │ SSH Key       │
             └───────┬───────┘
                     │
          ┌──────────┼──────────┐
          ▼          ▼          ▼
         k1         k2         k3
```

建议直接做成一个容器镜像，脚本**随镜像打包**而不是挂载宿主机目录——
这样「控制台用的脚本版本」和「镜像 tag」是绑定的，可复现，
也不会出现「镜像里是 A 版脚本、宿主机挂载的是 B 版」这种新的不可确认。

#### 这个位置带来的直接价值：集群全挂时它还在

这是「跑在集群外」最实际的收益，值得单独写出来。即使三台节点全部失联：

```text
k1 ❌
k2 ❌
k3 ❌
```

只要管理机还活着，你仍然能看到：

```text
Cluster unreachable
```

并且能继续做：

```text
SSH 检查  →  网络检查  →  重新部署  →  恢复
```

如果控制台跑在集群里（§6.2 选项 B），这个场景下**你会同时失去集群和控制台**，
连「它到底怎么了」都问不出来。**这才符合「控制台」的定位**——
它的职责是在系统不正常的时候仍然可用。

### 6.4 无论跑在哪都必须满足的两条

1. **SSH 私钥是控制台的最高价值凭据**，它能登录全部节点。
   控制台自身的认证/授权等级不能低于这把钥匙——否则它就是集群最弱的一环。
2. **`build-image` 是唯一一个「必须在有 docker 的机器上跑」的阶段。**
   要么在管理机上装 docker，要么把镜像构建挪到 CI（`docker-infra/` 已有 CI）。
   建议后者——构建本来就不该是控制台运行时的职责。

## 七、真正的缺口清单（读代码读出来的）

这些是**现有脚本里确实没有、但方案文档的 checklist 里已经要求**的项。
控制台的 V2 校验层就应该是补这些，而不是另起炉灶。

### 7.1 内网 Registry 端口可达性 —— 方案要求了，脚本没查

`K3s HA 一键部署方案` §6.1 的 checklist 明确写着：

> **镜像加速可用**：三台都能访问内网 Registry 的三个端口（`curl -sI http://172.16.0.222:5001/v2/` / `:5002/v2/` 有响应即可）。

但 `phase_preflight` 实际检查的是 **k3s 下载代理**（`K3S_DOWNLOAD_PROXY` 逐台真下载一次安装脚本），
**没有查 Registry 三个端口**。它只在 `README.md` 的排障表里以「怎么排查」的形式出现。

这个缺口的代价不低——README 自己写了：

> containerd **不会自动回退官方源**，所以要么修缓存，要么临时把镜像换成 `172.16.0.222:5000/...`

即：代理缓存不可达时，`ghcr.io` 上的 kube-vip 会**直接拉不动**，且没有回退。

`monitoring/deploy.sh` 里还有一处**死注释**，说明这个检查本来打算做：

```bash
  # 镜像可达性（只探测不拉取，省时间）
  log "镜像版本：prometheus=${PROMETHEUS_IMAGE} loki=${LOKI_IMAGE} alloy=${ALLOY_IMAGE}"
  ok "preflight 通过"
```

注释说「只探测不拉取」，但下面只 `log` 了版本号，**一个探测都没做**。

### 7.2 节点规格下限（OS / CPU / 内存）—— 方案假设了，脚本没验

`prepare` 阶段实际做的事是：`hostname` → `/etc/hosts` 托管块 → 关 swap → `modprobe` →
`apt update/upgrade` → `ufw` → 建 auto-manifests 目录。**没有一条检查节点规格。**

而方案 §2.1 是按「Ubuntu 24.04.2 LTS、7.7 GiB / 4 vCPU」写的，
监控方案的内存取值（`limits`、PVC、`ResourceQuota`）全都建立在这个规格上。

建议补的判据（都是 SSH 一条命令）：

```bash
# 这三条现在都没有，但都影响后续所有取值
. /etc/os-release && echo "$VERSION_ID"     # 期望 24.04
nproc                                        # 期望 >= 2
awk '/MemTotal/{print int($2/1024/1024)}' /proc/meminfo   # 期望 >= 4（GiB）
```

### 7.3 VIP 的「空闲」与「同网段」—— 只查了一半

`preflight` 查的是「VIP 不等于任何节点物理 IP」和「`VIP_CP ≠ VIP_TRAEFIK`」——
**这两条是防止漂移时抢 ARP 的硬铁律，是对的。**

但方案 §6.1 的 checklist 还要求：

- VIP 在局域网内 **ping 不通**（未被占用）
- VIP 与节点 **同网段**

这两条目前是纯手工步骤。控制台可以把它变成一条自动校验。

> **v9 的进展**：补了第三条「VIP 是不是**合法 IPv4**」（`is_ipv4`）。
> 它拦不住「这个地址被别的机器占了」——那是拓扑问题，不是形状问题；
> 但形状必须在写入前挡住，否则非法值会被原样写进 kube-vip 清单和 apiserver 证书，
> 报错点离原因很远（证书生成失败 / DaemonSet CrashLoop），很难倒推回来。
>
> ⚠️ **「空闲」与「同网段」仍然没查，本节未关闭** —— 别因为看到 `is_ipv4` 就以为关了。

### 7.4 跨配置文件的事实一致性 —— 只 warn，不拦

这是**两份 config.env 之间唯一的共享事实**，也是最容易漂移的地方：

```text
k3s-ha/config.env            monitoring/config.env
────────────────────         ────────────────────
NODE_TAINTS=                 TAINT_KEY="k3s-monitoring"
  "k3 k3s-monitoring:          TAINT_EFFECT="NoSchedule"
        NoSchedule"           INFRA_NODE="k3"
                             （且 INFRA_NODE 必须在 k3s-ha 的 NODES 里）
```

`monitoring/config.env` 自己写了「改一处必须改另一处」，但 `monitoring/deploy.sh` 的
`phase_preflight` 发现污点缺失时**只是 warn，不中断**：

```bash
  else
    warn "$INFRA_NODE 缺少污点 ${TAINT_KEY}:${TAINT_EFFECT}（当前：$(printf '%s' "$taints" | tr '\n' ' ')）"
    warn "  这不会挡住部署（nodeSelector 仍会把单实例组件钉住），但说明 k3s-ha 的"
    warn "  NODE_TAINTS 与这里的 TAINT_KEY 已经不一致了，请回去核对 cluster-infra/k3s-ha/config.env"
  fi
```

warn 的理由是成立的（不挡部署），但**这意味着没有任何一个地方能回答「这两份配置现在一致吗」**。
这正是控制台该拥有的「跨文件事实视图」。

### 7.5 逐项状态不可机器读

`verify` 现在输出的是给人看的东西，虽然结构已经很像机读：

```text
    ✓ 全部 3 台都能经代理下载安装脚本
    ! 本机 kubectl 经 172.16.0.210 访问失败（本机与该网段不通时属预期）
  [Traefik 业务入口]
    ✓ 业务 VIP 正确（可漂移）
```

`[ -t 1 ]` 已经做了 ANSI 自动降级（非 TTY 不带色），所以解析是**可行**的；
但靠正则匹配 `✓`/`!` 属于**脆弱耦合**——文案改一个字就断。

**建议 P0 就做**：给 `verify` / `preflight` 加 `--json`，**判定逻辑一行不改**，只改输出层。
协议形状见 §8.1——注意这不是「加个输出格式」，而是给 Bash 定一份**结果协议**。

## 八、分期

| 期 | 内容 | 为什么是这个顺序 |
| :--- | :--- | :--- |
| **P0** | `verify` / `preflight` 加 `--json` —— **Bash 的结果协议**（§8.1） | 不动判定逻辑、不改现有工作流；这是整个项目唯一的地基 |
| **P1** | 只读控制台：状态首页 + 拓扑事实 + **方案配置视图** + 配置一致性视图（§8.2） | 零风险，且立刻兑现「不可确认 → 可确认」 |
| **P2** | 动作编排：带**影响面标签** + 二次确认 + 实时日志流（§8.3） | 此时才有「按钮」，且每个按钮的危险等级已明确（§四） |
| **P3** | 补 §7.1~7.3 的校验 + `client-go` / Prometheus API 指标面板（§8.4） | 校验补齐后，指标才有意义（否则「面板全绿」和「真的正常」还是两件事） |
| **P4** | **第二类受管对象：PassUp**（状态 → 动作）（§8.7） | 此时「协议 → 控制层 → UI → 任务」的骨架已经被 P0~P2 验证过一轮，PassUp 是它的第二个消费者。**第一版不改名、不扩范围** |

> **v9 不新开一期，它横跨 P1/P2**：前半是**配置分层**（§8.2 配置视图的前置 ——
> 视图要能说清「这个值从哪来」，得先有「值在哪」），后半是**改配置**（§8.3 写通道的延伸）。
> 之所以插在这里做，是因为用户实际提出了一个问题：**换环境时配置会被 git 静默回退**
> —— 而它恰好是 P1 的视图和 P2 的写通道都绕不开的地基。见附录 v9。

### 8.1 P0：`--json` 不是「输出格式」，是 Bash 的**结果协议**

这一点值得单独强调，因为它决定了整个项目的性质。

做完 P0 之后，链路不再是「Shell → GUI」，而是：

```text
deploy.sh
  │
  ├── 人类输出（现在的 ✓ / ! / [错误]）
  │
  └── JSON 输出  ←── 结果协议
          ↓
        Go API
          ↓
       React
          ↓
      （未来）AI
```

> **GUI 不应该理解 Bash 的文字输出，而应该理解 Bash 的结果协议。**

逐项的最小形状（**四字段是必须的，P0 可以只做这四个**）：

```json
{
  "id": "traefik.replica_eligible",
  "level": "ok",
  "detail": "replicas=2, eligible_nodes=2",
  "fix": null
}
```

失败项：

```json
{
  "id": "tls.apiserver_san",
  "level": "error",
  "detail": "VIP 172.16.0.210 is not included in certificate SAN",
  "fix": "检查 TLS_SAN 配置并重新执行 server-first"
}
```

**建议再加两个字段**（可选，但趁 P0 一起做最省事——事后补要动所有判定点）：

```json
{
  "id": "traefik.replica_eligible",
  "level": "ok",
  "detail": "replicas=2, eligible_nodes=2",
  "fix": null,
  "source": "cluster",
  "duration_ms": 183
}
```

| 可选字段 | 取值 | 用途 |
| :--- | :--- | :--- |
| `source` | `node` / `cluster` / `local` / `config` | UI 直接显示「数据来自：节点 / Kubernetes / 本机 / 配置」。**它同时也是 §3.2 那条边界的自证**——`source=cluster` 的项就是由 Bash 判定的 |
| `duration_ms` | 整数 | 排查「为什么 `verify` 花了 12 秒」。一眼能看出是谁慢：`registry.reachability 85ms` / `tls.apiserver_san 120ms` / `business.connectivity 7800ms` |

> `source` 的取值正好对应 §5.3 里 verify 三项数据来源的划分（k8s API / SSH 到节点 / 本地），
> 所以它不是额外发明的维度，而是把已有的事实显式化。

字段约定：

| 字段 | 含义 |
| :--- | :--- |
| `id` | **稳定标识**——文案可以改，`id` 不能改。前端按 `id` 做跳转 / 筛选 / 忽略，不按文案匹配 |
| `level` | `ok` / `warn` / `error` / `skip`，对应现有三种前缀，也是 §5.1 那组状态色 token 的来源（`skip` 见下） |
| `detail` | 给人看的一句话，含关键数值（`replicas=2, eligible_nodes=2`） |
| `fix` | **可复制的修复命令或动作**，没有则 `null`。脚本里其实已经写了不少（如 `→ 跑 ./deploy.sh taint`），P0 只是把它们变成结构化字段 |

顶层再包一层：

```json
{
  "phase": "verify",
  "started_at": "2026-09-24T11:40:09+08:00",
  "summary": { "ok": 14, "warn": 2, "error": 0, "skip": 1 },
  "items": []
}
```

#### `skip` 是第 4 态，不是「通过」

实施时补的（初稿只约定了三态）。理由：判定项会因为**外部条件不具备**而根本没跑——
节点上没有 `openssl`、本机没有 kubectl、`MANAGE_REGISTRY=false`、首台还没装 k3s……
这些既不是 `ok` 也不是 `error`。如果它们干脆不出现在结果里，消费方就无法区分
「这项通过了」和「这项压根没跑」——**而这恰好就是本项目要防的「看起来一切正常」**。

所以 `skip` 必须显式出现，且**在 UI 上不能和 `ok` 共用颜色**。

#### 人类输出保持逐字节不变

`--json` 是**增量**：不加参数时 `./deploy.sh verify` 的输出与加协议层之前**逐字节一致**，
退出码也不变（判定逻辑一行没改）。实现上靠两个约定：

- 原来打印过的行（`ok` / `warn` / `log`）**原样保留**，协议项用「只记录不打印」的通道挂上去；
- 原来没打印的项（如 `kubevip.daemonset_*` 只在缺失时才 warn）不会凭空多出一行 ✓。

验证方式：同一场景下 `git stash` 前后各跑一次 `verify`，`diff` 应为空。

#### P0 已落地的判定项 id（实施结果）

> 下面是 P0（`verify` / `preflight`）的 32 项。**P1 又加了 6 项**（三个只读命令的判定项），
> 快照总数 38 项 —— 见 §8.2「三个只读命令」与附录 v6。
> `selftest.sh` 第 12 节对这份清单做快照断言：**id 是对外契约，文案可改、id 不可改**。

`preflight`（含被 `verify` 复用的部分）：

| id | source | 说明 |
| :--- | :--- | :--- |
| `env.tools` | local | ssh / scp / sed 是否存在（`die`） |
| `config.nodes` | config | `NODES` 声明是否可解析 |
| `config.vip_cp` / `config.vip_traefik` | config | 两个 VIP 是否已填 |
| `vip.not_node_ip` | config | **VIP 不得等于任何节点物理 IP**（硬约束 1） |
| `vip.distinct` | config | 控制面 VIP 与业务 VIP 不得相同 |
| `node.count_ha` | config | 节点数是否够 etcd 高可用 |
| `ssh.reachable` | node | 逐台 SSH 是否可登录 |
| `k3s.install_source` | config | 官方源但没配代理 → warn |
| `k3s.download_proxy` | node | 逐台经代理真下载一次安装脚本 |
| `render.placeholders` | local | 渲染后是否残留 `__PLACEHOLDER__`（`die`） |
| `traefik.replica_eligible` | cluster | 副本数 vs 可承载节点数（preflight 硬校验 / verify 软复核共用同一 id） |

`verify`：

| id | source | 说明 |
| :--- | :--- | :--- |
| `node.list` | cluster | `kubectl get nodes` 是否成功（**NotReady 本身不算失败**，与改动前一致） |
| `node.taint_spec` | config | `NODE_TAINTS` 条目是否合法 |
| `node.taint_present` | cluster | 逐节点核对声明的污点是否真的在 |
| `kubevip.daemonset_controlplane` / `_services` | cluster | 两套 DaemonSet 是否在位 |
| `vip.cp_bound` | node | 控制面 VIP 的 `/32` 落在哪个节点 |
| `vip.tcp_6443` | node | 经 VIP 的 6443 TCP 是否可达 |
| `apiserver.readyz` | node | 经 VIP 请求 `/readyz`（含 k3s 关闭匿名时 401/403 属预期的分支） |
| `apiserver.readyz_authed` | cluster | 节点侧已认证复核 apiserver 是否真健康 |
| `tls.apiserver_san` | node | **证书 SAN 是否含 VIP**（`curl -k` 查不出来） |
| `local.direct_readyz` | local | 本机 kubectl 经 VIP 直连（不通属预期，只提示） |
| `traefik.external_ip` | cluster | 业务 VIP 是否为配置值（是节点物理 IP 则不漂移） |
| `traefik.external_traffic_policy` | cluster | 是否 `Local` |
| `traefik.replica_distribution` | cluster | 副本是否摊在 ≥2 个节点（反亲和是 preferred，会静默失效） |
| `traefik.replica_landing` | cluster | 落点数是否 ≥ `TRAEFIK_REPLICAS` |
| `business.connectivity` | node | `http://VIP/` 是否有响应（404 也算通） |
| `business.vip_advertised@<节点名>` | node | 逐台确认「本机 → 业务 VIP」（Local 策略下各自通告） |
| `registry.registries_yaml@<节点名>` | node | 逐台比对 `registries.yaml` 与脚本产物 |
| `registry.registries_yaml` | config | 未开启 `MANAGE_REGISTRY` 时的整段 `skip` |
| `version.pinned` | config | `K3S_VERSION` 是否钉死（可复现的最后一环） |

> 带 `@<节点名>` 后缀的 id 是**逐节点展开**的：同一项在 3 台节点上会产出 3 条，
> 前缀稳定、后缀可变，消费方按前缀分组即可。

**为什么 `id` 必须稳定**：现在外部若要读 `verify` 的结论，只能正则匹配 `✓` / `!` ——
这是**脆弱耦合，文案改一个字就断**。而 `id` 让「判定项是什么」和「怎么显示」彻底解耦。

**这一层做对了，后面所有消费者（CLI、Go API、Web UI、未来的 AI）复用同一套事实结果。**
这比「做 GUI」本身更重要。

### 8.2 P1：只读控制台

P1 的全部内容是「一张状态卡 + 若干表格」，**不引入任何图表库**（§5.4）。四个视图：

| 视图 | 内容 |
| :--- | :--- |
| **状态首页** | `verify --json` 的渲染结果，按 `level` 分组，每项带 `detail` 与 `fix` |
| **拓扑事实** | VIP 持有者（`plndr-cp-lock` 的 `holderIdentity`）、漂移次数（`leaseTransitions`）、`eligible nodes` vs `TRAEFIK_REPLICAS` |
| **方案配置视图** | 见下 |
| **配置一致性视图** | §7.4 的跨文件事实（`k3s-ha` 的 `NODE_TAINTS` ↔ `monitoring` 的 `TAINT_KEY` / `INFRA_NODE`） |

#### 方案配置视图：只读，但必须标注来源

P1 需要能「看到当前方案配置是什么」，**但不是自由编辑器**：

```text
┌─────────────────────────────────────┐
│ K3s HA 配置                         │
├─────────────────────────────────────┤
│ 节点                                │
│   k1   172.16.0.211    control      │
│   k2   172.16.0.212    control      │
│   k3   172.16.0.213    control      │
│                                     │
│ Control Plane VIP     172.16.0.210  │
│ Traefik VIP           172.16.0.180  │
│ Traefik replicas      2             │
│ Registry              172.16.0.222:5000
└─────────────────────────────────────┘

来源：k3s-ha/config.env + config.local.env
[查看原始配置]
```

**P1 不提供修改（v9 起可以了，见本节末）。** 这与 §十「配置的唯一事实来源必须是可 git diff 的文件，GUI 只是它的视图」
是同一条约束。真要改配置时走的是「打开文件 / 提交 PR」，而不是在网页上填表单
——后者会重新引入决策复杂度（§十）。

「来源」那一行是刻意加的：它让用户始终知道**这个值是从哪个文件读出来的**，
而不是让界面变成一个凭空产生配置的地方。

#### v9：提供了「改配置」，但那两条约束一条都没放弃

先把可能被误读的地方说清：**「提供修改」不等于「把配置搬进网页」。**

| 原来的约束 | v9 怎么满足的 |
| :--- | :--- |
| 唯一事实来源必须是**可 git diff 的文件** | 控制台写的就是**文件**（`config.local.env`），不是数据库、不是内存状态。而且写的是**不进 Git** 的那个 —— 「改配置」不会让工作区变脏，也就不会出现「`git checkout` 之后配置被静默回退成上个环境」 |
| 自由表单会**重新引入决策复杂度** | 编辑的是**一个键的值**，不是自由表单。而且：能改哪些键由 `config.env` 里的【环境相关】标记决定（**脚本说了算**，控制台不自己判断）；改之前必须过 `validate-config`（规则与真正部署时同一份）；`source` 如实报出实际加载了哪几个文件 |

「来源」那一行在分层之后变得更必要了：现在可能是
`k3s-ha/config.env + config.local.env`，而**同一个键可能被后面的文件覆盖**——
只报一个写死的文件名，会让人对着 `config.env` 里那行根本不生效的值发呆。

界面上的形态是「**本环境取值**」这张表（11 个可改的键 + 说明 + 当前值 + 来源），
以及一个两步的编辑对话框。它**不依赖先跑一次 `show-config`**：
服务端自己会去问 `deploy.sh` 哪些键可以改。

#### P1 已落地的数据出口：三个只读命令（实施结果）

P1 前端需要三份数据：「当前方案配置」「拓扑事实」「跨文件一致性」。
它们**没有**让 Go 去解析 `config.env`，而是给 `deploy.sh` 加了三个只读子命令：

| 命令 | 回答什么 | 连集群吗 |
| :--- | :--- | :--- |
| `show-config --json` | 脚本当前解析到的方案配置（节点 / 污点 / VIP / 副本 / 版本 / registry） | 否 |
| `topology --json` | VIP 持有者与漂移次数、可承载节点数 vs 副本数、副本实际落点 | 是（不可达时如实报 `reachable:false`，不失败） |
| `consistency --json` | §7.4 的跨文件事实（`NODE_TAINTS` ↔ `TAINT_KEY` / `INFRA_NODE`） | 否 |

> **v9 加了第 4 个：`validate-config --json`** —— 只做**纯本地**配置校验，连集群都不连。
> 它是「控制台改配置之前先验证候选值」的落点，做法是把 `preflight` 里的本地校验抽成
> `validate_config_local()` 两边共用。**这也是为什么它必须存在**：
> 如果控制台自己判断「这个 VIP 合不合法」，规则就有两份定义，
> 而两份定义必然在某个边界条件下给出不同答案。
>
> 它配合 `CONFIG_LOCAL_OVERRIDE` 使用：把候选值写成临时文件指过来跑一遍，
> **通过了才写盘**，而不是写完再回滚。

**为什么不放在 Go 里**：

1. **`config.env` 是 bash 语法**（含数组），`source` 才是它的权威解析器。
   在 Go 里重写一遍解析器，等于把「配置的唯一事实来源」复制成两份，必然漂移。
2. 这三个命令**故意不跑 `preflight`**。`preflight` 要 SSH 到每台机器，
   而这些命令存在的意义之一恰恰是「集群或网络不正常时也能看」——
   配置有问题时它们如实报出「脚本读到了什么」，而不是被 preflight 挡在门外。

这三个命令是 §8.5「Go 后端可以薄到不可思议」的兑现：
**Go 侧没有一行配置解析、没有一行方案判定**，只有「放行哪些命令」这一张表。

#### Go 侧的边界落在哪：一张命令表（实施结果）

§3.2 划的边界「控制台是否允许执行某操作 → Go」，实施后就是 `internal/api/commands.go`
里的一张表。P1 时它是两个**正交**字段：

- `read_only`：这个命令改不改集群；
- `allowed`：**P1 允不允许从控制台发起**。

写操作全部 `read_only:false, allowed:false`，并且**照样列出来**，附上影响面文案。
理由就是 §四 那一条：用户看到一个没有按钮的界面，会自己去服务器上敲 `all` ——
那才是真正危险的，因为他不会有机会看到「`traefik` 会滚动重启入口」。

所以 `/api/commands` 把写操作连同影响面一起返回，界面照实展示「为什么这里没有按钮」；
`POST /api/run` 拒绝时返回 **403 + `command_not_allowed` + 影响面原文**，而不是一个通用错误。

另外把「命令存在」与「命令被允许」分成两个错误码：未知命令是 `400 unknown_command`，
存在但不放行是 `403 command_not_allowed`。两者的排错方向完全不同。

> P2 把 `read_only` 升级成了 `severity`（三档），`allowed` 保持不变。见 §8.3。
>
> **v9 加了第三类：控制台自己的写能力。** `config-edit` 受同一套闸门管控
> （`-allow-write=config-edit`），但**刻意不进这张表** ——
> 这张表的语义是「`deploy.sh` 的子命令」，混进别的东西会让它不再可信，
> 而「控制台认识哪些命令」这件事的全部依据就是它。
> 它在 Go 里是一个独立的 `consoleCapabilities` 列表，`AllowedWrites()` 把两者合并。
>
> 放行方式也**单独开**、不与写操作合并：有人只放行 `kubeconfig` 这种低风险命令，
> 那份最小授权的意图不该被「顺手也能改配置」破坏 ——
> 改配置影响的是**之后每一次部署**，比跑一条命令的后果更长远。

#### 前端相对 §5.1 基线的两处偏离（实施结果）

| 偏离 | 理由 |
| :--- | :--- |
| `@k3s-console/vite-config` 砍掉 `@tailwindcss/vite` / `code-inspector-plugin` / `react-compiler` / `assetsDir: assets<version>` | 前三个与「把已正确的判定逻辑可视化」无关，少一个 babel 环节就少一处构建风险；`assetsDir` 是给「静态资源走对象存储、新旧 chunk 并存」用的（见 PassUp 前端），而控制台的前端产物由 Go 二进制直接托管，Vite 默认的 content-hash 文件名已经够用。因为不用 Tailwind，`main.tsx` 里那层 `StyleProvider layer` 也随之消失 |
| 新增第 5 个视图「动作清单」 | §8.2 定的是 4 个视图。实现时发现 §四 那条约束（影响面是一等公民）如果不落到界面上，就永远只是一句文档；而把「当前不开放的动作 + 各自影响面」列出来，恰恰是最诚实的呈现方式 |

#### P1 实施结果（实测）

- **四态语义在真实数据上成立**：`verify --json` 22 项 → `ok 21 / warn 1 / error 0 / skip 0`。
  唯一那条 warn 是 `local.direct_readyz`（本机 kubectl 经控制面 VIP 访问失败）——
  控制台所在的本机不在集群网段，属预期。
- **§5.3 的判断被实测确认**：`source` 分布 `cluster 10 / node 10 / local 1 / config 1`。
  `client-go` 只能覆盖 `cluster` 那 10 项，另外 12 项它拿不到。
- **`skip` 不是纸面设计**：静态检查发现脚本里共 12 处 `skip` 分支。
- **三个只读命令的实测耗时**（Windows + Git Bash）：`show-config` 14.7s、
  `consistency` 6.6s、`topology` 31.5s、`verify` 202.6s。
  ⚠️ 这个量级**不代表部署形态**：同一条 `show-config` 在 Linux 管理机上是百毫秒级，
  这里 13 秒几乎全是 Git Bash 的进程开销。它的直接后果是**前端必须统一走 SSE**——
  「快的轮询一次、慢的流式」这种按快慢分叉的写法，在本机开发时会让「快」的那条看起来像卡住。
- **真浏览器验收**：5 个视图全部渲染成功，**控制台 0 error**（含 `pageerror`）。
  这一条不能省：接口返回正确 JSON 和页面渲染出来是两件事，中间任何一步出问题
  （构建产物、路由、antd 版本、CSS-in-JS 注入）都会得到 **HTTP 全 200 的白屏**。
- **Go 侧测试 13 项**：`api` 8 项（拒绝写操作 / 未知命令 / 多余参数 / 命令表自洽）、
  `task` 5 项。其中最关键的一条是把「**退出码 1 + 合法协议 JSON → `Done` 而不是 `Failed`**」
  固定成回归测试 —— 这是整个 P1 最容易搞错的语义：把它归成失败，
  界面就会把「一次成功的检查，结论是有问题」显示成「控制台出故障了」。
- **`deploy.sh` 离线自测 84 项（80 通过 / 0 失败 / 4 跳过）**；协议项 id 快照由 32 项扩到 **38 项**
  （新增的 6 项就是上面三个只读命令的判定项）。

### 8.3 P2：动作编排

`preflight` / `taint` / `traefik` / `all` 带**影响面标签**（§四）、二次确认、实时日志流。

`uninstall` 建议**放到 P2 的最后**，且必须带 §四 那条「不会恢复什么」的清单。

#### 三档 `severity` 取代 `read_only`

P2 把 P1 的 `read_only` 换成了 `severity`。原因是那个布尔值一直在承担两件事，
而 P2 需要它同时驱动「上什么色」和「要不要确认」：

| `severity` | 含义 | 颜色 | 默认确认 |
| :--- | :--- | :--- | :--- |
| `readonly` | 不改任何东西 | 绿 | 点了就跑 |
| `write` | 会改集群或节点 | 黄 | 二次确认 |
| `destructive` | 不可逆，且**不等于恢复出厂** | 红 | 手打 + 逐条勾选 |

`IsReadOnly()` 由 `severity` 派生，所以「只读」这个概念没有消失，只是不再是**存储形态** ——
这也是为什么它和 `allowed` 仍然是正交的（`allowed` 是「这台控制台当前允不允许」，
由启动参数决定，与命令本身的性质无关）。

颜色沿用与判定项同一套语义 token（`--k3s-ok/warn/error`）：界面上
「绿 = 没影响、黄 = 有影响、红 = 不可逆」应当是**同一个心智**，
不管它是用来标记一个判定项还是一个动作。

#### 确认阶梯：分级而不是一刀切

`confirm` 四档，对应四种代价：

| `confirm` | 命令 | 界面要求 |
| :--- | :--- | :--- |
| `none` | 全部只读命令 | 不弹窗 |
| `button` | `kubeconfig` / `prepare` / `server-first` / `join` / `taint` / `kube-vip` / `traefik` | 摊开影响面，点确认 |
| `type` | `all` | 还要**手打命令名** |
| `type+ack` | `uninstall` | 手打 + 逐条勾选「不会恢复什么」 |

**为什么不统一成「确定吗？」**：如果 `all` 和 `verify` 弹的是同一个框，用户会养成
闭眼点确定的习惯 —— 而那个习惯恰好会在 `uninstall` 上出事。分级的目的是让确认**携带信息量**。

`phrase` 必须**原样**相等：Go 侧直接比较 `req.Confirm != spec.Phrase`，
**刻意不做 `TrimSpace`、不做大小写归一**。`" all"` 和 `"ALL"` 恰恰是「粘贴 / 手滑」
最容易产生的两种形态，放宽等于把这道闸门关掉。这条被
`TestWriteCommandWithWrongPhraseIsRejected` 钉死（第一版实现就漏了 `TrimSpace`，是测试抓住的）。

**确认必须在服务端校验**，不能只做在界面上：

- 界面上的确认是给人看的；
- Go 侧的校验是给「绕过界面直接打 API」看的。

两者都要有，但**只有后者是边界**。`handleRun` 的顺序是
`command_not_allowed`(403) → `confirmation_required`(400) → `acknowledgement_required`(400)，
`writer_busy`(409) 在最后。

#### `uninstall` 的「不会恢复什么」是脚本自己的原文

四条勾选项**不是**在 Go 里另写一遍，而是 `phase_uninstall` 收尾时 `warn` 打印的那四行原文。
§四 说「脚本用文档写了，控制台要把它变成界面的一部分」—— 变成勾选项就是字面意义上的「一部分」。

`missingAcks()` 返回**缺哪几条**，接口把缺的那几条原文回给前端，
所以界面能说「还有 N 条未确认」，而不是一个通用错误。

`CONFIRM_UNINSTALL=yes` 由 Go 在放行之后注入，并且**刻意不下发到前端**（`json:"-"`）：
前端不该也不需要知道这个开关。脚本的闸门仍然有效，而不是被控制台绕过去 ——
`TestUninstallSatisfiesScriptGate` 用一个会回显该环境变量的桩脚本把这条固定住。

#### 写操作串行，且写期间只读也一并拒绝

`taint` 和 `traefik` 同时跑，结果不是「更快」，而是两个进程按各自的节奏改同一批节点对象 ——
那种状态连排错都无从下手。所以 `Manager` 持一个 `exclusiveID`，第二个写操作拿到
**409 `writer_busy`**。

**写操作期间只读命令也一并拒绝**：写操作跑到一半时读出来的状态是**中间态**，
拿它做判断只会误导人。界面如实反映这一点（所有按钮都禁用并说明原因），
而不是让用户点下去才发现。

#### 取消：是 `cancelled`，不是 `failed`

`POST /api/tasks/{id}/cancel` 杀掉脚本进程。它单列一个状态，因为**用户看到的文案完全不同**：

- `failed`：脚本自己坏了 → 去查日志；
- `cancelled`：人按了取消 → 「**不会回滚已经做完的步骤**，集群可能停在中间状态；
  `deploy.sh` 的每个阶段都是幂等的，重跑对应阶段即可收敛」。

把它归成 `failed` 会让人以为是脚本的问题，去查半天日志。

`Cancel()` 只对「正在跑」的任务有效。每个任务从 `Manager` 的父 ctx 派生自己的子 ctx，
**不能用 HTTP 请求的 ctx** —— `verify` 要跑 3 分钟，请求早就返回了。

#### 部署级闸门：`-allow-write=false`

启动参数。打开时整台控制台退化成 P1 只读：`/api/commands` 会把写操作的 `allowed`
改写成 `false`，所以界面上**根本不会渲染出可点的执行按钮**，而不是点了才报错；
直接打 API 也得到 403。

这一层存在的理由是部署形态：把控制台放到一台别人也能访问的机器上时，
不必指望界面上的确认弹窗。

#### 前端：动作清单从「说明」变成「操作」

- 按 `severity` 分三组（只读 / 写操作 / 破坏性），每组带语义色图标与一句话提示。
- 新增**「发起」列**，写的是「点了就跑 / 弹窗确认 / 手打命令名 / 手打 + 逐条勾选」。
  这一列是这一页存在的主要理由：它让用户在**点之前**就能看出 `uninstall` 比 `verify` 贵。
  把阶梯藏进弹窗里就晚了 —— 那时人已经决定要点了。
- 点执行后按 `confirm` 弹确认，确认后**执行面板**（Drawer）自动打开：
  实时日志 + 结果 + 中止按钮。
- 执行槽位放在 store 而不是组件里：`all` 要跑几分钟，用户中途切去看拓扑是很自然的事。
  组件卸载会断掉 SSE 订阅，但脚本挂在 Go 的 Manager 上下文上照跑；
  回来时凭 `taskId` 重新订阅，服务端会把缓冲的日志整段重放
  （`Task.Subscribe` 在同一把锁里原子地取「已缓冲日志 + 实时通道」）。
  重放意味着本地**必须先清空日志**，否则会看到重复行。
- 只读视图也补上了「中止」：`verify` 在本环境要跑一分钟，只有写操作能停是没道理的。

#### P2 实施结果（实测）

- **写通道在真机上跑通**：通过界面发起 `kubeconfig`（§四 点名的「风险最低的写操作」），
  确认阶梯 → 执行 → 实时日志 → 结果全部走通。产物 `server` 指向控制面 VIP
  （`172.16.0.210`），**重跑前后 sha256 完全一致** —— 幂等成立。
- **确认阶梯四级全部实测**（真浏览器）：`button` 级没有输入框且确认按钮直接可点；
  `type` 级在 `ALL` / `" all"` 下保持禁用、原样输入 `all` 才可点；
  `type+ack` 级列出 4 条勾选项，只手打短语仍禁用、4 条全勾才可点。
  破坏性确认按钮实测计算样式为 `rgb(255, 77, 79)` 的 danger 样式 ——
  「破坏性要看起来就是破坏性」这条被断言固定住，而不是靠肉眼看截图。
- **执行面板**：日志在 1~2 秒内出现（**早于结果**，所以「实时」是真的），
  `show-config` 全流程约 11~20 秒，**控制台 0 error**（含 `pageerror`）。
- **只读模式验收**：`-allow-write=false` 下 5 个只读命令仍可点、
  9 个写操作**仍然列出**但按钮全部禁用，绕过界面直接打 API 得到 `403 command_not_allowed`。
- **Go 侧测试 20 项**（`api` 15 + `task` 5）。新增的关键几条：确认串必须原样、
  勾选必须覆盖全部、写操作串行（第二个 409 `writer_busy`，且写期间的只读也 409）、
  取消后状态是 `cancelled` 且文案含「不会回滚」、`-allow-write=false` 下既隐藏又拒绝。
- **`deploy.sh` 离线自测 85 项（81 通过 / 0 失败 / 4 跳过）**；
  集成测试 88 项（87 通过 / 0 失败 / 1 跳过）。
  协议项 id 快照由 38 项扩到 **43 项**（新增的 5 项见下）。

#### 顺带修掉的一个 `deploy.sh` 缺陷：写操作「报告成功但什么都没做」

验证写通道时发现 `phase_kubeconfig` 会**打印「已写入」并返回 0，而目标文件一个字都没改**。

根因有两层：

1. **`main` 里的 `case ... || rc=$?` 让 `set -e` 在阶段内部整体失效。**
   那一层是刻意加的（`verify` 判定不通过要 `return 1`，而不是在 `emit_json` 之前退出），
   但 bash 对 AND-OR 列表有豁免：位于 `||` 左边的命令，其 `set -e` 被忽略，
   **而且这个豁免贯穿整个调用树**。所以「命令失败会自动停」这个假设在 `phase_*` 内部不成立。
   （最小复现：函数里写 `false`，它后面的语句照样执行。）
2. `phase_kubeconfig` 原本用 `sed -i` 且**不看退出码**。`sed -i` 内部是
   「写临时文件 + rename」，rename 被拒时（受控目录 / 只读挂载 / 沙箱）原文件不改，
   却在同目录留下一个**含集群凭据**的临时文件 —— 实测权限是默认的 `644`。

后果正是本方案要消灭的那种「看起来一切正常」：控制台会把一次什么都没做的写操作显示成「完成」。

改法：这个阶段的每一步都显式检查退出码，失败时 `die` 出带
`DIE_ID` / `DIE_SOURCE` / `DIE_FIX` 的 error 判定项（新增 5 个 id：
`kubeconfig.fetch` / `.rewrite` / `.vip` / `.chmod` / `.replace`），
并且不再用 `sed -i`，改成「显式临时文件 + `mv`」；`mv` 是唯一的原子替换步骤。
另外补了一条「改写后必须真的出现 VIP」的检查 —— 否则正则会安静地写出一份指向
**节点 IP** 的 kubeconfig：平时能用，一换主就断，而且看不出是哪一步出的问题。

实测：写不进去时现在返回 **退出码 1 + 一条 error 判定项 + `fix` 建议**，
界面显示「完成，但有判定未通过」并**列出是哪一项**（`kubeconfig.replace`）。

⚠️ **同一类问题在其他阶段仍然存在**：任何 `remote_script` 调用的失败都不会中止阶段。
这次只修了被证明坏掉的那一处（它恰好是 P2 暴露给用户的写操作），
`main` 里加了注释说明这个陷阱，`selftest` 也加了一条静态断言防止它被改回去。
其余阶段要不要逐个补检查，是 P3 的活。

#### v9 补充：改配置这条写路径（实施结果）

它复用 P2 的全部写机制（确认 / 独占 / 审计），但多了一步「先预览」：

```text
预览（不写盘） → 确认令牌 → 写入（重新校验 + 备份 + 审计）
```

| 环节 | 实测结果 |
| :--- | :--- |
| 预览拦下非法值 | `not-an-ip` / `999.1.1.1` / 节点 IP `192.168.1.201` → 全部 `blocked`，给出具体原因与 `fix` |
| 预览放行合法值 | `192.168.1.215` → 9 项通过 |
| 预览**不写盘** | 与备份逐字节一致 |
| 令牌 | 错误令牌 → 409 且不写盘；文件在预览之后被改过 → 令牌失效，强制重新预览 |
| 落盘 | 只改 1 行（`diff` 是 `34c34`）、文件行数 67→67 不变、注释完好、留 `.bak` |
| 不进 Git | `config.local.env` 不在 `git status` 里 |
| 审计 | `config-edit done :: VIP_CP 改为 192.168.1.215` |
| 界面（真浏览器） | 非法值 → 校验「1 异常 / 2 正常」+ 确认写入**禁用**；合法值 → 绿色 diff + 「9 正常」+ 确认写入**可用**；写入后显示备份路径与「改动不会自动生效」 |

数字同步：`deploy.sh` 离线自测 **126 项（126 通过 / 0 失败）**；
协议项 id 快照 **43 → 44 项**（新增 `config.node_ips_unique`）；
Go 侧测试 **73 项**（新增 `configfile` 包 15 项 + `api` 包配置编辑 14 项）。

⚠️ 界面那两个 bug（加载中显示错误的「只读」结论、确认按钮该亮却是灰的）
是**拿真浏览器截图时才发现的**，`tsc` 与单测都放过 —— 见 v9 附录。

### 8.4 P3：补齐校验 + 指标

补 §7.1~7.3 的校验，再接 `client-go` / Prometheus API。

> **v9 提前落了一小块**：`is_ipv4`（VIP 格式）与 `config.node_ips_unique`（节点 IP 重复）。
> 它们本来排在这里，但「控制台改配置」必须先有校验才敢让人改 —— 没有校验的编辑器
> 只是把「手滑写错」从 vim 搬到了网页。**§7.1 / §7.2 / §7.3 的主体仍未关闭。**

顺序不能反：**校验补齐后指标才有意义**——否则「面板全绿」和「真的正常」仍然是两件事
（这正是 §一 里「看起来一切正常」的另一种形态）。

注意 §5.3 的结论：`client-go` 是**可选项**，定位是「补充交互和实时状态，而不是替代 `verify`」。

### 8.5 Go 后端第一版的结构：刻意保持薄

第一版真的只有这些：

```text
internal/
├── task/
│   ├── manager.go
│   └── task.go
├── executor/
│   └── shell.go
├── protocol/          ← 解析 deploy.sh 的 --json 结果协议
│   └── protocol.go
├── configfile/        ← v9：保留注释的逐行编辑 config.local.env
│   └── configfile.go
├── api/
│   ├── task.go
│   └── state.go
└── auth/
    └── ...
```

`configfile` 这个包值得单说一句，因为它的存在方式本身是个设计结论：
**它不是「解析配置」的包，而是「替换某一行的值」的包。**
如果做成「解析成 map → 改完序列化回去」，`config.local.env` 里那些注释
（为什么这么填、踩过什么坑）会被全部抹掉 —— 而抹掉之后的文件仍然「能用」，
只是再没人知道为什么这么填。

它也不含任何方案判定：不校验 VIP 是否合法、不知道 `NODES` 是什么。
那些整包交给 `deploy.sh`。**它只负责「把这一行的值换成那个字符串，别动别的」。**

**不要**一开始就铺成这样：

```text
domain/  application/  infrastructure/
repository/  kubernetes/  cluster/  deployment/  ...
```

后者是「一个业务后端」的形状，而这个项目本质上是一个**控制层**——
它自己不持有领域模型，领域模型在 `deploy.sh` 和 `config.env` 里。
照 DDD 分层铺开，很容易把一个本该很薄的控制层重新做成一个复杂后端，
而且会诱使你把 §3.2 明令禁止的方案判定逻辑搬进来（有了 `domain/cluster/` 就会想往里放东西）。

### 8.6 开工顺序

```text
① 改 deploy.sh：verify --json / preflight --json      ✅ P0
        ↓
② CLI 自己验证 JSON（此时还没有任何 UI，但协议已被真实使用）  ✅ P0
        ↓
③ Go 最小 API（exec + SSE）                            ✅ P1
        ↓
④ React 只读首页                                       ✅ P1
        ↓
⑤ P1 跑通                                             ✅ P1
        ↓
⑥ 再做 P2 的执行按钮                                   ✅ P2
```

**最关键的一条：不要先做 UI。**

真正的第一步是**把 `deploy.sh verify` / `preflight` 做成稳定的机器可读结果协议**。
协议一旦稳定，后面的 CLI、Go、React、AI、日志、监控**都只是消费者**。

这也正是本方案与「给脚本套一个网页」的分界线：
先有协议，再有界面；而不是先有界面，再想办法从彩色文本里抠信息。

**P2 之后追加的一条经验**：⑥ 之所以放在最后，不只是因为「界面后做」——
写按钮会**逼你去验证写通道**，而正是在那一步才发现 `phase_kubeconfig`
一直在「报告成功但什么都没做」（见 §8.3 末节）。
换句话说，**只读界面永远发现不了这个缺陷**：它只消费结果，不执行动作。

### 8.7 P4（下一阶段）：应用受管对象 —— PassUp

> 本节只定**定位与边界**，不改动 P0~P3 的任何设计，也没有任何代码落地。

**决策：要加，但作为「第二类受管对象」，而不是把 PassUp 塞进 `deploy.sh`。**

现在只有一个受管对象，而且这一点没被显式写出来过：

```text
控制台
└── 受管对象：K3s 基础设施方案
      deploy.sh  →  verify --json
```

P4 之后变成两个，**各自持有自己的事实来源与验证协议**：

```text
控制台
├── 受管对象：K3s 基础设施方案
│     deploy.sh         →  verify --json
└── 受管对象：PassUp 应用
      passup-deploy.sh  →  verify --json
```

控制台只负责统一消费这两份协议。**第一版不改名、不扩范围** ——
「K3s 方案控制台」这个名字先留着，等真的有两类受管对象跑起来了再谈改名。

#### 为什么不能塞进 `deploy.sh`

```text
deploy.sh
  ├── k3s
  ├── kube-vip
  ├── traefik
  ├── monitoring
  └── passup        ← 不要这样
```

`deploy.sh` 是「K3s 方案」的事实来源。一旦它开始管 PassUp，它就会重新长成一个**巨型总管**，
而 §3.2 那条边界（判定归 Bash）会随之变模糊 —— 因为「Bash 现在管两件事」，
「哪件事该由 Bash 判定」就说不清了。

正确形态是**每个受管对象自己带协议**，控制台做统一的消费者。
这和 §三 的边界并不冲突：判定仍然全在 Bash，只是从「一个脚本」变成「一组脚本」。

#### 硬边界：控制台不许理解 PassUp 的业务代码

```go
// 明确禁止出现这种代码
if passupBackendReady && redisReady && minioReady && flywayVersion == xxx { ... }
```

理由与 §3.2 完全同源：**判定留在脚本里**。PassUp 的状态判定写在 `passup-deploy.sh` 里，
控制台只消费它的 `--json`。否则这套规则会散落在 Go 里，既无法被 CLI 复用，也无法被测试。

#### 协议统一：顶层加一个 `system`

现有协议顶层是 `phase` / `summary` / `items`。要让两个受管对象共用一个消费端，
顶层加 `system`（`k3s` / `monitoring` / `passup`）：

```json
{
  "system": "passup",
  "phase": "verify",
  "summary": { "ok": 12, "warn": 1, "error": 0, "skip": 2 },
  "items": [
    { "id": "backend.ready", "level": "ok", "detail": "2/2 ready", "source": "cluster" }
  ]
}
```

这是**向后兼容**的增量（Go 侧对未知字段本来就不敏感），但它会牵动一处实现：
**命令表要加一个 `system` 维度** —— 现在 `-script` 只指向一个脚本，脚本路径不再唯一。

#### 先做「PassUp 状态」，不要先做「部署 PassUp」

只读控制台解决的是「**我不知道现在到底对不对**」，这个价值高于「点一下部署」。
而且 P2 已经证明了一件事（§8.6）：写按钮会**逼你去验证写通道**，
而那一步才暴露了「报告成功但什么都没做」。所以顺序是：

```text
① PassUp 状态（只读，含版本 / 入口 / 各组件 Ready / 迁移版本 / 外部依赖）
        ↓
② PassUp verify（并入统一协议）
        ↓
③ 动作面：部署 / 重启 / 回滚 / 迁移 / 停止
```

**动作面直接复用 P2 的机制**：`severity` / `confirm` / `task` / SSE / `cancelled` / `writer_busy`。
也就是说 P2 那套不是 K3s 专用 —— P4 会让它升格成**通用任务执行框架**。

动作的危险等级建议（实施时再定）：

| 动作 | 建议 `severity` | 备注 |
| :--- | :--- | :--- |
| 部署 / 重启 | `write` | 可重复执行 |
| 迁移（Flyway） | `destructive` + `type+ack` | 见下方「回滚 ≠ 迁移回退」 |
| 回滚 | `write` | 指镜像回滚 |
| 停止 | `destructive` | — |

⚠️ **`回滚` 和 `迁移回退` 是两件事，界面上必须分开写。**
数据库迁移（Flyway）**没有**真正的回滚；能回滚的是镜像版本。
把两者合成一个「回滚」按钮，等于给出虚假的安全感 —— 这正是 §四 要防的东西。

#### ⚠️ 两个必须先定的问题

**① 写串行是全局的，还是按受管对象的？**

现在是**全局一把锁**（`ErrBusy`）。PassUp 部署要 `kubectl apply`，K3s 的 `taint` / `all`
也要动同一个集群 —— 这两个**应该互斥**。但 `kubeconfig`（只写本机文件）和 PassUp 部署
之间其实没有冲突。第一版建议**保持全局锁**（简单、保守），等真的被挡住再按对象细分。

**② PassUp 的「状态」里，PostgreSQL / Redis / MinIO 根本不在集群里。**

PassUp 后端依赖目前仍在**宿主机 Docker（172.16.0.222）**，`pass-up.backend` 仓库里
**没有** postgres / redis / minio 清单（见 `PassUp后端部署清单.md` 第九节勘误）。
所以状态视图里这几项的正确来源是**宿主机 Docker**，不是 k8s 对象 ——
照搬 `kubectl get pods` 会得到「查不到」，而「查不到」和「不健康」是两回事。
同理 `ai-service` 当前是宿主机上的 `resume-ai:8006`，要不要独立成一个受管对象待定。

#### P4 的价值：一条完整的验证链

K3s 单独看，更像一个「基础设施验收工具」。接上 PassUp 之后才形成完整的一条链：

```text
机器 → 集群 → 基础设施 → 应用 → 用户入口
```

即 `Node → K3s HA → kube-vip / Traefik → PassUp → Backend / AI → PostgreSQL / Redis / MinIO → 外网入口`。

这条链上任何一环断掉，「整体状态」都不该是绿的。**这才是本项目一开始要回答的那个问题。**

## 九、参考项目

| 项目 | 关系 |
| :--- | :--- |
| **Headlamp** | Kubernetes Dashboard 已于 2026-06 归档，官方博客推荐迁移到 Headlamp。**重点学它的交互**（多集群、可扩展、应用视图） |
| Kubernetes Dashboard | 虽已归档，`Node` / `Pod` / `Deployment` / `Events` 的页面组织方式仍有参考价值 |

**但要看清区别**——这决定了本项目不是「再做一个 Dashboard」：

```text
Headlamp            →  管理「任意 Kubernetes」
本项目控制台        →  固化「这一套 K3s 方案」
```

本项目最有价值的部分，恰恰是 Headlamp 天然不会有的那些：

```text
3 节点 → K3s HA → embedded etcd → kube-vip
      → 控制面 VIP → Traefik → 业务 VIP
      → externalTrafficPolicy: Local
      → 副本数 / eligible nodes 约束   ← 通用工具不知道这条
      → Registry → Verify Chain
```

「副本数必须 ≥ 可承载 Traefik 的节点数」这种约束，**通用工具不可能内置**，
因为它不是 K8s 的规则，是**这套方案的规则**。把它固化进界面，才是这个项目存在的理由。

## 十、明确不做什么

| 不做 | 理由 |
| :--- | :--- |
| 图形化 YAML 编辑器 | 配置的唯一事实来源必须是**可 git diff 的文件**，GUI 只是它的视图 |
| 任意字段的自由表单 | 自由表单会**重新引入决策复杂度**，正好是控制台要消灭的东西。要的是「声明拓扑，副本数自己算」 |
| 在 Go 里复制 **K3s 方案判定**（VIP / Traefik / TLS SAN / Registry…） | 见 §3.2。这是硬约束。注意边界：**控制台自身的**任务/权限/并发/参数校验属于 Go，不在此列 |
| 多集群管理 | P2 之前不引入。本项目的价值在「深」不在「广」 |
| 一个笼统的「一键部署」大按钮 | 见 §四。动作必须带影响面 |
| P1 阶段提供「修改配置」 | 见 §8.2。P1 的方案配置视图**只读**；**v9 补上了修改**，但走的是「编辑文件 + 跑校验」而不是自由表单 —— 见 §8.2 末节 |
| P1 引入图表库 | 见 §5.4。P1 是「一张状态卡 + 若干表格」，表格能表达的不画图 |

## 十一、关联阅读

- [K3s HA 一键部署方案（自动化）](./K3s HA 一键部署方案.md) —— §四「多固化的八件事」、§七「16 项验收判据」
- [可观测性栈 k3s 部署方案](./可观测性栈 k3s 部署方案.md) —— §十「实施结果」
- 代码：`cluster-infra/k3s-ha/`（`deploy.sh` + `config.env` + `manifests/` + `tests/`）
- 代码：`cluster-infra/monitoring/`（`deploy.sh` + `config.env` + 各组件清单）
- **前端基线**：`pass-up.frontend` —— §5.1 的技术栈与工程结构直接取自该仓
  （`pnpm-workspace.yaml` 的 `catalog:` + `apps/*` + `packages/{shared,vite-config,tsconfig,eslint-config}`）

## 附录：修订记录

### v9（配置分层 + 控制台改配置）

这一版与 v5~v7 有一个**根本区别**：**`deploy.sh` 的判定逻辑被改动了**。
前三版都写着「判定逻辑一行未改」，这次不成立 —— 见下表第 3 条。

起因是用户提出的一个问题：**`config.env` 不该被 git 跟踪吧？换个环境怎么办？**
查下来这不是洁癖：`config.env` 里全是本环境的 IP / 主机名 / SSH 用户名 / 代理 / Registry，
而它是被跟踪的。于是 `git checkout .` / `git stash` / `git reset --hard`
会**静默**把配置回退成上一个环境的值 —— 没有任何报错，下次 `./deploy.sh all`
就往错的网段装了。历史里已经留下了 `172.16.0.180/210/211/212/213/222`。

| # | 改动 | 说明 |
| :--- | :--- | :--- |
| 1 | **配置分三层** | `config.env`（进 Git，**只放模板 + 默认值 + 注释文档**）/ `config.local.env.example`（进 Git，**「该填哪些键」的清单**）/ `config.local.env`（**不进 Git**，本环境取值）。加载顺序不变：后者覆盖前者。换环境只改 local 文件，Git 完全不受影响 |
| 2 | **把「环境相关」做成可机器校验的契约** | `config.env` 里 `# 【环境相关】…` 注释**紧跟的**那一行 `KEY=` 即环境相关的键。自检第 14 节双向检查（模板里不许有 `config.env` 没有的键；标记过的键必须都出现在模板里），并额外断言模板只用 `10.0.0.x` 举例、不泄漏真实地址。**清单与模板各维护一份必然漂移**，所以用断言把两边钉在一起 —— 漏填的症状要到「部署到一半」才暴露 |
| 3 | ⚠️ **`deploy.sh` 的判定逻辑被改动了（首次）** | 新增 `is_ipv4()` 形状校验（非法值会被原样写进 kube-vip 清单和 apiserver 证书，报错点离原因很远，很难倒推）；新增判定项 `config.node_ips_unique`（两个节点用同一个 IP —— 复制粘贴节点清单时最容易犯）。**这两条本属 §8.4 P3 的范围，提前落地了** |
| 4 | **新增只读命令 `validate-config`** | 把 `preflight` 里的**纯本地**校验抽成 `validate_config_local()`，`preflight` 与 `validate-config` **共用同一份实现**。两处各写一份的话，「控制台说没问题、真去部署却报错」这种最难查的不一致迟早会出现 —— 而它恰好会出现在最需要信任的时候 |
| 5 | **新增 `CONFIG_LOCAL_OVERRIDE`** | 允许把「本环境覆盖文件」换成别的路径。控制台「改配置之前先验证候选值」靠它：候选值写成临时文件指过来跑 `validate-config`，**通过了才写盘**，而不是写完再回滚 |
| 6 | **`show-config` 的 `data` 扩了两块** | `editable`（`[{key, hint}]` —— 键与说明都来自 `config.env` 的标记，**决定权在脚本里，控制台只转手**）+ 11 个原本不可见的配置键（`vip_interface` / `k3s_download_no_proxy` / `ssh_port` / `ufw_mode` …）。另外 `source` 从写死的 `k3s-ha/config.env` 改成**如实报出实际加载了哪些文件**（分层之后，「这个值从哪来」成了排查的第一个问题） |
| 7 | **新增 `config-edit`：控制台自己的写能力** | 编辑 `config.local.env`（**不进 Git**）。它**刻意不进命令表** —— 那张表的语义是「`deploy.sh` 的子命令」，混进别的东西会让它不再可信，而「控制台认识哪些命令」的全部依据就是那张表。放行方式也**单独开**（`-allow-write=config-edit`），不与写操作合并：有人只放行 `kubeconfig` 这种低风险命令，那份最小授权的意图不该被「顺手也能改配置」破坏 |
| 8 | **§3.2 补第三类：控制台自己的写能力** | 原来的二分（K3s 判定 → Bash / 控制台自身校验 → Go）不够用了。`config-edit` 既不是 K3s 判定，也不是「校验」，而是控制台**自己写文件**。它落在 Go 这一侧，但「这份值合不合法」仍然整包交给 `validate-config` —— 控制台连「VIP 不能等于节点 IP」这条规则都不认识 |
| 9 | **§4 影响面表加「改配置」一行** | 它不改集群，但改的是**之后每一次部署**，比跑一条命令的后果更长远。确认令牌绑的是「键 + **改前全文** + 改后全文」—— 文件被别人动过（哪怕动的是别的键）令牌就失效，强制重新预览 |
| 10 | **id 快照 43 → 44 项** | 新增 `config.node_ips_unique` |

实施中确认 / 修正的四点：

- **只替换值本身，保留全部注释。** 那些注释就是这个文件的文档（为什么这么填、踩过什么坑）。
  「解析成 map、改完再序列化回去」会把它们全部抹掉 —— 而抹掉之后的文件仍然「能用」，
  只是再没人知道为什么这么填。实测改一行 `VIP_CP`，`diff` 只有 `34c34`、文件行数不变。
  写盘前留 `.bak`：校验已经做过了，但备份防的不是「值不合法」，而是「我们自己把文件写坏」——
  那是两件不同的事。
- **§7.3 的「VIP 空闲」仍然没查。** 这次只补了**格式**（是不是合法 IPv4）。
  「ping 不通」与「同网段」需要扫网段，是另一件事，仍在 P3 范围内 ——
  不要因为看到 `is_ipv4` 就以为 §7.3 关掉了。
- **Git Bash 的进程开销直接决定了交互设计。** `validate-config` 实测 **28 秒**、
  `show-config` **11.5 秒**（`sys` 时间占 19s，全花在 fork 上；同样的命令在 Linux 上是一两秒）。
  非法值只要 1 秒（前几项就 `die` 了），合法值要跑完 `render_manifests`。
  界面上已如实写明等待时间，按钮转圈就是它在跑。**要优化就减少 `$(...)` 子 shell 数量，
  不要砍检查** —— 那些检查正是这个功能存在的理由。
- **视觉验收抓到的两个 bug 都不是逻辑错误，而是「不该显示的显示了」**：
  ① 加载中显示「只读：控制台没有放行配置编辑」（三元表达式在 `data` 还是 `null` 时落到了警告分支）
  —— **把「还不知道」说成了「确定没有」**，正是本项目最忌的；
  ② 「确认写入」按钮该亮却是灰的（条件写成 `!dirty`，正好反了：没改过才是不该写入的时候）。
  这两个 `tsc` 和单测都发现不了，是拿真浏览器截图时看出来的。

#### 一个需要说清的悬空项

v4 第 6 条把「填一次配置」改成「准备一次方案配置」，理由是
「避免让人误以为 P1 就有网页表单；配置来自文件，不是来自表单」。
本版**确实提供了网页改配置**，但那条约束仍然成立 —— 因为控制台写的就是**文件**
（`config.local.env`），校验也是重新跑 `deploy.sh`。
变的是「不提供修改」这一条，不是「配置来自文件」这一条。
§8.2 与 §十 的对应表述已同步。

### v8（P4 定位决策回填，**未实施**）

P2 实施完成后确定了下一阶段的定位：**控制台从「一个受管对象」演进为「一组受管对象」，
PassUp 作为第二类接入**。本版**只记录决策与边界**，不改动 P0~P3 的任何设计，
也没有任何代码落地。

| # | 改动 | 说明 |
| :--- | :--- | :--- |
| 1 | **新增 §8.7（P4：应用受管对象）** | 明确「不把 PassUp 塞进 `deploy.sh`」：那会让它重新长成巨型总管，并让 §3.2 的边界变模糊。正确形态是每个受管对象各自持有事实来源与验证协议（`passup-deploy.sh` + `verify --json`），控制台只做统一消费 |
| 2 | **§八 分期表加 P4 行** | **第一版不改名、不扩范围**；「K3s 方案控制台」这个名字等真有两类对象跑起来再谈 |
| 3 | **协议顶层加 `system`** | `k3s` / `monitoring` / `passup`。向后兼容的增量（Go 对未知字段不敏感），但会牵动实现：**命令表要加 `system` 维度**，因为 `-script` 不再只指向一个脚本 |
| 4 | **明确禁止控制台理解 PassUp 业务代码** | 与 §3.2 同源：`if backendReady && redisReady && flywayVersion == xxx` 这类判定不许进 Go，否则规则散落且不可测 |
| 5 | **动作面复用 P2 机制** | `severity` / `confirm` / `task` / SSE / `cancelled` / `writer_busy` 不是 K3s 专用 —— P4 会让它升格成**通用任务执行框架** |
| 6 | **记录两个待定问题** | ① 写串行保持**全局一把锁**还是按受管对象细分（建议先全局）；② PassUp 的 PG / Redis / MinIO 在**宿主机 Docker** 而非集群，状态来源不能照搬 k8s 对象 ——「查不到」和「不健康」是两回事 |
| 7 | **记下「回滚 ≠ 迁移回退」** | Flyway 迁移没有真回滚，能回滚的是镜像版本。合成一个「回滚」按钮等于给出虚假的安全感 |

### v7（P2 实施后回填）

P2 已在 `console/` 落地（动作编排）。**判定逻辑仍然一行未改**，
`deploy.sh` 只动了一处**被写通道暴露出来的缺陷**（`phase_kubeconfig`，见 §8.3 末节）。

| # | 改动 | 说明 |
| :--- | :--- | :--- |
| 1 | **`read_only` 布尔 → `severity` 三档** | 那个布尔值一直在承担两件事（上什么色 / 要不要确认）。P2 需要它同时驱动两者，于是拆成 `readonly`/`write`/`destructive`。`IsReadOnly()` 改为派生，概念没丢，只是不再是存储形态 |
| 2 | **新增 `confirm` 确认阶梯（四档）** | `none`/`button`/`type`/`type+ack`。理由：如果 `all` 和 `verify` 弹同一个「确定吗？」，确认就不携带信息量，用户会养成闭眼点确定的习惯 —— 而那个习惯会在 `uninstall` 上出事 |
| 3 | **`phrase` 必须原样相等** | 明确写进方案：不做 `TrimSpace`、不做大小写归一。`" all"` / `"ALL"` 正是「粘贴 / 手滑」的典型形态，放宽等于关掉闸门 |
| 4 | **`acknowledge` 用脚本自己的原文** | `uninstall` 的四条勾选项直接取自 `phase_uninstall` 打印的原文，而不是在 Go 里另写一遍 —— §四「变成界面的一部分」的字面实现 |
| 5 | **取消是 `cancelled`，不是 `failed`** | 单列状态：两者的用户文案完全不同（前者要说清「不会回滚，重跑对应阶段即可收敛」） |
| 6 | **写操作串行 + 写期间只读也拒绝** | 新增 `409 writer_busy`。写操作跑到一半时读出来的是中间态，拿它做判断只会误导人 |
| 7 | **部署级闸门 `-allow-write=false`** | 打开时 `/api/commands` 改写 `allowed`，界面不渲染可点按钮；直接打 API 也 403。界面上的确认是给人看的，这一层才是给「绕过界面」看的 |
| 8 | **§8.3 补「`set -e` 在阶段内部失效」这一条** | `main` 的 `case ... || rc=$?` 让所有 `phase_*` 内部的 `set -e` 失效（bash 的 AND-OR 列表豁免贯穿调用树）。这是 `phase_kubeconfig` 能「报告成功但什么都没做」的根因，必须写进方案，否则下一个阶段还会踩 |
| 9 | **id 快照 38 → 43 项** | 新增 `kubeconfig.{fetch,rewrite,vip,chmod,replace}` |
| 10 | **§8.6 补「只读界面发现不了这类缺陷」** | ⑥ 放最后的另一层理由：写按钮会逼你去验证写通道，而正是在那一步才暴露了「报告成功但什么都没做」 |

### v6（P1 实施后回填）

P1（只读控制台）已落地：`deploy.sh` 加三个只读数据出口，Go 后端 + React 前端四个只读视图跑通。
**判定逻辑仍然一行未改**。按实际情况回填了五处：

| # | 改动 | 说明 |
| :--- | :--- | :--- |
| 1 | **§8.2 补「三个只读命令」** | P1 需要的配置 / 拓扑 / 一致性数据，做成 `show-config` / `topology` / `consistency --json`，而不是让 Go 解析 `config.env`。理由：`config.env` 是 bash 语法，`source` 才是权威解析器；且这三个命令**故意不跑 preflight**，集群坏的时候也要能用 |
| 2 | **§8.2 补「Go 侧边界 = 一张命令表」** | §3.2 划的边界落到实处：`internal/api/commands.go` 用 `read_only` / `allowed` 两个正交字段。**写操作照样列出来**并带影响面文案，因为「没有按钮」会把人逼回服务器手敲 `all` |
| 3 | **§8.2 补「前端两处偏离」** | ① `vite-config` 砍掉 Tailwind / code-inspector / react-compiler / 版本化 assetsDir；② 新增第 5 个视图「动作清单」——让 §四 那条约束真的落到界面上 |
| 4 | **§8.2 补 P1 实测结果** | 四态语义在真机上成立（22 项 = ok 21 / warn 1）；`source` 分布 `cluster 10 / node 10 / local 1 / config 1` 确认 §5.3；真浏览器 5 视图 0 error |
| 5 | **协议项 id 快照 32 → 38** | 新增的 6 项即三个只读命令的判定项（`topology.*` 3 项 + `config.*` 3 项；`show-config` 只出 `data`、无判定项）。`selftest.sh` 第 12 节已同步 |

实施中确认 / 修正的三点：

- **「退出码 1」的语义必须固定成回归测试**。`verify` 判定不通过时返回 1，
  那是**业务结果**不是执行失败 —— Go 侧必须把它归成 `Done`。搞错了，界面就会把
  「一次成功的检查，结论是有问题」显示成「控制台出故障了」。
  已写成 `internal/task/manager_test.go` 的第一条用例。
- **Windows 开发期的耗时**让「按快慢分叉」的客户端写法被排除掉了。
  `show-config` 在 Git Bash 下要 13 秒（Linux 上百毫秒级），
  于是前端对**所有**命令统一走 SSE，不做「快的轮询一次」。
  这条是环境逼出来的结论，不是设计偏好。
- **`--json` 的 `data` 字段是 P1 加的**（`emit_json` 多接一个可选参数）。
  它只由三个只读命令使用，`verify` / `preflight` 仍然只有 `items` ——
  **判定项与事实数据刻意分开**：`items` 回答「符不符合要求」，`data` 只是「读到了什么」。

### v5（P0 实施后回填）

P0 已在 `cluster-infra/k3s-ha/deploy.sh` 落地（`verify` / `preflight` 加 `--json`）。
**判定逻辑一行未改**，只加了报告层。实施中按实际情况回填了四处：

| # | 改动 | 说明 |
| :--- | :--- | :--- |
| 1 | **§8.1 补第 4 态 `skip`** | 初稿只约定 `ok`/`warn`/`error`。实施时发现判定项会因外部条件不具备而**根本没跑**（节点没 `openssl`、本机没 kubectl、`MANAGE_REGISTRY=false`、首台未装 k3s）。若不显式出现，消费方分不清「通过」和「没跑」——**正是本项目要防的「看起来一切正常」** |
| 2 | **§8.1 补「人类输出逐字节不变」** | 把「`--json` 是增量」写成可验证的约定：原来打印过的行原样保留，协议项走「只记录不打印」的通道；原来没打印的项不会凭空多出一行 ✓。验证方式 `git stash` 前后 `diff` 为空 |
| 3 | **§8.1 补已落地 id 清单** | 32 个 id（`preflight` 11 + `verify` 20，其中 `traefik.replica_eligible` 两阶段共用）。逐节点项用 `xxx@<节点名>` 后缀展开，前缀稳定、后缀可变 |
| 4 | **`level` 取值表同步为四态** | `summary` 示例也补上 `skip` 计数 |

实施中确认的两点（与方案一致，无需改）：

- **`source` 的四分类与实际数据来源完全对得上**。真机跑 `verify --json` 的实测分布：
  22 个协议项里 `cluster` 10、`node` 10、`local` 1、`config` 1。
  协议项比 §5.3 的「16 项判据」多，是因为**逐节点判据会展开**（`business.vip_advertised` ×2、
  `registry.registries_yaml` ×3），加上 `traefik.replica_eligible` 与 preflight 共用。
  但方向与 §5.3 一致：**`client-go` 只能覆盖 `cluster` 那 10 项，另外 12 项它拿不到** ——
  其中 `node` 那 10 项（证书 SAN / 逐节点 VIP 通告 / `registries.yaml` 逐字节一致）恰恰是最值钱的。
- **`duration_ms` 直接暴露了 Git Bash 的进程开销**：初版用 `$(date ...)` 取值，
  单项耗时被算成 1071ms（实际 20ms 的 `sleep`）。改成 bash 5 内建的 `EPOCHREALTIME`
  （零 fork）后才准。这个字段在 Windows 上排查脚本性能时意外地好用。
  实测最慢的三项都是「逐台 SSH」类：`traefik.replica_eligible` 9.2s、
  `vip.cp_bound` 6.7s、`business.vip_advertised@k2` 4.3s —— 一眼能看出该优化谁。

### v4（定稿前的七处收敛）

评审意见认为 v3 已可作为**方案定稿**，只提了七处小修正。逐条落实：

| # | 改动 | 说明 |
| :--- | :--- | :--- |
| 1 | **§3.2 判据换成本质定义** | 原判据「要不要 SSH 到节点」被替换为：**判定「集群是否符合本方案要求」→ Bash；判定「控制台是否允许执行某操作」→ Go**。补充说明两者易混：前者**消费** `verify` 的结论，后者自己**产出**结论 |
| 2 | **§8.1 补两个可选字段** | `source`（`node`/`cluster`/`local`/`config`）+ `duration_ms`。明确**四字段是必须的、P0 可只做四个**；两个可选字段建议趁 P0 一起加（事后补要动所有判定点） |
| 3 | **§5.2 删掉「~10 MB RSS」** | 具体数字会随 Go runtime / HTTP server / SSH 连接 / 子进程 / 并发任务 / K8s client 变化，写成选型依据会变成过时且误导的硬指标。改为定性描述「资源占用低，适合部署在资源受限的 Linux 管理机上」，稳定的理由只留三条 |
| 4 | **§5.3 降 `client-go` 优先级** | 从「P3 要做」降为**「有明确增量才做的可选项」**，并写明定位：**补充交互和实时状态，而不是替代 `verify`**。补一句「**不做也是一种合法结论**」 |
| 5 | **§6.3 补「集群全挂时它还在」** | 把「跑在集群外」的收益写实：k1/k2/k3 全失联时仍能看到 `Cluster unreachable`，并继续做 SSH 检查 / 网络检查 / 重新部署。这是「控制台」定位的正面证明 |
| 6 | **开头「填一次配置」→「准备一次方案配置」** | 避免让人误以为 P1 就有网页表单。真实流程是 `config.env → Git → 控制台读取 → 展示 → preflight 重算`，**配置来自文件，不是来自表单** |
| 7 | **新增 §8.5 / §8.6** | §8.5 给出 Go 第一版的薄结构（`task`/`executor`/`api`/`auth`），并明确警告不要铺 DDD 分层——那会诱使把 §3.2 禁止的方案判定搬进来。§8.6 给出开工顺序，核心是**不要先做 UI** |

另：文档定位由「设计文档」改为「**方案定稿**」。

### v3（定位提升 + 四处收敛）

按评审意见改的四处，其中第 1 条是**表述准确性问题**，第 4 条是**架构问题**：

| # | 改动 | 说明 |
| :--- | :--- | :--- |
| 1 | **§3.2 边界表述改准** | 初稿「校验逻辑必须留在脚本里，一行都不许搬进后端」**原则对但表述过死**。改为：K3s 集群的**事实判定与方案校验**以 `deploy.sh` 为唯一事实来源，Go 不复制；**Go 可以拥有控制台自身的**任务 / 权限 / 状态 / 参数校验。判据：这条判定要不要 SSH 到节点或读集群状态？ |
| 2 | **§8.1 把 P0 提升为「结果协议」** | `--json` 不是输出格式，是 Bash 的结果协议——**GUI 不该理解 Bash 的文字，而该理解它的结果协议**。补了字段约定（`id`/`level`/`detail`/`fix` + 顶层 `summary`），并写明 `id` 必须稳定（文案可改、`id` 不可改），因为正则匹配 `✓`/`!` 是脆弱耦合 |
| 3 | **§8.2 P1 新增「方案配置视图」** | 展示解析后的配置，**标注来源文件**，提供 `[查看原始配置]` 而非 `[修改]`。P1 不提供修改 |
| 4 | **§六「跑在哪」由未决改为结论** | **跑在一台集群外的 Linux 管理机上**，控制台自己不进被它管理的集群。决定性理由：控制台的价值是「集群不对时告诉你哪里不对」，它必须在集群之外；且节点只有 7.7 GiB，不该和业务抢内存。建议做成容器镜像、脚本随镜像打包 |
| 5 | **定位提升半级** | 标题由「K3s 部署控制台」改为「**K3s 方案控制台**」——把已验证的 K3s HA / VIP / Traefik / Registry / 监控方案，固化为可配置、可校验、可执行、可观测的操作系统。「部署控制台」容易被理解成「一个带网页的部署脚本」 |
| 6 | **新增 §3.1 目标架构图** | 含 Browser → React → Go API → `deploy.sh` → SSH → k1/k2/k3 主链路，以及 P3 才接入的 Kubernetes API / Prometheus 虚线区 |

### v2（前端技术栈修正）

初稿把前端定为 Vue 3 + Element Plus，依据是「主力项目 `mgmt-frontend-inner` 是 Vue」。
**这个判断是错的**——它只看了那一个仓，漏了 `pass-up.frontend`，而后者才是 React：

| 仓 | 栈 | 事实 |
| :--- | :--- | :--- |
| `mgmt-frontend-inner` | Vue 3 + Element Plus | 确实存在（Vben 风格 monorepo） |
| `pass-up.frontend` | **React 19 + antd 6 + Vite 8 + zustand** | `apps/admin`、`apps/user`、`packages/shared` 全是 `react` + `react-dom` + `antd` |

本版修正：

- **§5.1 重写**为 React + TypeScript + Ant Design，技术栈逐项对齐 `pass-up.frontend`，
  并明确**不用 Tailwind CSS**（可读性 + 与 antd CSS-in-JS 的优先级冲突）；
- **新增 §5.4**：图表延后到 P3，P1 不引入任何图表库；
- §十一 补上前端基线的出处。

其余结论（§三 校验逻辑留在 bash、§四 动作影响面、§5.3 `client-go` 的定位、
§七 五个缺口、§八 分期）**不受影响**——它们与前端栈无关。

### v1（初稿）

首次成文：把两份已实机验证的方案（k3s-ha / monitoring）作为上游，
给出控制台的边界、动作影响面、五个缺口清单与分期。

