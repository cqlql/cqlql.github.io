```python
@router.get("/export/form_excel")
def export_forms(request: ExportFormRequest):
    exporter = FormExcelExporter(request.fields, request.form_data)
    excel_stream = exporter.export()

    filename = f"form_export_{datetime.now().strftime('%Y%m%d_%H%M%S')}.xlsx"
    # 方式1：：使用 StreamingResponse + 正确 BytesIO--目前有问题
    # wb.save(f"uploads/{filename}")
    # return StreamingResponse(
    #     content=stream,
    #     media_type="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
    #     headers={
    #         "Content-Disposition": f"attachment; filename={filename}",
    #     },
    # )
    # 方式2：用 FileResponse（大文件推荐）
    # temp_path = f"uploads/{uuidv4()}.xlsx"
    # wb.save(temp_path)
    # return FileResponse(
    #     path=temp_path,
    #     media_type="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
    #     filename=filename
    # )
    # 方式3：最安全的方式，小文件推荐用
    content = excel_stream.read()
    return Response(
        content=content,
        media_type="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
        headers={
            "Content-Disposition": f"attachment; filename={filename}",
            "Content-Length": str(len(content)),
        },
    )

```

