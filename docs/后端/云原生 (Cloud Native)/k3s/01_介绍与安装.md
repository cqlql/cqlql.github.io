

## VIP
通过：
- Keepalived
- kube-vip（K3s 官方推荐）



## 打 taint

`打 taint` 的意思是：**给节点打上污点（Taint），阻止普通业务 Pod 调度到这个节点。**

Kubernetes 中，**Taint（污点）** 和 **Toleration（容忍）** 是一对机制：

- **Taint**：节点说「普通 Pod 不要来我这里」。
- **Toleration**：Pod 说「虽然你有污点，但我允许运行在这里」。

只有 3 台Server（推荐不加）

3 台 Server + 多台 Agent（推荐加）

## 业务 Pod


## etcd
 

etcd 是整个集群的数据库，存储了所有节点、Pod、配置的状态。

postgresql 数据库？
- 否，etcd 是一个键值存储，用于存储集群的状态和配置。


## Worker 节点
也就是 Agent，负责运行业务 Pod。

## 是否有必要装管理界面？
前期没必要，很多时候命令可能更快，维护一套管理界面也需要额外的资源。后期人多可以考虑。

折中方案：使用本地客户端：Lens (或 OpenLens)
以后需要 Web 可视化时：优先考虑 Headlamp，，再根据是否发展为多集群、多团队协作决定是否升级到 Rancher。

前期推荐组合：

```
Ubuntu
    │
K3s
    │
kubectl
    │
k9s
    │
Grafana
Prometheus
Loki
```
