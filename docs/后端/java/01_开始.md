## jar war 如何选择？为什么？怎么用？



## 部署方式推荐

| **特性**     | **传统 JAR (Systemd)** | **Docker 容器** | **GraalVM 原生镜像** |
| ------------ | ---------------------- | --------------- | -------------------- |
| **部署难度** | 简单                   | 中等            | 较高 (编译慢)        |
| **启动速度** | 较慢                   | 中等            | **极快**             |
| **内存开销** | 高                     | 高              | **低**               |
| **隔离性**   | 弱                     | **强**          | **强**               |
| **推荐等级** | ⭐⭐                     | ⭐⭐⭐⭐⭐           | ⭐⭐⭐⭐ (特定场景)      |

### 💡 核心建议

1. **不管用哪种，先做 CI/CD：** 永远不要手动 `scp` 一个 Jar 包去覆盖生产环境。哪怕是用 GitHub Actions 写一个简单的脚本，也能减少 90% 的低级错误。
2. **关注 Java 版本：** 2026 年，请确保你的生产环境至少在 **Java 21 (LTS)** 以上，利用虚拟线程（Virtual Threads）可以显著提升 Web 服务的并发能力，而无需修改部署架构。
3. **可观测性：** 部署时记得集成 **Prometheus** 和 **Grafana**（或者使用云厂商自带的监控），看不见内部运行状态的部署是不及格的。

## 虚拟线程（Virtual Threads）

## CI/CD 工具格局对比 (2026 视角)

| **工具**              | **份额特征**    | **核心标签**         | **为什么选它？**                       |
| --------------------- | --------------- | -------------------- | -------------------------------------- |
| **Jenkins**           | 34% (缓慢下降)  | 老大哥 / 万能 / 复杂 | 极度灵活，支持各种古董级环境。         |
| **GitHub Actions**    | ~28% (快速上升) | 生态丰富 / 零维护    | 开发者体验最好，Marketplace 插件极多。 |
| **GitLab CI**         | ~18% (稳步增长) | 安全 / K8s 原生      | 一站式平台，适合规范化的大中型团队。   |
| **Azure/AWS/GCP**     | ~12%            | 厂商集成 / 合规      | 与云资源深度绑定，适合“全家桶”用户。   |
| **其他 (CircleCI等)** | ~8%             | 极速 / 细分市场      | 追求极致的构建速度和特定领域的自动化。 |

### 1. **GitHub Actions (约 25% - 30%) —— 增长最快的“新霸主”**

GitHub Actions 是近年来份额增长最猛的工具。

- **核心逻辑：** “代码在哪，CI/CD 就在哪”。
- **为什么占领市场：** 它消除了“安装服务器”这个步骤。对于 2026 年的开发者来说，在 `.github/workflows` 下写一个 YAML 文件比维护一台 Jenkins 虚拟机要香得多。
- **用户群：** 开源项目（几乎统治级）、中小型初创公司、以及从传统 Jenkins 迁移出来的现代企业团队。

### 2. **GitLab CI/CD (约 15% - 20%) —— 企业私有化的首选**

GitLab 不仅仅是一个代码仓库，它是一个“全栈 DevOps 平台”。

- **核心逻辑：** “从需求分析到生产监控，全在一个网页里”。
- **为什么占领市场：** 它的权限管理、安全扫描（SAST/DAST）和对 Kubernetes 的原生支持非常强大。很多对安全性要求极高的金融、电信企业会选择自建 GitLab 实例，顺便就用了它的 CI/CD。
- **用户群：** 重度使用 K8s 的团队、对合规性有严格要求的企业。

### 3. **公有云原生工具 (约 10% - 15%) —— 厂商绑定的力量**

随着企业全面上云，各大云厂商的“全家桶”也分走了一大块蛋糕：

- **代表工具：** Azure DevOps Pipelines (微软系首选), AWS CodePipeline, Google Cloud Build。
- **优势：** 如果你的 Java 应用跑在 AWS 的 Lambda 或 ECS 上，用原生的 CodePipeline 权限配置最简单，账单也能合并。

vscode、intelliIDE清理缓存

本质是**清理并重建 Language Server Protocol (LSP) 的缓存数据和索引**。

## 打包

```
mvn clean package
```

常用组合命令，包含清理和打包：纯粹的重新打包。

更多命名见()[./构建工具/Maven/01_开始.md]

## 本地依赖 mvn install

把当前项目的打包结果（Jar/War）放到你电脑的“本地仓库”里，供其他项目引用。

### 引用本地仓库

项目中使用本地依赖与线上依赖一致，Maven 会先找本地仓库，找不到才会找线上。如果远程也找不到，就会抛出经典的 `Dependency resolution failed`。

```
<dependencies>
    <dependency>
        <groupId>com.example</groupId>
        <artifactId>my-utils</artifactId>
        <version>1.0-SNAPSHOT</version>
    </dependency>
</dependencies>
```



## 快照版 (Snapshot)

如果你的版本号后面带着 `-SNAPSHOT`（如 `1.0-SNAPSHOT`），这是一个非常实用的特性：

1. **自动更新**：当你在 A 项目里修改了代码并再次执行 `mvn install` 时，B 项目**不需要**改版本号。
2. **强制刷新**：如果 B 项目没感应到变化，你可以执行 `mvn clean compile -U`。这里的 `-U` 会强制 Maven 检查本地和远程仓库的最新快照。

### Maven找寻核心区别：SNAPSHOT vs RELEASE

Maven 对这两种版本的“找寻态度”完全不同，这决定了它会不会“自动”去远程找：

#### **情况 A：正式版 (Release)**

- **版本号示例**：`1.0.0`, `2.1.4`
- **逻辑**：Maven 认为正式版是**不可变的**。只要本地仓库里有了这个版本，它**永远不会**再去远程仓库检查更新。如果你在远程改了代码但没改版本号，本地是感应不到的。

#### **情况 B：快照版 (Snapshot)**

- **版本号示例**：`1.0-SNAPSHOT`
- **逻辑**：Maven 认为快照版是**正在开发中**的。即使本地有了，它也会**定期**（默认每天一次，或者当你使用 `-U` 参数时）去远程仓库检查有没有更新的时间戳。如果有更新，它会自动下载覆盖本地。