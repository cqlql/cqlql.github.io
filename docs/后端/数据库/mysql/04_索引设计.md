---
title: 索引设计
icon: mdi:index
sort: 4
---

# MySQL 索引设计速查

## 1. 索引的几种定义方式

```
- PRIMARY KEY → 主键索引（聚簇）
- UNIQUE KEY  → 唯一索引
- KEY / INDEX → 普通索引
```

## 2. 索引 ≠ 约束（必记）

```
- PRIMARY / UNIQUE：索引 + 约束
- INDEX：只有索引
- FOREIGN KEY：只有约束（不自动建索引，需手动加）
```

📌 **面试 & 生产踩坑点**

## 3. 什么时候该建索引

```
- WHERE / JOIN / ORDER BY / GROUP BY 出现的字段
- 区分度高的字段
- 经常作为查询入口的字段
```

## 4. 不要乱建索引

```
- 低基数字段（sex / status）
- 经常更新的字段
- 索引 ≠ 越多越好（拖慢写入）
```

## 5. 组合索引核心规则

```
- 最左前缀原则
- 等值在前，范围在后
- ORDER BY 顺序要和索引一致
```

## 6. 业务模板

```
【用户表】
- uk_user_username (username)
- idx_user_status (user_id, status)

【树结构】
- idx_parent_id (parent_id)

【时间查询】
- idx_created_at (created_at)
```

## 7. 建索引的方式

### 建表时定义

```sql
CREATE TABLE user (
  id BIGINT PRIMARY KEY,
  user_name VARCHAR(50),
  parent_id BIGINT,
  UNIQUE KEY uk_user_username (user_name),
  KEY idx_parent (parent_id)
);
```

### CREATE INDEX（事后补，不能建主键）

```sql
CREATE INDEX idx_parent ON user(parent_id);
CREATE UNIQUE INDEX uk_user_username ON user(user_name);
```

### ALTER TABLE（生产最常用）

```sql
ALTER TABLE user ADD INDEX idx_parent (parent_id);
ALTER TABLE user ADD UNIQUE uk_user_username (user_name);
ALTER TABLE user ADD PRIMARY KEY (id);
```

### 组合索引

```sql
KEY idx_user_status (user_id, status)
```

- 最左前缀：`(a,b,c)` 可用 `a`、`a,b`、`a,b,c`，❌ 不能直接用 `b`、`c`。

### 前缀索引（省空间）

```sql
KEY idx_email (email(20))
```

- 仅对 `VARCHAR / TEXT` 有效；唯一索引也可加前缀，但需保证前缀足够区分。

### 全文索引

```sql
FULLTEXT KEY idx_content (content)
```

- 用于 `MATCH ... AGAINST`，适合文章/搜索；InnoDB 5.6+ 支持。

### 空间索引

```sql
SPATIAL KEY idx_location (point)
```

- GIS / 地理位置，一般业务很少用。
