---
title: 环境变量与软链接
sort: 7
---

## 环境变量

配置文件作用范围不同：

| 文件 | 作用范围 |
| --- | --- |
| `/etc/profile`、`/etc/environment` | 全局（所有用户） |
| `~/.bashrc`、`~/.profile` | 当前用户 |

生效方式（`source` 或重新登录）：

```bash
source ~/.bashrc
```

示例（nodejs 加入 PATH）：

```bash
export NODE_HOME=/usr/local/nodejs
export PATH=$PATH:$NODE_HOME/bin
export NODE_PATH=$NODE_HOME/lib/node_modules
```

## 符号链接（软链接）

`ln -s` 常用于把二进制软链到 `/usr/local/bin` 以便全局调用：

```bash
sudo ln -s /usr/local/nodejs/bin/node /usr/local/bin/node
sudo ln -s /usr/local/nodejs/bin/npm /usr/local/bin/npm
```

> 软链接类似 Windows 快捷方式，源文件删除后链接失效（悬空）。

参考：[Linux 环境变量和符号链接](https://www.jianshu.com/p/ac17d8a3d0c4)
