---
title: 编译忽略检查
icon: mdi:eye-off
sort: 16
---

在个别无法（或不值得）修掉类型错误的地方临时跳过检查。

```vue
// @ts-ignore
```

```vue
// @ts-ignore: 2322
```

```typescript
// @ts-nocheck
```

- `@ts-ignore`：忽略下一行错误，可附加错误码（如 `: 2322`）。
- `@ts-nocheck`：忽略整个文件。
- 配合 `// @ts-expect-error` 可在"本应报错却没报"时主动报错，更适合长期标记。
